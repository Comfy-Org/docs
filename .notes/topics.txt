Server:

hidden inputs
subclassing nodes
execution order
custom data types

Extending the UI:

Defining an extension (code and deployment)
Methods you can invoke
Intercepting calls
The ComfyNode object
Inputs, outputs and widgets
Custom widgets  (*)
Adding to the menus (node, canvas)
Other key JavaScript objects (prompt, app, graph, workflow)
Group nodes  (*)


Server-Client messaging:

adding python server calls (JS calling Python)
sending custom messages (Python messaging JS)

