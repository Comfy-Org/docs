---
title: "OpenAI GPT Image 1 - ComfyUI 原生节点文档"
description: "使用OpenAI的GPT-4 Vision模型生成图像的节点"
sidebarTitle: "OpenAI GPT Image 1"
icon: "circle"
---

![ComfyUI 原生OpenAI GPT Image 1  节点](/images/built-in-nodes/api_nodesopenai/openai-gpt-image1.jpg)

OpenAI GPT Image 1 节点让你能够使用OpenAI的GPT-4 Vision模型生成图像，结合了GPT模型的理解能力和图像生成技术。

## 节点功能

此节点连接到OpenAI的GPT-4 Vision API，让用户能够通过详细的文本提示词生成图像。GPT Image 1与传统的DALL·E模型不同，它利用了GPT-4的语言理解能力，可以处理更复杂和语境丰富的提示词，生成更符合用户意图的图像内容。

## 参数说明

### 基本参数

| 参数 | 类型 | 默认值 | 说明 |
|-----|-----|-------|------|
| prompt | 字符串 | "" | 详细描述要生成内容的文本提示词 |
| quality | 选择项 | "standard" | 图像质量级别 |
| size | 选择项 | "1024x1024" | 输出图像尺寸 |
| model | 选择项 | "gpt4" | 使用的GPT模型版本 |

### 图像编辑参数

| 参数 | 类型 | 说明 |
|-----|-----|------|
| image | 图像 | 用于图像编辑的输入图像 |
| mask | 掩码 | 指定图像中要修改的区域(可选) |

### 可选参数

| 参数 | 类型 | 说明 |
|-----|-----|------|
| background | 选择项 | 背景处理选项 |
| seed | 整数 | 生成的随机种子 |
| n | 整数 | 生成的图像数量 |

### 输出

| 输出 | 类型 | 说明 |
|-----|-----|------|
| IMAGE | 图像 | 生成的图像结果 |

## 工作原理

OpenAI GPT Image 1 节点结合了GPT-4的语言理解能力和图像生成技术。它首先分析用户提供的文本提示词，理解其语义内容和意图，然后生成符合描述的图像。

当提供输入图像时，节点可以在图像编辑模式下工作，允许对现有图像进行修改。通过额外提供掩码，用户可以精确控制哪些区域应该被修改，哪些应该保持不变。

用户可以通过调整各种参数来控制生成结果，包括质量级别、尺寸、背景处理和生成数量。生成的图像链接有效期较短，建议及时下载或缓存重要的生成结果。

## 源码参考

[节点源码 (更新于2025-05-03)]

```python
class OpenAIGPTImage1(ComfyNodeABC):
    """
    Generates or edits images synchronously via OpenAI's GPT Image 1 endpoint.

    Uses the proxy at /proxy/openai/images/generations or /proxy/openai/images/edits.
    Returned URLs are short‑lived, so download or cache results if you need to keep them.
    """

    def __init__(self):
        pass

    @classmethod
    def INPUT_TYPES(cls) -> InputTypeDict:
        return {
            "required": {
                "prompt": (
                    IO.STRING,
                    {
                        "multiline": True,
                        "default": "",
                        "tooltip": "Text prompt for GPT Image",
                    },
                ),
                "quality": (
                    IO.COMBO,
                    {
                        "options": ["standard", "hd"],
                        "default": "standard",
                        "tooltip": "Image quality",
                    },
                ),
                # ... 其他参数定义 ...
``` 