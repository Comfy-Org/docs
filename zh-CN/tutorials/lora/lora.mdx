---
title: "ComfyUI 中 Lora 模型的使用示例"
sidebarTitle: "Lora 模型"
description: "本篇将引导了解并完成 Lora 模型的使用"
---

**Lora 模型​（Low-Rank Adaptation）** 是一种用于微调大型生成模型（如 Stable Diffusion）的高效技术。
它通过在预训练模型的基础上引入可训练的低秩矩阵，仅调整部分参数，而非重新训练整个模型，从而以较低的计算成本实现特定任务的优化。

我们将演示如何使用 Lora 的示例。所有 Lora 变体：Lycoris, loha, lokr, locon, 等... 都是以这种方式使用。

在本篇教程中，我们将完成以下完整示例，来掌握如何在[ComfyUI](https://github.com/comfyanonymous/ComfyUI) 中加载并使用 Lora 模型。

- 安装 Lora 模型
- 使用 Lora 模型生成图片
- 相关节点的简单介绍

## 相关模型安装

### SD1.5 模型

本篇指南将使用：[SD1.5 模型](https://civitai.com/models/496058?modelVersionId=1000076364) 作为示例，请下载对应的模型文件，并将其保存在 `ComfyUI/models/checkpoints` 目录下，在这个文件夹内的模型文件会被`Load Checkpoint`节点识别到

请下载对应的模型文件，并将其保存在 `ComfyUI/models/loras` 目录下，在这个文件夹内的模型文件会被`Load Lora`节点识别到

### Lora 模型

本教程使用[Lora]()

请访问并下载对应的模型文件，并保存在`ComfyUI/models/loras`
## 工作流

请下载下面的图片，使用菜单 `Workflows` -> `Open（ctrl+o）` 加载下面的工作流  
![alt text](image.png)

## 使用 Lora 模型生成图片

请参照下图步骤，来确保对应的工作流可以正常运行 

![]()

1. 确保`Load Checkpoint`可以识别到你安装的 **checkpoint** 模型文件
2. 确保`Load Lora`可以识别到你安装的 **lora** 模型文件
3. 点击 `Queue` 按钮，或者使用快捷键 `Ctrl(cmd) + Enter(回车)` 来执行图片的生成

### Lora 相关节点介绍

![Load Lora 节点](/images/comfy_core/loaders/load_lora.jpg)

位于`ComfyUI\models\loras` 会被 ComfyUI 检测，并在这个节点中加载

**输入类型**

| 参数名称          | 数据类型        | 作用 |
|------------------|---------------|-------------------------------------------------------------------------------------------------|
| `model`          | MODEL         | 通常用于连接基础模型                                                                               |
| `clip`           | CLIP          | 通常用于连接CLIP模型                                                                               |
| `lora_name`      | COMBO[STRING] | 选择要使用的LoRA模型名称                                                                            |
| `strength_model` | FLOAT         |  取值范围 -100.0 到 100.0 ,日常使用用于图片生成时我们通常使用0~1之间, 取值越大, 应用的这个模型调整的效果越明显 |
| `strength_clip`  | FLOAT         | 取值范围 -100.0 到 100.0 ,日常使用用于图片生成时我们通常使用0~1之间, 取值越大, 应用的这个模型调整的效果越明显  |

**输出类型**

| 参数名称 | 数据类型 | 作用                  |
|---------|--------|-----------------------|
| `model` | MODEL  | 应用了LoRA调整的模型     |
| `clip`  | CLIP   | 应用了LoRA调整的CLIP实例 |

该节点支持链式连接，可以将多个Load Lora 串联来应用多个 Lora 模型，具体请参考[在ComfyUI 中使用多个 Lora 模型示例](/zh-CN/tutorials/lora/multiple-loras)

### 开始你的尝试

试着修改提示词，或者调整 Load Lora 节点的 `strength_model` 的参数，来观察生成图片的变化，了解对应节点的使用


