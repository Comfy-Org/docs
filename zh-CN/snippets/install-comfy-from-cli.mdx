创建高于 Python 3.9 版本的虚拟环境。
<CodeGroup>

```bash conda
conda create -n comfy-env python=3.11
conda activate comfy-env
```

```bash venv
python3 -m venv comfy-env
source comfy-env/bin/activate
```

</CodeGroup>

安装ComfyUI
```bash
comfy install
```

<Warning>您依旧需要安装 CUDA 或 ROCm，取决于您的显卡。</Warning>