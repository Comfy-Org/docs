---
title: "使用 Comfy Router 调用 Claude Sonnet 4.6"
description: "通过 Comfy Router 调用 anthropic/claude-sonnet-4-6：端点、请求形状以及 Router 返回的响应。"
sidebarTitle: "Claude Sonnet 4.6"
translationSourceHash: 508d46b3
translationFrom: development/comfy-router/models/anthropic/claude-sonnet-4-6/code.mdx
translationBlockHashes:
  "_intro": fdc3383a
  "Quick start": 91b031a4
  "Schema": 7e4df7aa
  "Examples": 7fb5285d
---
{/* GENERATED FILE. Generated from router-schemas/anthropic/claude-sonnet-4-6.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/zh/comfy-router/model-code-footer.mdx";

`anthropic/claude-sonnet-4-6` 的 API 参考，由 Comfy Router 提供，模型来自 Anthropic。

## 快速开始

在[你的 Comfy 工作区](https://platform.comfy.org/profile/api-keys)中创建一个密钥，并将其导出为 `COMFY_API_KEY`。Python 和 TypeScript 代码片段使用 Comfy SDK（`pip install comfy-sdk`、`npm install @comfyorg/sdk`）；cURL 代码片段则是通过原始 HTTP 发起的同一调用。

**模型 ID：** `anthropic/claude-sonnet-4-6`

**端点：** `POST https://api.comfy.org/v2/models/anthropic/claude-sonnet-4-6`

<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "anthropic/claude-sonnet-4-6",
        {
            "max_tokens": 16,
            "messages": [
                {
                    "content": "Reply with the single word: ok",
                    "role": "user",
                },
            ],
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const { data } = await comfy.models.run("anthropic/claude-sonnet-4-6", {
  max_tokens: 16,
  messages: [
    {
      content: "Reply with the single word: ok",
      role: "user",
    },
  ],
});

console.log(data);
```

```bash cURL
curl https://api.comfy.org/v2/models/anthropic/claude-sonnet-4-6 \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"max_tokens\": 16, \"messages\": [{\"content\":\"Reply with the single word: ok\",\"role\":\"user\"}]}"
```
</CodeGroup>

## Schema

### 输入

<ParamField body="max_tokens" type="integer" required>
  停止前可生成的最大 token 数量。
</ParamField>

<ParamField body="messages" type="object[]" required>
  对话轮次。完整的 content block 分类请参阅 Anthropic Messages API 文档。
</ParamField>

<ParamField body="messages[].content" type="object" required>
  可以是字符串简写，也可以是 content block 数组（text、image、document、tool_use、tool_result 等）。
</ParamField>

<ParamField body="messages[].role" type="string" required>
  可选值：`user`、`assistant`
</ParamField>

<ParamField body="model" type="string">
  Anthropic 模型标识符（例如 `claude-sonnet-4-5`、`claude-opus-4-7`）。
</ParamField>

<ParamField body="stream" type="boolean">
  为 true 时，响应是 Anthropic 消息事件的 `text/event-stream`，而不是单个 JSON 响应体。
</ParamField>

<ParamField body="system" type="object">
  顶层系统提示词。可以是字符串，也可以是 content block 数组；两者都会原样传递给 Anthropic。
</ParamField>

由 Router 在 `GET /v2/models/anthropic/claude-sonnet-4-6/openapi.json` 提供的 schema 生成，与请求到达提供商之前用于校验调用的文档为同一份。

### 输出

<ResponseField name="id" type="string">
   
</ResponseField>

<ResponseField name="model" type="string">
   
</ResponseField>

<ResponseField name="role" type="string">
   
</ResponseField>

<ResponseField name="stop_reason" type="string">
   
</ResponseField>

<ResponseField name="stop_sequence" type="string">
   
</ResponseField>

<ResponseField name="type" type="string">
   
</ResponseField>

<ResponseField name="usage" type="object">
  Anthropic Messages API 调用的 token 用量。
</ResponseField>

<ResponseField name="usage.cache_creation" type="object">
  Anthropic Messages API 调用中，按 TTL 细分的缓存写入输入 token。
</ResponseField>

<ResponseField name="usage.cache_creation.ephemeral_1h_input_tokens" type="integer">
   
</ResponseField>

<ResponseField name="usage.cache_creation.ephemeral_5m_input_tokens" type="integer">
   
</ResponseField>

<ResponseField name="usage.cache_creation_input_tokens" type="integer">
   
</ResponseField>

<ResponseField name="usage.cache_read_input_tokens" type="integer">
   
</ResponseField>

<ResponseField name="usage.input_tokens" type="integer">
   
</ResponseField>

<ResponseField name="usage.output_tokens" type="integer">
   
</ResponseField>

<ResponseField name="content" type="object[]" required>
  回复的 content block，按顺序排列。每条已完成的消息都会包含该字段；当该轮次未产生任何内容时为空。
</ResponseField>

<ResponseField name="content[].text" type="string">
  该 block 的文本。仅在 `text` block 上存在，其他类型均不包含。
</ResponseField>

<ResponseField name="content[].type" type="string">
  block 类型。`text` 是携带 `text` 的那一种；Anthropic 目前还会发送 `thinking`、`redacted_thinking`、`tool_use`、`server_tool_use` 以及各工具结果 block，且该列表是开放的。
</ResponseField>

## 示例

### 输入

```json
{
  "max_tokens": 16,
  "messages": [
    {
      "content": "Reply with the single word: ok",
      "role": "user"
    }
  ]
}
```

### 输出

```json
{
  "content": [
    {
      "text": "ok",
      "type": "text"
    }
  ],
  "id": "msg_01ExampleInvalidPlaceholder",
  "model": "claude-sonnet-4-6",
  "role": "assistant",
  "stop_reason": "end_turn",
  "stop_sequence": null,
  "type": "message",
  "usage": {
    "input_tokens": 16,
    "output_tokens": 3
  }
}
```


<RouterCodeFooter />
