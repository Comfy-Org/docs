---
title: "将 Higgsfield Kling 3.4k 与 Comfy Router 搭配使用"
description: "通过 Comfy Router 调用 higgsfield/higgsfield-kling-3-4k：端点、请求结构以及 Router 返回的响应。"
sidebarTitle: "Higgsfield Kling 3.4k"
translationSourceHash: 062eae1e
translationFrom: development/comfy-router/models/higgsfield/higgsfield-kling-3-4k/code.mdx
translationBlockHashes:
  "_intro": b2932c1e
  "Quick start": 9932a4ff
  "Schema": 78fccccf
  "Examples": f00e952a
---
{/* GENERATED FILE. Generated from router-schemas/higgsfield/higgsfield-kling-3-4k.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/zh/comfy-router/model-code-footer.mdx";

`higgsfield/higgsfield-kling-3-4k` 的 API 参考，由 Comfy Router 从 Higgsfield 提供。

## 快速开始

在[你的 Comfy 工作区](https://platform.comfy.org/profile/api-keys?onboarding=router)中创建一个密钥，并将其导出为 `COMFY_API_KEY`。Python、TypeScript 和 Swift 代码片段使用 Comfy SDK（`pip install comfy-sdk`、`npm install @comfyorg/sdk` 以及 [`ComfySwiftSDK`](https://github.com/Comfy-Org/comfy-swift-sdk) Swift 包）；cURL 代码片段则是通过原始 HTTP 执行的同一调用。

**模型 ID:** `higgsfield/higgsfield-kling-3-4k`

**端点:** `POST https://api.comfy.org/v2/models/higgsfield/higgsfield-kling-3-4k`

<Tabs><Tab title="等待结果">
<CodeGroup>
```python Python
from comfy_sdk import Comfy

# 从环境变量中读取 COMFY_API_KEY。
# SDK 会自动创建一个幂等键，并在自动重试时复用它。
with Comfy() as client:
    result = client.models.run(
        "higgsfield/higgsfield-kling-3-4k",
        {
            "aspect_ratio": "16:9",
            "cfg_scale": 0.5,
            "duration": 5,
            "prompt": "A cinematic glass pavilion in a misty pine forest at sunrise",
            "sound": "on",
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// 从环境变量中读取 COMFY_API_KEY。
// SDK 会自动创建一个幂等键，并在自动重试时复用它。
const { data } = await comfy.models.run("higgsfield/higgsfield-kling-3-4k", {
  aspect_ratio: "16:9",
  cfg_scale: 0.5,
  duration: 5,
  prompt: "A cinematic glass pavilion in a misty pine forest at sunrise",
  sound: "on",
});

console.log(data);
```

```swift Swift
import Foundation
import ComfySwiftSDK

// 从环境变量中读取 COMFY_API_KEY。
// SDK 每次调用都会生成一个幂等键，并在自动重试时复用它。
let client = ComfyCloudClient(apiKey: ProcessInfo.processInfo.environment["COMFY_API_KEY"]!)
let result = try await client.models.run(
    "higgsfield/higgsfield-kling-3-4k",
    input: [
        "aspect_ratio": "16:9",
        "cfg_scale": 0.5,
        "duration": 5,
        "prompt": "A cinematic glass pavilion in a misty pine forest at sunrise",
        "sound": "on",
    ]
)

print(result.output)
```

```bash cURL
curl https://api.comfy.org/v2/models/higgsfield/higgsfield-kling-3-4k \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"aspect_ratio\": \"16:9\", \"cfg_scale\": 0.5, \"duration\": 5, \"prompt\": \"A cinematic glass pavilion in a misty pine forest at sunrise\", \"sound\": \"on\"}"
```
</CodeGroup>
  </Tab><Tab title="加入队列，稍后收集">
同一个请求体，发送到 `POST https://api.comfy.org/v2/models/higgsfield/higgsfield-kling-3-4k/requests`。运行一经接纳，Router 就会返回 `201` 和 `request_id`，结果就绪后再进行收集，可以由本进程收集，也可以由另一个进程收集。[队列投递](/zh/development/comfy-router/queue) 会详细讲解状态、取消和收集。

<CodeGroup>
```python Python
from comfy_sdk import Comfy

# 从环境变量中读取 COMFY_API_KEY。
# 每次 submit() 调用都会生成自己的 Idempotency-Key，并在自动重试时复用它。
with Comfy() as client:
    handle = client.models.submit(
        "higgsfield/higgsfield-kling-3-4k",
        {
            "aspect_ratio": "16:9",
            "cfg_scale": 0.5,
            "duration": 5,
            "prompt": "A cinematic glass pavilion in a misty pine forest at sunrise",
            "sound": "on",
        },
    )
    print("request_id:", handle.request_id)  # 加上模型 ID，就是另一个进程所需的全部信息

    # 持续轮询直到请求完成，按服务器给出的 Retry-After 等待。
    for update in handle.iter_events():
        print(update.status, update.queue_position)

    # 提供商自身的 payload，与 models.run() 返回的值相同。
    # 失败或已取消的请求会在此处抛出带类型的 Router 错误。
    result = handle.get()

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// 从环境变量中读取 COMFY_API_KEY。
// 每次 submit() 调用都会生成自己的 Idempotency-Key，并在自动重试时复用它。
const handle = await comfy.models.submit("higgsfield/higgsfield-kling-3-4k", {
  aspect_ratio: "16:9",
  cfg_scale: 0.5,
  duration: 5,
  prompt: "A cinematic glass pavilion in a misty pine forest at sunrise",
  sound: "on",
});
console.log("requestId:", handle.requestId); // 加上模型 ID，就是另一个进程所需的全部信息

// 持续轮询直到请求完成，按服务器给出的 Retry-After 等待。
for await (const update of handle.events()) {
  console.log(update.status, update.queuePosition);
}

// 与 models.run() 返回的结果相同。失败或已取消的请求会在此处被拒绝。
const result = await handle.get();

console.log(result.data);
```

```swift Swift
import Foundation
import ComfySwiftSDK

// 从环境变量中读取 COMFY_API_KEY。
// 每次 submit() 调用都会生成自己的 Idempotency-Key，并在自动重试时复用它。
let client = ComfyCloudClient(apiKey: ProcessInfo.processInfo.environment["COMFY_API_KEY"]!)
let handle = try await client.models.submit(
    "higgsfield/higgsfield-kling-3-4k",
    input: [
        "aspect_ratio": "16:9",
        "cfg_scale": 0.5,
        "duration": 5,
        "prompt": "A cinematic glass pavilion in a misty pine forest at sunrise",
        "sound": "on",
    ]
)
print("requestId:", handle.requestId)  // 加上模型 ID，就是另一个进程所需的全部信息

// 持续轮询直到请求完成，按服务器给出的 Retry-After 等待。
for try await update in handle.events() {
    print(update.state.rawValue, update.queuePosition.map(String.init) ?? "unknown")
}

// 提供商自身的 payload，与 models.run() 返回的值相同。
// 失败或已取消的请求会在此处抛出带类型的 Router 错误。
let result = try await handle.result()

print(result.output)
```

```bash cURL
# 1. 提交。Router 返回 201，并附带 request_id、status_url、response_url 和 cancel_url。
curl https://api.comfy.org/v2/models/higgsfield/higgsfield-kling-3-4k/requests \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"aspect_ratio\": \"16:9\", \"cfg_scale\": 0.5, \"duration\": 5, \"prompt\": \"A cinematic glass pavilion in a misty pine forest at sunrise\", \"sound\": \"on\"}"

# 2. 持续轮询直到状态为 COMPLETED，按每个响应中给出的 Retry-After 秒数等待。
REQUEST_ID="<request_id from the 201 body>"
curl -i https://api.comfy.org/v2/models/higgsfield/higgsfield-kling-3-4k/requests/$REQUEST_ID/status \
  -H "X-API-Key: $COMFY_API_KEY"

# 3. 收集。200 返回模型的原生输出，仍在运行时 202 返回状态响应体。
curl https://api.comfy.org/v2/models/higgsfield/higgsfield-kling-3-4k/requests/$REQUEST_ID \
  -H "X-API-Key: $COMFY_API_KEY"
```
</CodeGroup>
  </Tab>
</Tabs>

## Schema

### 输入

<ParamField body="aspect_ratio" type="string" default="&quot;16:9&quot;">
  输出宽高比。

  可选值：`16:9`、`9:16`、`1:1`
</ParamField>

<ParamField body="cfg_scale" type="number" default="0.5">
  生成结果遵循提示词的程度。Higgsfield 文档中给出的范围是 0-1，步进为 0.01，但此处只声明了 0-1 这一边界：`multipleOf: 0.01` 会在二进制浮点下求值，从而拒绝 0.3 这类普通取值，因此更细粒度的值会被接受并转发，而不是被拒绝，这与 `prompt` 对其未声明的长度上限所作的说明方式相同。

  范围：`0` 到 `1`
</ParamField>

<ParamField body="duration" type="integer" default="5">
  输出时长，单位为秒。支持的取值范围：3-15。

  范围：`3` 到 `15`
</ParamField>

<ParamField body="elements" type="string[]">
  可复用的 Kling 元素 id，用于在多次生成之间保持主体一致性。
</ParamField>

<ParamField body="multi_prompt" type="object[]">
  有序的镜头定义，在 `multi_shots` 为 true 时使用。1-6 个镜头；每个镜头的时长受任务自身总时长的约束，这一点无法在此处表达。
</ParamField>

<ParamField body="multi_prompt[].duration" type="integer">
  该镜头的时长，单位为秒，1-15。所有镜头的时长之和不得超过任务的总时长，这一点 JSON Schema 无法表达；由 Higgsfield 强制执行。

  范围：`1` 到 `15`
</ParamField>

<ParamField body="multi_prompt[].prompt" type="string">
  该镜头的提示词。Higgsfield 文档说明上限为 512 个字符。
</ParamField>

<ParamField body="multi_shots" type="boolean" default="false">
  启用多镜头序列。为 true 时，`multi_prompt` 承载各个镜头的定义。
</ParamField>

<ParamField body="prompt" type="string">
  描述期望输出的提示词。Higgsfield 文档说明 Kling 最多使用前 2,500 个字符，超出部分会被截断，因此这里未声明 maxLength：较长的内容会被接受，而不是被拒绝。
</ParamField>

<ParamField body="sound" type="string" default="&quot;on&quot;">
  启用或禁用生成的声音。

  可选值：`on`、`off`
</ParamField>

根据 Router 在 `GET /v2/models/higgsfield/higgsfield-kling-3-4k/openapi.json` 提供的 schema 生成，该文档与 Router 在请求到达提供商之前用于校验调用的文档相同。

### 输出

<ResponseField name="cancel_url" type="string">
  用于在处理开始之前取消请求的绝对 URL。
</ResponseField>

<ResponseField name="error" type="string">
  失败详情，当 `status` 为 `failed` 时设置。
</ResponseField>

<ResponseField name="request_id" type="string" required>
  用于轮询、取消和支持的稳定标识符。
</ResponseField>

<ResponseField name="status" type="string" required>
  当前请求状态。

  可选值：`queued`、`in_progress`、`nsfw`、`failed`、`completed`、`canceled`
</ResponseField>

<ResponseField name="status_url" type="string">
  用于轮询直到请求达到终端状态的绝对 URL。
</ResponseField>

<ResponseField name="video" type="object">
  一个 Higgsfield 媒体输出。
</ResponseField>

<ResponseField name="video.url" type="string" required>
  已生成的媒体的下载 URL。
</ResponseField>

## 示例

### 输入

```json
{
  "aspect_ratio": "16:9",
  "cfg_scale": 0.5,
  "duration": 5,
  "prompt": "A cinematic glass pavilion in a misty pine forest at sunrise",
  "sound": "on"
}
```

### 输出

```json
{
  "cancel_url": "https://api.higgsfield.ai/requests/4b1f8ac2-0d35-4f2e-9c77-1e5a6b0c93d4/cancel",
  "request_id": "4b1f8ac2-0d35-4f2e-9c77-1e5a6b0c93d4",
  "status": "completed",
  "status_url": "https://api.higgsfield.ai/requests/4b1f8ac2-0d35-4f2e-9c77-1e5a6b0c93d4/status",
  "video": {
    "url": "https://example.invalid/higgsfield/higgsfield-kling-3-4k/generated.mp4"
  }
}
```


<RouterCodeFooter />
