---
title: "通过 Comfy Router 使用 LTX 2.5 Fast"
description: "通过 Comfy Router 调用 ltx/ltx-2-5-fast：端点、请求形状以及 Router 返回的响应。"
sidebarTitle: "LTX 2.5 Fast"
translationSourceHash: 1861291c
translationFrom: development/comfy-router/models/ltx/ltx-2-5-fast/code.mdx
---

{/* GENERATED FILE. Generated from router-schemas/ltx/ltx-2-5-fast.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/zh/comfy-router/model-code-footer.mdx";

`ltx/ltx-2-5-fast` 的 API 参考，由 Comfy Router 通过 LTX 提供。

## 请求设置

在[你的 Comfy 工作区](https://platform.comfy.org/profile/api-keys)中创建密钥，并将其导出为 `COMFY_API_KEY`。对于 Python，运行 `pip install comfy-sdk`。对于 TypeScript，运行 `npm install @comfyorg/sdk`。cURL 使用原始 HTTP。

**模型 ID：** `ltx/ltx-2-5-fast`

**端点：** `POST https://api.comfy.org/v2/models/ltx/ltx-2-5-fast`

<Note>
Router 尚未为此模型发布编写的输入 schema，因此没有可用于生成代码片段的请求示例。Router 会将请求体原样转发给 LTX：请根据 LTX 自身的 API 文档构建请求体，然后使用 [Router 快速入门](/zh/development/comfy-router/quickstart) 发送。
</Note>

## Schema

### 输入

<Note>
Router 尚未为此模型发布编写的输入 schema：`GET /v2/models/ltx/ltx-2-5-fast/openapi.json` 返回一个带有 `x-comfy-input-schema-authored: false` 的开放式对象。Router 会将请求体原样转发给 LTX，因此 LTX 自身的 API 文档是请求字段的权威依据，Router 不会执行特定于模型的输入验证。提供商的验证仍然适用。
</Note>

### 输出

<ResponseField name="completed_at" type="string">
  任务完成时间戳（ISO 8601）
</ResponseField>

<ResponseField name="created_at" type="string">
  任务创建时间戳（ISO 8601）
</ResponseField>

<ResponseField name="error" type="object">
  当状态为失败时存在
</ResponseField>

<ResponseField name="error.message" type="string">
   
</ResponseField>

<ResponseField name="error.type" type="string">
   
</ResponseField>

<ResponseField name="id" type="string">
  唯一任务标识符
</ResponseField>

<ResponseField name="result" type="object">
  当状态为已完成时存在；输出 URL 在完成后 24 小时过期
</ResponseField>

<ResponseField name="result.video_url" type="string">
  已生成视频的 URL
</ResponseField>

<ResponseField name="status" type="string">
  任务状态（待定、处理中、已完成、失败）
</ResponseField>

## 示例

### 输出

```json
{
  "completed_at": "2026-01-01T00:02:10Z",
  "created_at": "2026-01-01T00:00:00Z",
  "id": "3f7a1b28-5c0d-4e91-8a6f-1b2c3d4e5f60",
  "result": {
    "video_url": "https://example.invalid/ltx/generated.mp4"
  },
  "status": "completed"
}
```


<RouterCodeFooter />