---
title: "使用 Comfy Router 调用 LTX 2.5 Pro"
description: "通过 Comfy Router 调用 ltx/ltx-2-5-pro：端点、请求结构以及 Router 返回的响应。"
sidebarTitle: "LTX 2.5 Pro"
translationSourceHash: 29caa5e3
translationFrom: development/comfy-router/models/ltx/ltx-2-5-pro/code.mdx
---

{/* GENERATED FILE. Generated from router-schemas/ltx/ltx-2-5-pro.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/zh/comfy-router/model-code-footer.mdx";

`ltx/ltx-2-5-pro` 的 API 参考，由 Comfy Router 从 LTX 提供。

## 请求设置

在[你的 Comfy 工作区](https://platform.comfy.org/profile/api-keys)中创建密钥，并将其导出为 `COMFY_API_KEY`。Python 请运行 `pip install comfy-sdk`。TypeScript 请运行 `npm install @comfyorg/sdk`。cURL 使用原始 HTTP。

**模型 ID：** `ltx/ltx-2-5-pro`

**端点：** `POST https://api.comfy.org/v2/models/ltx/ltx-2-5-pro`

<Note>
Router 尚未为此模型发布编写的输入 schema，因此没有可用于生成代码片段的请求示例。Router 会将请求体原样转发给 LTX：请根据 LTX 自己的 API 文档构建请求体，然后使用 [Router 快速开始](/zh/development/comfy-router/quickstart)发送。
</Note>

## Schema

### 输入

<Note>
Router 尚未为此模型发布编写的输入 schema：`GET /v2/models/ltx/ltx-2-5-pro/openapi.json` 返回一个带有 `x-comfy-input-schema-authored: false` 的开放对象。Router 会将请求体原样转发给 LTX，因此 LTX 自己的 API 文档才是请求字段的权威依据，Router 不会执行特定于模型的输入验证。提供商验证仍然适用。
</Note>

### 输出

<ResponseField name="completed_at" type="string">
  任务完成时间戳（ISO 8601）
</ResponseField>

<ResponseField name="created_at" type="string">
  任务创建时间戳（ISO 8601）
</ResponseField>

<ResponseField name="error" type="object">
  当状态为 failed 时存在
</ResponseField>

<ResponseField name="error.message" type="string">
   
</ResponseField>

<ResponseField name="error.type" type="string">
   
</ResponseField>

<ResponseField name="id" type="string">
  唯一任务标识符
</ResponseField>

<ResponseField name="result" type="object">
  当状态为 completed 时存在；输出 URL 在完成后 24 小时过期
</ResponseField>

<ResponseField name="result.video_url" type="string">
  已生成视频的 URL
</ResponseField>

<ResponseField name="status" type="string">
  任务状态（pending、processing、completed、failed）
</ResponseField>

## 示例

### 输出

```json
{
  "completed_at": "2026-01-01T00:02:10Z",
  "created_at": "2026-01-01T00:00:00Z",
  "id": "3f7a1b28-5c0d-4e91-8a6f-1b2c3d4e5f60",
  "result": {
    "video_url": "https://example.invalid/ltx/generated.mp4"
  },
  "status": "completed"
}
```


<RouterCodeFooter />