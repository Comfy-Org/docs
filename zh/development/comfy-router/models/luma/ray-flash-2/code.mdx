---
title: "在 Comfy Router 中使用 Ray Flash 2"
description: "通过 Comfy Router 调用 luma/ray-flash-2：端点、请求形状以及 Router 返回的响应。"
sidebarTitle: "Ray Flash 2"
translationSourceHash: be36d17b
translationFrom: development/comfy-router/models/luma/ray-flash-2/code.mdx
translationBlockHashes:
  "_intro": 1fb59d13
  "Quick start": ff9e17ce
  "Schema": 8009af73
  "Examples": f6194e6a
---
{/* GENERATED FILE. Generated from router-schemas/luma/ray-flash-2.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/zh/comfy-router/model-code-footer.mdx";

`luma/ray-flash-2` 的 API 参考，由 Comfy Router 从 Luma 提供。

## 快速开始

在[你的 Comfy 工作区](https://platform.comfy.org/profile/api-keys)中创建一个密钥，并将其导出为 `COMFY_API_KEY`。Python 和 TypeScript 片段使用 Comfy SDK（`pip install comfy-sdk`、`npm install @comfyorg/sdk`）；cURL 片段则是通过原始 HTTP 发出的同一调用。

**模型 ID：** `luma/ray-flash-2`

**端点：** `POST https://api.comfy.org/v2/models/luma/ray-flash-2`

<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "luma/ray-flash-2",
        {
            "aspect_ratio": "16:9",
            "duration": "5s",
            "prompt": "a single red maple leaf resting on a plain white background",
            "resolution": "540p",
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const { data } = await comfy.models.run("luma/ray-flash-2", {
  aspect_ratio: "16:9",
  duration: "5s",
  prompt: "a single red maple leaf resting on a plain white background",
  resolution: "540p",
});

console.log(data);
```

```bash cURL
curl https://api.comfy.org/v2/models/luma/ray-flash-2 \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"aspect_ratio\": \"16:9\", \"duration\": \"5s\", \"prompt\": \"a single red maple leaf resting on a plain white background\", \"resolution\": \"540p\"}"
```
</CodeGroup>

## Schema

### 输入

<ParamField body="aspect_ratio" type="string" required default="&quot;16:9&quot;">
  生成任务的宽高比

  可能的值：`1:1`、`16:9`、`9:16`、`4:3`、`3:4`、`21:9`、`9:21`
</ParamField>

<ParamField body="callback_url" type="string (uri)">
  生成任务的回调 URL。当生成任务处于 dreaming、已完成或失败状态时，会向该回调 URL 发送一个携带 Generation 对象的 POST 请求

  格式：`uri`
</ParamField>

<ParamField body="duration" type="`5s`, `9s` | string" required>
   
</ParamField>

<ParamField body="generation_type" type="string" default="&quot;video&quot;">
  可能的值：`video`
</ParamField>

<ParamField body="keyframes" type="object">
  生成任务的关键帧
</ParamField>

<ParamField body="keyframes.frame0" type="object">
  关键帧可以是 Generation 引用、Image 或 Video
</ParamField>

<ParamField body="keyframes.frame1" type="object">
  关键帧可以是 Generation 引用、Image 或 Video
</ParamField>

<ParamField body="loop" type="boolean">
  是否循环播放视频
</ParamField>

<ParamField body="model" type="string">
  用于生成任务的视频模型。在 Comfy Router 路由 `POST /v2/models/luma/{model}` 上，该字段由路径提供，且不得发送；在 v1 的 `POST /proxy/luma/generations` 路由上，该字段为必填，且被限制为 LumaVideoModel 枚举（`ray-2`、`ray-flash-2`）。
</ParamField>

<ParamField body="prompt" type="string" required>
  生成任务的提示词
</ParamField>

<ParamField body="resolution" type="`540p`, `720p`, `1080p`, `4k` | string" required>
   
</ParamField>

由 Router 在 `GET /v2/models/luma/ray-flash-2/openapi.json` 提供的 schema 生成，这也是请求到达提供商之前 Router 用来校验调用的同一份文档。

### 输出

<ResponseField name="assets" type="object">
  生成任务的资产
</ResponseField>

<ResponseField name="assets.image" type="string (uri)">
  图像的 URL

  格式：`uri`
</ResponseField>

<ResponseField name="assets.progress_video" type="string (uri)">
  进度视频的 URL

  格式：`uri`
</ResponseField>

<ResponseField name="assets.video" type="string (uri)">
  视频的 URL

  格式：`uri`
</ResponseField>

<ResponseField name="created_at" type="string (date-time)">
  生成任务的创建日期和时间

  格式：`date-time`
</ResponseField>

<ResponseField name="failure_reason" type="string">
  生成任务所处状态的原因
</ResponseField>

<ResponseField name="generation_type" type="string">
  可能的值：`video`、`image`
</ResponseField>

<ResponseField name="id" type="string (uuid)">
  生成任务的 ID

  格式：`uuid`
</ResponseField>

<ResponseField name="model" type="string">
  用于生成任务的模型
</ResponseField>

<ResponseField name="request" type="object">
  生成任务的请求
</ResponseField>

<ResponseField name="state" type="string">
  生成任务的状态

  可能的值：`queued`、`dreaming`、`completed`、`failed`
</ResponseField>

## 示例

### 输入

```json
{
  "aspect_ratio": "16:9",
  "duration": "5s",
  "prompt": "a single red maple leaf resting on a plain white background",
  "resolution": "540p"
}
```

### 输出

```json
{
  "assets": {
    "video": "https://example.invalid/luma/ray-2/generated.mp4"
  },
  "created_at": "2027-01-01T00:00:00Z",
  "generation_type": "video",
  "id": "3f2a7c1e-8b40-4d59-9f6a-1c2d3e4f5a60",
  "model": "ray-flash-2",
  "state": "completed"
}
```


<RouterCodeFooter />
