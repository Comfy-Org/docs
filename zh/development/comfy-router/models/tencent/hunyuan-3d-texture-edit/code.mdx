---
title: "使用 Comfy Router 调用混元3D 纹理编辑"
description: "通过 Comfy Router 调用 tencent/hunyuan-3d-texture-edit：端点、请求结构以及 Router 返回的响应。"
sidebarTitle: "混元3D 纹理编辑"
translationSourceHash: e6f39b12
translationFrom: development/comfy-router/models/tencent/hunyuan-3d-texture-edit/code.mdx
translationBlockHashes:
  "_intro": d89c092e
  "Quick start": ccaf8e6a
  "Schema": e475b723
  "Examples": d9b41615
---

{/* 已生成文件。由 `pnpm code-pages:gen` 从 router-schemas/tencent/hunyuan-3d-texture-edit.json 生成。 */}

import RouterCodeFooter from "/snippets/zh/comfy-router/model-code-footer.mdx";

`tencent/hunyuan-3d-texture-edit` 的 API 参考，由 Comfy Router 从腾讯提供。

## 快速开始

在 [你的 Comfy 工作区](https://platform.comfy.org/profile/api-keys?onboarding=router) 中创建密钥，并将其导出为 `COMFY_API_KEY`。Python、TypeScript 与 Swift 代码片段使用 Comfy SDK（`pip install comfy-sdk`、`npm install @comfyorg/sdk`，以及 [`ComfySwiftSDK`](https://github.com/Comfy-Org/comfy-swift-sdk) Swift 包）；cURL 代码片段则是通过原始 HTTP 发起的相同调用。

**模型 ID：** `tencent/hunyuan-3d-texture-edit`

**端点：** `POST https://api.comfy.org/v2/models/tencent/hunyuan-3d-texture-edit`

<Tabs>
  <Tab title="等待结果">
<CodeGroup>
```python Python
from comfy_sdk import Comfy

# 从环境变量中读取 COMFY_API_KEY。
# SDK 会自动生成幂等键，并在自动重试时复用它。
with Comfy() as client:
    result = client.models.run(
        "tencent/hunyuan-3d-texture-edit",
        {
            "File3D": {
                "Type": "FBX",
                "Url": "https://vcg-test-1258344699.cos.ap-guangzhou.myqcloud.com/test/3d/test.fbx",
            },
            "Prompt": "a kitten",
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// 从环境变量中读取 COMFY_API_KEY。
// SDK 会自动生成幂等键，并在自动重试时复用它。
const { data } = await comfy.models.run("tencent/hunyuan-3d-texture-edit", {
  File3D: {
    Type: "FBX",
    Url: "https://vcg-test-1258344699.cos.ap-guangzhou.myqcloud.com/test/3d/test.fbx",
  },
  Prompt: "a kitten",
});

console.log(data);
```

```swift Swift
import Foundation
import ComfySwiftSDK

// 从环境变量中读取 COMFY_API_KEY。
// SDK 会在每次调用时生成幂等键，并在自动重试时复用它。
let client = ComfyCloudClient(apiKey: ProcessInfo.processInfo.environment["COMFY_API_KEY"]!)
let result = try await client.models.run(
    "tencent/hunyuan-3d-texture-edit",
    input: [
        "File3D": [
            "Type": "FBX",
            "Url": "https://vcg-test-1258344699.cos.ap-guangzhou.myqcloud.com/test/3d/test.fbx",
        ],
        "Prompt": "a kitten",
    ]
)

print(result.output)
```

```bash cURL
curl https://api.comfy.org/v2/models/tencent/hunyuan-3d-texture-edit \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"File3D\": {\"Type\":\"FBX\",\"Url\":\"https://vcg-test-1258344699.cos.ap-guangzhou.myqcloud.com/test/3d/test.fbx\"}, \"Prompt\": \"a kitten\"}"
```
</CodeGroup>
  </Tab>
  <Tab title="排队并稍后收集">
将相同的请求体发送到 `POST https://api.comfy.org/v2/models/tencent/hunyuan-3d-texture-edit/requests`。运行被受理后，Router 会立即返回 `201` 和 `request_id`；结果就绪后，可以从本进程或其他进程收集。状态、取消与收集的细节见 [队列投递](/zh/development/comfy-router/queue)。

<CodeGroup>
```python Python
from comfy_sdk import Comfy

# 从环境变量中读取 COMFY_API_KEY。
# 每次调用 submit() 都会生成自己的 Idempotency-Key，并在自动重试时复用它。
with Comfy() as client:
    handle = client.models.submit(
        "tencent/hunyuan-3d-texture-edit",
        {
            "File3D": {
                "Type": "FBX",
                "Url": "https://vcg-test-1258344699.cos.ap-guangzhou.myqcloud.com/test/3d/test.fbx",
            },
            "Prompt": "a kitten",
        },
    )
    print("request_id:", handle.request_id)  # 再加上模型 ID，就是另一个进程所需的全部信息

    # 轮询直到请求完成，按服务器给出的 Retry-After 等待。
    for update in handle.iter_events():
        print(update.status, update.queue_position)

    # 提供商自己的响应体，也就是 models.run() 返回的同一个值。
    # 失败或已取消的请求会在这里抛出带类型的 Router 错误。
    result = handle.get()

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// 从环境变量中读取 COMFY_API_KEY。
// 每次调用 submit() 都会生成自己的 Idempotency-Key，并在自动重试时复用它。
const handle = await comfy.models.submit("tencent/hunyuan-3d-texture-edit", {
  File3D: {
    Type: "FBX",
    Url: "https://vcg-test-1258344699.cos.ap-guangzhou.myqcloud.com/test/3d/test.fbx",
  },
  Prompt: "a kitten",
});
console.log("requestId:", handle.requestId); // 再加上模型 ID，就是另一个进程所需的全部信息

// 轮询直到请求完成，按服务器给出的 Retry-After 等待。
for await (const update of handle.events()) {
  console.log(update.status, update.queuePosition);
}

// 与 models.run() 返回的结果相同。失败或已取消的请求会在这里被拒绝。
const result = await handle.get();

console.log(result.data);
```

```swift Swift
import Foundation
import ComfySwiftSDK

// 从环境变量中读取 COMFY_API_KEY。
// 每次调用 submit() 都会生成自己的 Idempotency-Key，并在自动重试时复用它。
let client = ComfyCloudClient(apiKey: ProcessInfo.processInfo.environment["COMFY_API_KEY"]!)
let handle = try await client.models.submit(
    "tencent/hunyuan-3d-texture-edit",
    input: [
        "File3D": [
            "Type": "FBX",
            "Url": "https://vcg-test-1258344699.cos.ap-guangzhou.myqcloud.com/test/3d/test.fbx",
        ],
        "Prompt": "a kitten",
    ]
)
print("requestId:", handle.requestId)  // 再加上模型 ID，就是另一个进程所需的全部信息

// 轮询直到请求完成，按服务器给出的 Retry-After 等待。
for try await update in handle.events() {
    print(update.state.rawValue, update.queuePosition.map(String.init) ?? "unknown")
}

// 提供商自己的响应体，也就是 models.run() 返回的同一个值。
// 失败或已取消的请求会在这里抛出带类型的 Router 错误。
let result = try await handle.result()

print(result.output)
```

```bash cURL
# 1. 提交。Router 返回 201，并附带 request_id、status_url、response_url 和 cancel_url。
curl https://api.comfy.org/v2/models/tencent/hunyuan-3d-texture-edit/requests \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"File3D\": {\"Type\":\"FBX\",\"Url\":\"https://vcg-test-1258344699.cos.ap-guangzhou.myqcloud.com/test/3d/test.fbx\"}, \"Prompt\": \"a kitten\"}"

# 2. 轮询直到状态为 COMPLETED，按每次响应给出的 Retry-After 秒数等待。
REQUEST_ID="<request_id from the 201 body>"
curl -i https://api.comfy.org/v2/models/tencent/hunyuan-3d-texture-edit/requests/$REQUEST_ID/status \
  -H "X-API-Key: $COMFY_API_KEY"

# 3. 收集。200 表示模型的原始输出；仍在运行时返回 202 及状态响应体。
curl https://api.comfy.org/v2/models/tencent/hunyuan-3d-texture-edit/requests/$REQUEST_ID \
  -H "X-API-Key: $COMFY_API_KEY"
```
</CodeGroup>
  </Tab>
</Tabs>

## Schema

### 输入

<ParamField body="EnablePBR" type="boolean">
  是否启用 PBR 纹理参数；仅在使用 Prompt 时支持。
</ParamField>

<ParamField body="File3D" type="object" required>
  用于纹理编辑的 3D 文件输入
</ParamField>

<ParamField body="File3D.Type" type="string" required>
  3D 文件格式类型

  可选值：`FBX`
</ParamField>

<ParamField body="File3D.Url" type="string (uri)" required>
  需要编辑纹理的 3D 文件的 URL

  格式：`uri`
</ParamField>

<ParamField body="Image" type="object">
  参考图像：Base64 数据或图片网址
</ParamField>

<ParamField body="Image.Base64" type="string">
  Base64 编码的图像。每边分辨率 128-4096，转换后的 Base64 小于 10MB。格式为 jpg、jpeg、png。
</ParamField>

<ParamField body="Image.Url" type="string (uri)">
  图片网址。如果同时提供 Base64 和 Url，以 Url 为准。

  格式：`uri`
</ParamField>

<ParamField body="Prompt" type="string">
  描述纹理编辑。Image 或 Prompt 必须提供其一，两者不能同时提供。
</ParamField>

由 Router 在 `GET /v2/models/tencent/hunyuan-3d-texture-edit/openapi.json` 提供的数据结构生成，这也是请求到达提供商之前 Router 用于校验调用的同一份文档。

### 输出

<ResponseField name="Response" type="object" required>
   
</ResponseField>

<ResponseField name="Response.ErrorCode" type="string">
  腾讯的错误码，无错误时文档标注为空字符串。只要存在 `Status`，Router 就不会读取它。
</ResponseField>

<ResponseField name="Response.ErrorMessage" type="string">
  腾讯的报错信息，无错误时文档标注为空字符串。只要存在 `Status`，Router 就不会读取它。
</ResponseField>

<ResponseField name="Response.RequestId" type="string">
  腾讯自己的请求标识符，用于与腾讯联合排查问题。它既不是任务ID，也不是资源。
</ResponseField>

<ResponseField name="Response.ResultFile3Ds" type="object[]" required>
  已完成任务生成的 3D 文件。在 Router 返回的文档中存在且非空；至少一个条目带有可获取的 `Url`。这些链接属于腾讯，文档标注其有效期为 24 小时。
</ResponseField>

<ResponseField name="Response.ResultFile3Ds[].PreviewImageUrl" type="string (uri)">
  预览图像网址

  格式：`uri`
</ResponseField>

<ResponseField name="Response.ResultFile3Ds[].Type" type="string">
  3D 文件格式

  可选值：`GLB`、`OBJ`
</ResponseField>

<ResponseField name="Response.ResultFile3Ds[].Url" type="string (uri)">
  文件网址（有效期 24 小时）

  格式：`uri`
</ResponseField>

<ResponseField name="Response.Status" type="string" required>
  可选值：`DONE`
</ResponseField>

## 示例

### 输入

```json
{
  "File3D": {
    "Type": "FBX",
    "Url": "https://vcg-test-1258344699.cos.ap-guangzhou.myqcloud.com/test/3d/test.fbx"
  },
  "Prompt": "a kitten"
}
```

### 输出

```json
{
  "Response": {
    "ErrorCode": "",
    "ErrorMessage": "",
    "RequestId": "9a1c0d4e-77b2-4e0c-8f2e-2f9a1c0d4e77",
    "ResultFile3Ds": [
      {
        "PreviewImageUrl": "https://example.invalid/tencent/hunyuan-3d-texture-edit/preview.png",
        "Type": "GLB",
        "Url": "https://example.invalid/tencent/hunyuan-3d-texture-edit/retextured.glb"
      }
    ],
    "Status": "DONE"
  }
}
```

<RouterCodeFooter />
