---
title: "将 Flashvsr 与 Comfy Router 配合使用"
description: "通过 Comfy Router 调用 wavespeed/flashvsr：端点、请求形状以及 Router 返回的响应。"
sidebarTitle: "Flashvsr"
translationSourceHash: ff5118d0
translationFrom: development/comfy-router/models/wavespeed/flashvsr/code.mdx
---

{/* GENERATED FILE. Generated from router-schemas/wavespeed/flashvsr.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/zh/comfy-router/model-code-footer.mdx";

由 Comfy Router 从 WaveSpeed 提供的 `wavespeed/flashvsr` API 参考。

## 快速开始

在[你的 Comfy 工作区](https://platform.comfy.org/profile/api-keys)中创建密钥，并将其导出为 `COMFY_API_KEY`。Python 和 TypeScript 代码片段使用 Comfy SDK（`pip install comfy-sdk`、`npm install @comfyorg/sdk`）；cURL 代码片段是通过原始 HTTP 发出的同一调用。

**模型 ID：** `wavespeed/flashvsr`

**端点：** `POST https://api.comfy.org/v2/models/wavespeed/flashvsr`

<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "wavespeed/flashvsr",
        {
            "duration": 4,
            "target_resolution": "1080p",
            "video": "https://samplelib.com/mp4/sample-30s.mp4",
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const { data } = await comfy.models.run("wavespeed/flashvsr", {
  duration: 4,
  target_resolution: "1080p",
  video: "https://samplelib.com/mp4/sample-30s.mp4",
});

console.log(data);
```

```bash cURL
curl https://api.comfy.org/v2/models/wavespeed/flashvsr \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"duration\": 4, \"target_resolution\": \"1080p\", \"video\": \"https://samplelib.com/mp4/sample-30s.mp4\"}"
```
</CodeGroup>

## 模式

### 输入

<ParamField body="duration" type="number" required>
  视频的时长，单位为秒
</ParamField>

<ParamField body="target_resolution" type="string" default="&quot;1080p&quot;">
  要 upscale 到的目标分辨率。

  可选值：`720p`、`1080p`、`2k`、`4k`
</ParamField>

<ParamField body="video" type="string" required>
  要 upscale 的视频。可以是视频文件的 URL，也可以是 base64 编码的视频。
</ParamField>

由 Router 在 `GET /v2/models/wavespeed/flashvsr/openapi.json` 提供的模式生成，该文档与请求到达提供商之前用于校验调用的文档相同。

### 输出

Router 不为此模型发布输出模式。

## 示例

### 输入

```json
{
  "duration": 4,
  "target_resolution": "1080p",
  "video": "https://samplelib.com/mp4/sample-30s.mp4"
}
```


<RouterCodeFooter />