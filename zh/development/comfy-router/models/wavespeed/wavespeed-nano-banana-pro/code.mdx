---
title: "搭配 Comfy Router 使用 Wavespeed Nano Banana Pro"
description: "通过 Comfy Router 调用 wavespeed/wavespeed-nano-banana-pro：端点、请求形状以及 Router 返回的响应。"
sidebarTitle: "Wavespeed Nano Banana Pro"
translationSourceHash: 8e38cb95
translationFrom: development/comfy-router/models/wavespeed/wavespeed-nano-banana-pro/code.mdx
---

{/* 已生成文件。由 `pnpm code-pages:gen` 从 router-schemas/wavespeed/wavespeed-nano-banana-pro.json 生成。 */}

import RouterCodeFooter from "/snippets/zh/comfy-router/model-code-footer.mdx";

`wavespeed/wavespeed-nano-banana-pro` 的 API 参考，由 Comfy Router 从 WaveSpeed 提供服务。

## 请求设置

在[你的 Comfy 工作区](https://platform.comfy.org/profile/api-keys)中创建一个密钥，并将其导出为 `COMFY_API_KEY`。Python 请运行 `pip install comfy-sdk`。TypeScript 请运行 `npm install @comfyorg/sdk`。cURL 使用原始 HTTP。

**模型 ID:** `wavespeed/wavespeed-nano-banana-pro`

**端点:** `POST https://api.comfy.org/v2/models/wavespeed/wavespeed-nano-banana-pro`

<Note>
Router 尚未为该模型发布编写的输入 schema，因此没有可用于生成代码片段的请求示例。Router 会将请求体原样转发给 WaveSpeed：请根据 WaveSpeed 自身的 API 文档构建请求体，然后使用 [Router 快速入门](/zh/development/comfy-router/quickstart) 发送。
</Note>

## Schema

### 输入

<Note>
Router 尚未为该模型发布编写的输入 schema：`GET /v2/models/wavespeed/wavespeed-nano-banana-pro/openapi.json` 返回一个带有 `x-comfy-input-schema-authored: false` 的开放对象。Router 会将请求体原样转发给 WaveSpeed，因此 WaveSpeed 自身的 API 文档是请求字段的权威依据，并且 Router 不会执行针对具体模型的输入验证。提供商的验证仍然适用。
</Note>

### 输出

_此 schema 未声明具名属性。_

<RouterCodeFooter />