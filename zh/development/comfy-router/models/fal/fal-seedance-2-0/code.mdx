---
title: "将 Fal Seedance 2.0 与 Comfy Router 配合使用"
description: "通过 Comfy Router 调用 fal/fal-seedance-2.0：端点、请求结构，以及 Router 返回的响应。"
sidebarTitle: "Fal Seedance 2.0"
translationSourceHash: 70e76437
translationFrom: development/comfy-router/models/fal/fal-seedance-2-0/code.mdx
---

{/* 已生成文件。由 `pnpm code-pages:gen` 从 router-schemas/fal/fal-seedance-2.0.json 生成。 */}

import RouterCodeFooter from "/snippets/zh/comfy-router/model-code-footer.mdx";

`fal/fal-seedance-2.0` 的 API 参考，由 Comfy Router 从 fal 提供服务。

## 请求设置

在[你的 Comfy 工作区](https://platform.comfy.org/profile/api-keys)中创建密钥，并将其导出为 `COMFY_API_KEY`。对于 Python，运行 `pip install comfy-sdk`。对于 TypeScript，运行 `npm install @comfyorg/sdk`。cURL 使用原始 HTTP。

**模型 ID：** `fal/fal-seedance-2.0`

**端点：** `POST https://api.comfy.org/v2/models/fal/fal-seedance-2.0`

<Note>
Router 尚未为此模型发布编写好的输入 schema，因此没有可用于生成代码片段的请求示例。Router 会将请求体原样转发给 fal：请根据 fal 自己的 API 文档构建请求体，然后使用 [Router 快速入门](/zh/development/comfy-router/quickstart)发送请求。
</Note>

## Schema

### 输入

<Note>
Router 尚未为此模型发布编写好的输入 schema：`GET /v2/models/fal/fal-seedance-2.0/openapi.json` 返回一个开放对象，其中 `x-comfy-input-schema-authored: false`。Router 会将请求体原样转发给 fal，因此 fal 自己的 API 文档才是请求字段的权威依据，Router 不会执行针对该模型的输入验证。提供商的验证仍然适用。
</Note>

### 输出

_该 schema 未声明任何具名属性。_

<RouterCodeFooter />