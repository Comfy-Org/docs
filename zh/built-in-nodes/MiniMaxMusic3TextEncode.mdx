---
title: "MiniMaxMusic3TextEncode - ComfyUI Built-in Node Documentation"
description: "MiniMax Music3 Text Encode 使用 MiniMax Music3 CLIP 模型将文本描述和歌词转换为用于音乐生成的声学条件序列。"
sidebarTitle: "MiniMaxMusic3TextEncode"
icon: "circle"
mode: wide
---

MiniMax Music3 Text Encode 使用 MiniMax Music3 CLIP 模型将文本描述和歌词转换为用于音乐生成的声学条件序列。该节点返回生成的 CONDITIONING 数据，以及根据输入的最大时长计算出的实际音频时长（以秒为单位）。

## 输入

| 参数 | 描述 | 数据类型 | 必填 | 范围 |
|-----------|-------------|-----------|----------|-------|
| `clip` | 用于文本编码和条件序列生成的 MiniMax Music3 CLIP 模型。 | CLIP | 是 | - |
| `caption` | 描述要生成的音乐的文本。支持多行文本和动态提示词。 | STRING | 是 | - |
| `歌词` | 用于生成音乐的歌词文本。支持多行文本和动态提示词。 | STRING | 是 | - |
| `种子` | 用于生成过程的可复现随机种子。默认：0。提供了一个生成后可控制的 widget。 | INT | 是 | 0 到 18446744073709551615 (0xffffffffffffffff) |
| `最大时长` | 最大时长（秒）；模型可以提前结束歌曲。默认：120.0。 | FLOAT | 是 | 0.04 到模型的最大音频时长 (MAX_AUDIO_FRAMES / AUDIO_FRAMES_PER_SECOND)，步长 0.04 |
| `cfg 缩放` | 无分类器引导比例。默认：模型常量 CFG_SCALE。高级参数。 | FLOAT | 是 | 0.0 到 100.0，步长 0.1（保留 2 位小数） |
| `top_k` | 用于声学 token 选择的 Top-k 采样值。默认：模型常量 CFG_TOP_K。高级参数。 | INT | 是 | 1 到模型的词表大小 (C0_VOCAB_SIZE) |

## 输出

| 输出名称 | 描述 | 数据类型 |
|-------------|-------------|-----------|
| `conditioning` | 生成的条件序列，用于引导后续音乐生成。 | CONDITIONING |
| `seconds` | 条件序列的实际时长（秒）。 | FLOAT |

> 本文档由 AI 生成。如果您发现任何错误或有改进建议，欢迎贡献！ [在 GitHub 上编辑](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/MiniMaxMusic3TextEncode/zh.md)

---
**Source fingerprint (SHA-256):** `c3fbfd189d0358ebf081dd4f9c32be9231a9d0b97fd767401ea4b7955224c25c`
