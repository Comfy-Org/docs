---
title: "LoadLatent - ComfyUI Built-in Node Documentation"
description: "LoadLatent 节点从输入目录中的 .latent 文件加载先前保存的潜在表示。"
sidebarTitle: "LoadLatent"
icon: "circle"
mode: wide
---

LoadLatent 节点从输入目录中的 .latent 文件加载先前保存的潜在表示。它会从所选文件中读取潜在张量数据，并在返回潜在数据以供其他节点使用之前应用必要的缩放调整。

## 输入
| Parameter | Description | Data Type | Required | Range |
| --- | --- | --- | --- | --- |
| `Latent` | 从输入目录中的可用文件里选择要加载的 .latent 文件 | COMBO | 是 | 输入目录中的所有 .latent 文件（动态列表，按字母顺序排序） |

注意：可用文件列表是动态生成的，仅包含输入目录中存在的以 .latent 结尾的文件。如果所选文件不再存在，该节点会将其报告为无效的潜在文件。

## 输出
| Output Name | Description | Data Type |
| --- | --- | --- |
| `LATENT` | 返回从所选文件加载的潜在表示数据，作为浮点张量。如果文件不包含 `latent_format_version_0` 标记，则张量在返回前会按 1/0.18215 缩放；包含该标记的文件则按其存储的缩放比例返回。 | LATENT |

> 本文档由 AI 生成。如果您发现任何错误或有改进建议，欢迎贡献！ [在 GitHub 上编辑](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/LoadLatent/zh.md)

---
**Source fingerprint (SHA-256):** `0938214361687a3a98e03878b8cbc0240100cbeacc0b157c4a299e59e7728a13`
