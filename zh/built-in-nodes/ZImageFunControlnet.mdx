---
title: "ZImageFunControlnet - ComfyUI Built-in Node Documentation"
description: "ZImageFunControlnet 将控制网络补丁应用于基础模型，以便其能够引导图像生成或编辑过程。"
sidebarTitle: "ZImageFunControlnet"
icon: "circle"
mode: wide
---

ZImageFunControlnet 将控制网络补丁应用于基础模型，以便其能够引导图像生成或编辑过程。它组合模型、模型补丁和 VAE，并允许你控制控制效果对结果的影响强度。可选的图像、修复图像和蒙版输入允许进行更有针对性的编辑。

## 输入

| 参数 | 描述 | 数据类型 | 必需 | 范围 |
| --- | --- | --- | --- | --- |
| `模型` | 用于生成过程的基础模型。 | MODEL | 是 | - |
| `模型补丁` | 应用控制网络引导的专用补丁模型。 | MODEL_PATCH | 是 | - |
| `VAE` | 用于编码和解码图像的变分自编码器。 | VAE | 是 | - |
| `强度` | 控制网络影响的强度。正值会应用该效果，而负值可将其反转（默认值：1.0）。 | FLOAT | 是 | -10.0 到 10.0 （步长：0.01） |
| `图像` | 可选的基图像，用于引导生成过程。 | IMAGE | 否 | - |
| `重绘图像` | 可选图像，专门用于由蒙版定义的修复区域。 | IMAGE | 否 | - |
| `遮罩` | 可选蒙版，定义图像中应被编辑或修复的区域。 | MASK | 否 | - |

**注意：** `inpaint_image` 参数通常与 `mask` 一起使用，以指定用于修复的内容。节点的行为可能会根据提供了哪些可选输入而变化（例如，使用 `image` 进行引导，或使用 `image`、`mask` 和 `inpaint_image` 进行修复）。

## 输出

| 输出名称 | 描述 | 数据类型 |
| --- | --- | --- |
| `model` | 已应用控制网络补丁的模型，可直接用于采样流程。 | MODEL |
| `positive` | 正向条件，可能会受到控制网络输入的影响而修改。 | CONDITIONING |
| `negative` | 负向条件，可能会受到控制网络输入的影响而修改。 | CONDITIONING |

> 本文档由 AI 生成。如果您发现任何错误或有改进建议，欢迎贡献！ [在 GitHub 上编辑](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/ZImageFunControlnet/zh.md)

---
**Source fingerprint (SHA-256):** `e1946190a06c52dd951078d9cb753962081957cb6c38accdea26eb4129a51793`
