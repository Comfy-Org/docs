---
title: "ZImageFunControlnet - ComfyUI Built-in Node Documentation"
description: "ZImageFunControlnet 应用一种专门的控制网络来影响图像生成或编辑过程。"
sidebarTitle: "ZImageFunControlnet"
icon: "circle"
mode: wide
---

ZImageFunControlnet 应用一种专门的控制网络来影响图像生成或编辑过程。它使用基础模型、模型补丁和 VAE，允许您调整控制效果的强度。此节点可与基础图像、修补图像和遮罩配合使用，以实现更有针对性的编辑。

## 输入

| 参数 | 描述 | 数据类型 | 是否必填 | 范围 |
| --- | --- | --- | --- | --- |
| `模型` | 用于生成过程的基础模型。 | MODEL | 是 | - |
| `模型补丁` | 应用控制网络引导的专门补丁模型。 | MODEL_PATCH | 是 | - |
| `VAE` | 用于编码和解码图像的自编码器（VAE）。 | VAE | 是 | - |
| `强度` | 控制网络影响的强度。正值应用效果，负值可使其反转（默认值：1.0）。 | FLOAT | 是 | -10.0 至 10.0 |
| `图像` | 用于引导生成过程的可选基础图像。 | IMAGE | 否 | - |
| `重绘图像` | 专门用于修补遮罩定义区域的可选图像。 | IMAGE | 否 | - |
| `遮罩` | 可选遮罩，定义图像中应被编辑或修补的区域。 | MASK | 否 | - |

**注意：** `inpaint_image` 参数通常与 `mask` 结合使用，以指定修补内容。节点的行为可能因提供的可选输入不同而变化（例如，使用 `image` 进行引导，或使用 `image`、`mask` 和 `inpaint_image` 进行修补）。

## 输出

| 输出名称 | 描述 | 数据类型 |
| --- | --- | --- |
| `model` | 已应用控制网络补丁的模型，可供采样流程使用。 | MODEL |
| `positive` | 正向条件，可能已被控制网络输入修改。 | CONDITIONING |
| `negative` | 负向条件，可能已被控制网络输入修改。 | CONDITIONING |

> 本文档由 AI 生成。如果您发现任何错误或有改进建议，欢迎贡献！ [在 GitHub 上编辑](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/ZImageFunControlnet/zh.md)

---
**Source fingerprint (SHA-256):** `e1946190a06c52dd951078d9cb753962081957cb6c38accdea26eb4129a51793`
