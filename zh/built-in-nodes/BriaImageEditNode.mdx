---
title: "BriaImageEditNode - ComfyUI Built-in Node Documentation"
description: "Complete documentation for the BriaImageEditNode node in ComfyUI. Learn its inputs, outputs, parameters and usage."
sidebarTitle: "BriaImageEditNode"
icon: "circle"
mode: wide
---

Bria FIBO 图像编辑节点允许您通过文本指令修改现有图像。它会将图像和提示词发送至 Bria API，由 Bria API 使用 FIBO 模型根据您的请求生成新的编辑版本。您还可以提供遮罩，将编辑限制在特定区域。

## 输入

| 参数 | 描述 | 数据类型 | 是否必填 | 范围 |
| --- | --- | --- | --- | --- |
| `model` | 用于图像编辑的模型版本。 | COMBO | 是 | `"FIBO"` |
| `image` | 您想要编辑的输入图像。 | IMAGE | 是 | - |
| `prompt` | 描述如何编辑图像的文本指令（默认：空）。 | STRING | 否 | - |
| `negative_prompt` | 描述您不希望出现在编辑后图像中的文本（默认：空）。 | STRING | 否 | - |
| `structured_prompt` | 包含 JSON 格式的结构化编辑提示词的字符串。使用此参数替代常规提示词，以实现精确、程序化的控制（默认：空）。 | STRING | 否 | - |
| `seed` | 用于初始化随机生成的数字，确保结果可复现（默认：1）。 | INT | 是 | 1 到 2147483647 |
| `guidance_scale` | 控制生成图像遵循提示词的程度。值越高，遵循程度越强（默认：3.0）。 | FLOAT | 是 | 3.0 到 5.0 |
| `steps` | 模型将执行的去噪步数（默认：50）。 | INT | 是 | 20 到 50 |
| `moderation` | 启用或禁用内容审核。选择 `"true"` 会显示针对提示词内容、视觉输入和视觉输出的额外审核选项。 | DYNAMICCOMBO | 是 | `"false"`<br />`"true"` |
| `mask` | 可选的遮罩图像。如果提供，编辑将仅应用于图像的遮罩区域。 | MASK | 否 | - |

**重要约束：**

* 您必须至少提供 `prompt` 或 `structured_prompt` 输入之一。两者不能同时为空。
* 恰好需要一个输入 `image`。
* 当 `moderation` 参数设置为 `"true"` 时，将额外显示三个布尔输入：`prompt_content_moderation`（默认：false）、`visual_input_moderation`（默认：false）和 `visual_output_moderation`（默认：true）。

## 输出

| 输出名称 | 描述 | 数据类型 |
| --- | --- | --- |
| `structured_prompt` | Bria API 返回的编辑后图像。 | IMAGE |
| `structured_prompt` | 编辑过程中使用或生成的结构化提示词。 | STRING |

> 本文档由 AI 生成。如果您发现任何错误或有改进建议，欢迎贡献！ [在 GitHub 上编辑](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/BriaImageEditNode/zh.md)

---
**Source fingerprint (SHA-256):** `30148261f43f5bfd14339f5ff1ec250381a615cc05c67eee21b0a2423ebe349d`
