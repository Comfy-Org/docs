---
title: "Vidu2StartEndToVideoNode - ComfyUI Built-in Node Documentation"
description: "Complete documentation for the Vidu2StartEndToVideoNode node in ComfyUI. Learn its inputs, outputs, parameters and usage."
sidebarTitle: "Vidu2StartEndToVideoNode"
icon: "circle"
mode: wide
---
此文档由 AI 生成。如发现任何错误或有改进建议，欢迎随时贡献！[在 GitHub 上编辑](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/Vidu2StartEndToVideoNode/en.md)

此节点通过在提供的起始帧和结束帧之间进行插值，并在文本提示的引导下生成视频。它使用指定的 Vidu 模型，在设定的时长内创建两幅图像之间的平滑过渡。

## 输入

| 参数 | 描述 | 数据类型 | 是否必填 | 取值范围 |
| --- | --- | --- | --- | --- |
| `模型` | 用于视频生成的 Vidu 模型。 | COMBO | 是 | `"viduq2-pro-fast"`<br />`"viduq2-pro"`<br />`"viduq2-turbo"` |
| `起始帧` | 视频序列的起始图像。仅允许单张图像。 | IMAGE | 是 | - |
| `结束帧` | 视频序列的结束图像。仅允许单张图像。 | IMAGE | 是 | - |
| `提示词` | 指导视频生成的文本描述（最多 2000 个字符）。 | STRING | 是 | - |
| `时长` | 生成视频的时长，单位为秒（默认值：5）。 | INT | 否 | 2 到 8 |
| `种子` | 用于初始化随机生成的数值，以实现可重复的结果（默认值：1）。 | INT | 否 | 0 到 2147483647 |
| `分辨率` | 生成视频的输出分辨率。 | COMBO | 否 | `"720p"`<br />`"1080p"` |
| `运动幅度` | 画面中物体的运动幅度。 | COMBO | 否 | `"auto"`<br />`"small"`<br />`"medium"`<br />`"large"` |

**注意：** `first_frame` 和 `end_frame` 图像必须具有相似的宽高比。节点将验证它们的宽高比是否在 0.8 到 1.25 的相对范围内。

## 输出

| 输出名称 | 描述 | 数据类型 |
| --- | --- | --- |
| `output` | 生成的视频文件。 | VIDEO |

> 本文档由 AI 生成。如果您发现任何错误或有改进建议，欢迎贡献！ [在 GitHub 上编辑](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/Vidu2StartEndToVideoNode/zh.md)

---
**Source fingerprint (SHA-256):** `0a2a125fcb0a519e3aa98ed846f0c7bdc14644a27aaaab3953d55945f787de2a`
