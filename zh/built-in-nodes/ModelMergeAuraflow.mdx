---
title: "ModelMergeAuraflow - ComfyUI Built-in Node Documentation"
description: "Complete documentation for the ModelMergeAuraflow node in ComfyUI. Learn its inputs, outputs, parameters and usage."
sidebarTitle: "ModelMergeAuraflow"
icon: "circle"
mode: wide
---
ModelMergeAuraflow 节点允许您通过调整各个模型组件的特定混合权重，将两个不同的模型融合在一起。它提供了从初始层到最终输出的精细控制，让您能够精确地决定模型不同部分的合并方式。此节点特别适用于创建自定义模型组合，并对合并过程进行精确控制。

## 输入

| 参数 | 描述 | 数据类型 | 是否必填 | 范围 |
| --- | --- | --- | --- | --- |
| `模型1` | 要合并的第一个模型 | MODEL | 是 | - |
| `模型2` | 要合并的第二个模型 | MODEL | 是 | - |
| `init_x_linear.` | 初始线性变换的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `位置编码` | 位置编码组件的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `cond_seq_linear.` | 条件序列线性层的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `注册令牌` | 令牌注册组件的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `t_embedder.` | 时间嵌入组件的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `double_layers.0.` | 双层组 0 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `double_layers.1.` | 双层组 1 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `double_layers.2.` | 双层组 2 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `double_layers.3.` | 双层组 3 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `single_layers.0.` | 单层 0 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `single_layers.1.` | 单层 1 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `single_layers.2.` | 单层 2 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `single_layers.3.` | 单层 3 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `single_layers.4.` | 单层 4 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `single_layers.5.` | 单层 5 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `single_layers.6.` | 单层 6 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `single_layers.7.` | 单层 7 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `single_layers.8.` | 单层 8 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `single_layers.9.` | 单层 9 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `single_layers.10.` | 单层 10 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `single_layers.11.` | 单层 11 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `single_layers.12.` | 单层 12 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `single_layers.13.` | 单层 13 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `single_layers.14.` | 单层 14 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `single_layers.15.` | 单层 15 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `single_layers.16.` | 单层 16 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `single_layers.17.` | 单层 17 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `single_layers.18.` | 单层 18 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `single_layers.19.` | 单层 19 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `single_layers.20.` | 单层 20 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `single_layers.21.` | 单层 21 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `single_layers.22.` | 单层 22 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `single_layers.23.` | 单层 23 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `single_layers.24.` | 单层 24 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `single_layers.25.` | 单层 25 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `single_layers.26.` | 单层 26 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `single_layers.27.` | 单层 27 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `single_layers.28.` | 单层 28 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `single_layers.29.` | 单层 29 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `single_layers.30.` | 单层 30 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `single_layers.31.` | 单层 31 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `modF.` | modF 组件的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `final_linear.` | 最终线性变换的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |

## 输出

| 输出名称 | 描述 | 数据类型 |
| --- | --- | --- |
| `model` | 根据指定的混合权重，结合两个输入模型特征的合并模型 | MODEL |

> 本文档由 AI 生成。如果您发现任何错误或有改进建议，欢迎贡献！ [在 GitHub 上编辑](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/ModelMergeAuraflow/zh.md)

---
**Source fingerprint (SHA-256):** `c4959321bba252eb24c945343198d72f50d6021d4dac9945f94e3eb28f1bc3c9`
