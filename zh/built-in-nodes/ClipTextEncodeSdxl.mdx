---
title: "CLIPTextEncodeSDXL - ComfyUI Built-in Node Documentation"
description: "Complete documentation for the CLIPTextEncodeSDXL node in ComfyUI. Learn its inputs, outputs, parameters and usage."
sidebarTitle: "CLIPTextEncodeSDXL"
icon: "circle"
mode: wide
---

此节点专门用于通过针对 SDXL 架构定制的 CLIP 模型对文本输入进行编码。它采用双编码器系统（CLIP-L 和 CLIP-G）来处理文本描述，从而生成更精确的图像。

## 输入

| 参数 | 描述 | 数据类型 |
| --- | --- | --- |
| `clip` | 用于文本编码的 CLIP 模型实例。 | CLIP |
| `宽度` | 指定图像宽度（像素），默认为 1024。 | INT |
| `高度` | 指定图像高度（像素），默认为 1024。 | INT |
| `裁剪宽` | 裁剪区域的宽度（像素），默认为 0。 | INT |
| `裁剪高` | 裁剪区域的高度（像素），默认为 0。 | INT |
| `目标宽度` | 输出图像的目标宽度，默认为 1024。 | INT |
| `目标高度` | 输出图像的目标高度，默认为 1024。 | INT |
| `文本_g` | 用于整体场景描述的全局文本描述。 | STRING |
| `文本_l` | 用于细节描述的局部文本描述。 | STRING |

## 输出

| 参数 | 描述 | 数据类型 |
| --- | --- | --- |
| `CONDITIONING` | 包含图像生成所需的编码文本和条件信息。 | CONDITIONING |

> 本文档由 AI 生成。如果您发现任何错误或有改进建议，欢迎贡献！ [在 GitHub 上编辑](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/CLIPTextEncodeSDXL/zh.md)
