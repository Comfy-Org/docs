---
title: "GeminiNodeV2 - ComfyUI Built-in Node Documentation"
description: "使用 Google 的 Gemini 模型生成文本响应。"
sidebarTitle: "GeminiNodeV2"
icon: "circle"
mode: wide
---

使用 Google 的 Gemini 模型生成文本响应。提供一个文本提示，并可选择提供一个或多个图像、音频剪辑、视频或文件作为多模态上下文。

## 输入

### 通用输入

| 参数 | 描述 | 数据类型 | 是否必需 | 范围 |
|-----------|-------------|-----------|----------|-------|
| `model` | 用于生成响应的 Gemini 模型。 | DYNAMIC_COMBO | 是 | `"Gemini 3.7 Flash"`<br />`"Gemini 3.5 Flash"`<br />`"Gemini 3.1 Pro"`<br />`"Gemini 3.1 Flash-Lite"` |
| `prompt` | 输入给模型的文本。包含详细的指令、问题或上下文。必须至少包含一个非空白字符。（默认值：""） | STRING | 是 |  |
| `seed` | 采样种子。设置为 0 以使用随机种子。不保证确定性输出。（默认值：42） | INT | 是 | 0 到 2147483647 |
| `system_prompt` | 决定模型行为的基础指令。（默认值：""） | STRING | 否 |  |

### Gemini 3.7 Flash 输入

当 `model` 设置为 `"Gemini 3.7 Flash"` 时，显示以下输入。

| 参数 | 描述 | 数据类型 | 是否必需 | 范围 |
|-----------|-------------|-----------|----------|-------|
| `thinking_level` | 模型在回答前进行内部推理的强度。HIGH 可提高困难任务的质量，但会消耗更多（思考）token，且速度较慢。（默认值："MEDIUM"） | COMBO | 是 | `"LOW"`<br />`"MEDIUM"`<br />`"HIGH"` |
| `temperature` | 控制随机性。较低的值更专注/确定性更强，较高的值更具创造性。（默认值：1.0） | FLOAT | 是 | 0.0 到 2.0 |
| `top_p` | 核采样：从累积概率达到 `top_p` 的最小 token 集合中采样。（默认值：0.95） | FLOAT | 是 | 0.0 到 1.0 |
| `max_output_tokens` | 要生成的最大 token 数，包括模型的内部思考。当 `thinking_level` 为 HIGH 时，较低的值可能没有空间留给回答；如果响应为空或被截断，请调高此值。模型在完成时会提前停止，因此对于简短回复，较高的上限不会产生额外成本。（默认值：32768） | INT | 是 | 16 到 65536 |

### Gemini 3.5 Flash 输入

当 `model` 设置为 `"Gemini 3.5 Flash"` 时，显示以下输入。

| 参数 | 描述 | 数据类型 | 是否必需 | 范围 |
|-----------|-------------|-----------|----------|-------|
| `thinking_level` | 模型在回答前进行内部推理的强度。HIGH 可提高困难任务的质量，但会消耗更多（思考）token，且速度较慢。（默认值："MEDIUM"） | COMBO | 是 | `"MINIMAL"`<br />`"LOW"`<br />`"MEDIUM"`<br />`"HIGH"` |
| `temperature` | 控制随机性。较低的值更专注/确定性更强，较高的值更具创造性。（默认值：1.0） | FLOAT | 是 | 0.0 到 2.0 |
| `top_p` | 核采样：从累积概率达到 `top_p` 的最小 token 集合中采样。（默认值：0.95） | FLOAT | 是 | 0.0 到 1.0 |
| `max_output_tokens` | 要生成的最大 token 数，包括模型的内部思考。当 `thinking_level` 为 HIGH 时，较低的值可能没有空间留给回答；如果响应为空或被截断，请调高此值。模型在完成时会提前停止，因此对于简短回复，较高的上限不会产生额外成本。（默认值：32768） | INT | 是 | 16 到 65536 |

### Gemini 3.1 Pro 输入

当 `model` 设置为 `"Gemini 3.1 Pro"` 时，显示以下输入。

| 参数 | 描述 | 数据类型 | 是否必需 | 范围 |
|-----------|-------------|-----------|----------|-------|
| `thinking_level` | 模型在回答前进行内部推理的强度。HIGH 可提高困难任务的质量，但会消耗更多（思考）token，且速度较慢。（默认值："HIGH"） | COMBO | 是 | `"LOW"`<br />`"HIGH"` |
| `temperature` | 控制随机性。较低的值更专注/确定性更强，较高的值更具创造性。（默认值：1.0） | FLOAT | 是 | 0.0 到 2.0 |
| `top_p` | 核采样：从累积概率达到 `top_p` 的最小 token 集合中采样。（默认值：0.95） | FLOAT | 是 | 0.0 到 1.0 |
| `max_output_tokens` | 要生成的最大 token 数，包括模型的内部思考。当 `thinking_level` 为 HIGH 时，较低的值可能没有空间留给回答；如果响应为空或被截断，请调高此值。模型在完成时会提前停止，因此对于简短回复，较高的上限不会产生额外成本。（默认值：32768） | INT | 是 | 16 到 65536 |

### Gemini 3.1 Flash-Lite 输入

当 `model` 设置为 `"Gemini 3.1 Flash-Lite"` 时，显示以下输入。

| 参数 | 描述 | 数据类型 | 是否必需 | 范围 |
|-----------|-------------|-----------|----------|-------|
| `thinking_level` | 模型在回答前进行内部推理的强度。HIGH 可提高困难任务的质量，但会消耗更多（思考）token，且速度较慢。（默认值："LOW"） | COMBO | 是 | `"LOW"`<br />`"HIGH"` |
| `temperature` | 控制随机性。较低的值更专注/确定性更强，较高的值更具创造性。（默认值：1.0） | FLOAT | 是 | 0.0 到 2.0 |
| `top_p` | 核采样：从累积概率达到 `top_p` 的最小 token 集合中采样。（默认值：0.95） | FLOAT | 是 | 0.0 到 1.0 |
| `max_output_tokens` | 要生成的最大 token 数，包括模型的内部思考。当 `thinking_level` 为 HIGH 时，较低的值可能没有空间留给回答；如果响应为空或被截断，请调高此值。模型在完成时会提前停止，因此对于简短回复，较高的上限不会产生额外成本。（默认值：32768） | INT | 是 | 16 到 65536 |

### 媒体和文件输入

以下输入由所有四个模型共享，并与模型特定输入一起显示。

| 参数 | 描述 | 数据类型 | 是否必需 | 范围 |
|-----------|-------------|-----------|----------|-------|
| `images` | 可扩展插槽：连接 1 到 16 张图像（`image_1` ... `image_16`）。可选，作为模型上下文的图像。最多 16 张图像。 | IMAGE | 否 | 0 到 16 images |
| `audio` | 可扩展插槽：连接一个音频剪辑（`audio_1`）。可选，作为模型上下文的音频剪辑。 | AUDIO | 否 | 0 到 1 clip |
| `video` | 可扩展插槽：连接一个视频剪辑（`video_1`）。可选，作为模型上下文的视频剪辑。 | VIDEO | 否 | 0 到 1 clip |
| `files` | 可选，作为模型上下文的文件。接受来自 Gemini Input Files 节点的输入。 | GEMINI_INPUT_FILES | 否 |  |

**注意：** 当附加媒体（图像、音频或视频）时，节点会将前 10 个媒体项目上传到 ComfyAPI 存储，并以 URL 形式传递；此 URL 配额由所有媒体类型共享，并按顺序消耗（先视频，再音频，最后图像）。其余媒体将以内联 base64 数据编码，最大组合内联负载为 18 MB。如果内联负载超过 18 MB，节点将报错。`prompt` 参数必须至少包含一个非空白字符。将 `seed` 设置为 0 则请求随机种子。

## 输出

| 输出名称 | 描述 | 数据类型 |
|-------------|-------------|-----------|
| `output` | Gemini 模型生成的文本响应。如果模型未生成任何文本，则返回字符串 "Empty response from Gemini model..."。 | STRING |

> 本文档由 AI 生成。如果您发现任何错误或有改进建议，欢迎贡献！ [在 GitHub 上编辑](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/GeminiNodeV2/zh.md)

---
**Source fingerprint (SHA-256):** `00e0f614303fa723eb787ad763e0b0c6322f89abf43d93b697357527b2fae49c`
