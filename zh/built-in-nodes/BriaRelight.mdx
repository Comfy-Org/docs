---
title: "BriaRelight - ComfyUI Built-in Node Documentation"
description: "此节点使用 Bria 改变图像的光照氛围和方向。"
sidebarTitle: "BriaRelight"
icon: "circle"
mode: wide
---

此节点使用 Bria 改变图像的光照氛围和方向。该图像会由 Bria 重新渲染，因此结果不会与输入像素对齐；整个画面会以约 100 万像素重新生成。

## 输入

### 通用输入

| 参数 | 描述 | 数据类型 | 必填 | 范围 |
|-----------|-------------|-----------|----------|-------|
| `图像` | 要改变光照的图像。图像上传前会丢弃任何 alpha 通道。 | IMAGE | 是 | - |
| `light_type` | 要应用的光照氛围。 | COMBO | 是 | `"midday"`<br />`"blue hour light"`<br />`"low-angle sunlight"`<br />`"sunrise light"`<br />`"spotlight on subject"`<br />`"overcast light"`<br />`"soft overcast daylight lighting"`<br />`"cloud-filtered lighting"`<br />`"fog-diffused lighting"`<br />`"moonlight lighting"`<br />`"starlight nighttime"`<br />`"soft bokeh lighting"`<br />`"harsh studio lighting"` |
| `light_direction` | 光线来源方向。诸如 midday、spotlight on subject 和 harsh studio lighting 等硬光氛围对此反应最明显。 | COMBO | 是 | `"front"`<br />`"side"`<br />`"bottom"`<br />`"top-down"` |
| `内容审核` | 审核设置。选择 `"true"` 可显示审核选项，选择 `"false"` 则可在不启用这些选项的情况下运行。 | DYNAMIC_COMBO | 是 | `"false"`<br />`"true"` |

### 审核输入

当 `moderation` 设置为 `"true"` 时，这些选项会出现。

| 参数 | 描述 | 数据类型 | 必填 | 范围 |
|-----------|-------------|-----------|----------|-------|
| `visual_input_moderation` | 对输入图像启用内容审核。默认值：false。 | BOOLEAN | 否 | `true`<br />`false` |
| `visual_output_moderation` | 对生成的输出图像启用内容审核。默认值：false。 | BOOLEAN | 否 | `true`<br />`false` |

注意：Bria 会以约 100 万像素重新渲染整个画面，因此结果与输入并非像素对齐。

## 输出

| 输出名称 | 描述 | 数据类型 |
|-------------|-------------|-----------|
| `image` | Bria 返回的重新打光后的图像。 | IMAGE |
| `structured_prompt` | 编辑后图像的结构化描述，用于通过 Bria FIBO Image Edit 进行后续编辑。 | STRING |

> 本文档由 AI 生成。如果您发现任何错误或有改进建议，欢迎贡献！ [在 GitHub 上编辑](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/BriaRelight/zh.md)

---
**Source fingerprint (SHA-256):** `21fbe2186c99a7e8d99d5659ac25c3ab1a757492916dba4135487d4b293cb4f6`
