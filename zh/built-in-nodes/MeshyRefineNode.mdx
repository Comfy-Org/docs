---
title: "MeshyRefineNode - ComfyUI Built-in Node Documentation"
description: "Meshy: Refine Draft Model 节点获取来自先前 Meshy 任务的 3D 草稿模型并对其进行改进，可选择使用文本提示或参考图像添加纹理。"
sidebarTitle: "MeshyRefineNode"
icon: "circle"
mode: wide
---

Meshy: Refine Draft Model 节点获取来自先前 Meshy 任务的 3D 草稿模型并对其进行改进，可选择使用文本提示或参考图像添加纹理。它会将细化任务提交至 Meshy API，并在任务完成后将成品模型以 GLB 和 FBX 文件形式返回。

## 输入

| 参数 | 描述 | 数据类型 | 是否必填 | 范围 |
|-----------|-------------|-----------|----------|-------|
| `model` | 用于细化草稿模型的 AI 模型。 | COMBO | 是 | `"meshy-7"`<br />`"meshy-6"`<br />`"latest"` |
| `meshy_task_id` | 要细化的草稿模型的唯一任务 ID。 | MESHY_TASK_ID | 是 | - |
| `enable_pbr` | 生成 PBR 贴图（金属度、粗糙度、法线）以及基础颜色。注意：使用雕塑风格时应将其设为 false，因为雕塑风格会生成自己的一套 PBR 贴图。（默认值：False） | BOOLEAN | 是 | - |
| `texture_prompt` | 提供文本提示以引导纹理生成过程。最多 600 个字符。不能与 `texture_image` 同时使用。（默认值：空字符串） | STRING | 是 | - |
| `texture_image` | `texture_image` 和 `texture_prompt` 中只能同时使用一个。 | IMAGE | 否 | - |
| `纹理分辨率` | 基础颜色纹理分辨率。更高的分辨率能捕捉更多表面细节。 | COMBO | 是 | `"2k"`<br />`"4k"`<br />`"8k"` |

**注意：** `texture_prompt` 和 `texture_image` 输入互斥。在同一操作中，不能同时提供文本提示和图像用于纹理生成。

## 输出

| 输出名 | 描述 | 数据类型 |
|-------------|-------------|-----------|
| `模型文件` | 生成的 GLB 模型的文件名。（仅用于向后兼容） | STRING |
| `meshy_task_id` | 已提交细化作业的唯一任务 ID。 | MESHY_TASK_ID |
| `GLB` | 最终细化的 3D 模型，格式为 GLB。 | FILE3DGLB |
| `FBX` | 最终细化的 3D 模型，格式为 FBX。 | FILE3DFBX |

> 本文档由 AI 生成。如果您发现任何错误或有改进建议，欢迎贡献！ [在 GitHub 上编辑](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/MeshyRefineNode/zh.md)

---
**Source fingerprint (SHA-256):** `73c9d712c4fd9fdd2792600ce874916ce9447d386407353c886f624641fa0e0f`
