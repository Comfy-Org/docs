---
title: "ByteDanceFirstLastFrameNode - ComfyUI Built-in Node Documentation"
description: "此节点使用文本提示词以及首帧和尾帧图像来生成视频。"
sidebarTitle: "ByteDanceFirstLastFrameNode"
icon: "circle"
mode: wide
---

此节点使用文本提示词以及首帧和尾帧图像来生成视频。它使用你的描述和这两个关键帧来创建一段在它们之间过渡的完整视频序列。该节点提供各种选项，用于控制视频的分辨率、宽高比、时长和其他生成参数。

## 输入

| 参数 | 描述 | 数据类型 | 必需 | 范围 |
| --- | --- | --- | --- | --- |
| `模型` | 用于视频生成的模型（默认值：`"seedance-1-5-pro-251215"`）。 | COMBO | 是 | `"seedance-1-5-pro-251215"`<br />`"seedance-1-0-pro-250528"` |
| `提示` | 用于生成视频的文本提示词。不得为空，且不得包含保留参数关键词（`resolution`、`ratio`、`duration`、`seed`、`camerafixed`、`watermark`）。 | STRING | 是 | - |
| `第一帧` | 用于视频的首帧。必须介于 300x300 和 6000x6000 像素之间，宽高比介于 0.4 和 2.5 之间。 | IMAGE | 是 | - |
| `最后一帧` | 用于视频的尾帧。必须介于 300x300 和 6000x6000 像素之间，宽高比介于 0.4 和 2.5 之间。 | IMAGE | 是 | - |
| `分辨率` | 输出视频的分辨率。 | COMBO | 是 | `"480p"`<br />`"720p"`<br />`"1080p"` |
| `宽高比` | 输出视频的宽高比。 | COMBO | 是 | `"adaptive"`<br />`"16:9"`<br />`"4:3"`<br />`"1:1"`<br />`"3:4"`<br />`"9:16"`<br />`"21:9"` |
| `时长` | 输出视频的时长（秒）。使用 `seedance-1-5-pro-251215` 时，最小时长为 4 秒。（默认值：5） | INT | 是 | 3 - 12 |
| `种子` | 用于生成的种子。（默认值：0） | INT | 否 | 0 - 2147483647 |
| `相机固定` | 指定是否固定摄像机。平台会在你的提示词中附加固定摄像机的指令，但不保证实际效果。（默认值：False） | BOOLEAN | 否 | - |
| `水印` | 是否在视频中添加 "AI generated" 水印。（默认值：False） | BOOLEAN | 否 | - |
| `generate_audio` | 对于除 `seedance-1-5-pro-251215` 以外的任何模型，此参数都会被忽略。（默认值：False） | BOOLEAN | 否 | - |

**注意：** 对于 `seedance-1-5-pro-251215` 模型，`duration` 必须为 4 秒或更长。`first_frame` 和 `last_frame` 都必须介于 300x300 和 6000x6000 像素之间，并且宽高比介于 0.4 和 2.5 之间。如果 `prompt` 包含保留参数关键词，则会对其进行检查并拒绝。

## 输出

| 输出名称 | 描述 | 数据类型 |
| --- | --- | --- |
| `output` | 生成的视频文件。 | VIDEO |

> 本文档由 AI 生成。如果您发现任何错误或有改进建议，欢迎贡献！ [在 GitHub 上编辑](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/ByteDanceFirstLastFrameNode/zh.md)

---
**Source fingerprint (SHA-256):** `ae0f3a34a21baad7f04f6917e98d16dc64496479a050896869ec6693a9a9ebaf`
