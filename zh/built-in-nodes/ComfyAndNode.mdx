---
title: "ComfyAndNode - ComfyUI Built-in Node Documentation"
description: "And 节点对一组输入值执行逻辑 AND 运算。"
sidebarTitle: "ComfyAndNode"
icon: "circle"
mode: wide
---

And 节点对一组输入值执行逻辑 AND 运算。仅当每个已连接的值都按照 Python 的真值判断规则被视为真值时，它才返回 `true`，因此可用于同时检查多个条件是否全部满足。

## 输入

| 参数 | 描述 | 数据类型 | 必需 | 范围 |
| --- | --- | --- | --- | --- |
| `值` | 可增长的一组待评估值。节点初始有一个槽位，你可以点击节点上的“+”按钮来添加更多槽位。接受任意数据类型。 | ANY | 是 | 最小 1（无上限） |

**注意：** 此输入是一个可增长的槽位组。槽位是逐个添加的（例如 `value_1`、`value_2` 等），并且至少必须存在一个槽位。

**注意：** 该节点使用 Python 的真值判断规则来决定一个值是 `true` 还是 `false`。例如，空字符串、数字 0、空列表和 `None` 都会被视为 `false`。所有其他值都会被视为 `true`。

## 输出

| 输出名称 | 描述 | 数据类型 |
| --- | --- | --- |
| `BOOLEAN` | 如果所有输入值都为真值，则返回 `true`，否则返回 `false`。 | BOOLEAN |

> 本文档由 AI 生成。如果您发现任何错误或有改进建议，欢迎贡献！ [在 GitHub 上编辑](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/ComfyAndNode/zh.md)

---
**Source fingerprint (SHA-256):** `e7359c46da62f9859ea4f4a239cf20c565b5f7de22d280afc00c7ca321f1c89d`
