---
title: "HeyGenAvatarVideoNode - ComfyUI Built-in Node Documentation"
description: "从 HeyGen 虚拟形象生成一个会说话的主持人视频。"
sidebarTitle: "HeyGenAvatarVideoNode"
icon: "circle"
mode: wide
---

从 HeyGen 虚拟形象生成一个会说话的主持人视频。此节点使用 HeyGen 的渲染引擎，创建一段 AI 虚拟形象朗读你提供的文本，或与你自己的音频进行唇形同步的视频。

## 输入

### 通用输入

| 参数 | 描述 | 数据类型 | 必填 | 范围 |
|------|------|----------|------|------|
| `引擎` | 渲染引擎；每个选项仅列出支持该引擎的虚拟形象。`"auto"` 会提供所有虚拟形象，并自动选择其支持的最佳引擎（优先 Avatar IV）。Avatar V 保真度最高，Avatar III 最经济实惠。 | DYNAMIC_COMBO | 是 | `"auto"`<br />`"avatar_iv"`<br />`"avatar_iii"`<br />`"avatar_v"` |
| `自定义虚拟人 ID` | 可选 HeyGen 虚拟形象外观 ID。设置后，会覆盖上方选择的 `avatar`。可以使用 HeyGen 的 3000+ 公共外观（或你的私有虚拟形象）中的任意一个。默认值：`""`。 | STRING | 否 |  |
| `语音` | 使用文本脚本（HeyGen 文本转语音）或你自己的音频来驱动虚拟形象。显示名称："speech source"。 | DYNAMIC_COMBO | 是 | `"script"`<br />`"audio"` |
| `分辨率` | 输出视频分辨率。默认值：`"1080p"`。 | COMBO | 否 | `"720p"`<br />`"1080p"` |
| `宽高比` | 输出宽高比。`"auto"` 会跟随虚拟形象的源素材。默认值：`"auto"`。 | COMBO | 否 | `"auto"`<br />`"16:9"`<br />`"9:16"`<br />`"1:1"`<br />`"4:5"`<br />`"5:4"` |
| `背景颜色` | 可选的纯色背景颜色，以十六进制颜色代码表示（例如 `"#00ff00"`）。留空则使用虚拟形象自身的背景。如果提供，该值必须以 `#` 开头。默认值：`""`。 | STRING | 否 |  |
| `种子` | 不会发送给 HeyGen；更改它以强制重新运行。默认值：`42`。 | INT | 否 | 最小：0<br />最大：2147483647 |

### `auto` 输入

当 `engine` 为 `"auto"` 时，以下子参数可用：

| 参数 | 描述 | 数据类型 | 必填 | 范围 |
|------|------|----------|------|------|
| `avatar` | 用于呈现视频的虚拟形象外观（从 HeyGen 公共库中精选）。会自动选择该外观支持的最佳引擎。 | COMBO | 是 | 提供多个选项 |

### `avatar_iv` 输入

当 `engine` 为 `"avatar_iv"` 时，以下子参数可用：

| 参数 | 描述 | 数据类型 | 必填 | 范围 |
|------|------|----------|------|------|
| `avatar` | 支持 Avatar IV 引擎的虚拟形象外观。 | COMBO | 是 | 提供多个选项 |

### `avatar_iii` 输入

当 `engine` 为 `"avatar_iii"` 时，以下子参数可用：

| 参数 | 描述 | 数据类型 | 必填 | 范围 |
|------|------|----------|------|------|
| `avatar` | 支持 Avatar III 引擎的虚拟形象外观。 | COMBO | 是 | 提供多个选项 |

### `avatar_v` 输入

当 `engine` 为 `"avatar_v"` 时，以下子参数可用：

| 参数 | 描述 | 数据类型 | 必填 | 范围 |
|------|------|----------|------|------|
| `avatar` | 支持 Avatar V 引擎的虚拟形象外观。 | COMBO | 是 | 提供多个选项 |

### `script` 输入

当 `speech` 为 `"script"` 时，以下子参数可用：

| 参数 | 描述 | 数据类型 | 必填 | 范围 |
|------|------|----------|------|------|
| `text` | 虚拟形象要说的文本（最多 5000 个字符）。生成的语音必须至少 1 秒长。默认值：`""`。 | STRING | 是 | 最小：1 个字符<br />最大：5000 个字符 |
| `voice` | 脚本使用的语音。默认选项使用 HeyGen 分配给该虚拟形象的语音。如果设置了 `custom_voice_id`，则忽略此项。 | COMBO | 是 | `"(avatar's default voice)"`<br />提供多个通用语音选项 |
| `custom_voice_id` | 可选 HeyGen 语音 ID。设置后，会覆盖上方选择的 `voice`。可以使用 HeyGen 语音库（2000+）中的任意语音。默认值：`""`。 | STRING | 否 |  |
| `voice_speed` | 语音速度倍数。默认值：`1.0`。 | FLOAT | 否 | 最小：0.5<br />最大：1.5<br />步长：0.05 |

### `audio` 输入

当 `speech` 为 `"audio"` 时，以下子参数可用：

| 参数 | 描述 | 数据类型 | 必填 | 范围 |
|------|------|----------|------|------|
| `audio` | 供虚拟形象进行唇形同步的音频，最长 10 分钟。 | AUDIO | 是 |  |

注意：`engine` 和 `speech` 是选择器，会根据所选值显示不同的子参数。`speech` 选择器有两种互斥模式：在 `"script"` 模式下，`text` 为必填；如果提供了 `custom_voice_id`，它会覆盖 `voice`。在 `"audio"` 模式下，虚拟形象会与提供的音频片段进行唇形同步。提供 `background_color` 时，它必须是以 `#` 开头的十六进制颜色代码。设置 `custom_avatar_id` 后，它会覆盖 `avatar` 选择，并且所选的 `engine` 必须受该虚拟形象外观支持；否则会引发错误，除非 `engine` 为 `"auto"`。

## 输出

| 输出名称 | 描述 | 数据类型 |
|----------|------|----------|
| `VIDEO` | 生成的虚拟形象主持人视频。 | VIDEO |

> 本文档由 AI 生成。如果您发现任何错误或有改进建议，欢迎贡献！ [在 GitHub 上编辑](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/HeyGenAvatarVideoNode/zh.md)

---
**Source fingerprint (SHA-256):** `86dc799d3a8cf2666449b0d422853b12feffb81ce002f84594f9b925d58b522a`
