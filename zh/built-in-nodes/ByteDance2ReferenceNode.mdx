---
title: "ByteDance2ReferenceNode - ComfyUI Built-in Node Documentation"
description: "此节点使用字节跳动的 Seedance 2.5 或 Seedance 2.0 AI 模型生成、编辑或延长视频。"
sidebarTitle: "ByteDance2ReferenceNode"
icon: "circle"
mode: wide
---

此节点使用字节跳动的 Seedance 2.5 或 Seedance 2.0 AI 模型生成、编辑或延长视频。您可以通过文本提示描述视频，并可以附加参考图像、视频和音频来引导结果，支持多模态参考、视频编辑和视频延长。这是 Seedance 参考生视频节点的旧版、已弃用版本。

## 输入

选择 `model` 决定以下哪些参数可用。`video_editing` 和 `output_format` 仅在选择 Seedance 2.5 时出现。可扩展参考槽位和参考视频自动调整大小选项由所有模型共享，并在“参考输入”下描述。

### 通用输入

| 参数 | 描述 | 数据类型 | 必需 | 范围 |
|-----------|-------------|-----------|----------|-------|
| `模型` | 用于生成视频的 AI 模型。Seedance 2.5 是最新模型，视频最长 30 秒，输出 mp4/mov；Seedance 2.0 用于最高质量和 4k；Fast 用于速度优化；Mini 用于最快、成本最低的生成。选择模型会显示下面列出的模型特定输入。 | DYNAMIC_COMBO | 是 | `"Seedance 2.5"`<br />`"Seedance 2.0"`<br />`"Seedance 2.0 Fast"`<br />`"Seedance 2.0 Mini"` |
| `种子` | 种子控制节点是否应重新运行；无论种子如何，结果都是非确定性的（默认：0）。 | INT | 是 | 0 到 2147483647<br />步长: 1 |
| `水印` | 是否向视频添加水印（默认：False）。高级设置。 | BOOLEAN | 是 | `True`<br />`False` |

### Seedance 2.5 输入

| 参数 | 描述 | 数据类型 | 必需 | 范围 |
|-----------|-------------|-----------|----------|-------|
| `prompt` | 用于视频生成的文本提示。将口头台词放在双引号中以引导生成的对话。必须包含至少一个非空白字符（默认：空）。 | STRING | 是 | 任意文本 |
| `resolution` | 输出视频的分辨率（默认：`"720p"`）。 | COMBO | 是 | `"480p"`<br />`"720p"`<br />`"1080p"` |
| `ratio` | 输出视频的宽高比（默认：`"16:9"`）。 | COMBO | 是 | `"16:9"`<br />`"4:3"`<br />`"1:1"`<br />`"3:4"`<br />`"9:16"`<br />`"21:9"`<br />`"adaptive"` |
| `duration` | 输出视频的时长，以秒为单位（默认：5）。 | INT | 是 | 4 到 30<br />步长: 1 |
| `generate_audio` | 为输出视频启用音频生成（默认：True）。 | BOOLEAN | 是 | `True`<br />`False` |
| `video_editing` | 当提示词编辑已连接的参考视频时启用，例如替换其中的对象。输出随后保持源剪辑自身的长度和宽高比，`duration` 和 `ratio` 控件将被忽略。保持禁用可生成新视频，或将视频延长到您设置的时长（默认：False）。 | BOOLEAN | 是 | `True`<br />`False` |
| `output_format` | 输出视频的容器格式（默认：`"mp4"`）。 | COMBO | 是 | `"mp4"` |

### Seedance 2.0 输入

| 参数 | 描述 | 数据类型 | 必需 | 范围 |
|-----------|-------------|-----------|----------|-------|
| `prompt` | 用于视频生成的文本提示。必须包含至少一个非空白字符（默认：空）。 | STRING | 是 | 任意文本 |
| `resolution` | 输出视频的分辨率。 | COMBO | 是 | `"480p"`<br />`"720p"`<br />`"1080p"`<br />`"4k"` |
| `ratio` | 输出视频的宽高比（默认：`"adaptive"`）。 | COMBO | 是 | `"16:9"`<br />`"4:3"`<br />`"1:1"`<br />`"3:4"`<br />`"9:16"`<br />`"21:9"`<br />`"adaptive"` |
| `duration` | 输出视频的时长，以秒为单位（默认：7）。 | INT | 是 | 4 到 15<br />步长: 1 |
| `generate_audio` | 为输出视频启用音频生成（默认：True）。 | BOOLEAN | 是 | `True`<br />`False` |

### Seedance 2.0 Fast 和 Seedance 2.0 Mini 输入

由 Seedance 2.0 Fast 和 Seedance 2.0 Mini 共享。这两个模型暴露与 Seedance 2.0 相同的输入集，只是 `resolution` 限制为 480p 和 720p。

| 参数 | 描述 | 数据类型 | 必需 | 范围 |
|-----------|-------------|-----------|----------|-------|
| `prompt` | 用于视频生成的文本提示。必须包含至少一个非空白字符（默认：空）。 | STRING | 是 | 任意文本 |
| `resolution` | 输出视频的分辨率。 | COMBO | 是 | `"480p"`<br />`"720p"` |
| `ratio` | 输出视频的宽高比（默认：`"adaptive"`）。 | COMBO | 是 | `"16:9"`<br />`"4:3"`<br />`"1:1"`<br />`"3:4"`<br />`"9:16"`<br />`"21:9"`<br />`"adaptive"` |
| `duration` | 输出视频的时长，以秒为单位（默认：7）。 | INT | 是 | 4 到 15<br />步长: 1 |
| `generate_audio` | 为输出视频启用音频生成（默认：True）。 | BOOLEAN | 是 | `True`<br />`False` |

### 参考输入

适用于所有模型。最大槽位数取决于所选模型：Seedance 2.5 支持的参考数量多于 Seedance 2.0 模型。

| 参数 | 描述 | 数据类型 | 必需 | 范围 |
|-----------|-------------|-----------|----------|-------|
| `reference_images` | 可扩展槽位：连接一个或多个参考图像（`image_1`、`image_2`、...），用于引导视频生成。图像会自动缩小到最大边长 6000 像素，并且必须至少为 300x300 像素，宽高比在 0.4 到 2.5 之间。 | IMAGE | 否 | 最多 30 (Seedance 2.5)<br />最多 9 (Seedance 2.0 模型) |
| `reference_videos` | 可扩展槽位：连接一个或多个参考视频（`video_1`、`video_2`、...），用于引导视频生成；用于视频编辑和延长。 | VIDEO | 否 | 最多 10 (Seedance 2.5)<br />最多 3 (Seedance 2.0 模型) |
| `reference_audios` | 可扩展槽位：连接一个或多个参考音频片段（`audio_1`、`audio_2`、...），用于引导视频生成。 | AUDIO | 否 | 最多 10 (Seedance 2.5)<br />最多 3 (Seedance 2.0 模型) |
| `auto_downscale` | 自动缩小超过所选分辨率下模型像素预算的参考视频。保持宽高比；已在限制内的视频不受影响（默认：True）。 | BOOLEAN | 否 | `True`<br />`False` |
| `auto_upscale` | 高级设置。自动放大低于所选分辨率下模型最小像素数的参考视频。保持宽高比；已满足最小值的视频不受影响。注意：放大低分辨率源不会增加真实细节，并可能产生质量较低的生成结果（默认：False）。 | BOOLEAN | 否 | `True`<br />`False` |
| `reference_assets` | 可扩展槽位：以前创建的 Seedance 虚拟库资产（图像、视频或音频）的 ID，用作参考（`asset_1`、`asset_2`、...）。每个资产必须存在且状态为 Active。在提示词中，资产可称为 `asset1`、`asset 1` 等；节点会将这些标记替换为诸如“Image 2”的标签。 | STRING | 否 | 最多 30 (Seedance 2.5)<br />最多 9 (Seedance 2.0 模型) |

**重要约束：**

* 至少需要一个参考。对于 Seedance 2.0、2.0 Fast 和 2.0 Mini，您必须通过 `reference_images`、`reference_videos` 或 `reference_assets` 中的图像或视频条目提供至少一个图像或视频参考。Seedance 2.5 还接受纯音频参考（通过 `reference_audios` 或音频 `reference_assets` 条目）。
* 参考数量取决于模型，并会跨直接输入和资产参考进行合并验证：Seedance 2.5 最多允许 30 个 `reference_images`、10 个 `reference_videos`、10 个 `reference_audios` 和 30 个 `reference_assets`；Seedance 2.0 模型最多允许 9 个图像、3 个视频、3 个音频片段和 9 个资产。
* 每个参考视频必须至少 1.8 秒长，每个参考音频片段必须至少 1.8 秒长。所有参考视频和所有参考音频的总时长必须保持在所选模型的限制内（Seedance 2.0 模型为 15.1 秒）。
* 参考视频还必须满足所选分辨率下模型的像素数限制。启用 `auto_downscale`（默认）时，过大的视频会自动调整大小；启用 `auto_upscale` 时，过小的视频会被放大。如果禁用任一自动调整，超出相应限制的视频会引发错误。
* 当在 Seedance 2.5 上启用 `video_editing` 时，`duration` 和 `ratio` 输入将被忽略；输出与参考视频自身的长度和宽高比匹配。如果提供方将提示词解释为编辑参考视频，则除非启用 `video_editing` 或改写提示词以描述新视频，否则生成会失败。
* 如果提供方拒绝为视频生成的音轨（例如可能存在版权匹配），任务会失败；禁用 `generate_audio` 会生成无声视频。

## 输出

| 输出名称 | 描述 | 数据类型 |
|-------------|-------------|-----------|
| `video` | 生成的视频文件。 | VIDEO |

> 本文档由 AI 生成。如果您发现任何错误或有改进建议，欢迎贡献！ [在 GitHub 上编辑](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/ByteDance2ReferenceNode/zh.md)

---
**Source fingerprint (SHA-256):** `4a1b62f65ff3515cdb749c9b3916e631e53523fe144e8cdf71ca020825196ae6`
