---
title: "ModelMergeCosmosPredict2_14B - ComfyUI Built-in Node Documentation"
description: "ModelMergeCosmosPredict214B 节点通过混合两个 AI 模型中匹配的内部组件，将它们合并为一个模型。"
sidebarTitle: "ModelMergeCosmosPredict2_14B"
icon: "circle"
mode: wide
---

ModelMergeCosmosPredict2_14B 节点通过混合两个 AI 模型中匹配的内部组件，将它们合并为一个模型。它允许你通过为每个嵌入器、块和最终层分别调整权重，来控制第二个模型混入第一个模型的程度。

## 输入

| 参数 | 描述 | 数据类型 | 必填 | 范围 |
| --- | --- | --- | --- | --- |
| `模型1` | 要合并的基础模型 | MODEL | 是 | - |
| `模型2` | 要合并到基础模型中的辅助模型 | MODEL | 是 | - |
| `pos_embedder.` | 位置嵌入器混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `x_embedder.` | 输入嵌入器混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `t_embedder.` | 时间嵌入器混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `t_embedding_norm.` | 时间嵌入归一化混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块0。` | 块 0 混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块1。` | 块 1 混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块2。` | 块 2 混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块3。` | 块 3 混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块4。` | 块 4 混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块5。` | 块 5 混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块6。` | 块 6 混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块7。` | 块 7 混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块8。` | 块 8 混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块9。` | 块 9 混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块10。` | 块 10 混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块11。` | 块 11 混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块12。` | 块 12 混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块13。` | 块 13 混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块14。` | 块 14 混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块15。` | 块 15 混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块16。` | 块 16 混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块17。` | 块 17 混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块18。` | 块 18 混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块19。` | 块 19 混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块20。` | 块 20 混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块21。` | 块 21 混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块22。` | 块 22 混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块23。` | 块 23 混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块24。` | 块 24 混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块25。` | 块 25 混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块26。` | 块 26 混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块27。` | 块 27 混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块28。` | 块 28 混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块29。` | 块 29 混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块30。` | 块 30 混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块31。` | 块 31 混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块32。` | 块 32 混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块33。` | 块 33 混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块34。` | 块 34 混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块35。` | 块 35 混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `final_layer.` | 最终层混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |

**注意：** 所有混合权重参数接受 0.0 到 1.0 之间的值，步长为 0.01，其中 0.0 表示该特定组件完全不包含来自 `model2` 的贡献，1.0 表示完全包含来自 `model2` 的贡献。

## 输出

| 输出名称 | 描述 | 数据类型 |
| --- | --- | --- |
| `model` | 合并后的模型，结合了两个输入模型的特征 | MODEL |

> 本文档由 AI 生成。如果您发现任何错误或有改进建议，欢迎贡献！ [在 GitHub 上编辑](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/ModelMergeCosmosPredict2_14B/zh.md)

---
**Source fingerprint (SHA-256):** `a5f34deda62dc03f22613517e43996b908a8673dc5da10d8f1b7f6411ece2f0a`
