---
title: "ModelMergeCosmosPredict2_14B - ComfyUI Built-in Node Documentation"
description: "ModelMergeCosmosPredict214B 节点通过混合两个 AI 模型的内部组件来合并它们。"
sidebarTitle: "ModelMergeCosmosPredict2_14B"
icon: "circle"
mode: wide
---

ModelMergeCosmosPredict2_14B 节点通过混合两个 AI 模型的内部组件来合并它们。它使用针对特定层和组件的可调节权重值，让您能够精确控制第二个模型的每个部分对最终合并结果的影响程度。

## 输入

| 参数 | 描述 | 数据类型 | 必需 | 范围 |
| --- | --- | --- | --- | --- |
| `模型1` | 要合并的基础模型 | MODEL | 是 | - |
| `模型2` | 要合并到基础模型中的次要模型 | MODEL | 是 | - |
| `pos_embedder.` | 位置嵌入器混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `x_embedder.` | 输入嵌入器混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `t_embedder.` | 时间嵌入器混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `t_embedding_norm.` | 时间嵌入归一化混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块0。` | 块 0 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块1。` | 块 1 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块2。` | 块 2 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块3。` | 块 3 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块4。` | 块 4 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块5。` | 块 5 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块6。` | 块 6 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块7。` | 块 7 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块8。` | 块 8 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块9。` | 块 9 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块10。` | 块 10 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块11。` | 块 11 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块12。` | 块 12 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块13。` | 块 13 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块14。` | 块 14 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块15。` | 块 15 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块16。` | 块 16 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块17。` | 块 17 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块18。` | 块 18 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块19。` | 块 19 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块20。` | 块 20 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块21。` | 块 21 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块22。` | 块 22 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块23。` | 块 23 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块24。` | 块 24 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块25。` | 块 25 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块26。` | 块 26 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块27。` | 块 27 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块28。` | 块 28 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块29。` | 块 29 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块30。` | 块 30 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块31。` | 块 31 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块32。` | 块 32 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块33。` | 块 33 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块34。` | 块 34 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `块35。` | 块 35 的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |
| `final_layer.` | 最终层的混合权重（默认值：1.0） | FLOAT | 是 | 0.0 - 1.0 |

**注意：** 所有混合权重参数均接受 0.0 到 1.0 之间的值，步长为 0.01。其中，0.0 表示 `model2` 对该特定组件没有贡献，1.0 表示 `model2` 对该特定组件完全贡献。

## 输出

| 输出名称 | 描述 | 数据类型 |
| --- | --- | --- |
| `model` | 合并后的模型，融合了两个输入模型的特征 | MODEL |

> 本文档由 AI 生成。如果您发现任何错误或有改进建议，欢迎贡献！ [在 GitHub 上编辑](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/ModelMergeCosmosPredict2_14B/zh.md)

---
**Source fingerprint (SHA-256):** `a5f34deda62dc03f22613517e43996b908a8673dc5da10d8f1b7f6411ece2f0a`
