---
title: "Tripo P2：在 ComfyUI 中生成四边形拓扑 3D 模型"
description: "使用 ComfyUI 中的 Tripo P2 合作节点生成可直接投入生产的 3D 模型：原生四边形拓扑，面数预算最高可达 50,000，纹理级别从 2K 到 8K。"
sidebarTitle: "Tripo P2"
translationSourceHash: 8e74631d
translationFrom: tutorials/partner-nodes/tripo/tripo-p2.mdx
translationBlockHashes:
  "_intro": 3fd4d2bd
  "What Tripo P2 is good at": 1b7b881b
  "Tripo P2: Text to Model": e1e4d29e
  "Tripo P2: Image to Model": 7ac87448
  "Tripo P2: Multiview to Model": 3e3708ad
  "Node options reference": 4a745c24
---
import ReqHint from "/snippets/zh/tutorials/partner-nodes/req-hint.mdx";
import UpdateReminder from "/snippets/zh/tutorials/update-reminder.mdx";

Tripo P2 是 Tripo 的 P 系列 3D 生成模型，可输出原生四边形拓扑网格。传统的 text-to-3D 和 image-to-3D 模型返回的是三角网格，而 Tripo P2 以四边面构建几何体，关节周围拥有干净的边环，平坦表面上密度均匀。四边形在绑定下变形可预测，细分干净，是游戏、影视和产品可视化中重度重拓扑流程的标准输入。

Tripo P2 支持三种输入模式：文本生成模型、单张图像生成模型，以及多视图生成模型（前、左、后、右参考图）。网格预算最高可达 50,000 个三角面，或在四边形模式下达到 25,000 个四边形。纹理输出分级提供：标准 2K、精细 4K、极致 8K，并可与基础色一同提供 PBR 贴图（metallic、roughness、normal）。

在 ComfyUI 中，P2 节点的使用方式与其他 Tripo 合作节点相同：添加节点、提供输入，工作流会自动处理贴图、绑定、转换和导出。无需本地 GPU，也无需下载模型。

<ReqHint/>
<UpdateReminder/>

## Tripo P2 擅长什么 {#what-tripo-p2-is-good-at}

- **原生四边面拓扑**：四边面网格拥有干净的边流，无需手动重拓扑即可用于绑定和动画
- **大网格面数预算**：48 至 50,000 个三角面，或在四边面模式下为 48 至 25,000 个四边面
- **分层纹理**：标准（2K）、精细（4K）或极致（8K）基础颜色纹理，并可选 PBR 贴图
- **三种输入模式**：文本提示、单张参考图像，或最多四张多视角图像（前、左、后、右）

价格取决于所选的纹理级别。详情请参阅[定价](/zh/tutorials/partner-nodes/pricing)。

## Tripo P2：文本生成模型

根据文本描述生成 3D 模型。Tripo P2 会先根据提示词绘制概念图像（由 `image_seed` 控制），再据此生成几何结构。

<img src="https://raw.githubusercontent.com/Comfy-Org/workflow_templates/main/templates/api_tripo_p2_text_to_model-1.webp" alt="Tripo P2 文本生成模型工作流" />

<CardGroup cols={2}>
  <Card title="在 Comfy Cloud 上运行" icon="cloud" href="https://cloud.comfy.org/?template=api_tripo_p2_text_to_model&utm_source=docs&utm_medium=referral&utm_campaign=tripo_p2_t2m">
    在 Comfy Cloud 中打开
  </Card>
  <Card title="下载工作流" icon="download" href="https://github.com/Comfy-Org/workflow_templates/blob/main/templates/api_tripo_p2_text_to_model.json">
    下载 JSON，或在模板库中搜索 "Tripo P2: Text to Model"
  </Card>
</CardGroup>

### 运行步骤

1. **输入提示词**：在 **Tripo P2: Text to Model** 节点中描述该 3D 模型（最多 1024 个字符）。高级选项中提供负面提示词
2. **选择纹理级别**：`standard`（2K）、`detailed`（4K）、`extreme`（8K），或 `none` 表示无纹理网格
3. **切换四边面输出**：启用 `quad` 时网格以 FBX 格式返回；禁用时以 GLB 格式返回
4. **设置面数上限**：目标面数介于 48 到 50,000 之间（启用 quad 时为 48 到 25,000），或保留为 -1 让 Tripo 自行选择
5. **点击队列**：按 `Ctrl+Enter` 生成

生成后，工作流会自动为模型添加纹理（**TripoTextureNodeV2**）、绑定骨骼（**TripoRigNode**）、应用预设动画（**TripoRetargetNode**）并转换结果（**TripoConversionNode**），然后保存。

### 四边面输出说明

启用 `quad` 时，将 **Save 3D** 连接到 FBX 输出槽。禁用 `quad` 时，使用 GLB 输出槽。

## Tripo P2: 图像转模型

从单张参考图像生成 3D 模型。

<img src="https://raw.githubusercontent.com/Comfy-Org/workflow_templates/main/templates/api_tripo_p2_image_to_model-1.webp" alt="Tripo P2 图像转模型工作流" />

<CardGroup cols={2}>
  <Card title="在 Comfy Cloud 上运行" icon="cloud" href="https://cloud.comfy.org/?template=api_tripo_p2_image_to_model&utm_source=docs&utm_medium=referral&utm_campaign=tripo_p2_i2m">
    在 Comfy Cloud 中打开
  </Card>
  <Card title="下载工作流" icon="download" href="https://github.com/Comfy-Org/workflow_templates/blob/main/templates/api_tripo_p2_image_to_model.json">
    下载 JSON，或在模板库中搜索 "Tripo P2: Image to Model"
  </Card>
</CardGroup>

### 运行步骤

1. **上传参考图像**：使用 **LoadImage** 节点加载主体的正视图图像
2. **选择纹理级别**：`standard`、`detailed`、`extreme` 或 `none`
3. **切换四边形输出**：启用时输出 FBX，禁用时输出 GLB
4. **设置纹理对齐**：`original_image` 会匹配输入图像的颜色，`geometry` 则让纹理贴合已生成的形状（高级选项）
5. **点击队列**：按 `Ctrl+Enter` 即可生成

### 输入提示

- 低分辨率或低质量的图像可在建模之前通过 `enable_image_autofix` 高级选项进行增强
- `align_image` 朝向会将模型旋转至输入图像的视角（高级选项）

## Tripo P2：多视图生成模型

多视图节点最多可接受四张图像：一张必填的正视图，以及可选的左视图、后视图和右视图。该节点位于节点库中同一个 **Tripo P2** 节点系列中；将参考图像连接到各个视图插槽，并沿用与上文相同的纹理和四边形选项。

## 节点选项参考

三个 P2 节点共用的选项：

| 选项 | 取值 | 默认值 | 说明 |
| ------ | ------ | ------- | ----- |
| `quad` | 开 / 关 | 关 | 开启时输出 FBX 格式的四边形网格，关闭时输出 GLB 格式的三角面网格 |
| `face_limit` | 48 到 50,000（启用 quad 时为 25,000） | -1 | 目标面数；实际结果可能超过该值 |
| `texture` | 标准 / 详细 / 极致 / 无 | 标准 | 2K / 4K / 8K 的基础颜色贴图；选择“无”时返回不带贴图的网格 |
| `pbr` | 开 / 关 | 开 | 金属度、粗糙度和法线贴图；texture 为“无”时忽略此项 |
| `model_seed` | 0 到 2147483647 | 42 | 几何体的种子 |

高级选项包括 `texture_seed`、`auto_size`（以米为单位的真实世界缩放）、`export_uv` 和 `compress_geometry`（Meshopt 压缩；压缩后的网格体积小得多，但无法在 ComfyUI 的 3D 视口中预览）。
