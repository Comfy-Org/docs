---
title: "OpenAI Chat 合作节点 ComfyUI 官方示例"
description: "本文将介绍如何在 ComfyUI 中使用 OpenAI Chat 合作节点来完成对话功能"
sidebarTitle: "Chat"
translationSourceHash: 58f13c6b
translationFrom: tutorials/partner-nodes/openai/chat.mdx
translationBlockHashes:
  "_intro": 2ae6e13c
  "What OpenAI Chat is good at": dc9efec1
  "OpenAI: ChatGPT {#api_openai_chat}": 6ad307cf
  "Complete the workflow execution step by step": 752378f6
  "Additional Notes": e4a48088
  "GPT-6 Astra template": 905ce401
---
import ReqHint from "/snippets/zh/tutorials/partner-nodes/req-hint.mdx";
import UpdateReminder from "/snippets/zh/tutorials/update-reminder.mdx";


OpenAI 是一家专注于生成式 AI 的公司，提供强大的对话能力。目前，ComfyUI 已集成 OpenAI API，你可以直接在 ComfyUI 中使用相关节点来完成对话功能。

**GPT-6 Astra** 是 OpenAI 能力最强的模型，面向最困难的端到端任务：复杂推理、编程、计算机操作、研究和文档创作。该模型于 2026 年 9 月 3 日发布，支持 1,050,000 令牌上下文窗口、文本和图像输入，以及从 `low` 到 `max` 的推理力度等级。在 ComfyUI 中，它出现在 `OpenAI Chat` 节点的模型列表中，并带有用于图像分析和提示词生成任务的示例模板。

## OpenAI Chat 擅长什么

- **智能对话**：直接在 ComfyUI 内与 OpenAI 的高级语言模型交流互动
- **图像理解**：加载图像供 AI 解读并作出回应
- **任务控制**：调整 `OpenAI Chat Advanced Options`，让 AI 执行特定任务，例如分析图像和生成提示词
- **模型选择**：在 `OpenAI Chat` 节点中选择不同的 OpenAI 模型
- **多图像输入**：当需要 AI 解读的图像不止一张时，可将多张图像一起批处理

<ReqHint/>
<UpdateReminder/>

## OpenAI：ChatGPT {#api_openai_chat}

与 OpenAI 的高级语言模型交互，进行智能对话。

<img src="https://raw.githubusercontent.com/Comfy-Org/workflow_templates/main/templates/api_openai_chat-1.webp" alt="OpenAI: ChatGPT 工作流预览" />

<CardGroup cols={2}>
  <Card title="在 Comfy Cloud 上运行" icon="cloud" href="https://cloud.comfy.org/?template=api_openai_chat&utm_source=docs&utm_medium=referral&utm_campaign=chat">
    在 Comfy Cloud 中打开
  </Card>
  <Card title="下载工作流" icon="download" href="https://github.com/Comfy-Org/workflow_templates/blob/main/templates/api_openai_chat.json">
    下载 JSON，或在模板库中搜索“OpenAI: ChatGPT”
  </Card>
</CardGroup>

**输入材料**

将此文件上传到匹配的 `LoadImage` 节点：

<CardGroup cols={2}>
  <Card title="example.png" icon="image" href="https://raw.githubusercontent.com/Comfy-Org/workflow_templates/main/input/example.png">
    `LoadImage` 节点 5 · `example.png`
  </Card>
</CardGroup>

<div style={{display: 'grid', gridTemplateColumns: 'repeat(2, minmax(0, 1fr))', gap: '1rem', alignItems: 'start'}}>
  <img src="https://raw.githubusercontent.com/Comfy-Org/workflow_templates/main/input/example.png" alt="example.png" style={{width: '100%', height: 'auto', objectFit: 'contain'}} />
</div>

## 逐步完成工作流执行

![OpenAI Chat 步骤指南](/images/tutorial/api_nodes/openai/openai_chat_step_guide.jpg)

<Note>
在对应的模板中，我们内置了一个用于分析提示词生成的角色设定。
</Note>

您可以参考图片中的编号，完成基础文生图工作流的执行：
1. 在 `Load Image` 节点中，加载您需要 AI 解读的图像。
2. （可选）如果需要，您可以修改 `OpenAI Chat Advanced Options` 中的设置，让 AI 执行特定任务。
3. 在 `OpenAI Chat` 节点中，您可以修改 `Prompt` 来设置对话提示词，或修改 `model` 以选择不同的模型。
4. 点击 `Run` 按钮，或使用快捷键 `Ctrl(cmd) + Enter` 来执行对话。
5. 等待 API 返回结果之后，您可以在 `Preview Any` 节点中查看 AI 返回的相应内容。

## 补充说明

- 目前，文件输入节点 `OpenAI Chat Input Files` 要求先将文件上传到 `ComfyUI/input/` 目录。此节点正在改进中，我们将在更新之后修改模板
- 该工作流提供了一个使用 `Batch Images` 作为输入的示例。如果你有多个需要 AI 解读的图像，可以参考步骤图，并使用右键将相应节点的模式设置为 `Always` 以启用它

## GPT-6 Astra 模板

另一个模板展示了 GPT-6 Astra 分析图像并从中生成结构化 AI 绘画提示词的过程。该工作流加载一张图像，将分析提示词发送给 `model` 设为 `gpt-6-astra` 的 `OpenAI Chat` 节点，并通过 `Save Text` 将结果写入文本文件。

![GPT-6 Astra 工作流预览](https://raw.githubusercontent.com/Comfy-Org/workflow_templates/main/templates/api_openai_gpt6_astra-1.webp)

<CardGroup cols={2}>
  <Card title="Run on Comfy Cloud" icon="cloud" href="https://cloud.comfy.org/?template=api_openai_gpt6_astra&utm_source=docs&utm_medium=referral&utm_campaign=gpt6-astra">
    在 Comfy Cloud 上运行
  </Card>
  <Card title="Download Workflow" icon="download" href="https://github.com/Comfy-Org/workflow_templates/blob/main/templates/api_openai_gpt6_astra.json">
    下载 JSON，或在模板库中搜索 "GPT-6 Astra"
  </Card>
</CardGroup>

**输入素材**

将此文件上传到对应的 `LoadImage` 节点：

<CardGroup cols={2}>
  <Card title="blue_poppies_field.png" icon="image" href="https://raw.githubusercontent.com/Comfy-Org/workflow_templates/main/input/blue_poppies_field.png">
    `LoadImage` 节点 · `blue_poppies_field.png`
  </Card>
</CardGroup>

步骤：

1. 在 `LoadImage` 节点中加载你的图像
2. 在 `OpenAI Chat` 节点中，根据需要调整分析提示词或切换 `model`
3. 点击 `Run`。生成的提示词会显示在预览中，并通过 `Save Text` 节点保存为 `.txt` 文件

GPT-6 Astra 按 API 令牌用量计费，费率高于其他 OpenAI Chat 模型。详情请查看[定价表](/zh/tutorials/partner-nodes/pricing)。
