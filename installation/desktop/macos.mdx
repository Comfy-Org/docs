---
title: "macOS — ComfyUI Desktop"
description: "Install and uninstall ComfyUI Desktop on macOS"
icon: "apple"
sidebarTitle: "macOS"
---

import TroubleshootingGpt from "/snippets/install/troubleshooting-gpt.mdx"
import TroubleshootingFeedback from "/snippets/install/troubleshooting-feedback.mdx"

**ComfyUI Desktop** is a multi-installation manager that lets you run multiple ComfyUI instances from a single launcher. This guide covers how to install and uninstall ComfyUI Desktop on macOS.

## System Requirements

- **OS:** macOS 13 (Ventura) or later
- **Hardware:** Apple Silicon (M1 or later)
- **Disk Space:** At least 15 GB recommended per installation

<CardGroup cols={1}>
  <Card title="Download ComfyUI Desktop" icon="download" href="https://comfy.org/download">
    Download the latest installer for macOS. The page will automatically detect your platform.
  </Card>
</CardGroup>

## Installation

1. Open the downloaded `.dmg` file
2. Drag **ComfyUI Desktop** into the **Applications** folder
3. Launch it from Launchpad or Spotlight

The first time you launch, macOS may show a security warning. Go to **System Settings → Privacy & Security** and click **Open Anyway** to allow it.

When the app opens, you'll see the **Welcome** screen where you can create your first installation.

## Uninstalling

### Remove ComfyUI Desktop

Drag **ComfyUI Desktop** from the Applications folder to the Trash.

### Remove Installation Data

ComfyUI Desktop and its installations are stored separately on disk. To fully clean up:

1. **Delete individual installations** from within the app, or delete the installation folders manually
2. **Delete the app data folder** at `~/Library/Application Support/ComfyUI Desktop`

## Data Locations

| Data | macOS Path |
|------|-----------|
| App data (settings, installation list) | `~/Library/Application Support/ComfyUI Desktop` |
| Logs | `~/Library/Application Support/ComfyUI Desktop/logs` |
| Installation directories | User-chosen during setup |

## Troubleshooting

### macOS security warning

If macOS blocks the app, go to **System Settings → Privacy & Security**, scroll to the bottom, and click **Open Anyway** next to the ComfyUI entry.

### Mirror errors during installation

If mirrors fail, expand the mirror settings in the app to configure custom mirrors for Python, PyPI, and PyTorch.

<TroubleshootingGpt/>
<TroubleshootingFeedback
log_path="~/Library/Application Support/ComfyUI Desktop/logs"
config_path="~/Library/Application Support/ComfyUI Desktop"
/>
