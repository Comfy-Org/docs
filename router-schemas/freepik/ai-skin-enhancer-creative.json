{"openapi":"3.0.2","info":{"title":"freepik/ai-skin-enhancer-creative","description":"The request body Comfy Router accepts for the model \"freepik/ai-skin-enhancer-creative\", and the response body it returns. The INPUT schema is the same schema the server validates a call against before it reaches the provider, so what is published and what is enforced cannot differ. The OUTPUT schema describes the provider's native result document exactly as Router returns it: Router does not validate, narrow or re-envelope the response, so the output schema is descriptive rather than enforced, and Comfy owns no output shape of its own.","version":"81e3668d0dd9"},"paths":{"/v2/models/freepik/ai-skin-enhancer-creative":{"post":{"operationId":"runRouterModel","summary":"Run freepik/ai-skin-enhancer-creative synchronously.","security":[{"BearerAuth":[]},{"ApiKeyAuth":[]}],"requestBody":{"required":true,"content":{"application/json":{"schema":{"example":{"image":"https://img.magnific.com/free-photo/portrait-woman_395237-33.jpg?w=740\u0026q=80"},"properties":{"image":{"description":"Input image. Supports Base64 encoding or HTTPS URL (must be publicly accessible).","example":"https://example.com/portrait.jpg","type":"string"},"sharpen":{"default":0,"description":"Sharpening intensity","maximum":100,"minimum":0,"type":"integer"},"smart_grain":{"default":2,"description":"Smart grain intensity","maximum":100,"minimum":0,"type":"integer"},"webhook_url":{"description":"Optional callback URL for async notifications.","example":"https://www.example.com/webhook","format":"uri","type":"string"}},"required":["image"],"type":"object"}}}},"responses":{"200":{"description":"OK - the model's native JSON output, returned unchanged; the schema describes the provider's terminal result document as Router returns it.","content":{"application/json":{"schema":{"allOf":[{"$ref":"#/components/schemas/FreepikTaskResponse"},{"properties":{"data":{"allOf":[{"$ref":"#/components/schemas/FreepikTaskData"},{"properties":{"generated":{"description":"The finished generation's image URLs. Present and non-empty on the document Router returns; this list IS the result. The links are Freepik's own and expire.","items":{"format":"uri","type":"string"},"minItems":1,"type":"array"},"status":{"enum":["COMPLETED"],"type":"string"}},"required":["status","generated"],"type":"object"}]}},"required":["data"],"type":"object"}],"description":"Comfy Router output schema for the Freepik upscale and skin-enhance models: the terminal Freepik task document, forwarded unchanged. All four ids are SUBMIT-AND-POLL - `routerresult/classification.go` records each `POST /proxy/freepik/v1/ai/...` submit as `ReturnModeSubmitPoll` - and Router polls on the caller's behalf, so the body a caller receives is the finished task rather than the `{\"data\": {\"task_id\": ..., \"status\": \"CREATED\"}}` handle the underlying submit answers with.\nONE document describes all four because Freepik answers one. The upscaler polls its own submit path and the three skin-enhancer tunings share a single status route, but both routes declare the same task document, and the three tunings (`creative`, `faithful`, `flexible`) are three settings of one operation rather than three result shapes. Each id publishes its own `example` so the asset the page shows is the asset that page's model produces.\nTHE ASSET IS `data.generated`, a list of URL STRINGS pointing at the generated images - not objects with a URL inside, so there is no deeper field to key completion off. A `COMPLETED` task carrying an empty `generated` is a success that produced nothing, and through Router that case never reaches the caller as this document: Router answers it as a Comfy Router error instead, so a `200` here always carries at least one image URL. `data.task_id` is the handle the poll was addressed with and must never be read as an asset.\nThe URLs are Freepik's own delivery links and they EXPIRE. Router does not re-host these images onto Comfy storage, so what a caller receives is Freepik's short-lived URL rather than a Comfy one - download the asset rather than storing the link.\n`data.status` is Freepik's own UPPERCASE vocabulary (`CREATED`, `IN_PROGRESS`, `COMPLETED`, `FAILED`), compared without case folding. `COMPLETED` is the one terminal success and the only value this document can carry: `FAILED` and a budget-exhausted `CREATED`/`IN_PROGRESS` reach a Router caller as a Comfy Router error rather than as this body.","example":{"data":{"generated":["https://example.invalid/freepik/ai-skin-enhancer-creative/enhanced.png"],"status":"COMPLETED","task_id":"046b6c7f-0b8a-43b9-b35d-6489e6daee91"}}}}}}}}}},"components":{"schemas":{"FreepikTaskData":{"properties":{"generated":{"description":"URLs to the generated images.","items":{"format":"uri","type":"string"},"type":"array"},"status":{"enum":["CREATED","IN_PROGRESS","COMPLETED","FAILED"],"type":"string"},"task_id":{"example":"046b6c7f-0b8a-43b9-b35d-6489e6daee91","format":"uuid","type":"string"}},"type":"object"},"FreepikTaskResponse":{"properties":{"data":{"$ref":"#/components/schemas/FreepikTaskData"}},"required":["data"],"type":"object"}},"securitySchemes":{"ApiKeyAuth":{"type":"apiKey","in":"header","name":"X-API-Key"},"BearerAuth":{"type":"http","scheme":"bearer","bearerFormat":"JWT"}}},"x-comfy-router-model-id":"freepik/ai-skin-enhancer-creative","x-comfy-input-schema-authored":true,"x-comfy-output-schema-authored":true}
