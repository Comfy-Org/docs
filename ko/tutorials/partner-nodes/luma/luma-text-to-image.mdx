---
title: "Luma 텍스트 기반 이미지 생성 파트너 노드 ComfyUI 공식 예제"
description: "이 가이드에서는 ComfyUI의 Luma 텍스트 기반 이미지 생성 파트너 노드를 사용하는 방법을 설명합니다."
sidebarTitle: "Luma 텍스트 기반 이미지 생성"
translationSourceHash: d54dc934
translationFrom: tutorials/partner-nodes/luma/luma-text-to-image.mdx
translationBlockHashes:
  "_intro": ade82ae1
  "What Luma Text to Image is good at": b70f11fc
  "Example outputs": c0f1401a
  "Luma Text to Image Partner Nodes Workflow": 5dc6fe2d
---

import ReqHint from "/snippets/ko/tutorials/partner-nodes/req-hint.mdx";
import UpdateReminder from "/snippets/ko/tutorials/update-reminder.mdx";


Luma 이미지 생성 (텍스트 → 이미지) 노드는 Luma AI의 고급 기술을 활용하여 텍스트 프롬프트에서 고품질 이미지를 생성할 수 있으며, 사실적인 콘텐츠와 예술적 스타일의 이미지를 제작할 수 있습니다.

## Luma 이미지 생성(텍스트 → 이미지)이 뛰어난 분야

- **고품질 텍스트 기반 이미지 생성**: 텍스트 프롬프트에서 사진처럼 사실적인 콘텐츠와 예술적 스타일의 이미지를 생성합니다.
- **스타일 참조**: `style_image` 입력이 결과물의 룩을 안내하며, `style_image_weight` 값이 스타일 참조 이미지의 영향력을 제어합니다.
- **이미지 및 캐릭터 참조**: `image_luma_ref` 입력이 참조 기반 생성을 지원하며, 최대 4개의 참조 이미지와 캐릭터 참조를 동시에 사용할 수 있습니다.

## 예제 출력

`style_image_weight` 값에 따른 스타일 가이드 생성으로, 스타일 참조 이미지의 영향을 보여줍니다:

![스타일 이미지 가중치 비교](/images/tutorial/api_nodes/luma/t2i_style_image_weight.jpg)

<ReqHint/>
<UpdateReminder/>

## Luma Text to Image Partner Nodes Workflow

When the `Luma Text to Image` node is used without any image inputs, it functions as a text-to-image workflow. In this guide, we've created examples using `style_image` and `image_luma_ref` to showcase Luma AI's excellent image processing capabilities.

### 1. Download Workflow Files

워크플로 정보는 아래 이미지의 메타데이터에 포함되어 있습니다. 이미지를 다운로드하여 ComfyUI로 드래그하면 워크플로가 로드됩니다.

![Luma Text to Image Workflow](https://raw.githubusercontent.com/Comfy-Org/example_workflows/main/api_nodes/luma/t2i/luma_t2i.png)

입력용으로 다음 이미지를 다운로드하세요:

![Input Image - Reference](https://raw.githubusercontent.com/Comfy-Org/example_workflows/main/api_nodes/luma/t2i/input_ref.png)
![Input Image - Style](https://raw.githubusercontent.com/Comfy-Org/example_workflows/main/api_nodes/luma/t2i/input_style.png)

### 2. Follow Steps to Run the Workflow

![Luma Text to Image Workflow Steps](/images/tutorial/api_nodes/luma/luma_t2i_step_guide.jpg)

이미지의 번호가 매겨진 단계에 따라 기본 워크플로를 실행하세요:
1. Upload the reference image in the `Load image` node
2. Upload the style reference image in the `Load image (renamed to styleref)` node
3. (Optional) Modify the prompts in the `Luma Text to Image` node
4. (Optional) Adjust the `style_image_weight` to control the style reference image's influence
5. Click the `Run` button or use the shortcut `Ctrl(cmd) + Enter` to generate the image
6. After the API returns results, view the generated image in the `Save Image` node. Images are saved to the `ComfyUI/output/` directory

![Style Image Weight Comparison](/images/tutorial/api_nodes/luma/t2i_style_image_weight.jpg)

### 3. Additional Notes

- The node allows up to 4 reference images and character references simultaneously.
- To enable multiple image inputs, right-click on the purple "Bypassed" nodes and set their `mode` to `always`
