---
title: "Luma 텍스트에서 비디오로 - ComfyUI 기본 노드 문서"
description: Luma AI를 사용해 텍스트 설명을 비디오로 변환하는 노드
sidebarTitle: "Luma 텍스트에서 비디오로"
icon: "circle"
translationSourceHash: 735b0500
translationFrom: built-in-nodes/partner-node/video/luma/luma-text-to-video.mdx
---

![ComfyUI 기본 Luma 텍스트에서 비디오로 노드](/images/built-in-nodes/api_nodes/luma/luma-text-to-video.jpg)

Luma 텍스트에서 비디오로 노드는 Luma AI의 비디오 생성 기술을 사용해 텍스트 설명으로 고품질의 부드러운 비디오를 만들 수 있게 해줍니다.

## 노드 기능

이 노드는 Luma AI의 텍스트에서 비디오로 API에 연결되어 사용자가 상세한 텍스트 프롬프트로부터 동적 비디오 콘텐츠를 생성할 수 있도록 합니다.

## 매개변수

### 기본 매개변수

| 매개변수    | 유형    | 기본값      | 설명                                             |
| ------------ | ------- | ------------ | ------------------------------------------------------- |
| 프롬프트       | 문자열  | ""           | 생성할 비디오 콘텐츠를 설명하는 텍스트 프롬프트    |
| 모델        | 선택    | -            | 사용할 비디오 생성 모델                           |
| 종횡비         | 선택    | "ratio_16_9" | 비디오 종횡비                                      |
| 해상도         | 선택    | "res_540p"   | 비디오 해상도                                        |
| 지속시간     | 선택    | -            | 비디오 길이 옵션                                    |
| 반복          | 불리언  | False        | 비디오를 반복할지 여부                               |
| 시드          | 정수    | 0            | 노드 재실행을 위한 시드 값, 결과는 비결정적입니다 |

<Note>
Ray 1.6 모델을 사용할 경우, 지속시간과 해상도 매개변수는 적용되지 않습니다.
</Note>

### 선택적 매개변수

| 매개변수     | 유형          | 설명                                              |
| ------------- | ------------- | -------------------------------------------------------- |
| luma_concepts | LUMA_CONCEPTS | Luma Concepts 노드를 통해 동작을 제어할 카메라 개념들 |

### 출력

| 출력 | 유형  | 설명     |
| ------ | ----- | --------------- |
| VIDEO  | 비디오 | 생성된 비디오 |

## 사용 예시

<Card title="Luma 텍스트에서 비디오로 워크플로우 예시" icon="book" href="/ko/tutorials/partner-nodes/luma/luma-text-to-video">
Luma 텍스트에서 비디오로 워크플로우 예시
</Card>

## 소스 코드

[노드 소스 코드 (2025-05-03 업데이트)]
```python

class LumaTextToVideoGenerationNode(ComfyNodeABC):
    """
    프롬프트와 출력 크기에 따라 동기적으로 비디오를 생성합니다.
    """

    RETURN_TYPES = (IO.VIDEO,)
    DESCRIPTION = cleandoc(__doc__ or "")  # None 값 처리
    FUNCTION = "api_call"
    API_NODE = True
    CATEGORY = "api node/video/Luma"

    @classmethod
    def INPUT_TYPES(s):
        return {
            "required": {
                "prompt": (
                    IO.STRING,
                    {
                        "multiline": True,
                        "default": "",
                        "tooltip": "비디오 생성을 위한 프롬프트",
                    },
                ),
                "model": ([model.value for model in LumaVideoModel],),
                "aspect_ratio": (
                    [ratio.value for ratio in LumaAspectRatio],
                    {
                        "default": LumaAspectRatio.ratio_16_9,
                    },
                ),
                "resolution": (
                    [resolution.value for resolution in LumaVideoOutputResolution],
                    {
                        "default": LumaVideoOutputResolution.res_540p,
                    },
                ),
                "duration": ([dur.value for dur in LumaVideoModelOutputDuration],),
                "loop": (
                    IO.BOOLEAN,
                    {
                        "default": False,
                    },
                ),
                "seed": (
                    IO.INT,
                    {
                        "default": 0,
                        "min": 0,
                        "max": 0xFFFFFFFFFFFFFFFF,
                        "control_after_generate": True,
                        "tooltip": "노드가 다시 실행될지 결정하는 시드; 실제 결과는 시드와 관계없이 비결정적입니다.",
                    },
                ),
            },
            "optional": {
                "luma_concepts": (
                    LumaIO.LUMA_CONCEPTS,
                    {
                        "tooltip": "선택적 카메라 개념들로 Luma Concepts 노드를 통해 카메라 동작을 지정합니다."
                    },
                ),
            },
            "hidden": {
                "auth_token": "AUTH_TOKEN_COMFY_ORG",
            },
        }

    def api_call(
        self,
        prompt: str,
        model: str,
        aspect_ratio: str,
        resolution: str,
        duration: str,
        loop: bool,
        seed,
        luma_concepts: LumaConceptChain = None,
        auth_token=None,
        **kwargs,
    ):
        duration = duration if model != LumaVideoModel.ray_1_6 else None
        resolution = resolution if model != LumaVideoModel.ray_1_6 else None

        operation = SynchronousOperation(
            endpoint=ApiEndpoint(
                path="/proxy/luma/generations",
                method=HttpMethod.POST,
                request_model=LumaGenerationRequest,
                response_model=LumaGeneration,
            ),
            request=LumaGenerationRequest(
                prompt=prompt,
                model=model,
                resolution=resolution,
                aspect_ratio=aspect_ratio,
                duration=duration,
                loop=loop,
                concepts=luma_concepts.create_api_model() if luma_concepts else None,
            ),
            auth_token=auth_token,
        )
        response_api: LumaGeneration = operation.execute()

        operation = PollingOperation(
            poll_endpoint=ApiEndpoint(
                path=f"/proxy/luma/generations/{response_api.id}",
                method=HttpMethod.GET,
                request_model=EmptyRequest,
                response_model=LumaGeneration,
            ),
            completed_statuses=[LumaState.completed],
            failed_statuses=[LumaState.failed],
            status_extractor=lambda x: x.state,
            auth_token=auth_token,
        )
        response_poll = operation.execute()

        vid_response = requests.get(response_poll.assets.video)
        return (VideoFromFile(BytesIO(vid_response.content)),)

```