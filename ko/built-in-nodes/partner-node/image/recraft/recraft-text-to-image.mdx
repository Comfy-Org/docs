---
title: "텍스트를 이미지로 재구성 - ComfyUI 내장 노드 문서"
description: 텍스트 설명을 기반으로 고품질 이미지를 생성하는 Recraft 파트너 노드입니다.
sidebarTitle: "텍스트를 이미지로 재구성"
icon: "circle"
translationSourceHash: 1fd205f8
translationFrom: built-in-nodes/partner-node/image/recraft/recraft-text-to-image.mdx
---

![ComfyUI 내장 Recraft 텍스트를 이미지로 변환 노드](/images/built-in-nodes/api_nodes/recraft/recraft-text-to-image.jpg)

Recraft 텍스트를 이미지로 변환 노드는 Recraft AI의 이미지 생성 API에 직접 연결하여 다양한 스타일의 고품질 이미지를 생성할 수 있도록 해줍니다.

## 매개변수

### 기본 매개변수

| 매개변수 | 유형   | 기본값   | 설명                    |
| --------- | ------ | --------- | ------------------------------ |
| 프롬프트    | 문자열 | ""        | 이미지에 대한 텍스트 설명 |
| 크기      | 선택   | 1024x1024 | 출력 이미지 크기             |
| n         | 정수    | 1         | 이미지 개수 (1~6)        |
| 시드      | 정수    | 0         | 난수 발생 시드 값             |

### 옵션 매개변수

| 매개변수        | 유형             | 설명                                        |
| --------------- | ---------------- | -------------------------------------------------- |
| recraft_style   | Recraft 스타일    | 이미지 스타일 설정, 기본값은 "사실적인 사진"  |
| negative_prompt | 문자열           | 생성에서 제외할 요소                |
| recraft_controls| Recraft 컨트롤     | 추가 제어 매개변수 (색상 등)       |

### 출력

| 출력 | 유형  | 설명        |
| ------ | ----- | ------------------ |
| IMAGE  | 이미지 | 생성된 이미지(들) |


## 소스 코드

[노드 소스 코드 (2025-05-03 업데이트)]

```python
class RecraftTextToImageNode:
    """
    프롬프트와 해상도를 기반으로 동기식으로 이미지를 생성합니다.
    """

    RETURN_TYPES = (IO.IMAGE,)
    DESCRIPTION = cleandoc(__doc__ or "")  # None 값 처리
    FUNCTION = "api_call"
    API_NODE = True
    CATEGORY = "api node/image/Recraft"

    @classmethod
    def INPUT_TYPES(s):
        return {
            "required": {
                "prompt": (
                    IO.STRING,
                    {
                        "multiline": True,
                        "default": "",
                        "tooltip": "이미지 생성을 위한 프롬프트.",
                    },
                ),
                "size": (
                    [res.value for res in RecraftImageSize],
                    {
                        "default": RecraftImageSize.res_1024x1024,
                        "tooltip": "생성된 이미지의 크기.",
                    },
                ),
                "n": (
                    IO.INT,
                    {
                        "default": 1,
                        "min": 1,
                        "max": 6,
                        "tooltip": "생성할 이미지의 개수.",
                    },
                ),
                "seed": (
                    IO.INT,
                    {
                        "default": 0,
                        "min": 0,
                        "max": 0xFFFFFFFFFFFFFFFF,
                        "control_after_generate": True,
                        "tooltip": "노드가 다시 실행될지 여부를 결정하는 시드; 실제 결과는 시드와 관계없이 비결정적입니다.",
                    },
                ),
            },
            "optional": {
                "recraft_style": (RecraftIO.STYLEV3,),
                "negative_prompt": (
                    IO.STRING,
                    {
                        "default": "",
                        "forceInput": True,
                        "tooltip": "이미지에서 원하지 않는 요소에 대한 선택적 텍스트 설명.",
                    },
                ),
                "recraft_controls": (
                    RecraftIO.CONTROLS,
                    {
                        "tooltip": "Recraft Controls 노드를 통해 생성을 추가로 제어할 수 있는 옵션.",
                    },
                ),
            },
            "hidden": {
                "auth_token": "AUTH_TOKEN_COMFY_ORG",
            },
        }

    def api_call(
        self,
        prompt: str,
        size: str,
        n: int,
        seed,
        recraft_style: RecraftStyle = None,
        negative_prompt: str = None,
        recraft_controls: RecraftControls = None,
        auth_token=None,
        **kwargs,
    ):
        default_style = RecraftStyle(RecraftStyleV3.realistic_image)
        if recraft_style is None:
            recraft_style = default_style

        controls_api = None
        if recraft_controls:
            controls_api = recraft_controls.create_api_model()

        if not negative_prompt:
            negative_prompt = None

        operation = SynchronousOperation(
            endpoint=ApiEndpoint(
                path="/proxy/recraft/image_generation",
                method=HttpMethod.POST,
                request_model=RecraftImageGenerationRequest,
                response_model=RecraftImageGenerationResponse,
            ),
            request=RecraftImageGenerationRequest(
                prompt=prompt,
                negative_prompt=negative_prompt,
                model=RecraftModel.recraftv3,
                size=size,
                n=n,
                style=recraft_style.style,
                substyle=recraft_style.substyle,
                style_id=recraft_style.style_id,
                controls=controls_api,
            ),
            auth_token=auth_token,
        )
        response: RecraftImageGenerationResponse = operation.execute()
        images = []
        for data in response.data:
            image = bytesio_to_image_tensor(
                download_url_to_bytesio(data.url, timeout=1024)
            )
            if len(image.shape) < 4:
                image = image.unsqueeze(0)
            images.append(image)
        output_image = torch.cat(images, dim=0)

        return (output_image,)
```