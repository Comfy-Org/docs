---
title: "WebcamCapture - ComfyUI 내장 노드 문서"
description: "ComfyUI의 WebcamCapture 노드에 대한 전체 문서입니다. 입력, 출력, 매개변수 및 사용법을 알아보세요."
sidebarTitle: "WebcamCapture"
icon: "circle"
mode: wide
translationSourceHash: 20a78d57
translationFrom: built-in-nodes/WebcamCapture.mdx
---
> 이 문서는 AI로 생성되었습니다. 오류를 발견하거나 개선할 점이 있다면 언제든지 기여해 주세요! [GitHub에서 편집하기](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/WebcamCapture/en.md)

WebcamCapture 노드는 웹캠 장치에서 이미지를 캡처하여 ComfyUI 워크플로우 내에서 사용 가능한 형식으로 변환합니다. 이 노드는 LoadImage 노드를 상속하며, 캡처 크기와 타이밍을 제어하는 옵션을 제공합니다. 활성화되면 워크플로우 큐가 처리될 때마다 새 이미지를 캡처할 수 있습니다.

## 입력

| 매개변수 | 데이터 유형 | 필수 | 범위 | 설명 |
|-----------|-----------|------|-------|-------------|
| `image` | WEBCAM | 예 | - | 이미지를 캡처할 웹캠 입력 소스 |
| `width` | INT | 예 | 0부터 MAX_RESOLUTION까지 | 캡처된 이미지의 원하는 너비 (기본값: 0, 웹캠의 기본 해상도 사용) |
| `height` | INT | 예 | 0부터 MAX_RESOLUTION까지 | 캡처된 이미지의 원하는 높이 (기본값: 0, 웹캠의 기본 해상도 사용) |
| `capture_on_queue` | BOOLEAN | 예 | - | 활성화 시 워크플로우 큐가 처리될 때마다 새 이미지를 캡처합니다 (기본값: True) |

**참고:** `width`와 `height` 모두 0으로 설정된 경우, 노드는 웹캠의 기본 해상도를 사용합니다. 어느 한쪽의 치수를 0이 아닌 값으로 설정하면 캡처된 이미지의 크기가 해당 값에 맞게 조정됩니다.

## 출력

| 출력 이름 | 데이터 유형 | 설명 |
|-------------|-----------|-------------|
| `IMAGE` | IMAGE | 캡처된 웹캠 이미지를 ComfyUI의 이미지 형식으로 변환한 결과 |

---
**소스 지문( SHA-256):** `551368150fc293309f917eabaa066f223b1fa1a016ffd3643b57b80c83f812cc`