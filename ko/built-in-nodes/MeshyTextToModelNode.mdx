---
title: "MeshyTextToModelNode - ComfyUI 내장 노드 문서"
description: ComfyUI의 MeshyTextToModelNode 노드에 대한 전체 문서입니다. 입력, 출력, 매개변수 및 사용법을 알아보세요.
sidebarTitle: "MeshyTextToModelNode"
icon: "circle"
mode: wide
translationSourceHash: a1692bf2
translationFrom: built-in-nodes/MeshyTextToModelNode.mdx
---
> 이 문서는 AI로 생성되었습니다. 오류를 발견하거나 개선할 점이 있다면 언제든지 기여해 주세요! [GitHub에서 편집하기](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/MeshyTextToModelNode/en.md)

Meshy: Text to Model 노드는 Meshy API를 사용해 텍스트 설명을 기반으로 3D 모델을 생성합니다. 프롬프트와 설정을 포함한 요청을 API에 전송한 후, 생성이 완료될 때까지 기다렸다가 결과 모델 파일을 다운로드합니다.

## 입력

| 매개변수 | 데이터 타입 | 필수 | 범위 | 설명 |
|-----------|-----------|----------|-------|-------------|
| `model` | COMBO | 예 | `"latest"` | 사용할 AI 모델 버전을 지정합니다. 현재는 "latest" 버전만 사용 가능합니다. |
| `prompt` | STRING | 예 | - | 생성하고자 하는 3D 모델의 텍스트 설명입니다. 길이는 1~600자 사이여야 합니다. |
| `style` | COMBO | 예 | `"realistic"`<br />`"sculpture"` | 생성된 3D 모델의 예술적 스타일입니다. |
| `should_remesh` | DYNAMIC COMBO | 예 | `"true"`<br />`"false"` | 생성된 메시지 처리 여부를 제어합니다. "false"로 설정하면 노드는 처리되지 않은 삼각형 메시를 반환합니다. "true"를 선택하면 토폴로지 및 폴리카운트에 대한 추가 매개변수가 나타납니다. |
| `topology` | COMBO | 아니요* | `"triangle"`<br />`"quad"` | 리메시된 모델의 목표 다각형 유형입니다. 이 매개변수는 `should_remesh`가 "true"로 설정된 경우에만 표시되며 필수입니다. |
| `target_polycount` | INT | 아니요* | 100~300000 | 리메시된 모델의 목표 다각형 수입니다. 기본값은 300000입니다. 이 매개변수는 `should_remesh`가 "true"로 설정된 경우에만 표시되며 필수입니다. |
| `symmetry_mode` | COMBO | 예 | `"auto"`<br />`"on"`<br />`"off"` | 생성된 모델의 대칭성을 제어합니다. |
| `pose_mode` | COMBO | 예 | `""`<br />`"A-pose"`<br />`"T-pose"` | 생성된 모델의 포즈 모드를 지정합니다. 빈 문자열은 특정 포즈를 요청하지 않는 것을 의미합니다. |
| `seed` | INT | 예 | 0~2147483647 | 생성을 위한 시드 값입니다. 이 값을 설정하면 노드가 다시 실행될지 여부를 제어할 수 있지만, 시드 값과 관계없이 결과는 비결정적입니다. 기본값은 0입니다. |

*참고: `topology`와 `target_polycount` 매개변수는 조건부로 필수입니다. `should_remesh` 매개변수가 "true"로 설정된 경우에만 나타나며 반드시 설정해야 합니다.

## 출력

| 출력 이름 | 데이터 타입 | 설명 |
|-------------|-----------|-------------|
| `model_file` | STRING | 생성된 GLB 모델의 파일명입니다. 이 출력은 하위 호환성을 위해 제공됩니다. |
| `meshy_task_id` | MESHY_TASK_ID | Meshy API 작업의 고유 식별자입니다. |
| `GLB` | FILE3DGLB | GLB 형식으로 생성된 3D 모델 파일입니다. |
| `FBX` | FILE3DFBX | FBX 형식으로 생성된 3D 모델 파일입니다. |

---
**소스 지문( SHA-256):** `4b02709a29d3df85e0ab1741b46d3ac74b0fe04135afa435f882b5a02114cc97`