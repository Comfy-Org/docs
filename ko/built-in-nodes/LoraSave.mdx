---
title: "LoraSave - ComfyUI 내장 노드 문서"
description: ComfyUI의 LoraSave 노드에 대한 전체 문서입니다. 입력, 출력, 매개변수 및 사용법을 알아보세요.
sidebarTitle: "LoraSave"
icon: "circle"
mode: wide
translationSourceHash: 09f34405
translationFrom: built-in-nodes/LoraSave.mdx
---
> 이 문서는 AI로 생성되었습니다. 오류를 발견하거나 개선할 점이 있다면 언제든지 기여해 주세요! [GitHub에서 편집하기](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/LoraSave/en.md)

LoraSave 노드는 모델 차이로부터 LoRA(Low-Rank Adaptation) 파일을 추출하고 저장합니다. 이 노드는 확산 모델 차이, 텍스트 인코더 차이 또는 둘 다를 처리하여 지정된 랭크와 유형으로 LoRA 형식으로 변환할 수 있습니다. 생성된 LoRA 파일은 출력 디렉터리에 저장되어 나중에 사용할 수 있습니다.

## 입력

| 매개변수 | 데이터 타입 | 필수 | 범위 | 설명 |
|-----------|-----------|------|-------|-------------|
| `filename_prefix` | 문자열 | 예 | - | 출력 파일명의 접두어 (기본값: "loras/ComfyUI_extracted_lora") |
| `rank` | 정수 | 예 | 1-4096 | LoRA의 랭크 값으로, 크기와 복잡성을 제어합니다 (기본값: 8) |
| `lora_type` | 콤보 | 예 | `"standard"`<br />`"full_diff"` | 생성할 LoRA의 유형입니다. "standard"는 SVD를 통해 저랭크 행렬을 추출하며, "full_diff"는 전체 가중치 차이를 저장합니다 (기본값: "standard") |
| `bias_diff` | 부울 | 예 | - | LoRA 계산에 바이어스 차이를 포함할지 여부 (기본값: True) |
| `model_diff` | 모델 | 아니요 | - | lora로 변환할 ModelSubtract 출력 |
| `text_encoder_diff` | CLIP | 아니요 | - | lora로 변환할 CLIPSubtract 출력 |

**참고:** 노드가 작동하려면 `model_diff` 또는 `text_encoder_diff` 중 적어도 하나가 제공되어야 합니다. 둘 다 생략되면 노드는 아무런 출력을 내놓지 않습니다.

## 출력

| 출력 이름 | 데이터 타입 | 설명 |
|-------------|-----------|-------------|
| - | - | 이 노드는 LoRA 파일을 출력 디렉터리에 저장하지만 워크플로우를 통해 어떠한 데이터도 반환하지 않습니다 |

---
**소스 지문( SHA-256):** `642510bfe083130cb2724d7ae38d30d4694c85a43c7ce27230690ddd9fe76b79`