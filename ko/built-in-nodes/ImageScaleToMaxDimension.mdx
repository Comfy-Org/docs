---
title: "ImageScaleToMaxDimension - ComfyUI 내장 노드 문서"
description: "ComfyUI의 ImageScaleToMaxDimension 노드에 대한 전체 문서입니다. 입력, 출력, 매개변수 및 사용법을 알아보세요."
sidebarTitle: "ImageScaleToMaxDimension"
icon: "circle"
mode: wide
translationSourceHash: 0aa93caf
translationFrom: built-in-nodes/ImageScaleToMaxDimension.mdx
---
> 이 문서는 AI로 생성되었습니다. 오류를 발견하거나 개선할 점이 있다면 언제든지 기여해 주세요! [GitHub에서 편집하기](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/ImageScaleToMaxDimension/en.md)

ImageScaleToMaxDimension 노드는 이미지를 지정된 최대 치수 안에 맞게 크기를 조정하면서 원래의 종횡비를 유지합니다. 노드는 이미지가 세로형인지 가로형인지 판단한 후, 더 큰 쪽의 치수를 목표 크기에 맞춰 확대하고 작은 쪽의 치수는 비례적으로 조정합니다. 이 노드는 다양한 품질과 성능 요구사항에 맞는 여러 가지 업스케일링 방법을 지원합니다.

## 입력

| 매개변수 | 데이터 타입 | 필수 | 범위 | 설명 |
|-----------|-----------|------|-------|-------------|
| `image` | IMAGE | 예 | - | 크기를 조정할 입력 이미지 |
| `upscale_method` | STRING | 예 | "area"<br />"lanczos"<br />"bilinear"<br />"nearest-exact"<br />"bilinear"<br />"bicubic" | 이미지를 스케일링할 때 사용하는 보간 방법 (기본값: "area") |
| `largest_size` | INT | 예 | 0부터 16384까지 | 조정된 이미지의 최대 치수 (기본값: 512) |

## 출력

| 출력 이름 | 데이터 타입 | 설명 |
|-------------|-----------|-------------|
| `image` | IMAGE | 가장 큰 치수가 지정된 크기에 맞도록 조정된 이미지 |

---
**소스 피ingerprint (SHA-256):** `83eba56d9f673fb570edc6504fd82f1274ead1d3e055a1a8bcc207c178388015`