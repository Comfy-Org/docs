---
title: "ComfySoftSwitchNode - ComfyUI 내장 노드 문서"
description: ComfyUI의 ComfySoftSwitchNode 노드에 대한 전체 문서입니다. 입력, 출력, 매개변수 및 사용법을 알아보세요.
sidebarTitle: "ComfySoftSwitchNode"
icon: "circle"
mode: wide
translationSourceHash: 2a173188
translationFrom: built-in-nodes/ComfySoftSwitchNode.mdx
---
> 이 문서는 AI로 생성되었습니다. 오류를 발견하거나 개선할 점이 있다면 언제든지 기여해 주세요! [GitHub에서 편집하기](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/ComfySoftSwitchNode/en.md)

소프트 스위치 노드는 불리언 조건에 따라 두 가지 가능한 입력 값 중 하나를 선택합니다. `switch`가 참일 때는 `on_true` 입력의 값을 출력하고, `switch`가 거짓일 때는 `on_false` 입력의 값을 출력합니다. 이 노드는 레이지 평가 방식으로 설계되어 있어, 스위치 상태에 따라 필요한 입력만 평가됩니다.

## 입력

| 매개변수 | 데이터 타입 | 필수 | 범위 | 설명 |
|-----------|-----------|----------|-------|-------------|
| `switch` | BOOLEAN | 예 | | 어떤 입력을 통과시킬지 결정하는 불리언 조건입니다. 참일 경우 `on_true` 입력이 선택되고, 거짓일 경우 `on_false` 입력이 선택됩니다. |
| `on_false` | MATCH_TYPE | 아니요 | | `switch` 조건이 거짓일 때 출력할 값입니다. 이 입력은 선택사항이지만, `on_false`와 `on_true` 중 적어도 하나는 연결되어야 합니다. |
| `on_true` | MATCH_TYPE | 아니요 | | `switch` 조건이 참일 때 출력할 값입니다. 이 입력은 선택사항이지만, `on_false`와 `on_true` 중 적어도 하나는 연결되어야 합니다. |

**참고:** `on_false`와 `on_true` 입력은 노드의 내부 템플릿에 정의된 대로 동일한 데이터 타입이어야 합니다. 이 두 입력 중 적어도 하나는 연결되어야 노드가 작동합니다.

## 출력

| 출력 이름 | 데이터 타입 | 설명 |
|-------------|-----------|-------------|
| `output` | MATCH_TYPE | 선택된 값입니다. 연결된 `on_false` 또는 `on_true` 입력의 데이터 타입과 일치합니다. |

---
**소스 지문( SHA-256):** `3ddfebaf67642f5bd79b0de38605f93507ede00d95e98cd78ec295f8de98d743`