---
title: "GeminiImage2Node - ComfyUI Built-in Node Documentation"
description: "Google Vertex AI Gemini API를 통해 이미지를 동기적으로 생성하거나 편집합니다. 텍스트 프롬프트를 제공하고 선택적으로 참조 이미지나 Gemini 입력 파일을 첨부할 수 있습니다. 노드는 생성된 이미지와 선택한 응답 모드에 따라 텍스트 응답을 반환합니다."
sidebarTitle: "GeminiImage2Node"
icon: "circle"
mode: wide
---

Google Vertex AI Gemini API를 통해 이미지를 동기적으로 생성하거나 편집합니다. 텍스트 프롬프트를 제공하고 선택적으로 참조 이미지나 Gemini 입력 파일을 첨부할 수 있습니다. 노드는 생성된 이미지와 선택한 응답 모드에 따라 텍스트 응답을 반환합니다.

## 입력

| 매개변수 | 설명 | 데이터 타입 | 필수 | 범위 |
|-----------|-------------|-----------|----------|-------|
| `프롬프트` | 이미지를 생성하거나 적용할 편집을 설명하는 텍스트 프롬프트입니다. 모델이 따라야 할 제약, 스타일 또는 세부 사항을 포함하세요. 프롬프트는 공백 제거 후 최소 한 문자를 포함해야 합니다. | STRING | 예 | N/A |
| `모델` | 생성에 사용할 Gemini 모델입니다. "Nano Banana 2 (Gemini 3.1 Flash Image)" 옵션은 `gemini-3.1-flash-image`로 전송되고, "gemini-3-pro-image-preview"는 `gemini-3-pro-image`로 전송됩니다. | COMBO | 예 | "gemini-3-pro-image-preview"<br />"Nano Banana 2 (Gemini 3.1 Flash Image)" |
| `시드` | 시드가 특정 값으로 고정되면 모델은 반복 요청에 대해 동일한 응답을 제공하기 위해 최선을 다합니다. 결정적 출력은 보장되지 않습니다. 또한 모델이나 temperature와 같은 매개변수 설정을 변경하면 동일한 시드 값을 사용하더라도 응답에 변동이 발생할 수 있습니다. 기본적으로 임의의 시드 값이 사용됩니다. 기본값: 42. | INT | 예 | 0  ~  18446744073709551615 |
| `종횡비` | 'auto'로 설정하면 입력 이미지의 종횡비와 일치합니다. 이미지가 제공되지 않으면 일반적으로 16:9 비율의 이미지가 생성됩니다. 기본값: "auto". | COMBO | 예 | "auto"<br />"1:1"<br />"2:3"<br />"3:2"<br />"3:4"<br />"4:3"<br />"4:5"<br />"5:4"<br />"9:16"<br />"16:9"<br />"21:9" |
| `해상도` | 목표 출력 해상도입니다. 2K/4K의 경우 네이티브 Gemini 업스케일러가 사용됩니다. | COMBO | 예 | "1K"<br />"2K"<br />"4K" |
| `응답 형식` | 이미지만 출력하려면 'IMAGE'를 선택하고, 생성된 이미지와 텍스트 응답을 모두 반환하려면 'IMAGE+TEXT'를 선택합니다. 고급 설정입니다. | COMBO | 예 | "IMAGE+TEXT"<br />"IMAGE" |
| `이미지` | 선택적 참조 이미지입니다. 여러 이미지를 포함하려면 Batch Images 노드를 사용하세요(최대 14개). | IMAGE | 아니요 | N/A |
| `파일` | 모델의 컨텍스트로 사용할 선택적 파일입니다. Gemini Generate Content Input Files 노드의 입력을 받습니다. | GEMINI_INPUT_FILES | 아니요 | N/A |
| `시스템 프롬프트` | AI의 동작을 규정하는 기본 지침입니다. 기본값: 이미지 생성을 위해 미리 정의된 시스템 프롬프트입니다. 고급 설정입니다. | STRING | 아니요 | N/A |

**제약 사항:**

* `images` 입력은 최대 14개의 이미지를 지원합니다. 더 많이 제공되면 오류가 발생합니다.
* 10개를 초과하는 이미지가 제공되면 처음 10개는 URL 참조로 업로드되고 나머지 이미지는 요청에 인라인으로 전송됩니다.
* `files` 입력은 `GEMINI_INPUT_FILES` 데이터 타입을 출력하는 노드에 연결해야 합니다.
* `response_modalities`가 `"IMAGE"`로 설정되면 이미지만 반환되고 텍스트 출력은 비어 있습니다.
* `prompt` 입력은 검증되며 공백 제거 후 최소 한 문자를 포함해야 합니다.

## 출력

| 출력 이름 | 설명 | 데이터 타입 |
|-------------|-------------|-----------|
| `image` | Gemini 모델에 의해 생성되거나 편집된 이미지입니다. | IMAGE |
| `string` | 모델의 텍스트 응답입니다. `response_modalities`가 `"IMAGE"`로 설정된 경우 이 출력은 비어 있습니다. | STRING |

> 이 문서는 AI에 의해 생성되었습니다. 오류를 발견하거나 개선 제안이 있으시면 기여해 주세요! [GitHub에서 편집](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/GeminiImage2Node/ko.md)

---
**Source fingerprint (SHA-256):** `02293dad786d4b441da3174fa76f6c5847f122d294bd7e1f765ffd72420034a4`
