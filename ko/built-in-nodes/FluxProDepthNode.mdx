---
title: "FluxProDepthNode - ComfyUI 내장 노드 문서"
description: ComfyUI의 FluxProDepthNode 노드에 대한 전체 문서입니다. 입력, 출력, 매개변수 및 사용법을 알아보세요.
sidebarTitle: "FluxProDepthNode"
icon: "circle"
mode: wide
translationSourceHash: f562b904
translationFrom: built-in-nodes/FluxProDepthNode.mdx
---
> 이 문서는 AI로 생성되었습니다. 오류를 발견하거나 개선할 점이 있다면 언제든지 기여해 주세요! [GitHub에서 편집하기](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/FluxProDepthNode/en.md)

이 노드는 깊이 제어 이미지를 안내로 사용하여 이미지를 생성합니다. 제어 이미지와 텍스트 프롬프트를 입력받아, 제어 이미지의 깊이 정보와 프롬프트의 설명을 모두 따르는 새로운 이미지를 생성합니다. 이 노드는 외부 API와 연결되어 이미지 생성 과정을 수행합니다.

## 입력

| 매개변수 | 데이터 타입 | 필수 | 범위 | 설명 |
|-----------|-----------|------|-------|-------------|
| `control_image` | IMAGE | 예 | - | 이미지 생성을 안내하는 깊이 제어 이미지 |
| `prompt` | STRING | 아니요 | - | 이미지 생성을 위한 프롬프트 (기본값: 빈 문자열) |
| `prompt_upsampling` | BOOLEAN | 아니요 | - | 프롬프트에 업샘플링을 수행할지 여부. 활성화 시 더 창의적인 생성을 위해 자동으로 프롬프트를 수정하지만 결과는 비결정적입니다(동일한 시드로도 정확히 동일한 결과가 나오지 않음). (기본값: False) |
| `skip_preprocessing` | BOOLEAN | 아니요 | - | 전처리를 건너뛸지 여부; `control_image`가 이미 깊이화된 이미지인 경우 True로 설정하고, 원시 이미지인 경우 False로 설정하세요. (기본값: False) |
| `guidance` | FLOAT | 아니요 | 1-100 | 이미지 생성 과정의 가이던스 강도 (기본값: 15) |
| `steps` | INT | 아니요 | 15-50 | 이미지 생성 과정의 단계 수 (기본값: 50) |
| `seed` | INT | 아니요 | 0-18446744073709551615 | 노이즈 생성에 사용되는 난수 시드. (기본값: 0) |

## 출력

| 출력 이름 | 데이터 타입 | 설명 |
|-------------|-----------|-------------|
| `output_image` | IMAGE | 깊이 제어 이미지와 프롬프트를 기반으로 생성된 이미지 |

---
**출처 지문 (SHA-256):** `34b80d7d63158b7dc4ad02da6b3a573b713d77efd0955d3477409f776f964462`