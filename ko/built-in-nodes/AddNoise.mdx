---
title: "AddNoise - ComfyUI 내장 노드 문서"
description: "ComfyUI의 AddNoise 노드에 대한 전체 문서입니다. 입력, 출력, 매개변수 및 사용법을 알아보세요."
sidebarTitle: "AddNoise"
icon: "circle"
mode: wide
translationSourceHash: 3259c118
translationFrom: built-in-nodes/AddNoise.mdx
---
> 이 문서는 AI로 생성되었습니다. 오류를 발견하거나 개선할 점이 있다면 언제든지 기여해 주세요! [GitHub에서 편집하기](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/AddNoise/en.md)

이 노드는 지정된 노이즈 생성기와 시그마 값을 사용하여 잠재 이미지에 제어된 노이즈를 추가합니다. 입력값을 모델의 샘플링 시스템을 통해 처리하여 주어진 시그마 범위에 적합한 노이즈 스케일링을 적용하고, 노이즈가 적용된 새로운 잠재 표현을 반환합니다.

## 입력

| 매개변수 | 데이터 유형 | 필수 | 범위 | 설명 |
|-----------|-----------|----------|-------|-------------|
| `model` | MODEL | 예 | - | 샘플링 파라미터와 처리 함수를 포함하는 모델 |
| `noise` | NOISE | 예 | - | 기본 노이즈 패턴을 생성하는 노이즈 생성기 |
| `sigmas` | SIGMAS | 예 | - | 노이즈 스케일링 강도를 제어하는 시그마 값들입니다. 비어 있으면 노드는 원래의 잠재 이미지를 그대로 반환합니다. 여러 시그마가 제공되면 첫 번째 시그마와 마지막 시그마 간의 절대 차이를 노이즈 스케일로 계산합니다. 단일 시그마만 제공되면 해당 값이 바로 스케일로 사용됩니다. |
| `latent_image` | LATENT | 예 | - | 노이즈가 추가될 입력 잠재 표현입니다. 빈 잠재 이미지(오직 0만 포함된 이미지)는 처리 과정에서 이동되지 않습니다. |

## 출력

| 출력 이름 | 데이터 유형 | 설명 |
|-------------|-----------|-------------|
| `LATENT` | LATENT | 노이즈가 추가된 수정된 잠재 표현입니다. 출력값에 NaN 또는 무한 값이 있으면 안정성을 위해 0으로 변환됩니다. |

---
**소스 지문( SHA-256):** `7e2b3e7f55c7c4380e831be5a558c506a6328ad2ec974f550ed30b40ddfab037`