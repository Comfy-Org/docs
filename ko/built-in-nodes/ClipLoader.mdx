---
title: "CLIPLoader - ComfyUI 내장 노드 문서"
description: "ComfyUI의 CLIPLoader 노드에 대한 전체 문서입니다. 입력, 출력, 매개변수 및 사용법을 알아보세요."
sidebarTitle: "CLIPLoader"
icon: "circle"
mode: wide
translationSourceHash: 975ee668
translationFrom: built-in-nodes/ClipLoader.mdx
---
> 이 문서는 AI로 생성되었습니다. 오류를 발견하거나 개선할 점이 있다면 언제든지 기여해 주세요! [GitHub에서 편집하기](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/CLIPLoader/en.md)

CLIPLoader 노드는 텍스트 인코더 모델(CLIP, T5 또는 유사한 모델)을 파일에서 로드하여, 텍스트 프롬프트를 수치적 표현으로 변환해야 하는 다른 노드에서 사용할 수 있도록 합니다. 이 노드는 다양한 모델 아키텍처를 지원하며, 각 아키텍처마다 특정한 인코더 유형이 필요합니다.

## 입력

| 매개변수 | 데이터 타입 | 필수 | 범위 | 설명 |
|-----------|-----------|------|-------|-------------|
| `clip_name` | 문자열 | 예 | `text_encoders` 폴더에서 찾은 파일 목록 | 로드할 텍스트 인코더 모델의 파일 이름입니다. 이 파일은 반드시 `ComfyUI/models/text_encoders/` 또는 `ComfyUI/models/clip/` 디렉토리에 있어야 합니다. |
| `type` | 문자열 | 예 | `"stable_diffusion"`<br />`"stable_cascade"`<br />`"sd3"`<br />`"stable_audio"`<br />`"mochi"`<br />`"ltxv"`<br />`"pixart"`<br />`"cosmos"`<br />`"lumina2"`<br />`"wan"`<br />`"hidream"`<br />`"chroma"`<br />`"ace"`<br />`"omnigen2"`<br />`"qwen_image"`<br />`"hunyuan_image"`<br />`"flux2"`<br />`"ovis"`<br />`"longcat_image"`<br />`"cogvideox"` | 로드하는 모델의 아키텍처 유형입니다. 이에 따라 사용할 특정 인코더 변형이 결정됩니다. 기본값은 `"stable_diffusion"`입니다. |
| `device` | 문자열 | 아니요 | `"default"`<br />`"cpu"` | 모델을 로드할 장치입니다. `"default"`는 GPU가 있으면 GPU를 사용하고, `"cpu"`는 CPU 로드를 강제합니다. 이는 고급 옵션입니다(기본값: `"default"`). |

### 지원되는 유형별 인코더 매핑

`type` 매개변수는 주어진 모델 아키텍처에 맞는 올바른 인코더를 선택합니다. 다음은 일반적인 매핑입니다:

| 유형 | 인코더 |
|------|---------|
| stable_diffusion | clip-l |
| stable_cascade | clip-g |
| sd3 | t5 xxl / clip-g / clip-l |
| stable_audio | t5 base |
| mochi | t5 xxl |
| cogvideox | t5 xxl (226토큰 패딩) |
| cosmos | old t5 xxl |
| lumina2 | gemma 2 2B |
| wan | umt5 xxl |
| hidream | llama-3.1 (권장) 또는 t5 |
| omnigen2 | qwen vl 2.5 3B |

## 출력

| 출력 이름 | 데이터 타입 | 설명 |
|-------------|-----------|-------------|
| `clip` | CLIP | 로드된 텍스트 인코더 모델로, 다른 노드와 연결하여 텍스트 인코딩 및 조건부 처리에 바로 사용할 수 있습니다. |

---
**소스 지문( SHA-256):** `1051bfe5570dff81719682cb09938bae4c03e94e0e72f7a2be84867cccb48017`