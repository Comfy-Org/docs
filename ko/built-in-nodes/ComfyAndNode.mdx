---
title: "ComfyAndNode - ComfyUI 내장 노드 문서"
description: ComfyUI의 ComfyAndNode 노드에 대한 전체 문서입니다. 입력, 출력, 매개변수 및 사용법을 알아보세요.
sidebarTitle: "ComfyAndNode"
icon: "circle"
mode: wide
translationSourceHash: 3083c461
translationFrom: built-in-nodes/ComfyAndNode.mdx
---
> 이 문서는 AI로 생성되었습니다. 오류를 발견하거나 개선할 점이 있으면 언제든지 기여해 주세요! [GitHub에서 편집하기](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/ComfyAndNode/en.md)

## 개요

And 노드는 입력값들의 집합에 논리적 AND 연산을 수행합니다. 제공된 모든 값이 파이썬의 참성 규칙에 따라 참으로 간주되는 경우에만 `true`를 반환합니다. 이 노드는 여러 조건이 모두 충족되었는지 확인한 후에 작업을 진행하는 데 유용합니다.

## 입력

| 매개변수 | 데이터 타입 | 필수 | 범위 | 설명 |
|-----------|-----------|------|-------|-------------|
| `values` | ANY | 예 | 1개 이상의 값 | 평가할 값들의 리스트입니다. 노드는 최소 1개의 값을 받으며, 노드의 "+" 버튼을 클릭하여 더 추가할 수 있습니다. |

**참고:** 노드는 파이썬의 참성 규칙을 사용하여 값이 `true`인지 `false`인지 판단합니다. 예를 들어, 빈 문자열, 숫자 0, 빈 리스트, `None`은 모두 `false`로 간주됩니다. 그 외의 모든 값은 `true`로 간주됩니다.

## 출력

| 출력 이름 | 데이터 타입 | 설명 |
|-------------|-----------|-------------|
| `BOOLEAN` | BOOLEAN | 모든 입력값이 참인 경우 `true`를 반환하고, 그렇지 않으면 `false`를 반환합니다. |

---
**소스 지문( SHA-256):** `fd9d18ce698472a7e35ad3082f2ccff8ae264b11bd887a498f929cd877ff38c4`