---
title: "CheckpointSave - ComfyUI 내장 노드 문서"
description: ComfyUI의 CheckpointSave 노드에 대한 완벽한 문서입니다. 입력, 출력, 매개변수 및 사용법을 알아보세요.
sidebarTitle: "CheckpointSave"
icon: "circle"
mode: wide
translationSourceHash: ef0ca4b6
translationFrom: built-in-nodes/CheckpointSave.mdx
---
`Save Checkpoint` 노드는 전체 스테이블 디퓨전 모델(UNet, CLIP, VAE 구성 요소 포함)을 **.safetensors** 형식의 체크포인트 파일로 저장하도록 설계되었습니다.

Save Checkpoint 노드는 주로 모델 병합 워크플로우에서 사용됩니다. `ModelMergeSimple`, `ModelMergeBlocks`와 같은 노드를 통해 새로운 병합 모델을 생성한 후, 이 노드를 사용해 결과를 재사용 가능한 체크포인트 파일로 저장할 수 있습니다.

## 입력

| 매개변수 | 데이터 유형 | 설명 |
|-----------|-----------|-------------|
| `model` | MODEL | 모델 매개변수는 상태를 저장하려는 기본 모델을 나타냅니다. 향후 복원이나 분석을 위해 모델의 현재 상태를 캡처하는 데 필수적입니다. |
| `clip` | CLIP | clip 매개변수는 기본 모델과 연관된 CLIP 모델을 위한 것으로, 기본 모델과 함께 상태를 저장할 수 있도록 합니다. |
| `vae` | VAE | vae 매개변수는 변분 오토인코더(VAE) 모델을 위한 것으로, 기본 모델 및 CLIP과 함께 상태를 저장해 향후 사용하거나 분석할 수 있도록 합니다. |
| `filename_prefix` | STRING | 이 매개변수는 체크포인트가 저장될 파일명의 접두어를 지정합니다. |

또한 노드에는 메타데이터를 위한 두 개의 숨겨진 입력이 있습니다:

**prompt (PROMPT)**: 워크플로우 프롬프트 정보  
**extra_pnginfo (EXTRA_PNGINFO)**: 추가 PNG 정보  

## 출력

이 노드는 체크포인트 파일을 출력하며, 해당 출력 파일 경로는 `output/checkpoints/` 디렉터리입니다.

## 아키텍처 호환성

- 현재 완벽히 지원됨: SDXL, SD3, SVD 및 기타 주류 아키텍처, [소스 코드](https://github.com/comfyanonymous/ComfyUI/blob/master/comfy_extras/nodes_model_merging.py#L176-L189) 참조
- 기본 지원: 다른 아키텍처도 저장 가능하지만 표준화된 메타데이터 정보는 제공되지 않습니다.

## 관련 링크

관련 소스 코드: [nodes_model_merging.py#L227](https://github.com/comfyanonymous/ComfyUI/blob/master/comfy_extras/nodes_model_merging.py#L227)