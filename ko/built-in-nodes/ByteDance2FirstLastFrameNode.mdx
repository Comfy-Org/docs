---
title: "ByteDance2FirstLastFrameNode - ComfyUI 내장 노드 문서"
description: ComfyUI의 ByteDance2FirstLastFrameNode 노드에 대한 전체 문서입니다. 입력, 출력, 매개변수 및 사용법을 알아보세요.
sidebarTitle: "ByteDance2FirstLastFrameNode"
icon: "circle"
mode: wide
translationSourceHash: 72e5e741
translationFrom: built-in-nodes/ByteDance2FirstLastFrameNode.mdx
---
> 이 문서는 AI로 생성되었습니다. 오류를 발견하거나 개선할 점이 있다면 언제든지 기여해 주세요! [GitHub에서 편집하기](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/ByteDance2FirstLastFrameNode/en.md)

이 노드는 ByteDance의 Seedance 2.0 모델을 사용하여 동영상을 생성합니다. 텍스트 프롬프트와 필수 첫 번째 프레임 이미지를 기반으로 동영상을 만듭니다. 선택적으로 마지막 프레임 이미지를 제공하여 동영상 시퀀스의 끝 부분을 안내할 수 있습니다.

## 입력

| 매개변수 | 데이터 타입 | 필수 | 범위 | 설명 |
|-----------|-----------|------|-------|-------------|
| `model` | COMBO | 예 | `"Seedance 2.0"`<br />`"Seedance 2.0 Fast"` | 동영상 생성에 사용할 모델입니다. Seedance 2.0은 최고 품질을 위해, Seedance 2.0 Fast는 속도를 최적화한 모델입니다. 모델을 선택하면 `prompt`, `resolution`, `ratio`, `duration`, `generate_audio`에 대한 추가 입력이 표시됩니다. |
| `first_frame` | IMAGE | 아니요 | - | 동영상의 첫 번째 프레임으로 사용할 이미지입니다. |
| `last_frame` | IMAGE | 아니요 | - | 동영상의 마지막 프레임으로 사용할 이미지입니다. |
| `first_frame_asset_id` | STRING | 아니요 | - | 첫 번째 프레임으로 사용할 Seedance asset_id입니다. 이는 `first_frame` 이미지 입력과 동시에 사용할 수 없습니다. 기본값은 빈 문자열입니다. |
| `last_frame_asset_id` | STRING | 아니요 | - | 마지막 프레임으로 사용할 Seedance asset_id입니다. 이는 `last_frame` 이미지 입력과 동시에 사용할 수 없습니다. 기본값은 빈 문자열입니다. |
| `seed` | INT | 아니요 | 0부터 2147483647까지 | 시드 값입니다. 이 시드를 변경하면 노드가 다시 실행되지만 결과는 비결정적입니다. 기본값은 0입니다. |
| `watermark` | BOOLEAN | 아니요 | - | 생성된 동영상에 워터마크를 추가할지 여부입니다. 기본값은 False입니다. |

**매개변수 제약사항:**
*   `first_frame` 이미지와 `first_frame_asset_id` 중 하나만 제공해야 합니다. 둘 다 제공하면 오류가 발생합니다.
*   같은 프레임에 대해 `last_frame` 이미지와 `last_frame_asset_id`를 동시에 제공할 수 없습니다.
*   `model` 입력은 동적 콤보입니다. 모델을 선택한 후에는 표시된 `prompt` 필드(텍스트 설명)를 반드시 입력하고, 다른 표시된 매개변수들(`resolution`, `ratio`, `duration`, `generate_audio`)도 설정해야 합니다.

## 출력

| 출력 이름 | 데이터 타입 | 설명 |
|-------------|-----------|-------------|
| `output` | VIDEO | 생성된 동영상입니다. |

---
**소스 지문자 (SHA-256):** `434d6ad8f005515677b0de73b57df916aa589adff94aad892827202e6ebcb6e1`