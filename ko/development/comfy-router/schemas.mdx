---
title: "Comfy Router 모델 스키마와 결과"
sidebarTitle: "스키마와 결과"
description: "Router 모델의 OpenAPI 입력 및 출력 스키마를 가져오고, ETag로 캐시하고, 모델의 네이티브 결과 형태를 확인합니다."
translationSourceHash: 9b69e6fc
translationFrom: development/comfy-router/schemas.mdx
---

각 Router 모델은 자체 네이티브 요청 본문을 받아들이고 자체 네이티브 결과를 반환합니다. 모델의 스키마를 확인하면 두 가지를 모두 파악할 수 있습니다.

## 입력 및 출력 스키마 확인

각 모델은 독립형 OpenAPI 문서를 제공합니다:

```bash
curl --dump-header schema-headers.txt \
  -H "X-API-Key: $COMFY_API_KEY" \
  https://api.comfy.org/v2/models/bfl/flux-2-pro/openapi.json
```

모델 오퍼레이션 내에서 `requestBody`는 입력을 설명하고, `200` 응답은 출력 스키마가 작성된 경우 출력을 설명합니다. 입력 검증과 출력 문서화는 서로 다릅니다. Router는 입력 스키마에 대해서는 검증하지만, 반환된 공급자 결과를 출력 스키마에 대해서는 검증하지 않습니다.

출력의 필드뿐 아니라 미디어 타입도 확인하세요. 작성되지 않은 출력은 `*/*`를 사용할 수 있으며, 일부 모델은 JSON 대신 바이너리 데이터를 반환합니다.

### 스키마 캐시하기

스키마와 해당 `ETag`를 저장하세요. 나중에 스키마를 가져올 때 그 ETag를 `If-None-Match`에 전달하세요. `304`에는 본문이 없으므로 캐시된 문서를 유지하세요. `200`은 대체 문서와 ETag를 제공합니다.

```bash
curl -H "X-API-Key: $COMFY_API_KEY" \
  -H 'If-None-Match: "previous-etag-value"' \
  https://api.comfy.org/v2/models/bfl/flux-2-pro/openapi.json
```

스키마 라우트는 `Cache-Control: private, must-revalidate`를 사용합니다. 인증된 응답은 공유 캐시에 넣지 마세요. 이 ETag/304 동작은 스키마 엔드포인트에만 적용됩니다.

## 검증 및 폴백 스키마

작성된 입력 스키마는 공급자 호출 이전에 `422`와 `detail[]` 배열로 잘못된 필드를 거부합니다. `loc`에 있는 필드 경로를 확인하세요. 자세한 내용은 [검증 오류](/ko/development/comfy-router/errors#검증-오류)를 참조하세요.

일부 스키마는 모든 JSON 객체를 허용하고 `x-comfy-input-schema-authored: false`를 설정합니다. Router는 모델별 검증 없이 해당 요청을 전달하므로, 공급자가 여전히 이를 거부할 수 있습니다.

## 결과 확인

Router는 각 모델의 터미널 결과 형태를 반환합니다. 공통된 이미지, 비디오, 텍스트 봉투는 없습니다. BFL 이미지 출력은 `result.sample`을 사용하는 반면, 다른 모델은 URL 목록이나 인라인 바이트를 반환할 수 있습니다.

일부 에셋 URL은 Comfy에서 재호스팅하며, 다른 URL은 공급자 URL 또는 인라인 바이트로 남습니다. [결과 에셋](/ko/development/comfy-router/reference#결과-에셋)을 확인하고 만료되는 에셋은 신속히 다운로드하세요. 재생(replay)은 URL을 갱신하지 않습니다.

## 다음

- [대기 중 요청](/ko/development/comfy-router/queue): 실행을 제출하고 나중에 결과를 수집합니다.
- [오류와 재시도](/ko/development/comfy-router/errors): 두 번 비용을 지불하지 않고 실패를 처리합니다.