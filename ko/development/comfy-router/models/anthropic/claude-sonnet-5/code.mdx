---
title: "Comfy Router에서 Claude Sonnet 5 사용하기"
description: "Comfy Router를 통해 anthropic/claude-sonnet-5를 호출합니다. 엔드포인트, 요청 형태, Router가 반환하는 응답을 다룹니다."
sidebarTitle: "Claude Sonnet 5"
translationSourceHash: 65026c35
translationFrom: development/comfy-router/models/anthropic/claude-sonnet-5/code.mdx
translationBlockHashes:
  "_intro": 816f3f43
  "Quick start": 54b3b756
  "Schema": ed894fd9
  "Examples": a3ab5037
---
{/* 생성된 파일. `pnpm code-pages:gen`으로 router-schemas/anthropic/claude-sonnet-5.json에서 생성되었습니다. */}

import RouterCodeFooter from "/snippets/ko/comfy-router/model-code-footer.mdx";

`anthropic/claude-sonnet-5`의 API 레퍼런스이며, Anthropic에서 Comfy Router를 통해 제공됩니다.

## 빠른 시작

[Comfy 워크스페이스](https://platform.comfy.org/profile/api-keys)에서 키를 생성하고 `COMFY_API_KEY`로 내보내세요. Python 및 TypeScript 스니펫은 Comfy SDK(`pip install comfy-sdk`, `npm install @comfyorg/sdk`)를 사용하며, cURL 스니펫은 동일한 호출을 원시 HTTP로 수행합니다.

**모델 ID:** `anthropic/claude-sonnet-5`

**엔드포인트:** `POST https://api.comfy.org/v2/models/anthropic/claude-sonnet-5`

<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "anthropic/claude-sonnet-5",
        {
            "max_tokens": 16,
            "messages": [
                {
                    "content": "Reply with the single word: ok",
                    "role": "user",
                },
            ],
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const { data } = await comfy.models.run("anthropic/claude-sonnet-5", {
  max_tokens: 16,
  messages: [
    {
      content: "Reply with the single word: ok",
      role: "user",
    },
  ],
});

console.log(data);
```

```bash cURL
curl https://api.comfy.org/v2/models/anthropic/claude-sonnet-5 \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"max_tokens\": 16, \"messages\": [{\"content\":\"Reply with the single word: ok\",\"role\":\"user\"}]}"
```
</CodeGroup>

## 스키마

### 입력

<ParamField body="max_tokens" type="integer" required>
  생성을 중단하기 전까지 생성할 최대 토큰 수입니다.
</ParamField>

<ParamField body="messages" type="object[]" required>
  대화 턴입니다. 전체 콘텐츠 블록 분류는 Anthropic Messages API 문서를 참고하세요.
</ParamField>

<ParamField body="messages[].content" type="object" required>
  문자열 축약형 또는 콘텐츠 블록의 배열(text, image, document, tool_use, tool_result, ...) 중 하나입니다.
</ParamField>

<ParamField body="messages[].role" type="string" required>
  사용 가능한 값: `user`, `assistant`
</ParamField>

<ParamField body="model" type="string">
  Anthropic 모델 식별자입니다(예: `claude-sonnet-4-5`, `claude-opus-4-7`).
</ParamField>

<ParamField body="stream" type="boolean">
  참인 경우, 응답은 단일 JSON 본문 대신 Anthropic 메시지 이벤트의 `text/event-stream`이 됩니다.
</ParamField>

<ParamField body="system" type="object">
  최상위 시스템 프롬프트입니다. 문자열 또는 콘텐츠 블록의 배열이며, 둘 다 변경 없이 Anthropic에 전달됩니다.
</ParamField>

Router가 `GET /v2/models/anthropic/claude-sonnet-5/openapi.json`에서 제공하는 스키마로부터 생성되었으며, 이는 요청이 공급자에 도달하기 이전에 호출을 검증하는 데 사용하는 것과 동일한 문서입니다.

### 출력

<ResponseField name="id" type="string">
   
</ResponseField>

<ResponseField name="model" type="string">
   
</ResponseField>

<ResponseField name="role" type="string">
   
</ResponseField>

<ResponseField name="stop_reason" type="string">
   
</ResponseField>

<ResponseField name="stop_sequence" type="string">
   
</ResponseField>

<ResponseField name="type" type="string">
   
</ResponseField>

<ResponseField name="usage" type="object">
  Anthropic Messages API 호출의 토큰 사용량입니다.
</ResponseField>

<ResponseField name="usage.cache_creation" type="object">
  Anthropic Messages API 호출의 캐시 쓰기 입력 토큰에 대한 TTL별 분석입니다.
</ResponseField>

<ResponseField name="usage.cache_creation.ephemeral_1h_input_tokens" type="integer">
   
</ResponseField>

<ResponseField name="usage.cache_creation.ephemeral_5m_input_tokens" type="integer">
   
</ResponseField>

<ResponseField name="usage.cache_creation_input_tokens" type="integer">
   
</ResponseField>

<ResponseField name="usage.cache_read_input_tokens" type="integer">
   
</ResponseField>

<ResponseField name="usage.input_tokens" type="integer">
   
</ResponseField>

<ResponseField name="usage.output_tokens" type="integer">
   
</ResponseField>

<ResponseField name="content" type="object[]" required>
  응답의 콘텐츠 블록을 순서대로 나타냅니다. 완료된 모든 메시지에 존재하며, 턴이 아무것도 생성하지 않은 경우 비어 있습니다.
</ResponseField>

<ResponseField name="content[].text" type="string">
  블록의 텍스트입니다. `text` 블록에만 존재하며 다른 종류에는 없습니다.
</ResponseField>

<ResponseField name="content[].type" type="string">
  블록의 종류입니다. `text`는 `text`를 담는 유일한 종류이며, `thinking`, `redacted_thinking`, `tool_use`, `server_tool_use` 및 도구 결과 블록은 현재 Anthropic이 전송하는 다른 종류들이고, 이 목록은 열려 있습니다.
</ResponseField>

## 예시

### 입력

```json
{
  "max_tokens": 16,
  "messages": [
    {
      "content": "Reply with the single word: ok",
      "role": "user"
    }
  ]
}
```

### 출력

```json
{
  "content": [
    {
      "text": "ok",
      "type": "text"
    }
  ],
  "id": "msg_01ExampleInvalidPlaceholder",
  "model": "claude-sonnet-5",
  "role": "assistant",
  "stop_reason": "end_turn",
  "stop_sequence": null,
  "type": "message",
  "usage": {
    "input_tokens": 16,
    "output_tokens": 3
  }
}
```


<RouterCodeFooter />
