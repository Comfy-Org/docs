---
title: "Comfy Router로 Claude Sonnet 4.6 사용하기"
description: "Comfy Router를 통해 anthropic/claude-sonnet-4-6을 호출합니다: 엔드포인트, 요청 형태, Router가 반환하는 응답을 설명합니다."
sidebarTitle: "Claude Sonnet 4.6"
translationSourceHash: 73dd4552
translationFrom: development/comfy-router/models/anthropic/claude-sonnet-4-6/code.mdx
translationBlockHashes:
  "_intro": fdc3383a
  "Quick start": ae6e2892
  "Schema": 7e4df7aa
  "Examples": 7fb5285d
---

{/* 생성된 파일. `pnpm code-pages:gen`이 router-schemas/anthropic/claude-sonnet-4-6.json에서 생성했습니다. */}

import RouterCodeFooter from "/snippets/ko/comfy-router/model-code-footer.mdx";

Anthropic으로부터 Comfy Router가 제공하는 `anthropic/claude-sonnet-4-6`의 API 레퍼런스입니다.

## 빠른 시작

[Comfy 워크스페이스](https://platform.comfy.org/profile/api-keys?onboarding=router)에서 키를 생성하고 `COMFY_API_KEY`로 내보내세요. Python, TypeScript, Swift 스니펫은 Comfy SDK(`pip install comfy-sdk`, `npm install @comfyorg/sdk`, 그리고 [`ComfySwiftSDK`](https://github.com/Comfy-Org/comfy-swift-sdk) Swift 패키지)를 사용하며, cURL 스니펫은 동일한 호출을 raw HTTP로 수행합니다.

**모델 ID:** `anthropic/claude-sonnet-4-6`

**엔드포인트:** `POST https://api.comfy.org/v2/models/anthropic/claude-sonnet-4-6`

<Tabs>
  <Tab title="결과 대기">
<CodeGroup>
```python Python
from comfy_sdk import Comfy

# 환경 변수에서 COMFY_API_KEY를 읽습니다.
# SDK는 자동으로 멱등성 키를 생성하고 자동 재시도를 위해 이를 재사용합니다.
with Comfy() as client:
    result = client.models.run(
        "anthropic/claude-sonnet-4-6",
        {
            "max_tokens": 16,
            "messages": [
                {
                    "content": "Reply with the single word: ok",
                    "role": "user",
                },
            ],
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// 환경 변수에서 COMFY_API_KEY를 읽습니다.
// SDK는 자동으로 멱등성 키를 생성하고 자동 재시도를 위해 이를 재사용합니다.
const { data } = await comfy.models.run("anthropic/claude-sonnet-4-6", {
  max_tokens: 16,
  messages: [
    {
      content: "Reply with the single word: ok",
      role: "user",
    },
  ],
});

console.log(data);
```

```swift Swift
import Foundation
import ComfySwiftSDK

// 환경 변수에서 COMFY_API_KEY를 읽습니다.
// SDK는 호출마다 멱등성 키를 발급하고 자동 재시도를 위해 이를 재사용합니다.
let client = ComfyCloudClient(apiKey: ProcessInfo.processInfo.environment["COMFY_API_KEY"]!)
let result = try await client.models.run(
    "anthropic/claude-sonnet-4-6",
    input: [
        "max_tokens": 16,
        "messages": [
            [
                "content": "Reply with the single word: ok",
                "role": "user",
            ],
        ],
    ]
)

print(result.output)
```

```bash cURL
curl https://api.comfy.org/v2/models/anthropic/claude-sonnet-4-6 \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"max_tokens\": 16, \"messages\": [{\"content\":\"Reply with the single word: ok\",\"role\":\"user\"}]}"
```
</CodeGroup>
  </Tab>
  <Tab title="대기열에 넣고 나중에 수집">
동일한 본문을 `POST https://api.comfy.org/v2/models/anthropic/claude-sonnet-4-6/requests` 로 보냅니다. Router는 실행이 접수되는 즉시 `201` 과 `request_id` 를 응답하며, 결과는 준비가 되면 이 프로세스든 다른 프로세스든 수집할 수 있습니다. 상태, 취소, 수집 방법은 [대기열 전달](/ko/development/comfy-router/queue)에서 다룹니다.

<CodeGroup>
```python Python
from comfy_sdk import Comfy

# 환경에서 COMFY_API_KEY를 읽습니다.
# 각 submit() 호출은 자체 Idempotency-Key를 생성하고 자동 재시도에 재사용합니다.
with Comfy() as client:
    handle = client.models.submit(
        "anthropic/claude-sonnet-4-6",
        {
            "max_tokens": 16,
            "messages": [
                {
                    "content": "Reply with the single word: ok",
                    "role": "user",
                },
            ],
        },
    )
    print("request_id:", handle.request_id)  # 모델 ID와 함께라면 다른 프로세스에 필요한 전부입니다

    # 서버가 지정한 Retry-After만큼 기다리면서 요청이 완료될 때까지 폴링합니다.
    for update in handle.iter_events():
        print(update.status, update.queue_position)

    # 공급자의 자체 페이로드로, models.run()이 반환하는 것과 동일한 값입니다.
    # 실패했거나 취소된 요청은 여기에서 타입이 지정된 Router 오류를 발생시킵니다.
    result = handle.get()

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// 환경에서 COMFY_API_KEY를 읽습니다.
// 각 submit() 호출은 자체 Idempotency-Key를 생성하고 자동 재시도에 재사용합니다.
const handle = await comfy.models.submit("anthropic/claude-sonnet-4-6", {
  max_tokens: 16,
  messages: [
    {
      content: "Reply with the single word: ok",
      role: "user",
    },
  ],
});
console.log("requestId:", handle.requestId); // 모델 ID와 함께라면 다른 프로세스에 필요한 전부입니다

// 서버가 지정한 Retry-After만큼 기다리면서 요청이 완료될 때까지 폴링합니다.
for await (const update of handle.events()) {
  console.log(update.status, update.queuePosition);
}

// models.run()이 반환하는 것과 동일한 결과입니다. 실패했거나 취소된 요청은 여기에서 거부됩니다.
const result = await handle.get();

console.log(result.data);
```

```swift Swift
import Foundation
import ComfySwiftSDK

// 환경에서 COMFY_API_KEY를 읽습니다.
// 각 submit() 호출은 자체 Idempotency-Key를 생성하고 자동 재시도에 재사용합니다.
let client = ComfyCloudClient(apiKey: ProcessInfo.processInfo.environment["COMFY_API_KEY"]!)
let handle = try await client.models.submit(
    "anthropic/claude-sonnet-4-6",
    input: [
        "max_tokens": 16,
        "messages": [
            [
                "content": "Reply with the single word: ok",
                "role": "user",
            ],
        ],
    ]
)
print("requestId:", handle.requestId)  // 모델 ID와 함께라면 다른 프로세스에 필요한 전부입니다

// 서버가 지정한 Retry-After만큼 기다리면서 요청이 완료될 때까지 폴링합니다.
for try await update in handle.events() {
    print(update.state.rawValue, update.queuePosition.map(String.init) ?? "unknown")
}

// 공급자의 자체 페이로드로, models.run()이 반환하는 것과 동일한 값입니다.
// 실패했거나 취소된 요청은 여기에서 타입이 지정된 Router 오류를 던집니다.
let result = try await handle.result()

print(result.output)
```

```bash cURL
# 1. 제출합니다. Router는 request_id, status_url, response_url, cancel_url과 함께 201을 응답합니다.
curl https://api.comfy.org/v2/models/anthropic/claude-sonnet-4-6/requests \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"max_tokens\": 16, \"messages\": [{\"content\":\"Reply with the single word: ok\",\"role\":\"user\"}]}"

# 2. 상태가 COMPLETED가 될 때까지 폴링하며, 각 응답이 지정한 Retry-After 초만큼 대기합니다.
REQUEST_ID="<request_id from the 201 body>"
curl -i https://api.comfy.org/v2/models/anthropic/claude-sonnet-4-6/requests/$REQUEST_ID/status \
  -H "X-API-Key: $COMFY_API_KEY"

# 3. 수집합니다. 완료되면 모델의 네이티브 출력과 함께 200, 아직 실행 중이면 상태 본문과 함께 202를 반환합니다.
curl https://api.comfy.org/v2/models/anthropic/claude-sonnet-4-6/requests/$REQUEST_ID \
  -H "X-API-Key: $COMFY_API_KEY"
```
</CodeGroup>
  </Tab>
</Tabs>

## 스키마

### 입력

<ParamField body="max_tokens" type="integer" required>
  중지하기 전에 생성할 최대 토큰 수입니다.
</ParamField>

<ParamField body="messages" type="object[]" required>
  대화 턴입니다. 전체 콘텐츠 블록 분류 체계는 Anthropic Messages API 문서를 참고하세요.
</ParamField>

<ParamField body="messages[].content" type="object" required>
  문자열 축약형이거나 콘텐츠 블록의 배열입니다 (text, image, document, tool_use, tool_result, ...).
</ParamField>

<ParamField body="messages[].role" type="string" required>
  가능한 값: `user`, `assistant`
</ParamField>

<ParamField body="model" type="string">
  Anthropic 모델 식별자입니다 (예: `claude-sonnet-4-5`, `claude-opus-4-7`).
</ParamField>

<ParamField body="stream" type="boolean">
  true이면 응답은 단일 JSON 본문 대신 Anthropic 메시지 이벤트의 `text/event-stream`입니다.
</ParamField>

<ParamField body="system" type="object">
  최상위 시스템 프롬프트입니다. 문자열이거나 콘텐츠 블록의 배열이며, 둘 다 변경 없이 Anthropic에 전달됩니다.
</ParamField>

Router가 `GET /v2/models/anthropic/claude-sonnet-4-6/openapi.json`에서 제공하는 스키마에서 생성되었으며, 이는 요청이 공급자에 도달하기 전에 호출을 검증하는 데 사용하는 것과 동일한 문서입니다.

### 출력

<ResponseField name="id" type="string">
   
</ResponseField>

<ResponseField name="model" type="string">
   
</ResponseField>

<ResponseField name="role" type="string">
   
</ResponseField>

<ResponseField name="stop_reason" type="string">
   
</ResponseField>

<ResponseField name="stop_sequence" type="string">
   
</ResponseField>

<ResponseField name="type" type="string">
   
</ResponseField>

<ResponseField name="usage" type="object">
  Anthropic Messages API 호출의 토큰 사용량입니다.
</ResponseField>

<ResponseField name="usage.cache_creation" type="object">
  Anthropic Messages API 호출의 캐시 쓰기 입력 토큰에 대한 TTL별 내역입니다.
</ResponseField>

<ResponseField name="usage.cache_creation.ephemeral_1h_input_tokens" type="integer">
   
</ResponseField>

<ResponseField name="usage.cache_creation.ephemeral_5m_input_tokens" type="integer">
   
</ResponseField>

<ResponseField name="usage.cache_creation_input_tokens" type="integer">
   
</ResponseField>

<ResponseField name="usage.cache_read_input_tokens" type="integer">
   
</ResponseField>

<ResponseField name="usage.input_tokens" type="integer">
   
</ResponseField>

<ResponseField name="usage.output_tokens" type="integer">
   
</ResponseField>

<ResponseField name="content" type="object[]" required>
  응답의 콘텐츠 블록이며 순서대로 나열됩니다. 완료된 모든 메시지에 존재하며, 턴이 아무것도 생성하지 않았다면 비어 있습니다.
</ResponseField>

<ResponseField name="content[].text" type="string">
  블록의 텍스트입니다. `text` 블록에 존재하며 다른 모든 종류에는 없습니다.
</ResponseField>

<ResponseField name="content[].type" type="string">
  블록의 종류입니다. `text`는 `text`를 담는 유일한 블록이며, `thinking`, `redacted_thinking`, `tool_use`, `server_tool_use` 및 도구 결과 블록은 오늘날 Anthropic이 전송하는 다른 종류들이고, 이 목록은 열려 있습니다.
</ResponseField>

## 예시

### 입력

```json
{
  "max_tokens": 16,
  "messages": [
    {
      "content": "Reply with the single word: ok",
      "role": "user"
    }
  ]
}
```

### 출력

```json
{
  "content": [
    {
      "text": "ok",
      "type": "text"
    }
  ],
  "id": "msg_01ExampleInvalidPlaceholder",
  "model": "claude-sonnet-4-6",
  "role": "assistant",
  "stop_reason": "end_turn",
  "stop_sequence": null,
  "type": "message",
  "usage": {
    "input_tokens": 16,
    "output_tokens": 3
  }
}
```

<RouterCodeFooter />
