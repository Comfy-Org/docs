---
title: "Comfy Router로 Photon 1 사용하기"
description: "Comfy Router를 통해 luma/photon-1을 호출합니다: endpoint, 요청 형태, 그리고 Router가 반환하는 응답입니다."
sidebarTitle: "Photon 1"
translationSourceHash: 310f8788
translationFrom: development/comfy-router/models/luma/photon-1/code.mdx
translationBlockHashes:
  "_intro": 021fca05
  "Quick start": 6d34bab0
  "Schema": 497edabd
  "Examples": 92a7a3f8
---
{/* GENERATED FILE. Generated from router-schemas/luma/photon-1.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/ko/comfy-router/model-code-footer.mdx";

`luma/photon-1`의 API 레퍼런스로, Luma에서 Comfy Router를 통해 제공됩니다.

## 빠른 시작

[Comfy 워크스페이스](https://platform.comfy.org/profile/api-keys)에서 키를 생성하고 `COMFY_API_KEY`로 내보내세요. Python과 TypeScript 스니펫은 Comfy SDK(`pip install comfy-sdk`, `npm install @comfyorg/sdk`)를 사용하며, cURL 스니펫은 동일한 호출을 원시 HTTP로 수행합니다.

**모델 ID:** `luma/photon-1`

**엔드포인트:** `POST https://api.comfy.org/v2/models/luma/photon-1`

<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "luma/photon-1",
        {
            "aspect_ratio": "1:1",
            "prompt": "a red circle on a plain white background",
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const { data } = await comfy.models.run("luma/photon-1", {
  aspect_ratio: "1:1",
  prompt: "a red circle on a plain white background",
});

console.log(data);
```

```bash cURL
curl https://api.comfy.org/v2/models/luma/photon-1 \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"aspect_ratio\": \"1:1\", \"prompt\": \"a red circle on a plain white background\"}"
```
</CodeGroup>

## 스키마

### 입력

<ParamField body="aspect_ratio" type="string" default="&quot;16:9&quot;">
  생성 이미지의 가로세로 비율

  가능한 값: `1:1`, `16:9`, `9:16`, `4:3`, `3:4`, `21:9`, `9:21`
</ParamField>

<ParamField body="callback_url" type="string (uri)">
  생성 작업의 콜백 URL

  형식: `uri`
</ParamField>

<ParamField body="character_ref" type="object">
   
</ParamField>

<ParamField body="character_ref.identity0" type="object">
  이미지 정체성(identity) 객체
</ParamField>

<ParamField body="character_ref.identity0.images" type="string (uri)[]">
  이미지 정체성(identity)의 URL 목록
</ParamField>

<ParamField body="generation_type" type="string" default="&quot;image&quot;">
  가능한 값: `image`
</ParamField>

<ParamField body="image_ref" type="object[]">
  이미지 참조 객체
</ParamField>

<ParamField body="image_ref[].url" type="string (uri)">
  이미지 참조의 URL

  형식: `uri`
</ParamField>

<ParamField body="image_ref[].weight" type="number">
  이미지 참조의 가중치
</ParamField>

<ParamField body="model" type="string">
  생성에 사용되는 이미지 모델입니다. Comfy Router의 `POST /v2/models/luma/{model}` 경로에서는 이 필드가 경로에서 제공되므로 반드시 전송해서는 안 됩니다. v1의 `POST /proxy/luma/generations/image` 경로에서는 LumaImageModel enum(`photon-1`, `photon-flash-1`)으로 제한되며, Rewrite는 모델이 누락되었거나 알 수 없는 경우 요청을 거부하여 이를 강제합니다.
</ParamField>

<ParamField body="modify_image_ref" type="object">
  수정 이미지 참조 객체
</ParamField>

<ParamField body="modify_image_ref.url" type="string (uri)">
  이미지 참조의 URL

  형식: `uri`
</ParamField>

<ParamField body="modify_image_ref.weight" type="number">
  수정 이미지 참조의 가중치
</ParamField>

<ParamField body="prompt" type="string">
  생성 프롬프트
</ParamField>

<ParamField body="style_ref" type="object[]">
  이미지 참조 객체
</ParamField>

<ParamField body="style_ref[].url" type="string (uri)">
  이미지 참조의 URL

  형식: `uri`
</ParamField>

<ParamField body="style_ref[].weight" type="number">
  이미지 참조의 가중치
</ParamField>

Router가 `GET /v2/models/luma/photon-1/openapi.json`에서 제공하는 스키마를 기반으로 생성되었으며, 이는 요청이 공급자에게 도달하기 이전에 호출을 검증하는 데 사용하는 동일한 문서입니다.### 출력

<ResponseField name="assets" type="object">
  이미지 생성의 에셋입니다. `image`가 결과이며, `video`와 `progress_video`는 이 상태 라우트를 공유하는 Dream Machine VIDEO 작업에 속하는데, Luma는 이를 여기에 명시적인 null로 보냅니다.
</ResponseField>

<ResponseField name="assets.image" type="string (uri)">
  생성된 이미지의 URL입니다. `luma/photon-*` 생성이 채우는 리프입니다. 생성이 `completed`에 도달할 때까지는 null입니다.

  Format: `uri`
</ResponseField>

<ResponseField name="assets.progress_video" type="string (uri)">
  이미지 생성에서는 항상 null입니다. VIDEO 생성이 실행되는 동안 Luma가 게시하는 진행 중 미리보기입니다.

  Format: `uri`
</ResponseField>

<ResponseField name="assets.video" type="string (uri)">
  이미지 생성에서는 항상 null입니다. 대신 `luma/ray-*` 비디오 모델이 이 필드를 채웁니다.

  Format: `uri`
</ResponseField>

<ResponseField name="created_at" type="string (date-time)">
  생성이 만들어진 날짜와 시간

  Format: `date-time`
</ResponseField>

<ResponseField name="failure_reason" type="string">
  생성 상태의 이유
</ResponseField>

<ResponseField name="generation_type" type="string">
  생성의 유형이며, 이 작업에서는 항상 `image`입니다

  Possible values: `image`
</ResponseField>

<ResponseField name="id" type="string (uuid)">
  생성의 ID

  Format: `uuid`
</ResponseField>

<ResponseField name="model" type="string">
  생성에 사용된 모델
</ResponseField>

<ResponseField name="request" type="object">
  이미지 생성 요청으로, 터미널 문서 안에 그대로 반환됩니다. Luma는 요청 모델의 모든 필드를 직렬화하면서 호출자가 설정하지 않은 각 필드에 명시적인 `null`을 기록하므로, 필드의 존재 여부는 키가 아니라 그 값으로 판단해야 합니다.
</ResponseField>

<ResponseField name="request.aspect_ratio" type="string" default="&quot;16:9&quot;">
  생성의 가로세로 비율

  Possible values: `1:1`, `16:9`, `9:16`, `4:3`, `3:4`, `21:9`, `9:21`
</ResponseField>

<ResponseField name="request.callback_url" type="string (uri)">
  생성의 콜백 URL

  Format: `uri`
</ResponseField>

<ResponseField name="request.character_ref" type="object">
   
</ResponseField>

<ResponseField name="request.character_ref.identity0" type="object">
  터미널 문서 안으로 다시 들어올 때의 이미지 아이덴티티입니다. `LumaImageIdentity`와 같은 형태이며, 멤버가 nullable인 이유는 `LumaImageRefEcho`에서 설명한 것과 같습니다.
</ResponseField>

<ResponseField name="request.character_ref.identity0.images" type="string (uri)[]">
  이미지 아이덴티티의 URL
</ResponseField>

<ResponseField name="request.generation_type" type="string">
  설정된 경우 항상 `image`이며, 작업을 그대로 반영합니다. 호출자가 보내지 않은 경우에는 null인데, 이 에코 객체 전체가 존재하는 이유와 같습니다.
</ResponseField>

<ResponseField name="request.image_ref" type="object[]">
  터미널 문서 안으로 다시 들어올 때의 이미지 참조입니다. `LumaImageRef`와 같은 형태이며, 각 멤버가 nullable인 이유는 Luma가 설정되지 않은 값을 생략하는 대신 명시적인 `null`로 에코하기 때문입니다.
</ResponseField>

<ResponseField name="request.image_ref[].url" type="string (uri)">
  이미지 참조의 URL

  Format: `uri`
</ResponseField>

<ResponseField name="request.image_ref[].weight" type="number">
  이미지 참조의 가중치
</ResponseField>

<ResponseField name="request.model" type="string" default="&quot;photon-1&quot;">
  생성에 사용된 이미지 모델

  Possible values: `photon-1`, `photon-flash-1`
</ResponseField>

<ResponseField name="request.modify_image_ref" type="object">
  터미널 문서 안으로 다시 들어올 때의 이미지 수정 참조입니다. `LumaModifyImageRef`와 같은 형태이며, 멤버가 nullable인 이유는 `LumaImageRefEcho`에서 설명한 것과 같습니다.
</ResponseField>

<ResponseField name="request.modify_image_ref.url" type="string (uri)">
  이미지 참조의 URL

  Format: `uri`
</ResponseField>

<ResponseField name="request.modify_image_ref.weight" type="number">
  이미지 수정 참조의 가중치
</ResponseField>

<ResponseField name="request.prompt" type="string">
  생성의 프롬프트
</ResponseField>

<ResponseField name="request.style_ref" type="object[]">
  터미널 문서 안으로 다시 들어올 때의 이미지 참조입니다. `LumaImageRef`와 같은 형태이며, 각 멤버가 nullable인 이유는 Luma가 설정되지 않은 값을 생략하는 대신 명시적인 `null`로 에코하기 때문입니다.
</ResponseField>

<ResponseField name="request.style_ref[].url" type="string (uri)">
  이미지 참조의 URL

  Format: `uri`
</ResponseField>

<ResponseField name="request.style_ref[].weight" type="number">
  이미지 참조의 가중치
</ResponseField>

<ResponseField name="state" type="string">
  생성의 상태

  Possible values: `queued`, `dreaming`, `completed`, `failed`
</ResponseField>

## 예시

### 입력

```json
{
  "aspect_ratio": "1:1",
  "prompt": "a red circle on a plain white background"
}
```

### 출력

```json
{
  "assets": {
    "image": "https://example.invalid/luma/photon-1/generated.png",
    "progress_video": null,
    "video": null
  },
  "created_at": "2027-01-01T00:00:00Z",
  "failure_reason": null,
  "generation_type": "image",
  "id": "5e8b06d7-91c4-4a2f-8b3d-6f7a8b9c0d1e",
  "model": "photon-1",
  "request": {
    "aspect_ratio": "1:1",
    "callback_url": null,
    "character_ref": null,
    "generation_type": "image",
    "image_ref": null,
    "model": "photon-1",
    "modify_image_ref": null,
    "prompt": "a red circle on a plain white background",
    "style_ref": null
  },
  "state": "completed"
}
```

<RouterCodeFooter />
