---
title: "Comfy Router로 Ray Flash 2 사용하기"
description: "Comfy Router를 통해 luma/ray-flash-2를 호출하는 방법을 설명합니다: 엔드포인트, 요청 형태, Router가 반환하는 응답."
sidebarTitle: "Ray Flash 2"
translationSourceHash: a76a94e1
translationFrom: development/comfy-router/models/luma/ray-flash-2/code.mdx
translationBlockHashes:
  "_intro": 1fb59d13
  "Quick start": ff9e17ce
  "Schema": 8009af73
  "Examples": cb7953b0
---

{/* GENERATED FILE. Generated from router-schemas/luma/ray-flash-2.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/ko/comfy-router/model-code-footer.mdx";

Luma에서 Comfy Router로 제공하는 `luma/ray-flash-2`의 API 레퍼런스입니다.

## 빠른 시작

[Comfy 워크스페이스](https://platform.comfy.org/profile/api-keys)에서 키를 생성하고 `COMFY_API_KEY`로 내보내세요. Python 및 TypeScript 스니펫은 Comfy SDK(`pip install comfy-sdk`, `npm install @comfyorg/sdk`)를 사용하며, cURL 스니펫은 동일한 호출을 순수 HTTP로 수행합니다.

**모델 ID:** `luma/ray-flash-2`

**엔드포인트:** `POST https://api.comfy.org/v2/models/luma/ray-flash-2`

<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "luma/ray-flash-2",
        {
            "aspect_ratio": "16:9",
            "duration": "5s",
            "prompt": "a single red maple leaf resting on a plain white background",
            "resolution": "540p",
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const { data } = await comfy.models.run("luma/ray-flash-2", {
  aspect_ratio: "16:9",
  duration: "5s",
  prompt: "a single red maple leaf resting on a plain white background",
  resolution: "540p",
});

console.log(data);
```

```bash cURL
curl https://api.comfy.org/v2/models/luma/ray-flash-2 \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"aspect_ratio\": \"16:9\", \"duration\": \"5s\", \"prompt\": \"a single red maple leaf resting on a plain white background\", \"resolution\": \"540p\"}"
```
</CodeGroup>

## 스키마

### 입력

<ParamField body="aspect_ratio" type="string" required default="&quot;16:9&quot;">
  생성의 가로세로 비율

  가능한 값: `1:1`, `16:9`, `9:16`, `4:3`, `3:4`, `21:9`, `9:21`
</ParamField>

<ParamField body="callback_url" type="string (uri)">
  생성의 콜백 URL입니다. 생성이 dreaming, completed 또는 failed 상태가 되면 Generation 객체를 담은 POST 요청이 콜백 URL로 전송됩니다.

  형식: `uri`
</ParamField>

<ParamField body="duration" type="`5s`, `9s` | string" required>
   
</ParamField>

<ParamField body="generation_type" type="string" default="&quot;video&quot;">
  가능한 값: `video`
</ParamField>

<ParamField body="keyframes" type="object">
  생성의 키프레임
</ParamField>

<ParamField body="keyframes.frame0" type="object">
  키프레임은 Generation 참조, 이미지 또는 비디오 중 하나일 수 있습니다
</ParamField>

<ParamField body="keyframes.frame1" type="object">
  키프레임은 Generation 참조, 이미지 또는 비디오 중 하나일 수 있습니다
</ParamField>

<ParamField body="loop" type="boolean">
  비디오를 반복 재생할지 여부
</ParamField>

<ParamField body="model" type="string">
  생성에 사용되는 비디오 모델입니다. Comfy Router 경로 `POST /v2/models/luma/{model}`에서는 이 필드가 경로에서 제공되며 전송해서는 안 됩니다(MUST NOT). v1 `POST /proxy/luma/generations` 경로에서는 필수이며 LumaVideoModel enum(`ray-2`, `ray-flash-2`)으로 제한됩니다.
</ParamField>

<ParamField body="prompt" type="string" required>
  생성의 프롬프트
</ParamField>

<ParamField body="resolution" type="`540p`, `720p`, `1080p`, `4k` | string" required>
   
</ParamField>

`GET /v2/models/luma/ray-flash-2/openapi.json`에서 Router가 제공하는 스키마에서 생성되었으며, 이는 요청이 공급자에 도달하기 전에 호출을 검증하는 데 사용하는 것과 동일한 문서입니다.

### 출력

<ResponseField name="assets" type="object">
  생성의 에셋
</ResponseField>

<ResponseField name="assets.image" type="string (uri)">
  이미지의 URL

  형식: `uri`
</ResponseField>

<ResponseField name="assets.progress_video" type="string (uri)">
  진행 비디오의 URL

  형식: `uri`
</ResponseField>

<ResponseField name="assets.video" type="string (uri)">
  비디오의 URL

  형식: `uri`
</ResponseField>

<ResponseField name="created_at" type="string (date-time)">
  생성이 만들어진 날짜와 시간

  형식: `date-time`
</ResponseField>

<ResponseField name="failure_reason" type="string">
  생성 상태의 원인
</ResponseField>

<ResponseField name="generation_type" type="string">
  가능한 값: `video`, `image`
</ResponseField>

<ResponseField name="id" type="string (uuid)">
  생성의 ID

  형식: `uuid`
</ResponseField>

<ResponseField name="model" type="string">
  생성에 사용된 모델
</ResponseField>

<ResponseField name="request" type="object">
  생성의 요청
</ResponseField>

<ResponseField name="state" type="string">
  생성의 상태

  가능한 값: `queued`, `dreaming`, `completed`, `failed`
</ResponseField>

## 예시

### 입력

```json
{
  "aspect_ratio": "16:9",
  "duration": "5s",
  "prompt": "a single red maple leaf resting on a plain white background",
  "resolution": "540p"
}
```

### 출력

```json
{
  "assets": {
    "video": "https://example.invalid/luma/ray-flash-2/generated.mp4"
  },
  "created_at": "2027-01-01T00:00:00Z",
  "generation_type": "video",
  "id": "8c41d0b6-2e7a-4f35-b1d8-9a0e6f3c2b71",
  "model": "ray-flash-2",
  "state": "completed"
}
```


<RouterCodeFooter />
