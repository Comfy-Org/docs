---
title: "Comfy Router로 AI Skin Enhancer Flexible 사용하기"
description: "Comfy Router를 통해 freepik/ai-skin-enhancer-flexible을 호출합니다: 엔드포인트, 요청 형태, Router가 반환하는 응답."
sidebarTitle: "AI Skin Enhancer Flexible"
translationSourceHash: 726097c0
translationFrom: development/comfy-router/models/freepik/ai-skin-enhancer-flexible/code.mdx
translationBlockHashes:
  "_intro": d6c2cb7e
  "Quick start": 4d0b7b2d
  "Schema": 70d4c10f
  "Examples": 4255df63
---

{/* GENERATED FILE. Generated from router-schemas/freepik/ai-skin-enhancer-flexible.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/ko/comfy-router/model-code-footer.mdx";

`freepik/ai-skin-enhancer-flexible`에 대한 API 레퍼런스입니다. Freepik의 모델을 Comfy Router에서 제공합니다.

## 빠른 시작

[Comfy 워크스페이스](https://platform.comfy.org/profile/api-keys?onboarding=router)에서 키를 생성하고 `COMFY_API_KEY`로 내보내세요. Python, TypeScript, Swift 스니펫은 Comfy SDK(`pip install comfy-sdk`, `npm install @comfyorg/sdk`, 그리고 [`ComfySwiftSDK`](https://github.com/Comfy-Org/comfy-swift-sdk) Swift 패키지)를 사용합니다. cURL 스니펫은 원시 HTTP를 통한 동일한 호출입니다.

**모델 ID:** `freepik/ai-skin-enhancer-flexible`

**엔드포인트:** `POST https://api.comfy.org/v2/models/freepik/ai-skin-enhancer-flexible`

<Tabs>
  <Tab title="결과 기다리기">
<CodeGroup>
```python Python
from comfy_sdk import Comfy

# 환경 변수에서 COMFY_API_KEY를 읽습니다.
# SDK는 자동으로 멱등성 키를 생성하고 자동 재시도에 재사용합니다.
with Comfy() as client:
    result = client.models.run(
        "freepik/ai-skin-enhancer-flexible",
        {
            "image": "https://img.magnific.com/free-photo/portrait-woman_395237-33.jpg?w=740&q=80",
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// 환경 변수에서 COMFY_API_KEY를 읽습니다.
// SDK는 자동으로 멱등성 키를 생성하고 자동 재시도에 재사용합니다.
const { data } = await comfy.models.run("freepik/ai-skin-enhancer-flexible", {
  image: "https://img.magnific.com/free-photo/portrait-woman_395237-33.jpg?w=740&q=80",
});

console.log(data);
```

```swift Swift
import Foundation
import ComfySwiftSDK

// 환경 변수에서 COMFY_API_KEY를 읽습니다.
// SDK는 호출마다 멱등성 키를 생성하고 자동 재시도에 재사용합니다.
let client = ComfyCloudClient(apiKey: ProcessInfo.processInfo.environment["COMFY_API_KEY"]!)
let result = try await client.models.run(
    "freepik/ai-skin-enhancer-flexible",
    input: [
        "image": "https://img.magnific.com/free-photo/portrait-woman_395237-33.jpg?w=740&q=80",
    ]
)

print(result.output)
```

```bash cURL
curl https://api.comfy.org/v2/models/freepik/ai-skin-enhancer-flexible \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"image\": \"https://img.magnific.com/free-photo/portrait-woman_395237-33.jpg?w=740&q=80\"}"
```
</CodeGroup>
  </Tab>
  <Tab title="실행 대기열에 넣고 나중에 수집">
동일한 본문을 `POST https://api.comfy.org/v2/models/freepik/ai-skin-enhancer-flexible/requests` 로 보냅니다. Router는 실행이 접수되는 즉시 `201` 과 `request_id` 를 응답하며, 결과는 준비가 되는 대로 이 프로세스나 다른 프로세스에서 수집할 수 있습니다. 상태, 취소, 수집 방법은 [큐 전송](/ko/development/comfy-router/queue) 에서 다룹니다.

<CodeGroup>
```python Python
from comfy_sdk import Comfy

# 환경 변수에서 COMFY_API_KEY 를 읽습니다.
# 각 submit() 호출은 자체 Idempotency-Key 를 생성하고 자동 재시도에 재사용합니다.
with Comfy() as client:
    handle = client.models.submit(
        "freepik/ai-skin-enhancer-flexible",
        {
            "image": "https://img.magnific.com/free-photo/portrait-woman_395237-33.jpg?w=740&q=80",
        },
    )
    print("request_id:", handle.request_id)  # 모델 ID 와 함께라면 다른 프로세스에 필요한 전부입니다

    # 요청이 완료될 때까지 폴링하며, 서버가 지정한 Retry-After 만큼 대기합니다.
    for update in handle.iter_events():
        print(update.status, update.queue_position)

    # 공급자 자체의 페이로드이며, models.run() 이 반환하는 것과 동일한 값입니다.
    # 실패하거나 취소된 요청은 여기에서 타입이 지정된 Router 오류를 발생시킵니다.
    result = handle.get()

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// 환경 변수에서 COMFY_API_KEY 를 읽습니다.
// 각 submit() 호출은 자체 Idempotency-Key 를 생성하고 자동 재시도에 재사용합니다.
const handle = await comfy.models.submit("freepik/ai-skin-enhancer-flexible", {
  image: "https://img.magnific.com/free-photo/portrait-woman_395237-33.jpg?w=740&q=80",
});
console.log("requestId:", handle.requestId); // 모델 ID 와 함께라면 다른 프로세스에 필요한 전부입니다

// 요청이 완료될 때까지 폴링하며, 서버가 지정한 Retry-After 만큼 대기합니다.
for await (const update of handle.events()) {
  console.log(update.status, update.queuePosition);
}

// models.run() 이 반환하는 것과 동일한 결과입니다. 실패하거나 취소된 요청은 여기에서 거부됩니다.
const result = await handle.get();

console.log(result.data);
```

```swift Swift
import Foundation
import ComfySwiftSDK

// 환경 변수에서 COMFY_API_KEY 를 읽습니다.
// 각 submit() 호출은 자체 Idempotency-Key 를 생성하고 자동 재시도에 재사용합니다.
let client = ComfyCloudClient(apiKey: ProcessInfo.processInfo.environment["COMFY_API_KEY"]!)
let handle = try await client.models.submit(
    "freepik/ai-skin-enhancer-flexible",
    input: [
        "image": "https://img.magnific.com/free-photo/portrait-woman_395237-33.jpg?w=740&q=80",
    ]
)
print("requestId:", handle.requestId)  // 모델 ID 와 함께라면 다른 프로세스에 필요한 전부입니다

// 요청이 완료될 때까지 폴링하며, 서버가 지정한 Retry-After 만큼 대기합니다.
for try await update in handle.events() {
    print(update.state.rawValue, update.queuePosition.map(String.init) ?? "unknown")
}

// 공급자 자체의 페이로드이며, models.run() 이 반환하는 것과 동일한 값입니다.
// 실패하거나 취소된 요청은 여기에서 타입이 지정된 Router 오류를 던집니다.
let result = try await handle.result()

print(result.output)
```

```bash cURL
# 1. 제출. Router는 request_id, status_url, response_url, cancel_url 과 함께 201 을 응답합니다.
curl https://api.comfy.org/v2/models/freepik/ai-skin-enhancer-flexible/requests \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"image\": \"https://img.magnific.com/free-photo/portrait-woman_395237-33.jpg?w=740&q=80\"}"

# 2. 상태가 COMPLETED 가 될 때까지 폴링하며, 각 응답이 지정한 Retry-After 초만큼 대기합니다.
REQUEST_ID="<request_id from the 201 body>"
curl -i https://api.comfy.org/v2/models/freepik/ai-skin-enhancer-flexible/requests/$REQUEST_ID/status \
  -H "X-API-Key: $COMFY_API_KEY"

# 3. 수집. 모델의 네이티브 출력과 함께 200, 아직 실행 중일 때는 상태 본문과 함께 202 입니다.
curl https://api.comfy.org/v2/models/freepik/ai-skin-enhancer-flexible/requests/$REQUEST_ID \
  -H "X-API-Key: $COMFY_API_KEY"
```
</CodeGroup>
  </Tab>
</Tabs>

## 스키마

### 입력

<ParamField body="image" type="string" required>
  입력 이미지. Base64 인코딩 또는 HTTPS URL(공개적으로 접근 가능해야 함)을 지원합니다.
</ParamField>

<ParamField body="optimized_for" type="string" default="&quot;enhance_skin&quot;">
  flexible skin enhancer의 최적화 대상

  사용 가능한 값: `enhance_skin`, `improve_lighting`, `enhance_everything`, `transform_to_real`, `no_make_up`
</ParamField>

<ParamField body="sharpen" type="integer" default="0">
  샤프닝 강도

  범위: `0` ~ `100`
</ParamField>

<ParamField body="smart_grain" type="integer" default="2">
  스마트 그레인 강도

  범위: `0` ~ `100`
</ParamField>

<ParamField body="webhook_url" type="string (uri)">
  비동기 알림을 위한 선택적 콜백 URL.

  형식: `uri`
</ParamField>

Router가 `GET /v2/models/freepik/ai-skin-enhancer-flexible/openapi.json`에서 제공하는 스키마를 기반으로 생성되었으며, 이는 요청이 공급자에게 도달하기 전에 Router가 호출을 검증하는 데 사용하는 것과 동일한 문서입니다.

### 출력

<ResponseField name="data" type="object" required>
   
</ResponseField>

<ResponseField name="data.generated" type="string (uri)[]" required>
  완료된 생성 작업의 이미지 URL입니다. Router가 반환하는 문서에 존재하며 비어 있지 않습니다. 이 목록이 곧 결과입니다. 링크는 Freepik 자체의 것이며 만료됩니다.
</ResponseField>

<ResponseField name="data.status" type="string" required>
  사용 가능한 값: `COMPLETED`
</ResponseField>

<ResponseField name="data.task_id" type="string (uuid)">
  형식: `uuid`
</ResponseField>

## 예시

### 입력

```json
{
  "image": "https://img.magnific.com/free-photo/portrait-woman_395237-33.jpg?w=740&q=80"
}
```

### 출력

```json
{
  "data": {
    "generated": [
      "https://example.invalid/freepik/ai-skin-enhancer-flexible/enhanced.png"
    ],
    "status": "COMPLETED",
    "task_id": "046b6c7f-0b8a-43b9-b35d-6489e6daee91"
  }
}
```

<RouterCodeFooter />
