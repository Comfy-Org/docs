---
title: "Comfy Router로 AI Skin Enhancer Creative 사용하기"
description: "Comfy Router를 통해 freepik/ai-skin-enhancer-creative를 호출합니다: endpoint, 요청 형태, Router가 반환하는 응답입니다."
sidebarTitle: "AI Skin Enhancer Creative"
translationSourceHash: d3af36a1
translationFrom: development/comfy-router/models/freepik/ai-skin-enhancer-creative/code.mdx
translationBlockHashes:
  "_intro": e723a5f3
  "Quick start": c26388b9
  "Schema": 914d2ba2
  "Examples": f51546bd
---
{/* GENERATED FILE. Generated from router-schemas/freepik/ai-skin-enhancer-creative.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/ko/comfy-router/model-code-footer.mdx";

`freepik/ai-skin-enhancer-creative`의 API 레퍼런스입니다. Comfy Router가 Freepik에서 제공합니다.

## 빠른 시작

[Comfy 워크스페이스](https://platform.comfy.org/profile/api-keys)에서 키를 생성하고 `COMFY_API_KEY`로 내보내세요. Python 및 TypeScript 스니펫은 Comfy SDK(`pip install comfy-sdk`, `npm install @comfyorg/sdk`)를 사용하며, cURL 스니펫은 동일한 호출을 순수 HTTP로 수행합니다.

**모델 ID:** `freepik/ai-skin-enhancer-creative`

**엔드포인트:** `POST https://api.comfy.org/v2/models/freepik/ai-skin-enhancer-creative`

<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "freepik/ai-skin-enhancer-creative",
        {
            "image": "https://img.magnific.com/free-photo/portrait-woman_395237-33.jpg?w=740&q=80",
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const { data } = await comfy.models.run("freepik/ai-skin-enhancer-creative", {
  image: "https://img.magnific.com/free-photo/portrait-woman_395237-33.jpg?w=740&q=80",
});

console.log(data);
```

```bash cURL
curl https://api.comfy.org/v2/models/freepik/ai-skin-enhancer-creative \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"image\": \"https://img.magnific.com/free-photo/portrait-woman_395237-33.jpg?w=740&q=80\"}"
```
</CodeGroup>

## 스키마

### 입력

<ParamField body="image" type="string" required>
  입력 이미지. Base64 인코딩 또는 HTTPS URL을 지원합니다 (공개적으로 접근 가능해야 함).
</ParamField>

<ParamField body="sharpen" type="integer" default="0">
  샤프닝 강도

  범위: `0` ~ `100`
</ParamField>

<ParamField body="smart_grain" type="integer" default="2">
  스마트 그레인 강도

  범위: `0` ~ `100`
</ParamField>

<ParamField body="webhook_url" type="string (uri)">
  비동기 알림을 위한 선택적 콜백 URL입니다.

  형식: `uri`
</ParamField>

Router가 `GET /v2/models/freepik/ai-skin-enhancer-creative/openapi.json`에서 제공하는 스키마에서 생성되었으며, 이는 요청이 공급자에 도달하기 이전에 호출을 검증하는 데 사용하는 것과 동일한 문서입니다.

### 출력

<ResponseField name="data" type="object" required>
   
</ResponseField>

<ResponseField name="data.generated" type="string (uri)[]" required>
  완료된 생성의 이미지 URL입니다. Router가 반환하는 문서에서 항상 존재하며 비어 있지 않습니다. 이 목록이 곧 결과입니다. 링크는 Freepik 자체 링크이며 만료됩니다.
</ResponseField>

<ResponseField name="data.status" type="string" required>
  가능한 값: `COMPLETED`
</ResponseField>

<ResponseField name="data.task_id" type="string (uuid)">
  형식: `uuid`
</ResponseField>

## 예시

### 입력

```json
{
  "image": "https://img.magnific.com/free-photo/portrait-woman_395237-33.jpg?w=740&q=80"
}
```

### 출력

```json
{
  "data": {
    "generated": [
      "https://example.invalid/freepik/ai-skin-enhancer-creative/enhanced.png"
    ],
    "status": "COMPLETED",
    "task_id": "046b6c7f-0b8a-43b9-b35d-6489e6daee91"
  }
}
```

<RouterCodeFooter />
