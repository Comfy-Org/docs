---
title: "Comfy Router로 AI Skin Enhancer Faithful 사용하기"
description: "Comfy Router를 통해 freepik/ai-skin-enhancer-faithful을 호출합니다: endpoint, 요청 형태, 그리고 Router가 반환하는 응답을 설명합니다."
sidebarTitle: "AI Skin Enhancer Faithful"
translationSourceHash: ed54bebb
translationFrom: development/comfy-router/models/freepik/ai-skin-enhancer-faithful/code.mdx
translationBlockHashes:
  "_intro": 79e82606
  "Quick start": 210f017c
  "Schema": dd345c18
  "Examples": b81e3b45
---


{/* GENERATED FILE. Generated from router-schemas/freepik/ai-skin-enhancer-faithful.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/ko/comfy-router/model-code-footer.mdx";

`freepik/ai-skin-enhancer-faithful`의 API 레퍼런스이며, Comfy Router가 Freepik에서 제공합니다.

## 빠른 시작

[Comfy 워크스페이스](https://platform.comfy.org/profile/api-keys?onboarding=router)에서 키를 생성하고 `COMFY_API_KEY`로 내보내세요. Python과 TypeScript 스니펫은 Comfy SDK(`pip install comfy-sdk`, `npm install @comfyorg/sdk`)를 사용하며, cURL 스니펫은 동일한 호출을 원시 HTTP로 수행합니다.

**모델 ID:** `freepik/ai-skin-enhancer-faithful`

**엔드포인트:** `POST https://api.comfy.org/v2/models/freepik/ai-skin-enhancer-faithful`

<Tabs defaultTabIndex={1}>
  <Tab title="결과 기다리기">
<CodeGroup>
```python Python
from comfy_sdk import Comfy

# 환경 변수에서 COMFY_API_KEY를 읽습니다.
# SDK는 자동으로 멱등성 키를 생성하고 자동 재시도에 재사용합니다.
with Comfy() as client:
    result = client.models.run(
        "freepik/ai-skin-enhancer-faithful",
        {
            "image": "https://img.magnific.com/free-photo/portrait-woman_395237-33.jpg?w=740&q=80",
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// 환경 변수에서 COMFY_API_KEY를 읽습니다.
// SDK는 자동으로 멱등성 키를 생성하고 자동 재시도에 재사용합니다.
const { data } = await comfy.models.run("freepik/ai-skin-enhancer-faithful", {
  image: "https://img.magnific.com/free-photo/portrait-woman_395237-33.jpg?w=740&q=80",
});

console.log(data);
```

```bash cURL
curl https://api.comfy.org/v2/models/freepik/ai-skin-enhancer-faithful \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"image\": \"https://img.magnific.com/free-photo/portrait-woman_395237-33.jpg?w=740&q=80\"}"
```
</CodeGroup>
  </Tab>
  <Tab title="대기열에 넣고 나중에 수집">
동일한 본문을 `POST https://api.comfy.org/v2/models/freepik/ai-skin-enhancer-faithful/requests`로 전송합니다. Router는 실행이 접수되는 즉시 `201`과 `request_id`로 응답하며, 결과는 준비가 되면 이 프로세스나 다른 프로세스에서 수집합니다. [대기열 전송](/ko/development/comfy-router/queue)에서 상태, 취소, 수집 방법을 자세히 설명합니다.

<CodeGroup>
```python Python
import asyncio
from comfy_sdk import AsyncComfy

# 환경 변수에서 COMFY_API_KEY를 읽습니다.
# 각 submit() 호출은 자체 Idempotency-Key를 발급하고 자동 재시도에 재사용합니다.
async def main():
    async with AsyncComfy() as client:
        handle = await client.models.submit(
            "freepik/ai-skin-enhancer-faithful",
            {
                "image": "https://img.magnific.com/free-photo/portrait-woman_395237-33.jpg?w=740&q=80",
            },
        )
        print("request_id:", handle.request_id)  # 모델 ID와 함께라면 다른 프로세스에 필요한 전부입니다

        # 서버가 알려주는 Retry-After만큼 기다리며 요청이 완료될 때까지 폴링합니다.
        async for update in handle.iter_events():
            print(update.status, update.queue_position)

        # 공급자의 자체 페이로드이며, models.run()이 반환하는 것과 동일한 값입니다.
        # 실패하거나 취소된 요청은 여기에서 타입이 지정된 Router 오류를 발생시킵니다.
        result = await handle.get()

    print(result)

asyncio.run(main())
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// 환경 변수에서 COMFY_API_KEY를 읽습니다.
// 각 submit() 호출은 자체 Idempotency-Key를 발급하고 자동 재시도에 재사용합니다.
const handle = await comfy.models.submit("freepik/ai-skin-enhancer-faithful", {
  image: "https://img.magnific.com/free-photo/portrait-woman_395237-33.jpg?w=740&q=80",
});
console.log("requestId:", handle.requestId); // 모델 ID와 함께라면 다른 프로세스에 필요한 전부입니다

// 서버가 알려주는 Retry-After만큼 기다리며 요청이 완료될 때까지 폴링합니다.
for await (const update of handle.events()) {
  console.log(update.status, update.queuePosition);
}

// models.run()이 반환하는 것과 동일한 결과입니다. 실패하거나 취소된 요청은 여기에서 거부됩니다.
const result = await handle.get();

console.log(result.data);
```

```bash cURL
# 1. 제출합니다. Router는 request_id, status_url, response_url, cancel_url과 함께 201로 응답합니다.
curl https://api.comfy.org/v2/models/freepik/ai-skin-enhancer-faithful/requests \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"image\": \"https://img.magnific.com/free-photo/portrait-woman_395237-33.jpg?w=740&q=80\"}"

# 2. 각 응답이 알려주는 Retry-After 초만큼 기다리며 status가 COMPLETED가 될 때까지 폴링합니다.
REQUEST_ID="<request_id from the 201 body>"
curl -i https://api.comfy.org/v2/models/freepik/ai-skin-enhancer-faithful/requests/$REQUEST_ID/status \
  -H "X-API-Key: $COMFY_API_KEY"

# 3. 수집합니다. 모델의 네이티브 출력과 함께 200, 아직 실행 중이면 상태 본문과 함께 202를 반환합니다.
curl https://api.comfy.org/v2/models/freepik/ai-skin-enhancer-faithful/requests/$REQUEST_ID \
  -H "X-API-Key: $COMFY_API_KEY"
```
</CodeGroup>
  </Tab>
</Tabs>

## 스키마

### 입력

<ParamField body="image" type="string" required>
  입력 이미지. Base64 인코딩 또는 HTTPS URL(공개적으로 접근 가능해야 함)을 지원합니다.
</ParamField>

<ParamField body="sharpen" type="integer" default="0">
  샤프닝 강도

  범위: `0` ~ `100`
</ParamField>

<ParamField body="skin_detail" type="integer" default="80">
  피부 디테일 향상 수준

  범위: `0` ~ `100`
</ParamField>

<ParamField body="smart_grain" type="integer" default="2">
  스마트 그레인 강도

  범위: `0` ~ `100`
</ParamField>

<ParamField body="webhook_url" type="string (uri)">
  비동기 알림을 위한 선택적 콜백 URL입니다.

  형식: `uri`
</ParamField>

Router가 `GET /v2/models/freepik/ai-skin-enhancer-faithful/openapi.json`에서 제공하는 스키마에서 생성되었으며, 이는 요청이 공급자에게 도달하기 전에 호출을 검증하는 데 사용하는 동일한 문서입니다.

### 출력

<ResponseField name="data" type="object" required>
   
</ResponseField>

<ResponseField name="data.generated" type="string (uri)[]" required>
  완료된 생성 결과의 이미지 URL입니다. Router가 반환하는 문서에서 존재하며 비어 있지 않습니다. 이 목록이 곧 결과입니다. 링크는 Freepik 자체 링크이며 만료됩니다.
</ResponseField>

<ResponseField name="data.status" type="string" required>
  가능한 값: `COMPLETED`
</ResponseField>

<ResponseField name="data.task_id" type="string (uuid)">
  형식: `uuid`
</ResponseField>

## 예시

### 입력

```json
{
  "image": "https://img.magnific.com/free-photo/portrait-woman_395237-33.jpg?w=740&q=80"
}
```

### 출력

```json
{
  "data": {
    "generated": [
      "https://example.invalid/freepik/ai-skin-enhancer-faithful/enhanced.png"
    ],
    "status": "COMPLETED",
    "task_id": "046b6c7f-0b8a-43b9-b35d-6489e6daee91"
  }
}
```

<RouterCodeFooter />
