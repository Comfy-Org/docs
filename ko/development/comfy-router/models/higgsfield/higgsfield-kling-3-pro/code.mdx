---
title: "Comfy Router로 Higgsfield Kling 3 Pro 사용하기"
description: "Comfy Router를 통해 higgsfield/higgsfield-kling-3-pro를 호출합니다: 엔드포인트, 요청 형태, 그리고 Router가 반환하는 응답을 설명합니다."
sidebarTitle: "Higgsfield Kling 3 Pro"
translationSourceHash: f235b0ec
translationFrom: development/comfy-router/models/higgsfield/higgsfield-kling-3-pro/code.mdx
translationBlockHashes:
  "_intro": dede62d5
  "Quick start": 075e9c05
  "Schema": 1b56c551
  "Examples": ec446261
---

{/* GENERATED FILE. Generated from router-schemas/higgsfield/higgsfield-kling-3-pro.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/ko/comfy-router/model-code-footer.mdx";

Higgsfield에서 제공하고 Comfy Router가 서비스하는 `higgsfield/higgsfield-kling-3-pro`의 API 레퍼런스입니다.

## 빠른 시작

[Comfy 워크스페이스](https://platform.comfy.org/profile/api-keys?onboarding=router)에서 키를 생성하고 `COMFY_API_KEY`로 내보내세요. Python과 TypeScript 스니펫은 Comfy SDK(`pip install comfy-sdk`, `npm install @comfyorg/sdk`)를 사용하며, cURL 스니펫은 동일한 호출을 순수 HTTP로 수행합니다.

**모델 ID:** `higgsfield/higgsfield-kling-3-pro`

**엔드포인트:** `POST https://api.comfy.org/v2/models/higgsfield/higgsfield-kling-3-pro`

<Tabs defaultTabIndex={1}>
  <Tab title="결과 대기">
<CodeGroup>
```python Python
from comfy_sdk import Comfy

# 환경 변수에서 COMFY_API_KEY를 읽습니다.
# SDK는 자동으로 멱등성 키를 생성하고 자동 재시도에 재사용합니다.
with Comfy() as client:
    result = client.models.run(
        "higgsfield/higgsfield-kling-3-pro",
        {
            "aspect_ratio": "16:9",
            "cfg_scale": 0.5,
            "duration": 5,
            "prompt": "A cinematic glass pavilion in a misty pine forest at sunrise",
            "sound": "on",
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// 환경 변수에서 COMFY_API_KEY를 읽습니다.
// SDK는 자동으로 멱등성 키를 생성하고 자동 재시도에 재사용합니다.
const { data } = await comfy.models.run("higgsfield/higgsfield-kling-3-pro", {
  aspect_ratio: "16:9",
  cfg_scale: 0.5,
  duration: 5,
  prompt: "A cinematic glass pavilion in a misty pine forest at sunrise",
  sound: "on",
});

console.log(data);
```

```bash cURL
curl https://api.comfy.org/v2/models/higgsfield/higgsfield-kling-3-pro \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"aspect_ratio\": \"16:9\", \"cfg_scale\": 0.5, \"duration\": 5, \"prompt\": \"A cinematic glass pavilion in a misty pine forest at sunrise\", \"sound\": \"on\"}"
```
</CodeGroup>
  </Tab>
  <Tab title="대기열에 넣고 나중에 수집하기">
동일한 본문을 `POST https://api.comfy.org/v2/models/higgsfield/higgsfield-kling-3-pro/requests` 로 전송합니다. Router는 실행이 접수되는 즉시 `request_id`와 함께 `201`을 응답하며, 결과는 준비되는 대로 이 프로세스나 다른 프로세스에서 수집합니다. [대기열 전달](/ko/development/comfy-router/queue)에서 상태, 취소, 수집 과정을 자세히 다룹니다.

<CodeGroup>
```python Python
import asyncio
from comfy_sdk import AsyncComfy

# 환경 변수에서 COMFY_API_KEY를 읽습니다.
# 각 submit() 호출은 자체 Idempotency-Key를 생성하고 자동 재시도에 재사용합니다.
async def main():
    async with AsyncComfy() as client:
        handle = await client.models.submit(
            "higgsfield/higgsfield-kling-3-pro",
            {
                "aspect_ratio": "16:9",
                "cfg_scale": 0.5,
                "duration": 5,
                "prompt": "A cinematic glass pavilion in a misty pine forest at sunrise",
                "sound": "on",
            },
        )
        print("request_id:", handle.request_id)  # 모델 ID와 함께라면 다른 프로세스에 필요한 전부입니다

        # 요청이 완료될 때까지 폴링하며, 서버가 지정한 Retry-After만큼 기다립니다.
        async for update in handle.iter_events():
            print(update.status, update.queue_position)

        # 공급자 자체의 페이로드이며, models.run()이 반환하는 것과 동일한 값입니다.
        # 실패하거나 취소된 요청은 여기에서 타입이 지정된 Router 오류를 발생시킵니다.
        result = await handle.get()

    print(result)

asyncio.run(main())
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// 환경 변수에서 COMFY_API_KEY를 읽습니다.
// 각 submit() 호출은 자체 Idempotency-Key를 생성하고 자동 재시도에 재사용합니다.
const handle = await comfy.models.submit("higgsfield/higgsfield-kling-3-pro", {
  aspect_ratio: "16:9",
  cfg_scale: 0.5,
  duration: 5,
  prompt: "A cinematic glass pavilion in a misty pine forest at sunrise",
  sound: "on",
});
console.log("requestId:", handle.requestId); // 모델 ID와 함께라면 다른 프로세스에 필요한 전부입니다

// 요청이 완료될 때까지 폴링하며, 서버가 지정한 Retry-After만큼 기다립니다.
for await (const update of handle.events()) {
  console.log(update.status, update.queuePosition);
}

// models.run()이 반환하는 것과 동일한 결과입니다. 실패하거나 취소된 요청은 여기에서 거부됩니다.
const result = await handle.get();

console.log(result.data);
```

```bash cURL
# 1. 제출. Router는 request_id, status_url, response_url, cancel_url과 함께 201로 응답합니다.
curl https://api.comfy.org/v2/models/higgsfield/higgsfield-kling-3-pro/requests \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"aspect_ratio\": \"16:9\", \"cfg_scale\": 0.5, \"duration\": 5, \"prompt\": \"A cinematic glass pavilion in a misty pine forest at sunrise\", \"sound\": \"on\"}"

# 2. 상태가 COMPLETED가 될 때까지 폴링하며, 각 응답이 지정한 Retry-After 초만큼 기다립니다.
REQUEST_ID="<request_id from the 201 body>"
curl -i https://api.comfy.org/v2/models/higgsfield/higgsfield-kling-3-pro/requests/$REQUEST_ID/status \
  -H "X-API-Key: $COMFY_API_KEY"

# 3. 수집. 모델의 네이티브 출력과 함께 200, 아직 실행 중이면 상태 본문과 함께 202를 반환합니다.
curl https://api.comfy.org/v2/models/higgsfield/higgsfield-kling-3-pro/requests/$REQUEST_ID \
  -H "X-API-Key: $COMFY_API_KEY"
```
</CodeGroup>
  </Tab>
</Tabs>

## 스키마

### 입력

<ParamField body="aspect_ratio" type="string" default="&quot;16:9&quot;">
  출력 화면 비율.

  가능한 값: `16:9`, `9:16`, `1:1`
</ParamField>

<ParamField body="cfg_scale" type="number" default="0.5">
  생성이 프롬프트를 얼마나 강하게 따르는지. Higgsfield는 0-1을 0.01 단위로 문서화하지만, 여기서는 0-1 경계만 선언합니다. `multipleOf: 0.01`은 이진 부동 소수점으로 평가되어 0.3과 같은 일반적인 값을 거부하게 되므로, 더 세밀한 값은 거부되지 않고 허용되어 전달됩니다. 이는 `prompt`가 선언되지 않은 길이 상한에 대해 주의를 덧붙이는 방식과 같습니다.

  범위: `0` ~ `1`
</ParamField>

<ParamField body="duration" type="integer" default="5">
  출력 재생 시간(초). 지원 범위: 3-15.

  범위: `3` ~ `15`
</ParamField>

<ParamField body="elements" type="string[]">
  여러 생성에 걸쳐 피사체 일관성을 유지하기 위한 재사용 가능한 Kling 요소 id.
</ParamField>

<ParamField body="multi_prompt" type="object[]">
  `multi_shots`가 참일 때 사용되는 순서가 지정된 샷 정의. 1-6개의 샷이며, 샷별 재생 시간은 작업 자체의 총 재생 시간에 의해 제한되는데, 이는 여기서 표현할 수 없습니다.
</ParamField>

<ParamField body="multi_prompt[].duration" type="integer">
  이 샷의 재생 시간(초), 1-15. 샷 전체의 합은 작업의 총 재생 시간을 초과할 수 없으며, 이는 JSON Schema로 표현할 수 없고 Higgsfield가 강제합니다.

  범위: `1` ~ `15`
</ParamField>

<ParamField body="multi_prompt[].prompt" type="string">
  이 샷에 대한 프롬프트. Higgsfield는 512자 상한을 문서화합니다.
</ParamField>

<ParamField body="multi_shots" type="boolean" default="false">
  멀티 샷 시퀀스를 활성화합니다. 참이면 `multi_prompt`가 샷별 정의를 담습니다.
</ParamField>

<ParamField body="prompt" type="string">
  원하는 출력을 설명하는 프롬프트. Higgsfield는 Kling이 처음 2,500자까지만 사용하고 그 이후는 잘라낸다고 문서화하므로, maxLength를 선언하지 않습니다. 더 긴 본문은 거부되지 않고 허용됩니다.
</ParamField>

<ParamField body="sound" type="string" default="&quot;on&quot;">
  생성된 사운드를 활성화하거나 비활성화합니다.

  가능한 값: `on`, `off`
</ParamField>

Router가 `GET /v2/models/higgsfield/higgsfield-kling-3-pro/openapi.json`에서 제공하는 스키마에서 생성되었으며, 이는 요청이 공급자에게 도달하기 전에 호출을 검증할 때 사용하는 것과 동일한 문서입니다.

### 출력

<ResponseField name="cancel_url" type="string">
  처리가 시작되기 전에 요청을 취소하는 데 사용되는 절대 URL.
</ResponseField>

<ResponseField name="error" type="string">
  `status`가 `failed`일 때 설정되는 실패 세부 정보.
</ResponseField>

<ResponseField name="request_id" type="string" required>
  폴링, 취소 및 지원팀에 사용되는 안정적인 식별자.
</ResponseField>

<ResponseField name="status" type="string" required>
  현재 요청 상태.

  가능한 값: `queued`, `in_progress`, `nsfw`, `failed`, `completed`, `canceled`
</ResponseField>

<ResponseField name="status_url" type="string">
  요청이 터미널 상태에 도달할 때까지 폴링할 절대 URL.
</ResponseField>

<ResponseField name="video" type="object">
  하나의 Higgsfield 미디어 출력.
</ResponseField>

<ResponseField name="video.url" type="string" required>
  생성된 미디어의 다운로드 URL.
</ResponseField>

## 예시

### 입력

```json
{
  "aspect_ratio": "16:9",
  "cfg_scale": 0.5,
  "duration": 5,
  "prompt": "A cinematic glass pavilion in a misty pine forest at sunrise",
  "sound": "on"
}
```

### 출력

```json
{
  "cancel_url": "https://api.higgsfield.ai/requests/d7e6c0f3-6699-4f6c-bb45-2ad7fd9158ff/cancel",
  "request_id": "d7e6c0f3-6699-4f6c-bb45-2ad7fd9158ff",
  "status": "completed",
  "status_url": "https://api.higgsfield.ai/requests/d7e6c0f3-6699-4f6c-bb45-2ad7fd9158ff/status",
  "video": {
    "url": "https://example.invalid/higgsfield/higgsfield-kling-3-pro/generated.mp4"
  }
}
```

<RouterCodeFooter />
