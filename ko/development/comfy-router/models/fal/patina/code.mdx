---
title: "Comfy Router에서 Patina 사용하기"
description: "Comfy Router를 통해 fal/patina를 호출하는 방법: endpoint, 요청 형태, 그리고 Router가 반환하는 응답."
sidebarTitle: "Patina"
translationSourceHash: fcac0478
translationFrom: development/comfy-router/models/fal/patina/code.mdx
translationBlockHashes:
  "_intro": 325742dd
  "Quick start": 834ddc5c
  "Schema": 44320e46
  "Examples": a3177476
---
{/* GENERATED FILE. Generated from router-schemas/fal/patina.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/ko/comfy-router/model-code-footer.mdx";

`fal/patina`에 대한 API 레퍼런스이며, Comfy Router가 fal에서 제공합니다.

## 빠른 시작

[Comfy 워크스페이스](https://platform.comfy.org/profile/api-keys)에서 키를 생성하고 `COMFY_API_KEY`로 내보내세요. Python 및 TypeScript 스니펫은 Comfy SDK(`pip install comfy-sdk`, `npm install @comfyorg/sdk`)를 사용하며, cURL 스니펫은 동일한 호출을 raw HTTP로 수행한 것입니다.

**모델 ID:** `fal/patina`

**엔드포인트:** `POST https://api.comfy.org/v2/models/fal/patina`

<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "fal/patina",
        {
            "image_url": "https://storage.googleapis.com/falserverless/gallery/patina-blog-hero-render.png",
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const { data } = await comfy.models.run("fal/patina", {
  image_url: "https://storage.googleapis.com/falserverless/gallery/patina-blog-hero-render.png",
});

console.log(data);
```

```bash cURL
curl https://api.comfy.org/v2/models/fal/patina \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"image_url\": \"https://storage.googleapis.com/falserverless/gallery/patina-blog-hero-render.png\"}"
```
</CodeGroup>

## 스키마

### 입력

<ParamField body="enable_safety_checker" type="boolean" default="true">
  이미지에 대한 안전 검사기를 활성화합니다.
</ParamField>

<ParamField body="image_url" type="string" required>
  입력 이미지(사진 또는 렌더)의 URL입니다.
</ParamField>

<ParamField body="maps" type="string[]" default="[&quot;basecolor&quot;,&quot;normal&quot;,&quot;roughness&quot;,&quot;metalness&quot;,&quot;height&quot;]">
  예측할 PBR 맵을 지정합니다. 기본값은 다섯 가지 모두입니다.
</ParamField>

<ParamField body="output_format" type="string" default="&quot;png&quot;">
  출력 이미지 형식: jpeg, png, webp.
</ParamField>

<ParamField body="seed" type="integer">
  재현 가능한 디노이징을 위한 랜덤 시드입니다.
</ParamField>

<ParamField body="sync_mode" type="boolean" default="false">
  참이면 이미지를 CDN URL 대신 data URI로 반환합니다.
</ParamField>

Router가 `GET /v2/models/fal/patina/openapi.json`에서 제공하는 스키마에서 생성되었으며, 이는 요청이 공급자에게 도달하기 이전에 호출을 검증하는 데 사용하는 동일한 문서입니다.

### 출력

<ResponseField name="images" type="object[]">
  생성된 타일링 가능한 텍스처 이미지와 예측된 PBR 재질 맵입니다. 기본 텍스처에는 url만 있으며, 각 맵 항목에는 map_type도 함께 포함됩니다.
</ResponseField>

<ResponseField name="images[].map_type" type="string">
  PBR 맵 유형(basecolor, normal, roughness, metalness, height)입니다. 기본 텍스처 이미지에는 없습니다.
</ResponseField>

<ResponseField name="images[].url" type="string">
  생성된 이미지의 URL입니다.
</ResponseField>

<ResponseField name="prompt" type="string">
  텍스처 생성에 사용된 프롬프트(확장되었을 수 있음)입니다.
</ResponseField>

<ResponseField name="seed" type="integer">
  텍스처 생성에 사용된 시드입니다.
</ResponseField>

<ResponseField name="timings" type="object">
  엔드투엔드 타이밍 분석(초)입니다.
</ResponseField>

## 예시

### 입력

```json
{
  "image_url": "https://storage.googleapis.com/falserverless/gallery/patina-blog-hero-render.png"
}
```

### 출력

```json
{
  "images": [
    {
      "map_type": "basecolor",
      "url": "https://example.invalid/fal/patina/basecolor.png"
    },
    {
      "map_type": "normal",
      "url": "https://example.invalid/fal/patina/normal.png"
    },
    {
      "map_type": "roughness",
      "url": "https://example.invalid/fal/patina/roughness.png"
    }
  ],
  "prompt": "weathered copper roof tiles",
  "seed": 1234567,
  "timings": {
    "inference": 6.25
  }
}
```

<RouterCodeFooter />
