---
title: "Comfy Router로 H3 Max 사용하기"
description: "Comfy Router를 통해 fal/h3-max를 호출하는 방법: endpoint, 요청 형태, 그리고 Router가 반환하는 응답."
sidebarTitle: "H3 Max"
translationSourceHash: 36eebfbb
translationFrom: development/comfy-router/models/fal/h3-max/code.mdx
translationBlockHashes:
  "_intro": 9639e44f
  "Quick start": 317cf450
  "Schema": 9d6db301
  "Examples": 53e0f9d0
---


{/* GENERATED FILE. Generated from router-schemas/fal/h3-max.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/ko/comfy-router/model-code-footer.mdx";

`fal/h3-max`에 대한 API 레퍼런스로, Comfy Router가 fal에서 제공합니다.

## 빠른 시작

[Comfy 워크스페이스](https://platform.comfy.org/profile/api-keys?onboarding=router)에서 키를 생성하고 `COMFY_API_KEY`로 내보내세요. Python 및 TypeScript 스니펫은 Comfy SDK(`pip install comfy-sdk`, `npm install @comfyorg/sdk`)를 사용하며, cURL 스니펫은 동일한 호출을 원시 HTTP로 수행합니다.

**모델 ID:** `fal/h3-max`

**엔드포인트:** `POST https://api.comfy.org/v2/models/fal/h3-max`

<Tabs defaultTabIndex={1}>
  <Tab title="결과 기다리기">
<CodeGroup>
```python Python
from comfy_sdk import Comfy

# 환경 변수에서 COMFY_API_KEY를 읽습니다.
# SDK는 자동으로 idempotency 키를 생성하고 자동 재시도에 재사용합니다.
with Comfy() as client:
    result = client.models.run(
        "fal/h3-max",
        {
            "duration": 5,
            "prompt": "a red fox running through a snowy forest",
            "prompt_expansion_mode": "balanced",
            "resolution": "480P",
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// 환경 변수에서 COMFY_API_KEY를 읽습니다.
// SDK는 자동으로 idempotency 키를 생성하고 자동 재시도에 재사용합니다.
const { data } = await comfy.models.run("fal/h3-max", {
  duration: 5,
  prompt: "a red fox running through a snowy forest",
  prompt_expansion_mode: "balanced",
  resolution: "480P",
});

console.log(data);
```

```bash cURL
curl https://api.comfy.org/v2/models/fal/h3-max \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"duration\": 5, \"prompt\": \"a red fox running through a snowy forest\", \"prompt_expansion_mode\": \"balanced\", \"resolution\": \"480P\"}"
```
</CodeGroup>
  </Tab>
  <Tab title="대기열에 넣고 나중에 수집">
동일한 본문을 `POST https://api.comfy.org/v2/models/fal/h3-max/requests` 로 보냅니다. Router는 실행이 접수되는 즉시 `201` 과 `request_id` 로 응답하며, 결과는 준비되는 즉시 이 프로세스 또는 다른 프로세스에서 수집합니다. 상태, 취소, 수집 방법은 [대기열 전송](/ko/development/comfy-router/queue) 을 참고하세요.

<CodeGroup>
```python Python
import asyncio
from comfy_sdk import AsyncComfy

# 환경 변수에서 COMFY_API_KEY를 읽습니다.
# 각 submit() 호출은 자체 Idempotency-Key를 발급하고 자동 재시도에 재사용합니다.
async def main():
    async with AsyncComfy() as client:
        handle = await client.models.submit(
            "fal/h3-max",
            {
                "duration": 5,
                "prompt": "a red fox running through a snowy forest",
                "prompt_expansion_mode": "balanced",
                "resolution": "480P",
            },
        )
        print("request_id:", handle.request_id)  # 모델 ID와 함께라면 다른 프로세스에 필요한 전부입니다

        # 서버가 지정한 Retry-After만큼 기다리며 요청이 완료될 때까지 폴링합니다.
        async for update in handle.iter_events():
            print(update.status, update.queue_position)

        # 공급자의 자체 페이로드이며, models.run()이 반환하는 것과 동일한 값입니다.
        # 실패했거나 취소된 요청은 여기에서 타입이 지정된 Router 오류를 발생시킵니다.
        result = await handle.get()

    print(result)

asyncio.run(main())
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// 환경 변수에서 COMFY_API_KEY를 읽습니다.
// 각 submit() 호출은 자체 Idempotency-Key를 발급하고 자동 재시도에 재사용합니다.
const handle = await comfy.models.submit("fal/h3-max", {
  duration: 5,
  prompt: "a red fox running through a snowy forest",
  prompt_expansion_mode: "balanced",
  resolution: "480P",
});
console.log("requestId:", handle.requestId); // 모델 ID와 함께라면 다른 프로세스에 필요한 전부입니다

// 서버가 지정한 Retry-After만큼 기다리며 요청이 완료될 때까지 폴링합니다.
for await (const update of handle.events()) {
  console.log(update.status, update.queuePosition);
}

// models.run()이 반환하는 것과 동일한 결과입니다. 실패했거나 취소된 요청은 여기에서 거부됩니다.
const result = await handle.get();

console.log(result.data);
```

```bash cURL
# 1. 제출합니다. Router는 request_id, status_url, response_url, cancel_url 과 함께 201을 응답합니다.
curl https://api.comfy.org/v2/models/fal/h3-max/requests \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"duration\": 5, \"prompt\": \"a red fox running through a snowy forest\", \"prompt_expansion_mode\": \"balanced\", \"resolution\": \"480P\"}"

# 2. 각 응답이 지정한 Retry-After 초만큼 기다리며 상태가 COMPLETED 가 될 때까지 폴링합니다.
REQUEST_ID="<request_id from the 201 body>"
curl -i https://api.comfy.org/v2/models/fal/h3-max/requests/$REQUEST_ID/status \
  -H "X-API-Key: $COMFY_API_KEY"

# 3. 수집합니다. 모델의 네이티브 출력과 함께 200, 아직 실행 중이면 상태 본문과 함께 202를 반환합니다.
curl https://api.comfy.org/v2/models/fal/h3-max/requests/$REQUEST_ID \
  -H "X-API-Key: $COMFY_API_KEY"
```
</CodeGroup>
  </Tab>
</Tabs>

## 스키마

### 입력

<ParamField body="aspect_ratio" type="string" default="&quot;16:9&quot;">
  화면 비율: 21:9, 16:9, 4:3, 1:1, 3:4, 9:16 중 하나입니다.
</ParamField>

<ParamField body="duration" type="integer" default="5">
  생성되는 비디오의 재생 시간(초)입니다.
</ParamField>

<ParamField body="enable_safety_checker" type="boolean" default="true">
  안전 검사기를 활성화합니다.
</ParamField>

<ParamField body="prompt" type="string" required>
  비디오 생성을 위한 텍스트 프롬프트입니다.
</ParamField>

<ParamField body="prompt_expansion_mode" type="string" required>
  생성 전에 프롬프트를 다시 작성하는 데 들일 노력의 정도입니다: balanced 또는 quality.
</ParamField>

<ParamField body="resolution" type="string" default="&quot;768P&quot;">
  생성되는 비디오의 해상도입니다: 480P 또는 768P.
</ParamField>

<ParamField body="seed" type="integer">
  랜덤 시드입니다. 생략하면 무작위 시드가 선택됩니다.
</ParamField>

<ParamField body="sync_mode" type="boolean">
  생성된 비디오를 CDN URL 대신 base64로 반환합니다.
</ParamField>

`GET /v2/models/fal/h3-max/openapi.json`에서 Router가 제공하는 스키마에서 생성되었으며, 이는 요청이 공급자에 도달하기 전에 호출을 검증할 때 사용하는 것과 동일한 문서입니다.

### 출력

<ResponseField name="expanded_prompt" type="string">
  확장된 이후의 프롬프트로, 모델에 전송된 그대로입니다.
</ResponseField>

<ResponseField name="timings" type="object">
  종단 간 타이밍 분석(초)입니다.
</ResponseField>

<ResponseField name="video" type="object" required>
  생성된 비디오 파일입니다.
</ResponseField>

<ResponseField name="video.content_type" type="string">
  비디오 파일의 MIME 타입입니다.
</ResponseField>

<ResponseField name="video.file_name" type="string">
  비디오 파일의 이름입니다.
</ResponseField>

<ResponseField name="video.file_size" type="integer">
  비디오 파일의 크기(바이트)입니다.
</ResponseField>

<ResponseField name="video.url" type="string">
  생성된 비디오의 URL입니다.
</ResponseField>

## 예시

### 입력

```json
{
  "duration": 5,
  "prompt": "a red fox running through a snowy forest",
  "prompt_expansion_mode": "balanced",
  "resolution": "480P"
}
```

### 출력

```json
{
  "expanded_prompt": "a single red maple leaf falling onto still water, cinematic, 4k",
  "timings": {
    "inference": 42.5
  },
  "video": {
    "content_type": "video/mp4",
    "file_name": "output.mp4",
    "file_size": 4194304,
    "url": "https://example.invalid/fal/minimax/h3-max/output.mp4"
  }
}
```

<RouterCodeFooter />
