---
title: "Comfy Router로 H3 Max Turbo 사용하기"
description: "Comfy Router를 통해 fal/h3-max-turbo를 호출합니다: endpoint, 요청 형태, 그리고 Router가 반환하는 응답."
sidebarTitle: "H3 Max Turbo"
translationSourceHash: 4587bb42
translationFrom: development/comfy-router/models/fal/h3-max-turbo/code.mdx
translationBlockHashes:
  "_intro": 000b76c4
  "Quick start": 3f4586dc
  "Schema": fa803ecb
  "Examples": 53e0f9d0
---
{/* GENERATED FILE. `pnpm code-pages:gen`에 의해 router-schemas/fal/h3-max-turbo.json에서 생성되었습니다. */}

import RouterCodeFooter from "/snippets/ko/comfy-router/model-code-footer.mdx";

fal에서 Comfy Router가 제공하는 `fal/h3-max-turbo`의 API 레퍼런스입니다.

## 빠른 시작

[Comfy 워크스페이스](https://platform.comfy.org/profile/api-keys)에서 키를 생성하고 `COMFY_API_KEY`로 내보내세요. Python 및 TypeScript 스니펫은 Comfy SDK(`pip install comfy-sdk`, `npm install @comfyorg/sdk`)를 사용하며, cURL 스니펫은 동일한 호출을 원시 HTTP로 수행합니다.

**모델 ID:** `fal/h3-max-turbo`

**엔드포인트:** `POST https://api.comfy.org/v2/models/fal/h3-max-turbo`

<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "fal/h3-max-turbo",
        {
            "duration": 5,
            "prompt": "a red fox running through a snowy forest",
            "prompt_expansion_mode": "balanced",
            "resolution": "480P",
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const { data } = await comfy.models.run("fal/h3-max-turbo", {
  duration: 5,
  prompt: "a red fox running through a snowy forest",
  prompt_expansion_mode: "balanced",
  resolution: "480P",
});

console.log(data);
```

```bash cURL
curl https://api.comfy.org/v2/models/fal/h3-max-turbo \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"duration\": 5, \"prompt\": \"a red fox running through a snowy forest\", \"prompt_expansion_mode\": \"balanced\", \"resolution\": \"480P\"}"
```
</CodeGroup>

## Schema

### 입력

<ParamField body="aspect_ratio" type="string" default="&quot;16:9&quot;">
  화면 비율: 21:9, 16:9, 4:3, 1:1, 3:4 또는 9:16.
</ParamField>

<ParamField body="duration" type="integer" default="5">
  생성된 비디오의 재생 시간(초)입니다.
</ParamField>

<ParamField body="enable_safety_checker" type="boolean" default="true">
  안전 검사기를 활성화합니다.
</ParamField>

<ParamField body="prompt" type="string" required>
  비디오 생성을 위한 텍스트 프롬프트입니다.
</ParamField>

<ParamField body="prompt_expansion_mode" type="string" required>
  생성 이전에 프롬프트를 다시 작성하는 데 들이는 노력의 정도: balanced 또는 quality.
</ParamField>

<ParamField body="resolution" type="string" default="&quot;768P&quot;">
  생성된 비디오의 해상도: 480P 또는 768P.
</ParamField>

<ParamField body="seed" type="integer">
  랜덤 시드. 생략하면 임의의 시드가 선택됩니다.
</ParamField>

<ParamField body="sync_mode" type="boolean">
  생성된 비디오를 CDN URL 대신 base64로 반환합니다.
</ParamField>

`GET /v2/models/fal/h3-max-turbo/openapi.json`에서 Router가 제공하는 스키마에서 생성되었으며, 이는 요청이 공급자에게 도달하기 이전에 Router가 호출을 검증하는 대상과 동일한 문서입니다.

### 출력

<ResponseField name="expanded_prompt" type="string">
  확장 이후 모델로 전송된 프롬프트입니다.
</ResponseField>

<ResponseField name="timings" type="object">
  종단 간 타이밍 분석(초)입니다.
</ResponseField>

<ResponseField name="video" type="object" required>
  생성된 비디오 파일입니다.
</ResponseField>

<ResponseField name="video.content_type" type="string">
  비디오 파일의 MIME 타입입니다.
</ResponseField>

<ResponseField name="video.file_name" type="string">
  비디오 파일의 이름입니다.
</ResponseField>

<ResponseField name="video.file_size" type="integer">
  비디오 파일의 크기(바이트)입니다.
</ResponseField>

<ResponseField name="video.url" type="string">
  URL of the generated video.
</ResponseField>

## 예시

### 입력

```json
{
  "duration": 5,
  "prompt": "a red fox running through a snowy forest",
  "prompt_expansion_mode": "balanced",
  "resolution": "480P"
}
```

### 출력

```json
{
  "expanded_prompt": "a single red maple leaf falling onto still water, cinematic, 4k",
  "timings": {
    "inference": 42.5
  },
  "video": {
    "content_type": "video/mp4",
    "file_name": "output.mp4",
    "file_size": 4194304,
    "url": "https://example.invalid/fal/minimax/h3-max/output.mp4"
  }
}
```

<RouterCodeFooter />
