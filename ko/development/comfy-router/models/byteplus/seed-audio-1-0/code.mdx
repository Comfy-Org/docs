---
title: "Comfy Router에서 Seed Audio 1.0 사용하기"
description: "Comfy Router를 통해 byteplus/seed-audio-1.0을 호출합니다: 엔드포인트, 요청 형태, 그리고 Router가 반환하는 응답입니다."
sidebarTitle: "Seed Audio 1.0"
translationSourceHash: 1971dfe6
translationFrom: development/comfy-router/models/byteplus/seed-audio-1-0/code.mdx
translationBlockHashes:
  "_intro": 8371b803
  "Quick start": 20fc3b16
  "Schema": c8c3aa89
  "Examples": be556f45
---
{/* GENERATED FILE. Generated from router-schemas/byteplus/seed-audio-1.0.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/ko/comfy-router/model-code-footer.mdx";

`byteplus/seed-audio-1.0`의 API 레퍼런스이며, BytePlus에서 Comfy Router를 통해 제공됩니다.

## 빠른 시작 {#quick-start}

[Comfy 워크스페이스](https://platform.comfy.org/profile/api-keys)에서 키를 생성하고 `COMFY_API_KEY`로 내보내세요. Python 및 TypeScript 스니펫은 Comfy SDK(`pip install comfy-sdk`, `npm install @comfyorg/sdk`)를 사용하며, cURL 스니펫은 동일한 호출을 raw HTTP로 수행합니다.

**모델 ID:** `byteplus/seed-audio-1.0`

**엔드포인트:** `POST https://api.comfy.org/v2/models/byteplus/seed-audio-1.0`

<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "byteplus/seed-audio-1.0",
        {
            "audio_config": {
                "format": "wav",
                "sample_rate": 24000,
            },
            "text_prompt": "Hello from Comfy Router.",
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const { data } = await comfy.models.run("byteplus/seed-audio-1.0", {
  audio_config: {
    format: "wav",
    sample_rate: 24000,
  },
  text_prompt: "Hello from Comfy Router.",
});

console.log(data);
```

```bash cURL
curl https://api.comfy.org/v2/models/byteplus/seed-audio-1.0 \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"audio_config\": {\"format\":\"wav\",\"sample_rate\":24000}, \"text_prompt\": \"Hello from Comfy Router.\"}"
```
</CodeGroup>

## 스키마

### 입력

<ParamField body="audio_config" type="object">
  출력 오디오 구성입니다.
</ParamField>

<ParamField body="audio_config.enable_subtitle" type="boolean">
  자막 서비스를 사용할지 여부입니다. 사용하도록 설정하면 응답에 발화 및 단어 단위 타임스탬프가 포함됩니다(기본값: false).
</ParamField>

<ParamField body="audio_config.format" type="string">
  출력 오디오 형식: wav(기본값), mp3, pcm 또는 ogg_opus.

  가능한 값: `wav`, `mp3`, `pcm`, `ogg_opus`
</ParamField>

<ParamField body="audio_config.loudness_rate" type="integer">
  -50에서 100까지이며, 100은 2.0배 볼륨, -50은 0.5배 볼륨입니다(기본값: 0).

  범위: `-50` ~ `100`
</ParamField>

<ParamField body="audio_config.pitch_rate" type="integer">
  -12에서 12까지입니다(기본값: 0).

  범위: `-12` ~ `12`
</ParamField>

<ParamField body="audio_config.sample_rate" type="integer">
  출력 샘플 레이트: 8000, 16000, 24000(기본값), 32000, 44100 또는 48000.

  가능한 값: `8000`, `16000`, `24000`, `32000`, `44100`, `48000`
</ParamField>

<ParamField body="audio_config.speech_rate" type="integer">
  -50에서 100까지이며, 100은 2.0배 속도, -50은 0.5배 속도입니다(기본값: 0).

  범위: `-50` ~ `100`
</ParamField>

<ParamField body="model" type="string">
  모델 식별자입니다. 지원되는 모델: seed-audio-1.0 및 seed-audio-1.0-multilingual. POST /proxy/byteplus/api/v3/tts/create에 대한 직접 v1 호출은 반드시 이를 제공해야 합니다. 프록시는 다른 값이나 값이 누락된 경우를 400으로 거부합니다. 이 스키마의 `required` 목록에 없는 이유는 Comfy Router가 /v2/models/byteplus/\{model}의 `{model}` 경로 세그먼트에서 이 값을 채우므로 Router 호출자는 이를 생략하기 때문입니다.
</ParamField>

<ParamField body="references" type="object[]">
  참조 리소스입니다. 텍스트 전용 생성에서는 생략합니다. 최대 3개의 오디오 참조(각각 최대 30초, 디코딩 기준 10 MB, wav, mp3, pcm 또는 ogg_opus) 또는 정확히 1개의 이미지 참조(디코딩 기준 최대 10 MB, jpeg, png 또는 webp)를 사용할 수 있습니다. 이미지 참조는 오디오 참조와 함께 사용할 수 없습니다. 이러한 상한은 BytePlus의 것으로 디코딩된 바이트 기준입니다. base64는 자산을 약 4/3로 부풀리며, Comfy Router 호출은 전체 JSON 문서에 대해 10 MiB로 제한되므로 /v2/models/byteplus/\{model}로 전송되는 인라인 `audio_data`/`image_data` 참조는 디코딩 기준 약 7.5 MB 미만이어야 합니다. 그보다 큰 경우에는 `audio_url`/`image_url`을 사용하십시오. BytePlus 자체 최대치인 10 MB의 참조를 인라인으로 보내면 검증이 실행되기 이전에 413으로 거부됩니다.
</ParamField>

<ParamField body="references[].audio_data" type="string">
  Base64로 인코딩된 참조 오디오입니다.
</ParamField>

<ParamField body="references[].audio_url" type="string">
  원격 참조 오디오 파일의 URL입니다.
</ParamField>

<ParamField body="references[].image_data" type="string">
  Base64로 인코딩된 참조 이미지입니다.
</ParamField>

<ParamField body="references[].image_url" type="string">
  원격 참조 이미지의 URL입니다.
</ParamField>

<ParamField body="references[].speaker" type="string">
  음성 ID입니다. 지원되는 Doubao TTS 음성 또는 음성 복제 음성 ID일 수 있습니다.
</ParamField>

<ParamField body="text_prompt" type="string" required>
  합성할 프롬프트 또는 텍스트입니다(1자에서 3,000자까지이며, 빈 문자열은 거부됩니다). 오디오 참조를 제공한 경우 @Audio1, @Audio2, @Audio3을 사용하여 순서대로 참조하십시오.
</ParamField>

<ParamField body="watermark" type="object">
  워터마크 구성 객체입니다. 빈 객체도 허용됩니다.
</ParamField>

<ParamField body="watermark.aigc_metadata" type="object">
  암시적 워터마크입니다. 합성된 오디오 헤더에 메타데이터를 추가합니다.
</ParamField>

<ParamField body="watermark.aigc_metadata.content_producer" type="string">
  합성 서비스 공급자의 이름 또는 코드입니다.
</ParamField>

<ParamField body="watermark.aigc_metadata.content_propagator" type="string">
  콘텐츠 배포 서비스 공급자의 이름 또는 코드입니다.
</ParamField>

<ParamField body="watermark.aigc_metadata.enable" type="boolean">
  암시적 워터마크를 사용할지 여부입니다(기본값: false).
</ParamField>

<ParamField body="watermark.aigc_metadata.produce_id" type="string">
  콘텐츠 제작 ID입니다.
</ParamField>

<ParamField body="watermark.aigc_metadata.propagate_id" type="string">
  콘텐츠 배포 ID입니다.
</ParamField>

<ParamField body="watermark.aigc_watermark" type="boolean">
  명시적 워터마크 전환입니다. 합성된 오디오 끝에 오디오 리듬 표시를 추가합니다(기본값: false).
</ParamField>

`GET /v2/models/byteplus/seed-audio-1.0/openapi.json`에서 Router가 제공하는 스키마, 즉 요청이 공급자에 도달하기 이전에 호출 검증에 사용하는 것과 동일한 문서에서 생성되었습니다.### 출력

<ResponseField name="audio" type="string">
  생성된 오디오 데이터이며, Base64로 인코딩됩니다.
</ResponseField>

<ResponseField name="code" type="integer">
  상태 코드입니다. 자세한 내용은 공식 오류 코드 문서를 참조하세요.
</ResponseField>

<ResponseField name="duration" type="number">
  배속 또는 후처리 이후의 재생 시간(초)입니다.

  형식: `double`
</ResponseField>

<ResponseField name="message" type="string">
  상태 세부 정보입니다.
</ResponseField>

<ResponseField name="original_duration" type="number">
  원본 모델 출력 재생 시간(초)입니다. 과금에 사용되며 최대 120초로 제한됩니다.

  형식: `double`
</ResponseField>

<ResponseField name="subtitle" type="object">
  오디오의 자막 정보입니다. 요청에서 audio_config.enable_subtitle이 true로 설정된 경우에만 존재합니다.
</ResponseField>

<ResponseField name="subtitle.sentences" type="object[]">
  발화 단위 자막 정보입니다.
</ResponseField>

<ResponseField name="subtitle.sentences[].end_time" type="integer">
  세그먼트 종료 시간으로, 오디오 시작부터의 밀리초 단위입니다.
</ResponseField>

<ResponseField name="subtitle.sentences[].start_time" type="integer">
  세그먼트 시작 시간으로, 오디오 시작부터의 밀리초 단위입니다.
</ResponseField>

<ResponseField name="subtitle.sentences[].text" type="string">
  세그먼트의 전체 텍스트입니다.
</ResponseField>

<ResponseField name="subtitle.text" type="string">
  오디오에 대응하는 자막 텍스트입니다.
</ResponseField>

<ResponseField name="subtitle.words" type="object[]">
  단어 단위 자막 정보입니다.
</ResponseField>

<ResponseField name="subtitle.words[].end_time" type="integer">
  세그먼트 종료 시간으로, 오디오 시작부터의 밀리초 단위입니다.
</ResponseField>

<ResponseField name="subtitle.words[].start_time" type="integer">
  세그먼트 시작 시간으로, 오디오 시작부터의 밀리초 단위입니다.
</ResponseField>

<ResponseField name="subtitle.words[].text" type="string">
  세그먼트의 전체 텍스트입니다.
</ResponseField>

<ResponseField name="url" type="string">
  임시 오디오 URL이며, 2시간 동안 유효합니다.
</ResponseField>

## 예시

### 입력

```json
{
  "audio_config": {
    "format": "wav",
    "sample_rate": 24000
  },
  "text_prompt": "Hello from Comfy Router."
}
```

### 출력

```json
{
  "audio": "PGJhc2U2ND4=",
  "duration": 1.2,
  "original_duration": 1.2,
  "url": "https://example.invalid/byteplus/seed-audio/generated.wav"
}
```


<RouterCodeFooter />
