---
title: "Comfy Router로 Videos Lip Sync 사용하기"
description: "Comfy Router를 통해 kling/videos-lip-sync를 호출합니다. endpoint, 요청 형태, 그리고 Router가 반환하는 응답을 설명합니다."
sidebarTitle: "Videos Lip Sync"
translationSourceHash: 18911f14
translationFrom: development/comfy-router/models/kling/videos-lip-sync/code.mdx
translationBlockHashes:
  "_intro": 2cd8e6e0
  "Quick start": eddd7537
  "Schema": 2993a979
  "Examples": 380debef
---
{/* GENERATED FILE. Generated from router-schemas/kling/videos-lip-sync.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/ko/comfy-router/model-code-footer.mdx";

Comfy Router가 Kling에서 제공하는 `kling/videos-lip-sync`의 API 레퍼런스입니다.

## 빠른 시작

[Comfy 워크스페이스](https://platform.comfy.org/profile/api-keys)에서 키를 생성하고 `COMFY_API_KEY`로 내보내세요. Python 및 TypeScript 스니펫은 Comfy SDK(`pip install comfy-sdk`, `npm install @comfyorg/sdk`)를 사용하며, cURL 스니펫은 동일한 호출을 원시 HTTP로 수행한 것입니다.

**모델 ID:** `kling/videos-lip-sync`

**엔드포인트:** `POST https://api.comfy.org/v2/models/kling/videos-lip-sync`

<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "kling/videos-lip-sync",
        {
            "input": {
                "mode": "text2video",
                "text": "Welcome to Comfy Cloud.",
                "video_id": "kling-video-6f5e4d3c2b1a",
                "voice_id": "genshin_vindi2",
                "voice_language": "en",
                "voice_speed": 1,
            },
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const { data } = await comfy.models.run("kling/videos-lip-sync", {
  input: {
    mode: "text2video",
    text: "Welcome to Comfy Cloud.",
    video_id: "kling-video-6f5e4d3c2b1a",
    voice_id: "genshin_vindi2",
    voice_language: "en",
    voice_speed: 1,
  },
});

console.log(data);
```

```bash cURL
curl https://api.comfy.org/v2/models/kling/videos-lip-sync \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"input\": {\"mode\":\"text2video\",\"text\":\"Welcome to Comfy Cloud.\",\"video_id\":\"kling-video-6f5e4d3c2b1a\",\"voice_id\":\"genshin_vindi2\",\"voice_language\":\"en\",\"voice_speed\":1}}"
```
</CodeGroup>

## Schema

### Input

<ParamField body="callback_url" type="string (uri)">
  콜백 알림 주소입니다. 작업 상태가 변경되면 서버가 알림을 보냅니다.

  형식: `uri`
</ParamField>

<ParamField body="input" type="object" required>
   
</ParamField>

<ParamField body="input.audio_file" type="string">
  오디오 파일의 로컬 경로입니다. 지원 형식: .mp3/.wav/.m4a/.aac, 최대 파일 크기 5MB. Base64 코드.
</ParamField>

<ParamField body="input.audio_type" type="string">
  립싱크를 위한 오디오 파일 전송 방법입니다. mode가 audio2video일 때 필수입니다.

  가능한 값: `file`, `url`
</ParamField>

<ParamField body="input.audio_url" type="string">
  오디오 파일 다운로드 URL입니다. 지원 형식: .mp3/.wav/.m4a/.aac, 최대 파일 크기 5MB.
</ParamField>

<ParamField body="input.mode" type="string" required>
  비디오 생성 모드입니다. text2video: 텍스트 기반 비디오 생성 모드, audio2video: 오디오 기반 비디오 생성 모드

  가능한 값: `text2video`, `audio2video`
</ParamField>

<ParamField body="input.text" type="string">
  립싱크 비디오 생성을 위한 텍스트 콘텐츠입니다. mode가 text2video일 때 필수입니다. 최대 길이는 120자입니다.
</ParamField>

<ParamField body="input.video_id" type="string">
  Kling AI가 생성한 비디오의 ID입니다. 최근 30일 이내에 생성된 5초 및 10초 비디오만 지원합니다.
</ParamField>

<ParamField body="input.video_url" type="string">
  업로드된 비디오의 링크를 가져옵니다. 비디오 파일은 .mp4/.mov를 지원하며, 파일 크기는 100MB를 초과하지 않고 비디오 길이는 2-10초 사이입니다.
</ParamField>

<ParamField body="input.voice_id" type="string">
  음성 ID입니다. mode가 text2video일 때 필수입니다. 시스템은 선택할 수 있는 다양한 음성 옵션을 제공합니다.
</ParamField>

<ParamField body="input.voice_language" type="string" default="&quot;en&quot;">
  음성 언어는 음성 ID에 대응합니다.

  가능한 값: `zh`, `en`
</ParamField>

<ParamField body="input.voice_speed" type="number" default="1">
  말하기 속도입니다. 유효 범위: 0.8~2.0, 소수점 첫째 자리까지 정확합니다.

  범위: `0.8` ~ `2`
</ParamField>

Router가 `GET /v2/models/kling/videos-lip-sync/openapi.json`에서 제공하는 스키마에서 생성되었으며, 이는 요청이 공급자에게 도달하기 전에 호출을 검증하는 데 사용하는 것과 동일한 문서입니다.

### Output

<ResponseField name="code" type="integer">
  오류 코드
</ResponseField>

<ResponseField name="data" type="object">
   
</ResponseField>

<ResponseField name="data.created_at" type="integer">
  작업 생성 시간, Unix 타임스탬프(밀리초)
</ResponseField>

<ResponseField name="data.final_unit_deduction" type="string">
  작업의 차감 단위
</ResponseField>

<ResponseField name="data.task_id" type="string">
  작업 ID
</ResponseField>

<ResponseField name="data.task_info" type="object">
   
</ResponseField>

<ResponseField name="data.task_info.external_task_id" type="string">
   
</ResponseField>

<ResponseField name="data.task_result" type="object">
   
</ResponseField>

<ResponseField name="data.task_result.videos" type="object[]">
   
</ResponseField>

<ResponseField name="data.task_result.videos[].duration" type="string">
  비디오 총 재생 시간(초)
</ResponseField>

<ResponseField name="data.task_result.videos[].id" type="string">
  생성된 비디오 ID
</ResponseField>

<ResponseField name="data.task_result.videos[].url" type="string (uri)">
  생성된 비디오의 URL

  형식: `uri`
</ResponseField>

<ResponseField name="data.task_result.videos[].watermark_url" type="string (uri)">
  워터마크가 포함된 생성 비디오의 URL, 핫링크 보호 형식

  형식: `uri`
</ResponseField>

<ResponseField name="data.task_status" type="string">
  작업 상태

  가능한 값: `submitted`, `processing`, `succeed`, `failed`
</ResponseField>

<ResponseField name="data.task_status_msg" type="string">
  작업 상태 정보로, 작업이 실패하면 실패 이유를 표시합니다.
</ResponseField>

<ResponseField name="data.updated_at" type="integer">
  작업 업데이트 시간, Unix 타임스탬프(밀리초)
</ResponseField>

<ResponseField name="data.watermark_info" type="object">
   
</ResponseField>

<ResponseField name="data.watermark_info.enabled" type="boolean">
   
</ResponseField>

<ResponseField name="message" type="string">
  오류 메시지
</ResponseField>

<ResponseField name="request_id" type="string">
  요청 ID
</ResponseField>

## 예시

### 입력

```json
{
  "input": {
    "mode": "text2video",
    "text": "Welcome to Comfy Cloud.",
    "video_id": "kling-video-6f5e4d3c2b1a",
    "voice_id": "genshin_vindi2",
    "voice_language": "en",
    "voice_speed": 1
  }
}
```

### 출력

```json
{
  "code": 0,
  "data": {
    "created_at": 1798761600000,
    "task_id": "kling-lip-sync-task-4f3e2d1c0b9a",
    "task_result": {
      "videos": [
        {
          "duration": "8",
          "id": "kling-video-9a8b7c6d5e4f",
          "url": "https://example.invalid/kling/videos-lip-sync/generated.mp4"
        }
      ]
    },
    "task_status": "succeed",
    "updated_at": 1798761780000
  },
  "message": "SUCCEED",
  "request_id": "5e0a9b17-6d3c-42f8-91b4-7a2c8d6e0f35"
}
```

<RouterCodeFooter />
