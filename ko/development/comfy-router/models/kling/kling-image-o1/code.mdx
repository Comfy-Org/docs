---
title: "Comfy Router와 함께 Kling Image O1 사용하기"
description: "Comfy Router를 통해 kling/kling-image-o1 호출하기: 엔드포인트, 요청 형태, 그리고 Router가 반환하는 응답."
sidebarTitle: "Kling Image O1"
translationSourceHash: 596065da
translationFrom: development/comfy-router/models/kling/kling-image-o1/code.mdx
translationBlockHashes:
  "_intro": 39366fac
  "Quick start": a99898b2
  "Schema": d18a5393
  "Examples": 826db173
---
{/* GENERATED FILE. Generated from router-schemas/kling/kling-image-o1.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/ko/comfy-router/model-code-footer.mdx";

Kling이 Comfy Router를 통해 제공하는 `kling/kling-image-o1`의 API 레퍼런스입니다.

## 빠른 시작

[Comfy 워크스페이스](https://platform.comfy.org/profile/api-keys)에서 키를 생성하고 `COMFY_API_KEY`로 내보내세요. Python 및 TypeScript 스니펫은 Comfy SDK(`pip install comfy-sdk`, `npm install @comfyorg/sdk`)를 사용하며, cURL 스니펫은 동일한 호출을 원시 HTTP로 수행합니다.

**모델 ID:** `kling/kling-image-o1`

**엔드포인트:** `POST https://api.comfy.org/v2/models/kling/kling-image-o1`

<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "kling/kling-image-o1",
        {
            "aspect_ratio": "1:1",
            "n": 1,
            "prompt": "A watercolour koi pond at dawn, soft light.",
            "resolution": "1k",
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const { data } = await comfy.models.run("kling/kling-image-o1", {
  aspect_ratio: "1:1",
  n: 1,
  prompt: "A watercolour koi pond at dawn, soft light.",
  resolution: "1k",
});

console.log(data);
```

```bash cURL
curl https://api.comfy.org/v2/models/kling/kling-image-o1 \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"aspect_ratio\": \"1:1\", \"n\": 1, \"prompt\": \"A watercolour koi pond at dawn, soft light.\", \"resolution\": \"1k\"}"
```
</CodeGroup>

## 스키마

### 입력

<ParamField body="aspect_ratio" type="string" default="&quot;auto&quot;">
  생성된 이미지의 가로세로 비율(너비:높이)입니다. auto는 입력 콘텐츠에 따라 지능적으로 이미지를 생성합니다.

  가능한 값: `16:9`, `9:16`, `1:1`, `4:3`, `3:4`, `3:2`, `2:3`, `21:9`, `auto`
</ParamField>

<ParamField body="callback_url" type="string (uri)">
  이 작업 결과에 대한 콜백 알림 주소입니다. 설정하면 작업 상태가 변경될 때 서버가 능동적으로 알림을 보냅니다.

  형식: `uri`
</ParamField>

<ParamField body="element_list" type="object[]">
  element ID 구성을 기반으로 한 참조 요소 목록입니다. 참조 요소와 참조 이미지의 합계는 10개를 초과할 수 없습니다.
</ParamField>

<ParamField body="element_list[].element_id" type="integer" required>
  요소 ID

  형식: `int64`
</ParamField>

<ParamField body="external_task_id" type="string">
  사용자 지정 작업 ID입니다. 단일 사용자 계정 내에서 고유해야 합니다.
</ParamField>

<ParamField body="image_list" type="object[]">
  참조 이미지 목록입니다. 이미지 Base64 인코딩 또는 이미지 URL(접근 가능해야 함) 입력을 지원합니다. 지원 형식은 .jpg/.jpeg/.png입니다. 파일 크기는 10MB를 초과할 수 없습니다. 너비와 높이 크기는 300px 이상이어야 하며, 가로세로 비율은 1:2.5 ~ 2.5:1 사이여야 합니다. 참조 요소와 참조 이미지의 합계는 10개를 초과할 수 없습니다.
</ParamField>

<ParamField body="image_list[].image" type="string" required>
  이미지 Base64 인코딩 또는 이미지 URL(접근 가능해야 함)
</ParamField>

<ParamField body="model_name" type="string">
  모델 이름입니다. Comfy Router를 사용할 때는 이를 생략하거나 null을 전송하세요. 모델은 요청 경로에 의해 선택됩니다. 이름을 제공하는 경우 해당 경로와 일치해야 합니다.
</ParamField>

<ParamField body="n" type="integer" default="1">
  생성할 이미지 수입니다. 값 범위 [1,9].

  범위: `1` ~ `9`
</ParamField>

<ParamField body="prompt" type="string" required>
  텍스트 프롬프트 단어로, 긍정 및 네거티브 설명을 포함할 수 있습니다. 2,500자를 초과할 수 없습니다. Omni 모델은 요소와 이미지가 포함된 Prompt를 통해 다양한 기능을 구현할 수 있습니다. 이미지는 \<\<\<image_1>>> 형식으로 지정하세요.
</ParamField>

<ParamField body="resolution" type="string" default="&quot;1k&quot;">
  이미지 생성 해상도입니다. 1k는 1K 표준, 2k는 2K 고해상도, 4k는 4K 고해상도입니다.

  가능한 값: `1k`, `2k`, `4k`
</ParamField>

<ParamField body="result_type" type="string" default="&quot;single&quot;">
  단일 이미지를 생성할지 일련의 이미지를 생성할지 제어합니다.

  가능한 값: `single`, `series`
</ParamField>

<ParamField body="series_amount" type="integer" default="4">
  시리즈의 이미지 수입니다. 값 범위 [2,9].

  범위: `2` ~ `9`
</ParamField>

Router가 `GET /v2/models/kling/kling-image-o1/openapi.json`에서 제공하는 스키마에서 생성되었으며, 이는 요청이 공급자에 도달하기 전에 호출을 검증하는 데 사용하는 동일한 문서입니다.

### 출력

<ResponseField name="code" type="integer">
  오류 코드
</ResponseField>

<ResponseField name="data" type="object">
   
</ResponseField>

<ResponseField name="data.created_at" type="integer">
  작업 생성 시간, 밀리초 단위의 Unix 타임스탬프
</ResponseField>

<ResponseField name="data.final_unit_deduction" type="string">
  작업의 차감 단위
</ResponseField>

<ResponseField name="data.task_id" type="string">
  작업 ID
</ResponseField>

<ResponseField name="data.task_info" type="object">
   
</ResponseField>

<ResponseField name="data.task_info.external_task_id" type="string">
  고객 정의 작업 ID
</ResponseField>

<ResponseField name="data.task_result" type="object">
   
</ResponseField>

<ResponseField name="data.task_result.images" type="object[]">
   
</ResponseField>

<ResponseField name="data.task_result.images[].index" type="integer">
  이미지 번호 (0-9)
</ResponseField>

<ResponseField name="data.task_result.images[].url" type="string (uri)">
  생성된 이미지의 URL

  형식: `uri`
</ResponseField>

<ResponseField name="data.task_result.result_type" type="string">
  결과가 단일 이미지인지 일련의 이미지인지 여부

  가능한 값: `single`, `series`
</ResponseField>

<ResponseField name="data.task_result.series_images" type="object[]">
  시리즈 이미지 결과 목록
</ResponseField>

<ResponseField name="data.task_result.series_images[].index" type="integer">
  시리즈 이미지 순번
</ResponseField>

<ResponseField name="data.task_result.series_images[].url" type="string (uri)">
  생성된 이미지의 URL

  형식: `uri`
</ResponseField>

<ResponseField name="data.task_status" type="string">
  작업 상태

  가능한 값: `submitted`, `processing`, `succeed`, `failed`
</ResponseField>

<ResponseField name="data.task_status_msg" type="string">
  작업 상태 정보로, 작업 실패 시 실패 원인(플랫폼의 콘텐츠 위험 제어 트리거 등)을 표시합니다.
</ResponseField>

<ResponseField name="data.updated_at" type="integer">
  작업 업데이트 시간, 밀리초 단위의 Unix 타임스탬프
</ResponseField>

<ResponseField name="message" type="string">
  오류 메시지
</ResponseField>

<ResponseField name="request_id" type="string">
  요청 ID
</ResponseField>

## 예제

### 입력

```json
{
  "aspect_ratio": "1:1",
  "n": 1,
  "prompt": "A watercolour koi pond at dawn, soft light.",
  "resolution": "1k"
}
```

### 출력

```json
{
  "code": 0,
  "data": {
    "created_at": 1798761600000,
    "task_id": "kling-image-task-3c4d5e6f7a8b",
    "task_result": {
      "images": [
        {
          "index": 0,
          "url": "https://example.invalid/kling/kling-image-o1/generated.png"
        }
      ],
      "result_type": "single"
    },
    "task_status": "succeed",
    "task_status_msg": "",
    "updated_at": 1798761660000
  },
  "message": "SUCCEED",
  "request_id": "6a4b2c80-1e93-4d57-b8f2-05c7e9a3d146"
}
```

<RouterCodeFooter />
