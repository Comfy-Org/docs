---
title: "Comfy Router로 P Image Ideogram 사용하기"
description: "Comfy Router를 통해 ideogram/p-image-ideogram을 호출합니다: endpoint, 요청 형태, 그리고 Router가 반환하는 응답을 설명합니다."
sidebarTitle: "P Image Ideogram"
translationSourceHash: 063a5bfe
translationFrom: development/comfy-router/models/ideogram/p-image-ideogram/code.mdx
translationBlockHashes:
  "_intro": de73d082
  "Quick start": c96cb422
  "Schema": 3aed066f
  "Examples": 20db9e6c
---
{/* GENERATED FILE. Generated from router-schemas/ideogram/p-image-ideogram.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/ko/comfy-router/model-code-footer.mdx";

`ideogram/p-image-ideogram`에 대한 API 레퍼런스이며, Comfy Router가 Ideogram에서 제공합니다.

## 빠른 시작

[Comfy 워크스페이스](https://platform.comfy.org/profile/api-keys)에서 키를 생성하고 `COMFY_API_KEY`로 내보내세요. Python 및 TypeScript 스니펫은 Comfy SDK(`pip install comfy-sdk`, `npm install @comfyorg/sdk`)를 사용하며, cURL 스니펫은 동일한 호출을 원시 HTTP로 수행합니다.

**모델 ID:** `ideogram/p-image-ideogram`

**엔드포인트:** `POST https://api.comfy.org/v2/models/ideogram/p-image-ideogram`

<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "ideogram/p-image-ideogram",
        {
            "prompt": "A beautiful mountain landscape",
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const { data } = await comfy.models.run("ideogram/p-image-ideogram", {
  prompt: "A beautiful mountain landscape",
});

console.log(data);
```

```bash cURL
curl https://api.comfy.org/v2/models/ideogram/p-image-ideogram \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"prompt\": \"A beautiful mountain landscape\"}"
```
</CodeGroup>

## 스키마

### 입력

<ParamField body="aspect_ratio" type="string">
  WxH 형식의 종횡비입니다. 지원 값: 1x3, 3x1, 1x2, 2x1, 9x16, 16x9, 10x16, 16x10, 2x3, 3x2, 3x4, 4x3, 4x5, 5x4, 1x1. 기본값은 1x1입니다.
</ParamField>

<ParamField body="enable_copyright_detection" type="boolean">
  생성 후 저작권 감지(Hive 유사성 및 로고 검사)를 선택적으로 활성화합니다.
</ParamField>

<ParamField body="prompt" type="string" required>
  이미지 생성을 위한 프롬프트로, 자연어 또는 Ideogram 4.0 JSON 구조화 프롬프트입니다.
</ParamField>

<ParamField body="prompt_upsampling" type="string">
  프롬프트 업샘플링 모드입니다. 지원 값은 AUTO, ON, OFF입니다. 기본값은 AUTO입니다.
</ParamField>

<ParamField body="quality" type="string">
  생성 품질 수준입니다. 지원 값은 VERY_LOW, LOW, MEDIUM, HIGH입니다. 기본값은 MEDIUM입니다.
</ParamField>

<ParamField body="resolution" type="string">
  출력 해상도입니다. 지원 값은 1K, 2K입니다. 기본값은 1K입니다.
</ParamField>

<ParamField body="seed" type="integer">
  재현 가능한 생성을 위한 시드 값입니다.
</ParamField>

Router가 `GET /v2/models/ideogram/p-image-ideogram/openapi.json`에서 제공하는 스키마에서 생성되었으며, 이는 요청이 공급자에 도달하기 이전에 호출을 검증하는 데 사용하는 것과 동일한 문서입니다.

### 출력

<ResponseField name="created" type="string (date-time)">
  생성이 생성된 시점의 타임스탬프입니다.

  형식: `date-time`
</ResponseField>

<ResponseField name="data" type="object[]">
  생성된 이미지 정보의 배열입니다.
</ResponseField>

<ResponseField name="data[].is_image_safe" type="boolean">
  이미지가 안전한 것으로 간주되는지 여부를 나타냅니다.
</ResponseField>

<ResponseField name="data[].prompt" type="string">
  이 이미지를 생성하는 데 사용된 프롬프트입니다.
</ResponseField>

<ResponseField name="data[].resolution" type="string">
  생성된 이미지의 해상도입니다(예: '1024x1024').
</ResponseField>

<ResponseField name="data[].seed" type="integer">
  이 생성에 사용된 시드 값입니다.
</ResponseField>

<ResponseField name="data[].style_type" type="string">
  생성에 사용된 스타일 유형입니다(예: 'REALISTIC', 'ANIME').
</ResponseField>

<ResponseField name="data[].url" type="string">
  생성된 이미지의 URL입니다.
</ResponseField>

## 예시

### 입력

```json
{
  "prompt": "A beautiful mountain landscape"
}
```

### 출력

```json
{
  "created": "2026-01-01T00:00:00Z",
  "data": [
    {
      "is_image_safe": true,
      "prompt": "A poster for a jazz festival, bold typography, warm colours",
      "resolution": "1024x1024",
      "seed": 918273645,
      "style_type": "REALISTIC",
      "url": "https://example.invalid/ideogram/p-image-ideogram/generated.png"
    }
  ]
}
```

<RouterCodeFooter />
