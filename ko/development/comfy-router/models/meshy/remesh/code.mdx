---
title: "Comfy Router에서 Remesh 사용하기"
description: "Comfy Router를 통해 meshy/remesh를 호출합니다: 엔드포인트, 요청 형태, 그리고 Router가 반환하는 응답입니다."
sidebarTitle: "Remesh"
translationSourceHash: e8059ce2
translationFrom: development/comfy-router/models/meshy/remesh/code.mdx
translationBlockHashes:
  "_intro": e9a2c81a
  "Quick start": 95cd0cea
  "Schema": 1e6e7f8f
  "Examples": 4eee8fa5
---
{/* GENERATED FILE. Generated from router-schemas/meshy/remesh.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/ko/comfy-router/model-code-footer.mdx";

# `meshy/remesh` API 레퍼런스: Meshy가 Comfy Router를 통해 제공

## 빠른 시작

[Comfy 워크스페이스](https://platform.comfy.org/profile/api-keys)에서 키를 생성하고 `COMFY_API_KEY`로 내보내세요. Python 및 TypeScript 스니펫은 Comfy SDK(`pip install comfy-sdk`, `npm install @comfyorg/sdk`)를 사용합니다. cURL 스니펫은 동일한 호출을 raw HTTP로 수행합니다.

**모델 ID:** `meshy/remesh`

**엔드포인트:** `POST https://api.comfy.org/v2/models/meshy/remesh`

<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "meshy/remesh",
        {
            "input_task_id": "0193abcd-0000-0000-0000-000000000000",
            "origin_at": "bottom",
            "resize_height": 1,
            "target_formats": ["glb", "fbx"],
            "target_polycount": 50000,
            "topology": "quad",
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const { data } = await comfy.models.run("meshy/remesh", {
  input_task_id: "0193abcd-0000-0000-0000-000000000000",
  origin_at: "bottom",
  resize_height: 1,
  target_formats: ["glb", "fbx"],
  target_polycount: 50000,
  topology: "quad",
});

console.log(data);
```

```bash cURL
curl https://api.comfy.org/v2/models/meshy/remesh \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"input_task_id\": \"0193abcd-0000-0000-0000-000000000000\", \"origin_at\": \"bottom\", \"resize_height\": 1, \"target_formats\": [\"glb\",\"fbx\"], \"target_polycount\": 50000, \"topology\": \"quad\"}"
```
</CodeGroup>

## Schema

### 입력

<ParamField body="convert_format_only" type="boolean" default="false">
  참이면 입력 모델 파일의 형식만 변경하고, topology, resize_height, target_polycount 등 다른 입력은 무시합니다.
</ParamField>

<ParamField body="input_task_id" type="string">
  리메시할 완료된 Image to 3D 또는 Text to 3D 작업의 ID입니다. model_url이 제공되지 않은 경우 필수입니다.
</ParamField>

<ParamField body="model_url" type="string">
  3D 모델에 대한 공개적으로 접근 가능한 URL 또는 데이터 URI입니다. 지원 형식은 glb, gltf, obj, fbx, stl입니다. input_task_id가 제공되지 않은 경우 필수입니다.
</ParamField>

<ParamField body="origin_at" type="string">
  원점의 위치입니다.

  가능한 값: `bottom`, `center`, ``
</ParamField>

<ParamField body="resize_height" type="number" default="0">
  모델의 크기를 미터 단위의 특정 높이로 조정합니다. 0은 크기 조정을 하지 않음을 의미합니다.
</ParamField>

<ParamField body="target_formats" type="`glb`, `fbx`, `obj`, `usdz`, `blend`, `stl`[]" default="[&quot;glb&quot;]">
  리메시된 모델의 대상 형식 목록입니다.
</ParamField>

<ParamField body="target_polycount" type="integer" default="30000">
  생성된 모델의 대상 폴리곤 수를 지정합니다. 유효 범위는 100에서 300,000입니다.

  범위: `100` ~ `300000`
</ParamField>

<ParamField body="topology" type="string" default="&quot;triangle&quot;">
  생성된 모델의 토폴로지를 지정합니다.

  가능한 값: `quad`, `triangle`
</ParamField>

Router가 `GET /v2/models/meshy/remesh/openapi.json`에서 제공하는 스키마에서 생성되었으며, 이는 요청이 공급자에게 도달하기 전에 호출을 검증하는 데 사용하는 것과 동일한 문서입니다.

### 출력

<ResponseField name="created_at" type="integer">
  작업이 생성된 시점의 타임스탬프(밀리초)입니다.

  형식: `int64`
</ResponseField>

<ResponseField name="finished_at" type="integer">
  작업이 완료된 시점의 타임스탬프(밀리초)입니다. 완료되지 않은 경우 0입니다.

  형식: `int64`
</ResponseField>

<ResponseField name="id" type="string" required>
  작업의 고유 식별자입니다.
</ResponseField>

<ResponseField name="model_urls" type="object">
  리메시된 3D 모델 파일의 다운로드 가능한 URL입니다.
</ResponseField>

<ResponseField name="model_urls.blend" type="string">
  Blender 파일의 다운로드 가능한 URL입니다.
</ResponseField>

<ResponseField name="model_urls.fbx" type="string">
  FBX 파일의 다운로드 가능한 URL입니다.
</ResponseField>

<ResponseField name="model_urls.glb" type="string">
  GLB 파일의 다운로드 가능한 URL입니다.
</ResponseField>

<ResponseField name="model_urls.obj" type="string">
  OBJ 파일의 다운로드 가능한 URL입니다.
</ResponseField>

<ResponseField name="model_urls.stl" type="string">
  STL 파일의 다운로드 가능한 URL입니다.
</ResponseField>

<ResponseField name="model_urls.usdz" type="string">
  USDZ 파일의 다운로드 가능한 URL입니다.
</ResponseField>

<ResponseField name="preceding_tasks" type="integer">
  선행 작업의 수입니다. status가 PENDING일 때만 의미가 있습니다.
</ResponseField>

<ResponseField name="progress" type="integer">
  작업의 진행률입니다. 시작되지 않은 경우 0, 성공한 경우 100입니다.

  범위: `0` ~ `100`
</ResponseField>

<ResponseField name="started_at" type="integer">
  작업이 시작된 시점의 타임스탬프(밀리초)입니다. 시작되지 않은 경우 0입니다.

  형식: `int64`
</ResponseField>

<ResponseField name="status" type="string" required>
  가능한 값: `SUCCEEDED`
</ResponseField>

<ResponseField name="task_error" type="object">
  작업이 실패한 경우 오류 메시지를 포함하는 오류 객체입니다.
</ResponseField>

<ResponseField name="task_error.message" type="string">
  자세한 오류 메시지입니다.
</ResponseField>

<ResponseField name="type" type="string">
  Remesh 작업의 유형입니다.

  가능한 값: `remesh`
</ResponseField>

## 예제

### 입력

```json
{
  "input_task_id": "0193abcd-0000-0000-0000-000000000000",
  "origin_at": "bottom",
  "resize_height": 1,
  "target_formats": [
    "glb",
    "fbx"
  ],
  "target_polycount": 50000,
  "topology": "quad"
}
```

### 출력

```json
{
  "created_at": 1767225600000,
  "finished_at": 1767225663000,
  "id": "018f2c7a-4b1e-7c3d-9a05-6e2f8b41d0c9",
  "model_urls": {
    "fbx": "https://example.invalid/meshy/remesh/model.fbx",
    "glb": "https://example.invalid/meshy/remesh/model.glb",
    "obj": "https://example.invalid/meshy/remesh/model.obj"
  },
  "progress": 100,
  "started_at": 1767225601000,
  "status": "SUCCEEDED",
  "type": "remesh"
}
```


<RouterCodeFooter />
