---
title: "Comfy Router로 Image Edit Erase By Text 사용하기"
description: "Comfy Router를 통해 bria/image-edit-erase-by-text를 호출합니다: 엔드포인트, 요청 형태, 그리고 Router가 반환하는 응답."
sidebarTitle: "Image Edit Erase By Text"
translationSourceHash: 6491fc09
translationFrom: development/comfy-router/models/bria/image-edit-erase-by-text/code.mdx
translationBlockHashes:
  "_intro": ced16dd8
  "Quick start": 8fed0f88
  "Schema": 878bc5e3
  "Examples": da52e39a
---
{/* GENERATED FILE. Generated from router-schemas/bria/image-edit-erase-by-text.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/ko/comfy-router/model-code-footer.mdx";

`bria/image-edit-erase-by-text`의 API 레퍼런스로, Comfy Router가 Bria에서 제공합니다.

## 빠른 시작

[Comfy 워크스페이스](https://platform.comfy.org/profile/api-keys)에서 키를 생성하고 `COMFY_API_KEY`로 내보내세요. Python, TypeScript, Swift 스니펫은 Comfy SDK(`pip install comfy-sdk`, `npm install @comfyorg/sdk`, 그리고 [`ComfySwiftSDK`](https://github.com/Comfy-Org/comfy-swift-sdk) Swift 패키지)를 사용하며, cURL 스니펫은 동일한 호출을 원시 HTTP로 보낸 것입니다.

**Model ID:** `bria/image-edit-erase-by-text`

**Endpoint:** `POST https://api.comfy.org/v2/models/bria/image-edit-erase-by-text`

<Tabs>
  <Tab title="결과 대기">
<CodeGroup>
```python Python
from comfy_sdk import Comfy

# 환경 변수에서 COMFY_API_KEY를 읽습니다.
# SDK는 자동으로 idempotency 키를 생성하고 자동 재시도에 재사용합니다.
with Comfy() as client:
    result = client.models.run(
        "bria/image-edit-erase-by-text",
        {
            "image": "https://img.freepik.com/free-psd/close-up-delicious-apple_23-2151868338.jpg",
            "object_name": "the pie",
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// 환경 변수에서 COMFY_API_KEY를 읽습니다.
// SDK는 자동으로 idempotency 키를 생성하고 자동 재시도에 재사용합니다.
const { data } = await comfy.models.run("bria/image-edit-erase-by-text", {
  image: "https://img.freepik.com/free-psd/close-up-delicious-apple_23-2151868338.jpg",
  object_name: "the pie",
});

console.log(data);
```

```swift Swift
import Foundation
import ComfySwiftSDK

// Reads COMFY_API_KEY from the environment.
// The SDK mints an idempotency key per call and reuses it for automatic retries.
let client = ComfyCloudClient(apiKey: ProcessInfo.processInfo.environment["COMFY_API_KEY"]!)
let result = try await client.models.run(
    "bria/image-edit-erase-by-text",
    input: [
        "image": "https://img.freepik.com/free-psd/close-up-delicious-apple_23-2151868338.jpg",
        "object_name": "the pie",
    ]
)

print(result.output)
```

```bash cURL
curl https://api.comfy.org/v2/models/bria/image-edit-erase-by-text \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"image\": \"https://img.freepik.com/free-psd/close-up-delicious-apple_23-2151868338.jpg\", \"object_name\": \"the pie\"}"
```
</CodeGroup>
  </Tab>
  <Tab title="실행 대기열에 넣고 나중에 수집">
동일한 본문을 `POST https://api.comfy.org/v2/models/bria/image-edit-erase-by-text/requests`로 전송합니다. 라우터는 실행이 접수되는 즉시 `request_id`와 함께 `201`을 응답하며, 결과는 준비되면 이 프로세스에서든 다른 프로세스에서든 수집할 수 있습니다. [큐 전송](/ko/development/comfy-router/queue)에서 상태, 취소, 수집 과정을 안내합니다.

<CodeGroup>
```python Python
from comfy_sdk import Comfy

# 환경 변수에서 COMFY_API_KEY를 읽습니다.
# 각 submit() 호출은 자체 Idempotency-Key를 생성하고 자동 재시도에 재사용합니다.
with Comfy() as client:
    handle = client.models.submit(
        "bria/image-edit-erase-by-text",
        {
            "image": "https://img.freepik.com/free-psd/close-up-delicious-apple_23-2151868338.jpg",
            "object_name": "the pie",
        },
    )
    print("request_id:", handle.request_id)  # 모델 ID와 함께라면 다른 프로세스에 필요한 전부입니다

    # 요청이 완료될 때까지 폴링하며, 서버가 명시한 Retry-After만큼 대기합니다.
    for update in handle.iter_events():
        print(update.status, update.queue_position)

    # 공급자의 자체 페이로드이며, models.run()이 반환하는 것과 동일한 값입니다.
    # 실패하거나 취소된 요청은 여기에서 타입이 지정된 Router 오류를 발생시킵니다.
    result = handle.get()

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// 환경 변수에서 COMFY_API_KEY를 읽습니다.
// 각 submit() 호출은 자체 Idempotency-Key를 생성하고 자동 재시도에 재사용합니다.
const handle = await comfy.models.submit("bria/image-edit-erase-by-text", {
  image: "https://img.freepik.com/free-psd/close-up-delicious-apple_23-2151868338.jpg",
  object_name: "the pie",
});
console.log("requestId:", handle.requestId); // 모델 ID와 함께라면 다른 프로세스에 필요한 전부입니다

// 요청이 완료될 때까지 폴링하며, 서버가 명시한 Retry-After만큼 대기합니다.
for await (const update of handle.events()) {
  console.log(update.status, update.queuePosition);
}

// models.run()이 반환하는 것과 동일한 결과입니다. 실패하거나 취소된 요청은 여기에서 거부됩니다.
const result = await handle.get();

console.log(result.data);
```

```swift Swift
import Foundation
import ComfySwiftSDK

// Reads COMFY_API_KEY from the environment.
// Each submit() call mints its own Idempotency-Key and reuses it for automatic retries.
let client = ComfyCloudClient(apiKey: ProcessInfo.processInfo.environment["COMFY_API_KEY"]!)
let handle = try await client.models.submit(
    "bria/image-edit-erase-by-text",
    input: [
        "image": "https://img.freepik.com/free-psd/close-up-delicious-apple_23-2151868338.jpg",
        "object_name": "the pie",
    ]
)
print("requestId:", handle.requestId)  // with the model ID, all another process needs

// Poll until the request completes, waiting the Retry-After the server names.
for try await update in handle.events() {
    print(update.state.rawValue, update.queuePosition.map(String.init) ?? "unknown")
}

// The provider's own payload, the same value models.run() returns.
// A request that failed or was cancelled throws the typed Router error here.
let result = try await handle.result()

print(result.output)
```

```bash cURL
# 1. 제출. 라우터는 request_id, status_url, response_url, cancel_url과 함께 201을 응답합니다.
curl https://api.comfy.org/v2/models/bria/image-edit-erase-by-text/requests \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"image\": \"https://img.freepik.com/free-psd/close-up-delicious-apple_23-2151868338.jpg\", \"object_name\": \"the pie\"}"

# 2. 상태가 COMPLETED가 될 때까지 폴링하며, 각 응답이 명시한 Retry-After 초만큼 대기합니다.
REQUEST_ID="<request_id from the 201 body>"
curl -i https://api.comfy.org/v2/models/bria/image-edit-erase-by-text/requests/$REQUEST_ID/status \
  -H "X-API-Key: $COMFY_API_KEY"

# 3. 수집. 모델의 원본 출력과 함께 200을, 아직 실행 중이면 상태 본문과 함께 202를 반환합니다.
curl https://api.comfy.org/v2/models/bria/image-edit-erase-by-text/requests/$REQUEST_ID \
  -H "X-API-Key: $COMFY_API_KEY"
```
</CodeGroup>
  </Tab>
</Tabs>

## 스키마

### 입력

<ParamField body="image" type="string" required>
  편집할 이미지입니다. 지원되는 입력 유형은 Base64로 인코딩된 문자열 또는 공개적으로 접근 가능한 이미지 파일을 가리키는 URL입니다. 허용되는 형식은 JPEG, JPG, PNG, WEBP입니다.
</ParamField>

<ParamField body="object_name" type="string" required>
  제거할 객체에 대한 텍스트 설명입니다(예: "the lamp").
</ParamField>

Router가 `GET /v2/models/bria/image-edit-erase-by-text/openapi.json`에서 제공하는 스키마에서 생성되며, 이는 요청이 공급자에게 도달하기 전에 호출을 검증하는 대상이 되는 동일한 문서입니다.

### 출력

<ResponseField name="error" type="object">
  오류 객체(status가 ERROR일 때만 존재)
</ResponseField>

<ResponseField name="error.code" type="integer">
  오류 코드.
</ResponseField>

<ResponseField name="error.details" type="string">
  추가 오류 세부 정보.
</ResponseField>

<ResponseField name="error.message" type="string">
  오류 메시지.
</ResponseField>

<ResponseField name="request_id" type="string">
  요청의 고유 식별자.
</ResponseField>

<ResponseField name="result" type="object">
  결과 객체(status가 COMPLETED일 때만 존재)
</ResponseField>

<ResponseField name="result.image_url" type="string">
  생성/편집된 이미지의 URL.
</ResponseField>

<ResponseField name="result.prompt" type="string">
  원본 프롬프트.
</ResponseField>

<ResponseField name="result.refined_prompt" type="string">
  프롬프트의 정제된 버전이며, Bria가 정제하지 않은 COMPLETED 생성에서는 null입니다. 키가 생략된 것이 아니라 null을 담은 채 PRESENT이기 때문에 nullable입니다. bria/image-edit-gen-fill에서 실제로 관측되었습니다.
</ResponseField>

<ResponseField name="result.seed" type="integer">
  생성에 사용된 시드.
</ResponseField>

<ResponseField name="result.structured_prompt" type="string">
  상세한 JSON 구조화 프롬프트.
</ResponseField>

<ResponseField name="result.video_url" type="string">
  생성된 비디오의 URL.
</ResponseField>

<ResponseField name="status" type="string">
  요청의 현재 상태.

  가능한 값: `IN_PROGRESS`, `COMPLETED`, `ERROR`, `UNKNOWN`
</ResponseField>

## 예시

### 입력

```json
{
  "image": "https://img.freepik.com/free-psd/close-up-delicious-apple_23-2151868338.jpg",
  "object_name": "the pie"
}
```

### 출력

```json
{
  "request_id": "0b3f9d7e-2c41-4a8b-9f10-6d5c8e2a4b71",
  "result": {
    "image_url": "https://example.invalid/bria/image-edit-erase-by-text/generated.png",
    "seed": 42
  },
  "status": "COMPLETED"
}
```

<RouterCodeFooter />
