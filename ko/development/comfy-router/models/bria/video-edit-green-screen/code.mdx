---
title: "Comfy Router로 Video Edit Green Screen 사용하기"
description: "Comfy Router를 통해 bria/video-edit-green-screen을 호출합니다: endpoint, 요청 형태, 그리고 Router가 반환하는 응답."
sidebarTitle: "Video Edit Green Screen"
translationSourceHash: a0c7be90
translationFrom: development/comfy-router/models/bria/video-edit-green-screen/code.mdx
translationBlockHashes:
  "_intro": b4683e93
  "Quick start": c3a2d6fa
  "Schema": 5008e45d
  "Examples": 13c57b2c
---
{/* GENERATED FILE. Generated from router-schemas/bria/video-edit-green-screen.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/ko/comfy-router/model-code-footer.mdx";

`bria/video-edit-green-screen`에 대한 API 레퍼런스입니다. Bria에서 Comfy Router를 통해 제공됩니다.

## Quick start {#quick-start}

[your Comfy workspace](https://platform.comfy.org/profile/api-keys)에서 키를 생성하고 `COMFY_API_KEY`로 내보내세요. Python 및 TypeScript 스니펫은 Comfy SDK(`pip install comfy-sdk`, `npm install @comfyorg/sdk`)를 사용하며, cURL 스니펫은 동일한 호출을 원시 HTTP로 수행합니다.

**Model ID:** `bria/video-edit-green-screen`

**Endpoint:** `POST https://api.comfy.org/v2/models/bria/video-edit-green-screen`

<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "bria/video-edit-green-screen",
        {
            "green_shade": "broadcast_green",
            "video": "https://example.com/input.mp4",
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const { data } = await comfy.models.run("bria/video-edit-green-screen", {
  green_shade: "broadcast_green",
  video: "https://example.com/input.mp4",
});

console.log(data);
```

```bash cURL
curl https://api.comfy.org/v2/models/bria/video-edit-green-screen \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"green_shade\": \"broadcast_green\", \"video\": \"https://example.com/input.mp4\"}"
```
</CodeGroup>

## 스키마

### 입력

<ParamField body="green_shade" type="string">
  크로마 키잉을 위해 전경 뒤에 적용되는 단색 배경 색상입니다. 기본값은 broadcast_green입니다.

  가능한 값: `broadcast_green`, `chroma_green`, `blue_screen`
</ParamField>

<ParamField body="output_container_and_codec" type="string">
  출력 컨테이너 및 코덱 프리셋입니다.

  가능한 값: `mp4_h264`, `mp4_h265`, `webm_vp9`, `mov_h265`, `mov_proresks`, `mkv_h264`, `mkv_h265`, `mkv_vp9`, `gif`
</ParamField>

<ParamField body="preserve_audio" type="boolean">
  입력 비디오의 오디오를 유지할지 여부입니다.
</ParamField>

<ParamField body="video" type="string" required>
  입력 비디오의 공개적으로 접근 가능한 URL입니다. 입력 해상도는 최대 16000x16000(16K)까지 지원됩니다. 최대 재생 시간 60초입니다.
</ParamField>

Router가 `GET /v2/models/bria/video-edit-green-screen/openapi.json`에서 제공하는 스키마를 기반으로 생성되었으며, 요청이 공급자에게 도달하기 전에 호출을 검증하는 데 사용하는 것과 동일한 문서입니다.

### 출력

<ResponseField name="error" type="object">
  오류 객체입니다(status가 ERROR일 때만 존재).
</ResponseField>

<ResponseField name="error.code" type="integer">
  오류 코드입니다.
</ResponseField>

<ResponseField name="error.details" type="string">
  추가 오류 세부 정보입니다.
</ResponseField>

<ResponseField name="error.message" type="string">
  오류 메시지입니다.
</ResponseField>

<ResponseField name="request_id" type="string">
  요청의 고유 식별자입니다.
</ResponseField>

<ResponseField name="result" type="object">
  결과 객체입니다(status가 COMPLETED일 때만 존재).
</ResponseField>

<ResponseField name="result.image_url" type="string">
  생성/편집된 이미지의 URL입니다.
</ResponseField>

<ResponseField name="result.prompt" type="string">
  원본 프롬프트입니다.
</ResponseField>

<ResponseField name="result.refined_prompt" type="string">
  프롬프트의 개선된 버전이며, Bria가 개선하지 않은 COMPLETED 생성에서는 null입니다. 키가 생략된 것이 아니라 null을 담은 채 존재하기 때문에 Nullable입니다. bria/image-edit-gen-fill에서 실제로 확인되었습니다.
</ResponseField>

<ResponseField name="result.seed" type="integer">
  생성에 사용된 시드입니다.
</ResponseField>

<ResponseField name="result.structured_prompt" type="string">
  상세한 JSON 구조화 프롬프트입니다.
</ResponseField>

<ResponseField name="result.video_url" type="string">
  생성된 비디오의 URL입니다.
</ResponseField>

<ResponseField name="status" type="string">
  요청의 현재 상태입니다.

  가능한 값: `IN_PROGRESS`, `COMPLETED`, `ERROR`, `UNKNOWN`
</ResponseField>

## 예시

### 입력

```json
{
  "green_shade": "broadcast_green",
  "video": "https://example.com/input.mp4"
}
```

### 출력

```json
{
  "request_id": "0b3f9d7e-2c41-4a8b-9f10-6d5c8e2a4b71",
  "result": {
    "video_url": "https://example.invalid/bria/video-edit-green-screen/generated.mp4"
  },
  "status": "COMPLETED"
}
```


<RouterCodeFooter />
