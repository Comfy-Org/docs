---
title: "Comfy Router로 Video Edit Replace Background 사용하기"
description: "Comfy Router를 통해 bria/video-edit-replace-background를 호출하는 방법과 엔드포인트, 요청 형태, Router가 반환하는 응답을 설명합니다."
sidebarTitle: "Video Edit Replace Background"
translationSourceHash: d12969c8
translationFrom: development/comfy-router/models/bria/video-edit-replace-background/code.mdx
translationBlockHashes:
  "_intro": 5955c067
  "Quick start": e405be35
  "Schema": 12e66309
  "Examples": 18e1a850
---
{/* GENERATED FILE. Generated from router-schemas/bria/video-edit-replace-background.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/ko/comfy-router/model-code-footer.mdx";

`bria/video-edit-replace-background`에 대한 API 레퍼런스로, Bria에서 Comfy Router를 통해 제공됩니다.

## 빠른 시작

[Comfy 워크스페이스](https://platform.comfy.org/profile/api-keys)에서 키를 생성하고 `COMFY_API_KEY`로 내보내세요. Python 및 TypeScript 스니펫은 Comfy SDK(`pip install comfy-sdk`, `npm install @comfyorg/sdk`)를 사용하며, cURL 스니펫은 원시 HTTP를 통한 동일한 호출입니다.

**모델 ID:** `bria/video-edit-replace-background`

**엔드포인트:** `POST https://api.comfy.org/v2/models/bria/video-edit-replace-background`

<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "bria/video-edit-replace-background",
        {
            "background_url": "https://example.com/background.mp4",
            "video": "https://example.com/input.mp4",
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const { data } = await comfy.models.run("bria/video-edit-replace-background", {
  background_url: "https://example.com/background.mp4",
  video: "https://example.com/input.mp4",
});

console.log(data);
```

```bash cURL
curl https://api.comfy.org/v2/models/bria/video-edit-replace-background \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"background_url\": \"https://example.com/background.mp4\", \"video\": \"https://example.com/input.mp4\"}"
```
</CodeGroup>

## 스키마

### 입력

<ParamField body="background_url" type="string" required>
  전경 뒤에 합성할 배경 애셋(이미지 또는 비디오)의 공개적으로 접근할 수 있는 URL입니다. 전경의 가로세로 비율과 일치해야 합니다.
</ParamField>

<ParamField body="output_container_and_codec" type="string">
  출력 컨테이너 및 코덱 프리셋입니다.

  가능한 값: `mp4_h264`, `mp4_h265`, `webm_vp9`, `mov_h265`, `mov_proresks`, `mkv_h264`, `mkv_h265`, `mkv_vp9`, `gif`
</ParamField>

<ParamField body="preserve_audio" type="boolean">
  입력(전경) 비디오의 오디오를 유지할지 여부입니다.
</ParamField>

<ParamField body="video" type="string" required>
  입력(전경) 비디오의 공개적으로 접근할 수 있는 URL입니다. 입력 해상도는 최대 16000x16000(16K)까지 지원합니다. 최대 재생 시간 60초.
</ParamField>

Router가 `GET /v2/models/bria/video-edit-replace-background/openapi.json`에서 제공하는 스키마에서 생성되었으며, 이는 요청이 공급자에 도달하기 전에 호출을 검증할 때 사용하는 것과 동일한 문서입니다.

### 출력

<ResponseField name="error" type="object">
  오류 객체 (status가 ERROR일 때만 존재)
</ResponseField>

<ResponseField name="error.code" type="integer">
  오류 코드.
</ResponseField>

<ResponseField name="error.details" type="string">
  추가 오류 세부 정보.
</ResponseField>

<ResponseField name="error.message" type="string">
  오류 메시지.
</ResponseField>

<ResponseField name="request_id" type="string">
  요청의 고유 식별자.
</ResponseField>

<ResponseField name="result" type="object">
  결과 객체 (status가 COMPLETED일 때만 존재)
</ResponseField>

<ResponseField name="result.image_url" type="string">
  생성/편집된 이미지의 URL.
</ResponseField>

<ResponseField name="result.prompt" type="string">
  원본 프롬프트.
</ResponseField>

<ResponseField name="result.refined_prompt" type="string">
  프롬프트의 정제된 버전이며, Bria가 정제하지 않은 COMPLETED 생성에서는 null입니다. 이 키는 생략되는 것이 아니라 null을 담은 채 PRESENT이므로 nullable입니다. bria/image-edit-gen-fill에서 실제로 관측되었습니다.
</ResponseField>

<ResponseField name="result.seed" type="integer">
  생성에 사용된 시드.
</ResponseField>

<ResponseField name="result.structured_prompt" type="string">
  상세한 JSON 구조화 프롬프트.
</ResponseField>

<ResponseField name="result.video_url" type="string">
  생성된 비디오의 URL.
</ResponseField>

<ResponseField name="status" type="string">
  요청의 현재 상태.

  가능한 값: `IN_PROGRESS`, `COMPLETED`, `ERROR`, `UNKNOWN`
</ResponseField>

## 예시

### 입력

```json
{
  "background_url": "https://example.com/background.mp4",
  "video": "https://example.com/input.mp4"
}
```

### 출력

```json
{
  "request_id": "0b3f9d7e-2c41-4a8b-9f10-6d5c8e2a4b71",
  "result": {
    "video_url": "https://example.invalid/bria/video-edit-replace-background/generated.mp4"
  },
  "status": "COMPLETED"
}
```


<RouterCodeFooter />
