---
title: "Comfy Router로 Recraft V4.1 사용하기"
description: "Comfy Router를 통해 recraft/recraftv4_1을 호출합니다: endpoint, request 형태, 그리고 Router가 반환하는 응답."
sidebarTitle: "Recraft V4.1"
translationSourceHash: 251cdeb6
translationFrom: development/comfy-router/models/recraft/recraftv4-1/code.mdx
translationBlockHashes:
  "_intro": 35b040e2
  "Quick start": 0a1e34d3
  "Schema": 2f348ae5
  "Examples": 5c6fff21
---

{/* 생성된 파일. `pnpm code-pages:gen`에 의해 router-schemas/recraft/recraftv4_1.json에서 생성됨. */}

import RouterCodeFooter from "/snippets/ko/comfy-router/model-code-footer.mdx";

`recraft/recraftv4_1`에 대한 API 레퍼런스로, Recraft에서 Comfy Router를 통해 제공됩니다.

## 빠른 시작

[Comfy 워크스페이스](https://platform.comfy.org/profile/api-keys?onboarding=router)에서 키를 생성하고 `COMFY_API_KEY`로 내보내세요. Python, TypeScript, Swift 스니펫은 Comfy SDK(`pip install comfy-sdk`, `npm install @comfyorg/sdk`, 그리고 [`ComfySwiftSDK`](https://github.com/Comfy-Org/comfy-swift-sdk) Swift 패키지)를 사용하며, cURL 스니펫은 동일한 호출을 raw HTTP로 수행합니다.

**모델 ID:** `recraft/recraftv4_1`

**엔드포인트:** `POST https://api.comfy.org/v2/models/recraft/recraftv4_1`

<Tabs>
  <Tab title="Wait for the result">
<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "recraft/recraftv4_1",
        {
            "n": 1,
            "prompt": "A single red maple leaf on a plain white background.",
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const { data } = await comfy.models.run("recraft/recraftv4_1", {
  n: 1,
  prompt: "A single red maple leaf on a plain white background.",
});

console.log(data);
```

```swift Swift
import Foundation
import ComfySwiftSDK

// Reads COMFY_API_KEY from the environment.
// The SDK mints an idempotency key per call and reuses it for automatic retries.
let client = ComfyCloudClient(apiKey: ProcessInfo.processInfo.environment["COMFY_API_KEY"]!)
let result = try await client.models.run(
    "recraft/recraftv4_1",
    input: [
        "n": 1,
        "prompt": "A single red maple leaf on a plain white background.",
    ]
)

print(result.output)
```

```bash cURL
curl https://api.comfy.org/v2/models/recraft/recraftv4_1 \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"n\": 1, \"prompt\": \"A single red maple leaf on a plain white background.\"}"
```
</CodeGroup>
  </Tab>
  <Tab title="Queue and collect later">
동일한 본문을 `POST https://api.comfy.org/v2/models/recraft/recraftv4-1/requests` 로 보냅니다. Router는 실행이 접수되는 즉시 `201` 과 `request_id` 를 응답하며, 결과는 준비가 되는 대로 이 프로세스나 다른 프로세스에서 수집할 수 있습니다. 상태, 취소, 수집 방법은 [큐 전송](/ko/development/comfy-router/queue) 를 참고하세요.

<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# Each submit() call mints its own Idempotency-Key and reuses it for automatic retries.
with Comfy() as client:
    handle = client.models.submit(
        "recraft/recraftv4_1",
        {
            "n": 1,
            "prompt": "A single red maple leaf on a plain white background.",
        },
    )
    print("request_id:", handle.request_id)  # with the model ID, all another process needs

    # Poll until the request completes, waiting the Retry-After the server names.
    for update in handle.iter_events():
        print(update.status, update.queue_position)

    # The provider's own payload, the same value models.run() returns.
    # A request that failed or was cancelled raises the typed Router error here.
    result = handle.get()

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// Each submit() call mints its own Idempotency-Key and reuses it for automatic retries.
const handle = await comfy.models.submit("recraft/recraftv4_1", {
  n: 1,
  prompt: "A single red maple leaf on a plain white background.",
});
console.log("requestId:", handle.requestId); // with the model ID, all another process needs

// Poll until the request completes, waiting the Retry-After the server names.
for await (const update of handle.events()) {
  console.log(update.status, update.queuePosition);
}

// The same result models.run() returns. A request that failed or was cancelled rejects here.
const result = await handle.get();

console.log(result.data);
```

```swift Swift
import Foundation
import ComfySwiftSDK

// Reads COMFY_API_KEY from the environment.
// Each submit() call mints its own Idempotency-Key and reuses it for automatic retries.
let client = ComfyCloudClient(apiKey: ProcessInfo.processInfo.environment["COMFY_API_KEY"]!)
let handle = try await client.models.submit(
    "recraft/recraftv4_1",
    input: [
        "n": 1,
        "prompt": "A single red maple leaf on a plain white background.",
    ]
)
print("requestId:", handle.requestId)  // with the model ID, all another process needs

// Poll until the request completes, waiting the Retry-After the server names.
for try await update in handle.events() {
    print(update.state.rawValue, update.queuePosition.map(String.init) ?? "unknown")
}

// The provider's own payload, the same value models.run() returns.
// A request that failed or was cancelled throws the typed Router error here.
let result = try await handle.result()

print(result.output)
```

```bash cURL
# 1. Submit. Router answers 201 with request_id, status_url, response_url and cancel_url.
curl https://api.comfy.org/v2/models/recraft/recraftv4_1/requests \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"n\": 1, \"prompt\": \"A single red maple leaf on a plain white background.\"}"

# 2. Poll until status is COMPLETED, waiting the Retry-After seconds each response names.
REQUEST_ID="<request_id from the 201 body>"
curl -i https://api.comfy.org/v2/models/recraft/recraftv4_1/requests/$REQUEST_ID/status \
  -H "X-API-Key: $COMFY_API_KEY"

# 3. Collect. 200 with the model's native output, 202 with the status body while it is still running.
curl https://api.comfy.org/v2/models/recraft/recraftv4_1/requests/$REQUEST_ID \
  -H "X-API-Key: $COMFY_API_KEY"
```
</CodeGroup>
  </Tab>
</Tabs>

## 스키마

### 입력

<ParamField body="controls" type="object">
  생성된 이미지에 대한 제어
</ParamField>

<ParamField body="controls.artistic_level" type="integer">
  이미지의 예술적 톤을 정의합니다. 단순한 수준에서는 인물이 정적이고 깔끔한 스타일로 카메라를 똑바로 응시합니다. 다이내믹하고 이색적인 수준은 움직임과 창의성을 도입합니다.

  범위: `0` ~ `5`
</ParamField>

<ParamField body="controls.background_color" type="object">
  RGB 색상 값
</ParamField>

<ParamField body="controls.background_color.rgb" type="integer[]" required>
   
</ParamField>

<ParamField body="controls.colors" type="object[]">
  선호하는 색상의 배열
</ParamField>

<ParamField body="controls.colors[].rgb" type="integer[]" required>
   
</ParamField>

<ParamField body="controls.no_text" type="boolean">
  텍스트 레이아웃을 삽입하지 않습니다
</ParamField>

<ParamField body="model" type="string">
  생성에 사용할 모델입니다 (예: "recraftv3"). 이 필드는 enum으로 제한되지 않습니다. 프록시는 호출자가 보낸 값을 그대로 전달합니다. Comfy가 제공하는 표기, 즉 Comfy Router가 `recraft/<model>`로 주소를 지정하는 집합은 recraftv2, recraftv3, recraftv4, recraftv4_pro, recraftv4_1, recraftv4_1_utility, recraftv4_1_pro, recraftv4_1_utility_pro, recraftv4_styles, recraftv4_styles_pro, recraftv4_1_vector, recraftv4_1_utility_vector, recraftv4_1_pro_vector, recraftv4_1_utility_pro_vector, recraftv4_styles_vector 및 recraftv4_styles_pro_vector입니다. 이를 참조로 이름을 지정하지 않고 여기에 직접 나열한 이유는, 이들을 선언하는 RecraftGenerationModel 컴포넌트가 어디에서도 `$ref`되지 않아 GET /openapi로 제공되는 명세에서 제거되므로, 이에 대한 포인터가 제공되는 문서에서 끊어진 참조가 되기 때문입니다. 네 가지 `recraftv4_styles*` 표기는 추가로 `style_id`가 필요합니다. 해당 필드를 참고하세요.
</ParamField>

<ParamField body="n" type="integer">
  생성할 이미지 수입니다. Recraft는 1-6을 허용합니다.

  범위: `1` ~ `6`
</ParamField>

<ParamField body="prompt" type="string" required>
  생성할 이미지를 설명하는 텍스트 프롬프트
</ParamField>

<ParamField body="response_format" type="string">
  가능한 값: `url`, `b64_json`
</ParamField>

<ParamField body="size" type="string">
  생성된 이미지의 크기 (예: "1024x1024")
</ParamField>

<ParamField body="style" type="string">
  생성된 이미지에 적용할 스타일 (예: "digital_illustration")
</ParamField>

<ParamField body="style_id" type="string">
  생성된 이미지에 적용할 스타일 ID (예: "123e4567-e89b-12d3-a456-426614174000"). style_id가 제공되면 style은 제공하지 않아야 합니다. 네 가지 `recraftv4_styles*` 모델에서 필수입니다. Recraft는 style_id 또는 스타일 참조가 없는 요청을 거부합니다. `POST /proxy/recraft/styles`로 스타일 ID를 발급받을 수 있으며, 이 동일한 프록시가 동일한 자격 증명으로 해당 엔드포인트를 제공합니다. 이 라우트에서 이 조합을 강제하는 것은 없습니다. 본문은 변경 없이 Recraft로 전달되므로, style_id 없이 `recraftv4_styles*`를 호출하면 파트너에 도달한 뒤 4xx로 반환됩니다.
</ParamField>

Router가 `GET /v2/models/recraft/recraftv4_1/openapi.json`에서 제공하는 스키마에서 생성되었으며, 이는 요청이 공급자에 도달하기 이전에 호출을 검증하는 데 사용하는 동일한 문서입니다.

### 출력

<ResponseField name="created" type="integer" required>
  생성이 만들어진 시점의 Unix 타임스탬프
</ResponseField>

<ResponseField name="credits" type="integer" required>
  생성에 사용된 크레딧 수
</ResponseField>

<ResponseField name="data" type="object[]" required>
  생성된 이미지 정보의 배열
</ResponseField>

<ResponseField name="data[].b64_json" type="string">
  Base64로 인코딩된 이미지 데이터 (요청에서 `response_format: b64_json`을 설정한 경우 `url` 대신 포함됩니다)
</ResponseField>

<ResponseField name="data[].image_id" type="string">
  생성된 이미지의 고유 식별자
</ResponseField>

<ResponseField name="data[].url" type="string">
  생성된 이미지에 접근할 수 있는 URL (`response_format`이 기본값인 `url`일 때 포함됩니다)
</ResponseField>

<ResponseField name="style_id" type="string">
  스타일 참조가 사용될 때 Recraft가 반환하는 확인된 스타일 id입니다 (특히 필수 입력 모델인 recraftv4_styles*에서 필요합니다). 나중에 요청에서 `style_id`로 재사용할 수 있습니다.
</ResponseField>

## 예시

### 입력

```json
{
  "n": 1,
  "prompt": "A single red maple leaf on a plain white background."
}
```

### 출력

```json
{
  "created": 1767225600,
  "credits": 1,
  "data": [
    {
      "image_id": "3f7a1b28-5c0d-4e91-8a6f-1b2c3d4e5f60",
      "url": "https://example.invalid/recraft/recraftv3/generated.png"
    }
  ]
}
```

<RouterCodeFooter />
