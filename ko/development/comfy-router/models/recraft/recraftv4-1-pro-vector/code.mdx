---
title: "Comfy Router로 Recraft V4.1 Pro Vector 사용하기"
description: "Comfy Router를 통해 recraft/recraftv4_1_pro_vector를 호출합니다: endpoint, 요청 형태, 그리고 Router가 반환하는 응답."
sidebarTitle: "Recraft V4.1 Pro Vector"
translationSourceHash: c82620f0
translationFrom: development/comfy-router/models/recraft/recraftv4-1-pro-vector/code.mdx
translationBlockHashes:
  "_intro": e13c9640
  "Quick start": b0083bd7
  "Schema": 1c243ff3
  "Examples": 5c6fff21
---


{/* GENERATED FILE. Generated from router-schemas/recraft/recraftv4_1_pro_vector.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/ko/comfy-router/model-code-footer.mdx";

`recraft/recraftv4_1_pro_vector`의 API 레퍼런스이며, Recraft에서 제공하고 Comfy Router를 통해 서비스됩니다.

## 빠른 시작

[Comfy 워크스페이스](https://platform.comfy.org/profile/api-keys?onboarding=router)에서 키를 생성하고 `COMFY_API_KEY`로 내보내세요. Python 및 TypeScript 스니펫은 Comfy SDK(`pip install comfy-sdk` 및 `npm install @comfyorg/sdk`)를 사용하며, cURL 스니펫은 동일한 호출을 raw HTTP로 실행합니다.

**모델 ID:** `recraft/recraftv4_1_pro_vector`

**엔드포인트:** `POST https://api.comfy.org/v2/models/recraft/recraftv4_1_pro_vector`

<Tabs defaultTabIndex={1}>
  <Tab title="결과 기다리기">
<CodeGroup>
```python Python
from comfy_sdk import Comfy

# 환경에서 COMFY_API_KEY를 읽습니다.
# SDK는 자동으로 멱등성 키를 생성하고 자동 재시도에 재사용합니다.
with Comfy() as client:
    result = client.models.run(
        "recraft/recraftv4_1_pro_vector",
        {
            "n": 1,
            "prompt": "A single red maple leaf on a plain white background.",
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// 환경에서 COMFY_API_KEY를 읽습니다.
// SDK는 자동으로 멱등성 키를 생성하고 자동 재시도에 재사용합니다.
const { data } = await comfy.models.run("recraft/recraftv4_1_pro_vector", {
  n: 1,
  prompt: "A single red maple leaf on a plain white background.",
});

console.log(data);
```

```bash cURL
curl https://api.comfy.org/v2/models/recraft/recraftv4_1_pro_vector \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"n\": 1, \"prompt\": \"A single red maple leaf on a plain white background.\"}"
```
</CodeGroup>
  </Tab>
  <Tab title="대기열에 제출하고 나중에 수집">
동일한 본문을 `POST https://api.comfy.org/v2/models/recraft/recraftv4_1_pro_vector/requests`로 전송합니다. Router는 실행이 승인되는 즉시 `request_id`와 함께 `201`을 응답하고, 결과는 준비가 되면 이 프로세스 또는 다른 프로세스에서 수집됩니다. [대기 중 전달](/ko/development/comfy-router/queue)에서 상태, 취소, 수집 과정을 자세히 설명합니다.

<CodeGroup>
```python Python
import asyncio
from comfy_sdk import AsyncComfy

# 환경에서 COMFY_API_KEY를 읽습니다.
# 각 submit() 호출은 자체 Idempotency-Key를 발급하고 자동 재시도에 재사용합니다.
async def main():
    async with AsyncComfy() as client:
        handle = await client.models.submit(
            "recraft/recraftv4_1_pro_vector",
            {
                "n": 1,
                "prompt": "A single red maple leaf on a plain white background.",
            },
        )
        print("request_id:", handle.request_id)  # 모델 ID와 함께라면 다른 프로세스에 필요한 전부입니다.

        # 서버가 지정한 Retry-After만큼 대기하면서 요청이 완료될 때까지 폴링합니다.
        async for update in handle.iter_events():
            print(update.status, update.queue_position)

        # 공급자 자체의 페이로드이며, models.run()이 반환하는 것과 동일한 값입니다.
        # 실패하거나 취소된 요청은 여기에서 타입이 지정된 Router 오류를 발생시킵니다.
        result = await handle.get()

    print(result)

asyncio.run(main())
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// 환경에서 COMFY_API_KEY를 읽습니다.
// 각 submit() 호출은 자체 Idempotency-Key를 발급하고 자동 재시도에 재사용합니다.
const handle = await comfy.models.submit("recraft/recraftv4_1_pro_vector", {
  n: 1,
  prompt: "A single red maple leaf on a plain white background.",
});
console.log("requestId:", handle.requestId); // 모델 ID와 함께라면 다른 프로세스에 필요한 전부입니다.

// 서버가 지정한 Retry-After만큼 대기하면서 요청이 완료될 때까지 폴링합니다.
for await (const update of handle.events()) {
  console.log(update.status, update.queuePosition);
}

// models.run()이 반환하는 것과 동일한 결과입니다. 실패하거나 취소된 요청은 여기에서 거부됩니다.
const result = await handle.get();

console.log(result.data);
```

```bash cURL
# 1. 제출합니다. Router는 request_id, status_url, response_url, cancel_url과 함께 201을 응답합니다.
curl https://api.comfy.org/v2/models/recraft/recraftv4_1_pro_vector/requests \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"n\": 1, \"prompt\": \"A single red maple leaf on a plain white background.\"}"

# 2. 상태가 COMPLETED가 될 때까지 폴링하며, 각 응답이 지정한 Retry-After 초만큼 대기합니다.
REQUEST_ID="<request_id from the 201 body>"
curl -i https://api.comfy.org/v2/models/recraft/recraftv4_1_pro_vector/requests/$REQUEST_ID/status \
  -H "X-API-Key: $COMFY_API_KEY"

# 3. 수집합니다. 모델의 네이티브 출력과 함께 200, 아직 실행 중이면 상태 본문과 함께 202를 반환합니다.
curl https://api.comfy.org/v2/models/recraft/recraftv4_1_pro_vector/requests/$REQUEST_ID \
  -H "X-API-Key: $COMFY_API_KEY"
```
</CodeGroup>
  </Tab>
</Tabs>

## 스키마

### 입력

<ParamField body="controls" type="object">
  생성된 이미지에 대한 제어
</ParamField>

<ParamField body="controls.artistic_level" type="integer">
  이미지의 예술적 분위기를 정의합니다. 단순한 수준에서는 인물이 정적이고 깔끔한 스타일로 카메라를 똑바로 응시합니다. 동적이고 기발한 수준은 움직임과 창의성을 더합니다.

  범위: `0` ~ `5`
</ParamField>

<ParamField body="controls.background_color" type="object">
  RGB 색상 값
</ParamField>

<ParamField body="controls.background_color.rgb" type="integer[]" required>
   
</ParamField>

<ParamField body="controls.colors" type="object[]">
  선호하는 색상의 배열
</ParamField>

<ParamField body="controls.colors[].rgb" type="integer[]" required>
   
</ParamField>

<ParamField body="controls.no_text" type="boolean">
  텍스트 레이아웃을 삽입하지 않습니다
</ParamField>

<ParamField body="model" type="string">
  생성에 사용할 모델입니다(예: "recraftv3"). 이 필드는 enum으로 제한되지 않습니다. 프록시는 호출자가 보낸 값을 그대로 전달합니다. Comfy가 제공하는 표기, 즉 Comfy Router가 `recraft/<model>`로 주소 지정하는 집합은 recraftv2, recraftv3, recraftv4, recraftv4_pro, recraftv4_1, recraftv4_1_utility, recraftv4_1_pro, recraftv4_1_utility_pro, recraftv4_styles, recraftv4_styles_pro, recraftv4_1_vector, recraftv4_1_utility_vector, recraftv4_1_pro_vector, recraftv4_1_utility_pro_vector, recraftv4_styles_vector, recraftv4_styles_pro_vector입니다. 이 값들을 참조로 언급하지 않고 여기에 직접 나열한 이유는, 이들을 선언하는 RecraftGenerationModel 컴포넌트가 어디에서도 `$ref`되지 않아 GET /openapi에서 제공되는 스펙에서 제거되므로, 해당 컴포넌트를 가리키는 포인터가 제공되는 문서에서 끊어진 참조가 되기 때문입니다. 네 개의 `recraftv4_styles*` 표기는 추가로 `style_id`를 필요로 합니다. 해당 필드를 참고하세요.
</ParamField>

<ParamField body="n" type="integer">
  생성할 이미지 수입니다. Recraft는 1~6을 허용합니다.

  범위: `1` ~ `6`
</ParamField>

<ParamField body="prompt" type="string" required>
  생성할 이미지를 설명하는 텍스트 프롬프트
</ParamField>

<ParamField body="response_format" type="string">
  가능한 값: `url`, `b64_json`
</ParamField>

<ParamField body="size" type="string">
  생성된 이미지의 크기(예: "1024x1024")
</ParamField>

<ParamField body="style" type="string">
  생성된 이미지에 적용할 스타일(예: "digital_illustration")
</ParamField>

<ParamField body="style_id" type="string">
  생성된 이미지에 적용할 스타일 ID입니다(예: "123e4567-e89b-12d3-a456-426614174000"). style_id가 제공되면 style은 제공하지 않아야 합니다. 네 개의 `recraftv4_styles*` 모델에서는 필수입니다. Recraft는 style_id 또는 스타일 참조가 없는 요청을 거부합니다. `POST /proxy/recraft/styles`로 스타일을 발급받을 수 있으며, 이 프록시가 동일한 자격 증명으로 해당 엔드포인트를 제공합니다. 이 라우트에서는 이 조합을 강제하지 않습니다. 본문은 변경 없이 Recraft로 전달되므로, style_id 없이 `recraftv4_styles*`를 호출하면 파트너에 도달한 뒤 4xx로 반환됩니다.
</ParamField>

Router가 `GET /v2/models/recraft/recraftv4_1_pro_vector/openapi.json`에서 제공하는 스키마에서 생성되었으며, 요청이 공급자에게 도달하기 전에 호출을 검증할 때 사용하는 문서와 동일합니다.

### 출력

<ResponseField name="created" type="integer" required>
  생성이 만들어진 시점의 Unix 타임스탬프
</ResponseField>

<ResponseField name="credits" type="integer" required>
  생성에 사용된 크레딧 수
</ResponseField>

<ResponseField name="data" type="object[]" required>
  생성된 이미지 정보의 배열
</ResponseField>

<ResponseField name="data[].b64_json" type="string">
  Base64로 인코딩된 이미지 데이터(요청에서 `response_format: b64_json`을 설정한 경우 `url` 대신 제공됨)
</ResponseField>

<ResponseField name="data[].image_id" type="string">
  생성된 이미지의 고유 식별자
</ResponseField>

<ResponseField name="data[].url" type="string">
  생성된 이미지에 접근할 수 있는 URL(`response_format`이 기본값인 `url`일 때 제공됨)
</ResponseField>

<ResponseField name="style_id" type="string">
  해석된 스타일 id로, 스타일 참조가 사용될 때(특히 필수 입력 모델 recraftv4_styles*) Recraft가 반환합니다. 이후 요청에서 `style_id`로 재사용할 수 있습니다.
</ResponseField>

## 예시

### 입력

```json
{
  "n": 1,
  "prompt": "A single red maple leaf on a plain white background."
}
```

### 출력

```json
{
  "created": 1767225600,
  "credits": 1,
  "data": [
    {
      "image_id": "3f7a1b28-5c0d-4e91-8a6f-1b2c3d4e5f60",
      "url": "https://example.invalid/recraft/recraftv3/generated.png"
    }
  ]
}
```

<RouterCodeFooter />
