---
title: "Comfy Router로 Krea 2 Medium Turbo 사용하기"
description: "Comfy Router를 통해 krea/krea-2-medium-turbo를 호출합니다: endpoint, 요청 형태, 그리고 Router가 반환하는 응답을 설명합니다."
sidebarTitle: "Krea 2 Medium Turbo"
translationSourceHash: 796ffdf4
translationFrom: development/comfy-router/models/krea/krea-2-medium-turbo/code.mdx
translationBlockHashes:
  "_intro": 711a935d
  "Quick start": a305dd42
  "Schema": 00afbe49
  "Examples": 91f72543
---


{/* GENERATED FILE. Generated from router-schemas/krea/krea-2-medium-turbo.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/ko/comfy-router/model-code-footer.mdx";

`krea/krea-2-medium-turbo`의 API 레퍼런스입니다. 이 모델은 Krea에서 제공하며 Comfy Router를 통해 서비스됩니다.

## 빠른 시작

[내 Comfy 워크스페이스](https://platform.comfy.org/profile/api-keys?onboarding=router)에서 키를 생성하고 `COMFY_API_KEY`로 내보내세요. Python 및 TypeScript 스니펫은 Comfy SDK(`pip install comfy-sdk`, `npm install @comfyorg/sdk`)를 사용하며, cURL 스니펫은 동일한 호출을 순수 HTTP로 수행한 것입니다.

**모델 ID:** `krea/krea-2-medium-turbo`

**엔드포인트:** `POST https://api.comfy.org/v2/models/krea/krea-2-medium-turbo`

<Tabs defaultTabIndex={1}>
  <Tab title="결과 기다리기">
<CodeGroup>
```python Python
from comfy_sdk import Comfy

# 환경 변수에서 COMFY_API_KEY를 읽습니다.
# SDK는 자동으로 idempotency 키를 생성하고 자동 재시도에 재사용합니다.
with Comfy() as client:
    result = client.models.run(
        "krea/krea-2-medium-turbo",
        {
            "aspect_ratio": "1:1",
            "prompt": "a red circle",
            "resolution": "1K",
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// 환경 변수에서 COMFY_API_KEY를 읽습니다.
// SDK는 자동으로 idempotency 키를 생성하고 자동 재시도에 재사용합니다.
const { data } = await comfy.models.run("krea/krea-2-medium-turbo", {
  aspect_ratio: "1:1",
  prompt: "a red circle",
  resolution: "1K",
});

console.log(data);
```

```bash cURL
curl https://api.comfy.org/v2/models/krea/krea-2-medium-turbo \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"aspect_ratio\": \"1:1\", \"prompt\": \"a red circle\", \"resolution\": \"1K\"}"
```
</CodeGroup>
  </Tab>
  <Tab title="대기열에 넣고 나중에 수집">
동일한 본문을 `POST https://api.comfy.org/v2/models/krea/krea-2-medium-turbo/requests`로 보냅니다. Router는 실행이 접수되는 즉시 `request_id`와 함께 `201`을 응답하며, 결과는 준비되면 이 프로세스에서든 다른 프로세스에서든 수집할 수 있습니다. [대기열 전달](/ko/development/comfy-router/queue)에서 상태, 취소, 수집 방법을 자세히 다룹니다.

<CodeGroup>
```python Python
import asyncio
from comfy_sdk import AsyncComfy

# 환경 변수에서 COMFY_API_KEY를 읽습니다.
# 각 submit() 호출은 자체 Idempotency-Key를 생성하고 자동 재시도에 재사용합니다.
async def main():
    async with AsyncComfy() as client:
        handle = await client.models.submit(
            "krea/krea-2-medium-turbo",
            {
                "aspect_ratio": "1:1",
                "prompt": "a red circle",
                "resolution": "1K",
            },
        )
        print("request_id:", handle.request_id)  # 모델 ID와 함께라면 다른 프로세스에 필요한 전부입니다

        # 요청이 완료될 때까지 폴링하며, 서버가 알려주는 Retry-After만큼 대기합니다.
        async for update in handle.iter_events():
            print(update.status, update.queue_position)

        # 공급자의 자체 페이로드로, models.run()이 반환하는 것과 같은 값입니다.
        # 실패하거나 취소된 요청은 여기에서 타입이 지정된 Router 오류를 발생시킵니다.
        result = await handle.get()

    print(result)

asyncio.run(main())
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// 환경 변수에서 COMFY_API_KEY를 읽습니다.
// 각 submit() 호출은 자체 Idempotency-Key를 생성하고 자동 재시도에 재사용합니다.
const handle = await comfy.models.submit("krea/krea-2-medium-turbo", {
  aspect_ratio: "1:1",
  prompt: "a red circle",
  resolution: "1K",
});
console.log("requestId:", handle.requestId); // 모델 ID와 함께라면 다른 프로세스에 필요한 전부입니다

// 요청이 완료될 때까지 폴링하며, 서버가 알려주는 Retry-After만큼 대기합니다.
for await (const update of handle.events()) {
  console.log(update.status, update.queuePosition);
}

// models.run()이 반환하는 것과 같은 결과입니다. 실패하거나 취소된 요청은 여기에서 거부됩니다.
const result = await handle.get();

console.log(result.data);
```

```bash cURL
# 1. 제출. Router는 request_id, status_url, response_url, cancel_url과 함께 201을 응답합니다.
curl https://api.comfy.org/v2/models/krea/krea-2-medium-turbo/requests \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"aspect_ratio\": \"1:1\", \"prompt\": \"a red circle\", \"resolution\": \"1K\"}"

# 2. 상태가 COMPLETED가 될 때까지 폴링하며, 각 응답이 알려주는 Retry-After 초만큼 대기합니다.
REQUEST_ID="<request_id from the 201 body>"
curl -i https://api.comfy.org/v2/models/krea/krea-2-medium-turbo/requests/$REQUEST_ID/status \
  -H "X-API-Key: $COMFY_API_KEY"

# 3. 수집. 모델의 네이티브 출력과 함께 200, 아직 실행 중이면 상태 본문과 함께 202입니다.
curl https://api.comfy.org/v2/models/krea/krea-2-medium-turbo/requests/$REQUEST_ID \
  -H "X-API-Key: $COMFY_API_KEY"
```
</CodeGroup>
  </Tab>
</Tabs>

## 스키마

### 입력

<ParamField body="aspect_ratio" type="string" required>
  비율. 다음 중 하나: 1:1, 4:3, 3:2, 16:9, 2.35:1, 4:5, 2:3, 9:16.

  가능한 값: `1:1`, `4:3`, `3:2`, `16:9`, `2.35:1`, `4:5`, `2:3`, `9:16`
</ParamField>

<ParamField body="creativity" type="string" default="&quot;medium&quot;">
  프롬프트 해석 강도: raw=0, low=10, medium=50, high=100.

  가능한 값: `raw`, `low`, `medium`, `high`
</ParamField>

<ParamField body="image_style_references" type="object[]">
  생성에 사용할 스타일 참조
</ParamField>

<ParamField body="image_style_references[].strength" type="number" required>
  범위: `-2` ~ `2`

  형식: `double`
</ParamField>

<ParamField body="image_style_references[].url" type="string (uri)">
  형식: `uri`
</ParamField>

<ParamField body="moodboards" type="object[]">
  생성에 사용할 무드보드. 현재는 무드보드 하나로 제한됩니다.
</ParamField>

<ParamField body="moodboards[].id" type="string (uuid)" required>
  형식: `uuid`
</ParamField>

<ParamField body="moodboards[].strength" type="number" default="0.35">
  범위: `-0.5` ~ `1.5`

  형식: `double`
</ParamField>

<ParamField body="prompt" type="string" required>
   
</ParamField>

<ParamField body="resolution" type="string" required>
  해상도 크기 조절. 다음 중 하나: 1K.

  가능한 값: `1K`
</ParamField>

<ParamField body="seed" type="number">
   
</ParamField>

<ParamField body="styles" type="object[]">
  생성에 사용할 스타일(일반적으로 LoRA)
</ParamField>

<ParamField body="styles[].id" type="string" required>
   
</ParamField>

<ParamField body="styles[].strength" type="number" required>
  범위: `-2` ~ `2`

  형식: `double`
</ParamField>

Router가 `GET /v2/models/krea/krea-2-medium-turbo/openapi.json`에서 제공하는 스키마에서 생성되었으며, 요청이 공급자에 도달하기 이전에 호출을 검증할 때 사용하는 것과 동일한 문서입니다.

### 출력

<ResponseField name="completed_at" type="string (date-time)" required>
  형식: `date-time`
</ResponseField>

<ResponseField name="created_at" type="string (date-time)" required>
  형식: `date-time`
</ResponseField>

<ResponseField name="job_id" type="string (uuid)" required>
  형식: `uuid`
</ResponseField>

<ResponseField name="result" type="object" required>
  완료된 생성 결과. `KreaJob`과 달리 여기서는 null이 될 수 없습니다. `result`에 `urls`가 없는 터미널 작업은 Comfy Router 오류로 응답되므로, `200`에는 항상 이 값이 포함됩니다.
</ResponseField>

<ResponseField name="result.style_id" type="string">
  생성이 아니라 loraTraining 작업에서 설정됩니다. 이 모델들에 대한 `200` 응답에는 `urls`가 포함됩니다.
</ResponseField>

<ResponseField name="result.urls" type="string (uri)[]" required>
  생성된 이미지로, 다운로드 가능한 링크입니다. 최소 한 개가 존재합니다. `urls`가 비어 있는 `completed` 작업은 `success_without_output`이며 이 문서에 도달하지 않기 때문입니다.
</ResponseField>

<ResponseField name="status" type="string" required>
  이 문서에서는 항상 `completed`입니다. `classifyKrea`가 `succeeded`로 응답하는 유일한 상태이며, Krea의 어휘는 대소문자 변환 없이 일치됩니다.

  가능한 값: `completed`
</ResponseField>

## 예시

### 입력

```json
{
  "aspect_ratio": "1:1",
  "prompt": "a red circle",
  "resolution": "1K"
}
```

### 출력

```json
{
  "completed_at": "2027-01-01T00:00:37Z",
  "created_at": "2027-01-01T00:00:00Z",
  "job_id": "7f1c2e84-5b90-4a37-8d61-2c0f9ab4e153",
  "result": {
    "urls": [
      "https://example.invalid/krea/krea-2/generated.png"
    ]
  },
  "status": "completed"
}
```

<RouterCodeFooter />
