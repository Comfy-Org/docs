---
title: "Comfy Router로 Flux 1.1 Pro Ultra 이미지 사용하기"
description: "Comfy Router를 통해 HTTP로 FLUX 1.1 [pro] Ultra와 FLUX 1.1 [pro]를 호출하기 위한 Python, TypeScript, cURL 스니펫과 요청 필드 및 결과 형태"
sidebarTitle: "Flux 1.1 Pro Ultra 이미지"
translationSourceHash: 70ee8564
translationFrom: development/comfy-router/models/black-forest-labs/flux-1-1-pro-ultra-image/code.mdx
---

{/* GENERATED FILE. Edit code.yaml in this directory and run `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/ko/comfy-router/model-code-footer.mdx";

Flux 1.1 Pro Ultra 이미지 API 레퍼런스입니다. FLUX 1.1 [pro]는 Black Forest Labs의 텍스트 기반 이미지 생성 모델입니다. Ultra 모드는 최대 4MP 해상도로 이미지를 생성합니다.

## 빠른 시작

[Comfy 워크스페이스](https://platform.comfy.org/profile/api-keys)에서 키를 생성하고 `COMFY_API_KEY`로 내보내세요. Python 및 TypeScript 스니펫은 Comfy SDK(`pip install comfy-sdk`, `npm install @comfyorg/sdk`)를 사용하며, cURL 스니펫은 동일한 호출을 원시 HTTP로 수행합니다.

호출할 모델을 선택하세요. 스니펫부터 스키마와 예제까지 아래의 모든 내용이 선택에 따라 달라집니다.

<Tabs>
  <Tab title="FLUX 1.1 [pro] Ultra">
**Model ID:** `bfl/flux-pro-1.1-ultra`

**Endpoint:** `POST https://api.comfy.org/v2/models/bfl/flux-pro-1.1-ultra`

<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "bfl/flux-pro-1.1-ultra",
        {
            "prompt": "a single red maple leaf on a plain white background, studio lighting",
            "aspect_ratio": "16:9",
            "raw": False,
        },
    )

print("image:", result["result"]["sample"])
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
type Result = { result: { sample: string } };
const { data } = await comfy.models.run<Result>("bfl/flux-pro-1.1-ultra", {
  prompt: "a single red maple leaf on a plain white background, studio lighting",
  aspect_ratio: "16:9",
  raw: false,
});

console.log("image:", data.result.sample);
```

```bash cURL
curl https://api.comfy.org/v2/models/bfl/flux-pro-1.1-ultra \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"prompt\": \"a single red maple leaf on a plain white background, studio lighting\", \"aspect_ratio\": \"16:9\", \"raw\": false}"
```
</CodeGroup>

<h2>스키마</h2>

<h3>입력</h3>

<ParamField body="aspect_ratio" type="string" default="&quot;16:9&quot;">
  21:9에서 9:21 사이의 이미지 비율입니다. 예: 16:9.
</ParamField>

<ParamField body="image_prompt" type="string">
  리믹스할 선택적 base64 인코딩 이미지입니다.
</ParamField>

<ParamField body="image_prompt_strength" type="number" default="0.1">
  프롬프트와 이미지 프롬프트 사이의 블렌딩 비율로, 0(프롬프트만)에서 1(이미지 프롬프트만)까지입니다.

  범위: `0` ~ `1`
</ParamField>

<ParamField body="output_format" type="string" default="&quot;jpeg&quot;">
  출력 이미지 형식입니다.

  가능한 값: `jpeg`, `png`, `webp`
</ParamField>

<ParamField body="prompt" type="string" required>
  이미지 생성을 위한 텍스트 프롬프트입니다.
</ParamField>

<ParamField body="prompt_upsampling" type="boolean" default="false">
  프롬프트를 업샘플링할지 여부입니다. 활성화하면 더 창의적인 생성을 위해 프롬프트가 자동으로 수정됩니다.
</ParamField>

<ParamField body="raw" type="boolean" default="false">
  처리 과정이 적고 더 자연스러운 이미지를 생성합니다.
</ParamField>

<ParamField body="safety_tolerance" type="integer" default="2">
  입력 및 출력 검열에 대한 허용치 수준으로, 0(가장 엄격)에서 6(가장 완화)까지입니다.

  범위: `0` ~ `6`
</ParamField>

<ParamField body="seed" type="integer">
  재현성을 위한 선택적 시드입니다. 생략하면 무작위 시드가 사용됩니다.
</ParamField>

<ParamField body="webhook_secret" type="string">
  웹훅 서명 검증을 위한 선택적 시크릿입니다.
</ParamField>

<ParamField body="webhook_url" type="string (uri)">
  웹훅 알림을 수신할 URL입니다.

  형식: `uri`
</ParamField>

Router가 `GET /v2/models/bfl/flux-pro-1.1-ultra/openapi.json`에서 제공하는 스키마에서 생성되었으며, 이는 요청이 공급자에게 도달하기 전에 Router가 호출을 검증하는 데 사용하는 동일한 문서입니다.

<h3>출력</h3>

<ResponseField name="cost" type="number">
  크레딧 단위로 공급자가 보고한 비용이며, 작업이 Ready 상태가 되면 채워집니다.

  형식: `float`
</ResponseField>

<ResponseField name="id" type="string" required>
  BFL 작업 식별자입니다.
</ResponseField>

<ResponseField name="progress" type="number">
  BFL이 보고하는 선택적 생성 진행률입니다.

  범위: `0` ~ `1`

  형식: `float`
</ResponseField>

<ResponseField name="result" type="object" required>
  완료된 생성 결과입니다. 여기서는 nullable이 아닙니다. 이 컴포넌트의 `required` 항목은 `200` 응답이 결과를 포함한다는 약속이며, `result`가 nullable이라면 이는 단순히 키 존재 여부 확인으로 축소되기 때문입니다.
</ResponseField>

<ResponseField name="result.cost" type="number">
  공급자가 보고한 생성 비용입니다. 이는 BFL의 수치이며 Comfy 청구 금액이 아닙니다.

  형식: `double`
</ResponseField>

<ResponseField name="result.duration" type="number">
  공급자가 보고한 생성 소요 시간(초)입니다.

  형식: `double`
</ResponseField>

<ResponseField name="result.end_time" type="number">
  공급자가 보고한 생성 완료 시각으로, Unix 에포크 이후 초 단위입니다. `start_time`과 같은 이유로 `double`입니다.

  형식: `double`
</ResponseField>

<ResponseField name="result.prompt" type="string">
  프롬프트 업샘플링 이후 실제로 생성에 사용된 프롬프트입니다.
</ResponseField>

<ResponseField name="result.sample" type="string (uri)">
  생성된 에셋의 서명된 URL입니다. Router는 에셋을 Comfy 스토리지에 다시 호스팅하고 이 필드를 재작성하므로, 일반적으로 최대 24시간 동안 유효한 Comfy 호스팅 URL입니다. 발급 시점에 24시간으로 서명되고 23시간짜리 메모에서 재생되므로, 나중에 폴링하면 남은 시간이 1시간도 되지 않는 URL을 받을 수 있습니다. 재호스팅이 수행되지 못한 leaf는 대신 BFL 자체의 수명이 짧은 전송 URL을 유지합니다. 동영상은 약 2시간, 이미지는 약 10분입니다. 어느 쪽이든 링크는 만료되므로 URL을 저장하지 말고 에셋을 다운로드하세요.

  형식: `uri`
</ResponseField>

<ResponseField name="result.seed" type="integer">
  공급자가 제공했든 직접 지정했든, 생성에 사용된 시드입니다. BFL이 2^31을 초과하는 시드(예: 2784347701)를 반환하기 때문에 `int64`로 선언되었습니다. 형식이 지정되지 않은 `integer`는 많은 SDK 생성기에서 32비트 필드로 생성됩니다.

  형식: `int64`
</ResponseField>

<ResponseField name="result.start_time" type="number">
  공급자가 보고한 생성 시작 시각으로, Unix 에포크 이후 초 단위입니다. `float`가 아니라 `double`입니다. 현재 시점의 에포크 값 근처에서 float32의 간격은 약 128초이므로, 생성 전체 구간이 하나의 디코딩된 값으로 뭉개지기 때문입니다.

  형식: `double`
</ResponseField>

<ResponseField name="status" type="string" required>
  작업 상태: Pending, Reasoning, Generating, Ready, Request Moderated, Content Moderated, Error 또는 Task not found.
</ResponseField>

<h2>예제</h2>

<h3>입력</h3>

```json
{
  "prompt": "a single red maple leaf on a plain white background, studio lighting",
  "aspect_ratio": "16:9",
  "raw": false
}
```

<h3>출력</h3>

```json
{
  "id": "0a1b2c3d-...",
  "status": "Ready",
  "result": {
    "sample": "https://.../out.jpeg",
    "prompt": "a single red maple leaf on a plain white background, studio lighting",
    "seed": 1234567890
  }
}
```

URL은 임시입니다. 이미지를 보관해야 한다면 즉시 다운로드하세요.
  </Tab>
  <Tab title="FLUX 1.1 [pro]">
**Model ID:** `bfl/flux-pro-1.1`

**Endpoint:** `POST https://api.comfy.org/v2/models/bfl/flux-pro-1.1`

<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "bfl/flux-pro-1.1",
        {
            "prompt": "a single red maple leaf on a plain white background, studio lighting",
            "width": 1024,
            "height": 768,
        },
    )

print("image:", result["result"]["sample"])
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
type Result = { result: { sample: string } };
const { data } = await comfy.models.run<Result>("bfl/flux-pro-1.1", {
  prompt: "a single red maple leaf on a plain white background, studio lighting",
  width: 1024,
  height: 768,
});

console.log("image:", data.result.sample);
```

```bash cURL
curl https://api.comfy.org/v2/models/bfl/flux-pro-1.1 \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"prompt\": \"a single red maple leaf on a plain white background, studio lighting\", \"width\": 1024, \"height\": 768}"
```
</CodeGroup>

<h2>스키마</h2>

<h3>입력</h3>

<ParamField body="height" type="integer" default="768">
  생성된 이미지의 높이(픽셀)입니다. 32의 배수여야 합니다.

  범위: `256` ~ `1440`
</ParamField>

<ParamField body="image_prompt" type="string">
  FLUX Redux와 함께 사용할 선택적 base64 인코딩 이미지입니다.
</ParamField>

<ParamField body="output_format" type="string" default="&quot;jpeg&quot;">
  출력 이미지 형식입니다.

  가능한 값: `jpeg`, `png`, `webp`
</ParamField>

<ParamField body="prompt" type="string" required>
  이미지 생성을 위한 텍스트 프롬프트입니다.
</ParamField>

<ParamField body="prompt_upsampling" type="boolean" default="false">
  프롬프트를 업샘플링할지 여부입니다. 활성화하면 더 창의적인 생성을 위해 프롬프트가 자동으로 수정됩니다.
</ParamField>

<ParamField body="safety_tolerance" type="integer" default="2">
  입력 및 출력 검열에 대한 허용치 수준으로, 0(가장 엄격)에서 6(가장 완화)까지입니다.

  범위: `0` ~ `6`
</ParamField>

<ParamField body="seed" type="integer">
  재현성을 위한 선택적 시드입니다. 생략하면 무작위 시드가 사용됩니다.
</ParamField>

<ParamField body="webhook_secret" type="string">
  웹훅 서명 검증을 위한 선택적 시크릿입니다.
</ParamField>

<ParamField body="webhook_url" type="string (uri)">
  웹훅 알림을 수신할 URL입니다.

  형식: `uri`
</ParamField>

<ParamField body="width" type="integer" default="1024">
  생성된 이미지의 너비(픽셀)입니다. 32의 배수여야 합니다.

  범위: `256` ~ `1440`
</ParamField>

Router가 `GET /v2/models/bfl/flux-pro-1.1/openapi.json`에서 제공하는 스키마에서 생성되었으며, 이는 요청이 공급자에게 도달하기 전에 Router가 호출을 검증하는 데 사용하는 동일한 문서입니다.

<h3>출력</h3>

<ResponseField name="cost" type="number">
  크레딧 단위로 공급자가 보고한 비용이며, 작업이 Ready 상태가 되면 채워집니다.

  형식: `float`
</ResponseField>

<ResponseField name="id" type="string" required>
  BFL 작업 식별자입니다.
</ResponseField>

<ResponseField name="progress" type="number">
  BFL이 보고하는 선택적 생성 진행률입니다.

  범위: `0` ~ `1`

  형식: `float`
</ResponseField>

<ResponseField name="result" type="object" required>
  완료된 생성 결과입니다. 여기서는 nullable이 아닙니다. 이 컴포넌트의 `required` 항목은 `200` 응답이 결과를 포함한다는 약속이며, `result`가 nullable이라면 이는 단순히 키 존재 여부 확인으로 축소되기 때문입니다.
</ResponseField>

<ResponseField name="result.cost" type="number">
  공급자가 보고한 생성 비용입니다. 이는 BFL의 수치이며 Comfy 청구 금액이 아닙니다.

  형식: `double`
</ResponseField>

<ResponseField name="result.duration" type="number">
  공급자가 보고한 생성 소요 시간(초)입니다.

  형식: `double`
</ResponseField>

<ResponseField name="result.end_time" type="number">
  공급자가 보고한 생성 완료 시각으로, Unix 에포크 이후 초 단위입니다. `start_time`과 같은 이유로 `double`입니다.

  형식: `double`
</ResponseField>

<ResponseField name="result.prompt" type="string">
  프롬프트 업샘플링 이후 실제로 생성에 사용된 프롬프트입니다.
</ResponseField>

<ResponseField name="result.sample" type="string (uri)">
  생성된 에셋의 서명된 URL입니다. Router는 에셋을 Comfy 스토리지에 다시 호스팅하고 이 필드를 재작성하므로, 일반적으로 최대 24시간 동안 유효한 Comfy 호스팅 URL입니다. 발급 시점에 24시간으로 서명되고 23시간짜리 메모에서 재생되므로, 나중에 폴링하면 남은 시간이 1시간도 되지 않는 URL을 받을 수 있습니다. 재호스팅이 수행되지 못한 leaf는 대신 BFL 자체의 수명이 짧은 전송 URL을 유지합니다. 동영상은 약 2시간, 이미지는 약 10분입니다. 어느 쪽이든 링크는 만료되므로 URL을 저장하지 말고 에셋을 다운로드하세요.

  형식: `uri`
</ResponseField>

<ResponseField name="result.seed" type="integer">
  공급자가 제공했든 직접 지정했든, 생성에 사용된 시드입니다. BFL이 2^31을 초과하는 시드(예: 2784347701)를 반환하기 때문에 `int64`로 선언되었습니다. 형식이 지정되지 않은 `integer`는 많은 SDK 생성기에서 32비트 필드로 생성됩니다.

  형식: `int64`
</ResponseField>

<ResponseField name="result.start_time" type="number">
  공급자가 보고한 생성 시작 시각으로, Unix 에포크 이후 초 단위입니다. `float`가 아니라 `double`입니다. 현재 시점의 에포크 값 근처에서 float32의 간격은 약 128초이므로, 생성 전체 구간이 하나의 디코딩된 값으로 뭉개지기 때문입니다.

  형식: `double`
</ResponseField>

<ResponseField name="status" type="string" required>
  작업 상태: Pending, Reasoning, Generating, Ready, Request Moderated, Content Moderated, Error 또는 Task not found.
</ResponseField>

<h2>예제</h2>

<h3>입력</h3>

```json
{
  "prompt": "a single red maple leaf on a plain white background, studio lighting",
  "width": 1024,
  "height": 768
}
```

<h3>출력</h3>

```json
{
  "id": "0a1b2c3d-...",
  "status": "Ready",
  "result": {
    "sample": "https://.../out.jpeg",
    "prompt": "a single red maple leaf on a plain white background, studio lighting",
    "seed": 1234567890
  }
}
```

URL은 임시입니다. 이미지를 보관해야 한다면 즉시 다운로드하세요.
  </Tab>
</Tabs>

<RouterCodeFooter />