---
title: "Comfy Router로 Video Edit V1 사용하기"
description: "Comfy Router를 통해 bfl/video-edit-v1을 호출합니다. 엔드포인트, 요청 형태, 그리고 Router가 반환하는 응답을 설명합니다."
sidebarTitle: "Video Edit V1"
translationSourceHash: 708e47c9
translationFrom: development/comfy-router/models/black-forest-labs/video-edit-v1/code.mdx
translationBlockHashes:
  "_intro": e7443ea9
  "Quick start": 5747cb6b
  "Schema": e992b26e
  "Examples": 9b07c268
---
{/* GENERATED FILE. Generated from router-schemas/bfl/video-edit-v1.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/ko/comfy-router/model-code-footer.mdx";

Black Forest Labs의 `bfl/video-edit-v1`에 대한 API 레퍼런스이며, Comfy Router에서 제공합니다.

## 빠른 시작

[Comfy 워크스페이스](https://platform.comfy.org/profile/api-keys)에서 키를 생성하고 `COMFY_API_KEY`로 내보내세요. Python 및 TypeScript 스니펫은 Comfy SDK(`pip install comfy-sdk`, `npm install @comfyorg/sdk`)를 사용하며, cURL 스니펫은 동일한 호출을 원시 HTTP로 수행합니다.

**모델 ID:** `bfl/video-edit-v1`

**엔드포인트:** `POST https://api.comfy.org/v2/models/bfl/video-edit-v1`

<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "bfl/video-edit-v1",
        {
            "prompt": "Remove the orange bucket.",
            "video": "https://example.com/clip.mp4",
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const { data } = await comfy.models.run("bfl/video-edit-v1", {
  prompt: "Remove the orange bucket.",
  video: "https://example.com/clip.mp4",
});

console.log(data);
```

```bash cURL
curl https://api.comfy.org/v2/models/bfl/video-edit-v1 \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"prompt\": \"Remove the orange bucket.\", \"video\": \"https://example.com/clip.mp4\"}"
```
</CodeGroup>

## 스키마

### 입력

<ParamField body="prompt" type="string" required>
  편집 지시문입니다. 변경할 객체, 배경, 스타일, 화면 텍스트 또는 대사를 지정하며, 지정되지 않은 모든 것은 보존됩니다.
</ParamField>

<ParamField body="safety_tolerance" type="integer" default="2">
  입력 및 출력 검열에 대한 허용치 수준이며, 0이 가장 엄격합니다.

  범위: `0` ~ `4`
</ParamField>

<ParamField body="video" type="string" required>
  편집할 비디오로, HTTP(S) URL 또는 base64로 인코딩된 MP4입니다. 최대 15초 길이의 소스 영상과 50 MiB까지 지원하며, 각 변은 최소 160픽셀이어야 합니다. 720p를 초과하는 소스는 720p로 다운스케일됩니다.
</ParamField>

Router가 `GET /v2/models/bfl/video-edit-v1/openapi.json`에서 제공하는 스키마에서 생성되었으며, 이는 요청이 공급자에게 도달하기 전에 호출을 검증하는 데 사용하는 것과 동일한 문서입니다.

### 출력

<ResponseField name="cost" type="number">
  공급자가 보고한 크레딧 단위 비용으로, 태스크가 Ready 상태가 되면 채워집니다.

  형식: `float`
</ResponseField>

<ResponseField name="id" type="string" required>
  BFL 태스크 식별자입니다.
</ResponseField>

<ResponseField name="progress" type="number">
  BFL이 보고하는 선택적 생성 진행률입니다.

  범위: `0` ~ `1`

  형식: `float`
</ResponseField>

<ResponseField name="result" type="object" required>
  완료된 생성 결과입니다. 여기서는 null을 허용하지 않습니다. 이 컴포넌트의 `required` 항목은 `200` 응답이 결과를 포함한다는 약속이며, `result`가 null을 허용하면 이는 키 존재 여부 확인으로 전락합니다.
</ResponseField>

<ResponseField name="result.cost" type="number">
  공급자가 보고한 생성 비용입니다. 이는 BFL의 수치이며 Comfy 청구 금액이 아닙니다.

  형식: `double`
</ResponseField>

<ResponseField name="result.duration" type="number">
  공급자가 보고한 생성 소요 시간(초)입니다.

  형식: `double`
</ResponseField>

<ResponseField name="result.end_time" type="number">
  공급자가 보고한 생성 완료 시간으로, Unix 에포크 이후 초 단위입니다. `start_time`과 같은 이유로 `double`입니다.

  형식: `double`
</ResponseField>

<ResponseField name="result.prompt" type="string">
  프롬프트 업샘플링 이후 실제로 생성에 사용된 프롬프트입니다.
</ResponseField>

<ResponseField name="result.sample" type="string (uri)">
  생성된 에셋에 대한 서명된 URL입니다. Router는 에셋을 Comfy 스토리지에 재호스팅하고 이 필드를 다시 작성하므로, 일반적으로 최대 24시간 동안 유효한 Comfy 호스팅 URL입니다. 발급 시 24시간 동안 서명되며 23시간 메모에서 재생되므로, 나중에 폴링하면 남은 시간이 1시간도 되지 않는 URL을 돌려받을 수 있습니다. 재호스팅을 수행할 수 없는 leaf는 대신 BFL 자체의 수명이 짧은 전달 URL을 유지합니다. 비디오의 경우 약 2시간, 이미지의 경우 약 10분입니다. 어느 경우든 링크는 만료되므로 URL을 저장하지 말고 에셋을 다운로드하세요.

  형식: `uri`
</ResponseField>

<ResponseField name="result.seed" type="integer">
  사용자가 제공했든 공급자가 선택했든, 생성에 사용된 시드입니다. BFL은 2^31을 초과하는 시드(예: 2784347701)를 반환하는데, 형식이 지정되지 않은 `integer`는 많은 SDK 생성기에서 이를 32비트 필드로 생성하므로 `int64`로 선언되었습니다.

  형식: `int64`
</ResponseField>

<ResponseField name="result.start_time" type="number">
  공급자가 보고한 생성 시작 시간으로, Unix 에포크 이후 초 단위입니다. `float`가 아닌 `double`입니다. 현재의 에포크 값 근처에서 float32의 간격은 약 128초이므로, 전체 생성 구간이 단일 디코딩 값으로 축소됩니다.

  형식: `double`
</ResponseField>

<ResponseField name="status" type="string" required>
  태스크 상태: Pending, Reasoning, Generating, Ready, Request Moderated, Content Moderated, Error, Task not found 중 하나입니다.
</ResponseField>

## 예시

### 입력

```json
{
  "prompt": "Remove the orange bucket.",
  "video": "https://example.com/clip.mp4"
}
```

### 출력

```json
{
  "cost": null,
  "id": "b2e0c1a4-0f2f-4a55-9f2e-2f9a1c0d4e77",
  "progress": null,
  "result": {
    "cost": null,
    "duration": 3.4,
    "end_time": 1767225603.4,
    "prompt": "A watercolor painting of a lighthouse at dawn, soft light on the water",
    "sample": "https://example.invalid/bfl/flux-pro-1.1/sample.png",
    "seed": 2784347701,
    "start_time": 1767225600
  },
  "status": "Ready"
}
```

<RouterCodeFooter />
