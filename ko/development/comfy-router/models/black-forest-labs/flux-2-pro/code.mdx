---
title: "Comfy Router로 FLUX 2 Pro 사용하기"
description: "Comfy Router를 통해 bfl/flux-2-pro를 호출하는 방법을 설명합니다: 엔드포인트, 요청 형태, 그리고 Router가 반환하는 응답."
sidebarTitle: "FLUX 2 Pro"
translationSourceHash: fb5fb051
translationFrom: development/comfy-router/models/black-forest-labs/flux-2-pro/code.mdx
translationBlockHashes:
  "_intro": 7b39815b
  "Quick start": 6fde3990
  "Schema": 942b3b42
  "Examples": 07dfd8d0
---
{/* GENERATED FILE. Generated from router-schemas/bfl/flux-2-pro.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/ko/comfy-router/model-code-footer.mdx";

Black Forest Labs에서 제공하고 Comfy Router가 서비스하는 `bfl/flux-2-pro`에 대한 API 레퍼런스입니다.

## 빠른 시작

[Comfy 워크스페이스](https://platform.comfy.org/profile/api-keys)에서 키를 생성하고 `COMFY_API_KEY`로 내보내세요. Python 및 TypeScript 스니펫은 Comfy SDK(`pip install comfy-sdk`, `npm install @comfyorg/sdk`)를 사용하며, cURL 스니펫은 원시 HTTP를 통한 동일한 호출입니다.

**모델 ID:** `bfl/flux-2-pro`

**엔드포인트:** `POST https://api.comfy.org/v2/models/bfl/flux-2-pro`

<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "bfl/flux-2-pro",
        {
            "prompt": "A single red maple leaf on a plain white background.",
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const { data } = await comfy.models.run("bfl/flux-2-pro", {
  prompt: "A single red maple leaf on a plain white background.",
});

console.log(data);
```

```bash cURL
curl https://api.comfy.org/v2/models/bfl/flux-2-pro \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"prompt\": \"A single red maple leaf on a plain white background.\"}"
```
</CodeGroup>

## 스키마

### 입력

<ParamField body="height" type="integer" default="1024">
  이미지의 높이.

  범위: `256` ~ `2048`
</ParamField>

<ParamField body="input_image" type="string">
  이미지 기반 이미지 생성을 위한 Base64 인코딩 이미지.
</ParamField>

<ParamField body="input_image_2" type="string">
  이미지 기반 이미지 생성을 위한 Base64 인코딩 이미지.
</ParamField>

<ParamField body="input_image_3" type="string">
  이미지 기반 이미지 생성을 위한 Base64 인코딩 이미지.
</ParamField>

<ParamField body="input_image_4" type="string">
  이미지 기반 이미지 생성을 위한 Base64 인코딩 이미지.
</ParamField>

<ParamField body="input_image_5" type="string">
  이미지 기반 이미지 생성을 위한 Base64 인코딩 이미지.
</ParamField>

<ParamField body="input_image_6" type="string">
  이미지 기반 이미지 생성을 위한 Base64 인코딩 이미지.
</ParamField>

<ParamField body="input_image_7" type="string">
  이미지 기반 이미지 생성을 위한 Base64 인코딩 이미지.
</ParamField>

<ParamField body="input_image_8" type="string">
  이미지 기반 이미지 생성을 위한 Base64 인코딩 이미지.
</ParamField>

<ParamField body="input_image_9" type="string">
  이미지 기반 이미지 생성을 위한 Base64 인코딩 이미지.
</ParamField>

<ParamField body="output_format" type="string" default="&quot;jpeg&quot;">
  생성된 이미지의 출력 형식.

  가능한 값: `jpeg`, `png`
</ParamField>

<ParamField body="prompt" type="string" required>
  생성할 이미지에 대한 텍스트 설명.
</ParamField>

<ParamField body="prompt_upsampling" type="boolean" default="true">
  생성을 위해 프롬프트를 자동으로 수정합니다.
</ParamField>

<ParamField body="safety_tolerance" type="integer" default="2">
  검열 허용치 수준 (Flux 2 Max 전용).

  범위: `0` ~ `5`
</ParamField>

<ParamField body="seed" type="integer">
  재현성을 위한 시드.
</ParamField>

<ParamField body="width" type="integer" default="1024">
  이미지의 너비.

  범위: `256` ~ `2048`
</ParamField>

Router가 `GET /v2/models/bfl/flux-2-pro/openapi.json`에서 제공하는 스키마에서 생성되었으며, 이는 요청이 공급자에 도달하기 이전에 호출을 검증하는 데 사용하는 것과 동일한 문서입니다.

### 출력

<ResponseField name="cost" type="number">
  공급자가 보고한 크레딧 단위 비용으로, 작업이 Ready 상태가 되면 채워집니다.

  형식: `float`
</ResponseField>

<ResponseField name="id" type="string" required>
  BFL 작업 식별자.
</ResponseField>

<ResponseField name="progress" type="number">
  BFL이 보고하는 선택적 생성 진행률.

  범위: `0` ~ `1`

  형식: `float`
</ResponseField>

<ResponseField name="result" type="object" required>
  완료된 생성 결과입니다. 여기서는 nullable이 아닙니다. 이 컴포넌트의 `required` 항목은 `200`이 결과를 포함한다는 약속이며, nullable한 `result`는 이를 키 존재 여부 검사로 격하시키기 때문입니다.
</ResponseField>

<ResponseField name="result.cost" type="number">
  공급자가 보고한 생성 비용입니다. 이는 BFL의 수치이며 Comfy 청구 금액이 아닙니다.

  형식: `double`
</ResponseField>

<ResponseField name="result.duration" type="number">
  공급자가 보고한 생성 재생 시간(초)입니다.

  형식: `double`
</ResponseField>

<ResponseField name="result.end_time" type="number">
  공급자가 보고한 생성 완료 시간으로, Unix 에포크 이후 초 단위입니다. `start_time`과 같은 이유로 `double`입니다.

  형식: `double`
</ResponseField>

<ResponseField name="result.prompt" type="string">
  프롬프트 업샘플링 이후, 생성이 실제로 실행한 프롬프트입니다.
</ResponseField>

<ResponseField name="result.sample" type="string (uri)">
  생성된 애셋에 대한 서명된 URL입니다. Router는 애셋을 Comfy 스토리지에 다시 호스팅하고 이 필드를 재작성하므로, 일반적으로 최대 24시간 동안 유효한 Comfy 호스팅 URL입니다. 발급 시 24시간으로 서명되고 23시간 메모에서 재생되므로, 나중에 폴링하면 남은 시간이 1시간도 되지 않을 수 있습니다. 재호스팅을 수행할 수 없는 리프는 대신 BFL 자체의 단기 전송 URL을 유지합니다. 비디오는 약 2시간, 이미지는 약 10분입니다. 어느 쪽이든 링크는 만료되므로 URL을 저장하기보다 애셋을 다운로드하세요.

  형식: `uri`
</ResponseField>

<ResponseField name="result.seed" type="integer">
  제공되었든 공급자가 선택했든, 생성에 사용된 시드입니다. BFL이 2^31을 초과하는 시드(예: 2784347701)를 반환하는데, 서식이 지정되지 않은 `integer`는 많은 SDK 생성기에서 32비트 필드로 생성되기 때문에 `int64`로 선언되었습니다.

  형식: `int64`
</ResponseField>

<ResponseField name="result.start_time" type="number">
  공급자가 보고한 생성 시작 시간으로, Unix 에포크 이후 초 단위입니다. `float`이 아닌 `double`입니다. 현재 시점의 에포크 값 근처에서 float32의 간격은 약 128초로, 생성 전체의 범위를 단일 디코딩 값으로 붕괴시키기 때문입니다.

  형식: `double`
</ResponseField>

<ResponseField name="status" type="string" required>
  작업 상태: Pending, Reasoning, Generating, Ready, Request Moderated, Content Moderated, Error 또는 Task not found.
</ResponseField>

## 예시

### 입력

```json
{
  "prompt": "A single red maple leaf on a plain white background."
}
```

### 출력

```json
{
  "cost": null,
  "id": "b2e0c1a4-0f2f-4a55-9f2e-2f9a1c0d4e77",
  "progress": null,
  "result": {
    "cost": null,
    "duration": 3.4,
    "end_time": 1767225603.4,
    "prompt": "A watercolor painting of a lighthouse at dawn, soft light on the water",
    "sample": "https://example.invalid/bfl/flux-pro-1.1/sample.png",
    "seed": 2784347701,
    "start_time": 1767225600
  },
  "status": "Ready"
}
```


<RouterCodeFooter />
