---
title: "Comfy Router로 FLUX 2 Max 사용하기"
description: "Comfy Router를 통해 bfl/flux-2-max를 호출합니다: 엔드포인트, 요청 형태, 그리고 Router가 반환하는 응답."
sidebarTitle: "FLUX 2 Max"
translationSourceHash: 4a461470
translationFrom: development/comfy-router/models/black-forest-labs/flux-2-max/code.mdx
translationBlockHashes:
  "_intro": b78e1772
  "Quick start": fdcfd144
  "Schema": 8da5f7ce
  "Examples": 07dfd8d0
---


{/* GENERATED FILE. Generated from router-schemas/bfl/flux-2-max.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/ko/comfy-router/model-code-footer.mdx";

Black Forest Labs에서 제공하며 Comfy Router가 서비스하는 `bfl/flux-2-max`의 API 레퍼런스입니다.

## 빠른 시작

[Comfy 워크스페이스](https://platform.comfy.org/profile/api-keys?onboarding=router)에서 키를 생성하고 `COMFY_API_KEY`로 내보내세요. Python과 TypeScript 스니펫은 Comfy SDK(`pip install comfy-sdk` 및 `npm install @comfyorg/sdk`)를 사용하며, cURL 스니펫은 동일한 호출을 원시 HTTP로 수행합니다.

**모델 ID:** `bfl/flux-2-max`

**엔드포인트:** `POST https://api.comfy.org/v2/models/bfl/flux-2-max`

<Tabs defaultTabIndex={1}>
  <Tab title="결과를 기다리기">
<CodeGroup>
```python Python
from comfy_sdk import Comfy

# 환경에서 COMFY_API_KEY를 읽습니다.
# SDK는 자동으로 멱등성 키를 생성하고 자동 재시도에 재사용합니다.
with Comfy() as client:
    result = client.models.run(
        "bfl/flux-2-max",
        {
            "prompt": "A single red maple leaf on a plain white background.",
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// 환경에서 COMFY_API_KEY를 읽습니다.
// SDK는 자동으로 멱등성 키를 생성하고 자동 재시도에 재사용합니다.
const { data } = await comfy.models.run("bfl/flux-2-max", {
  prompt: "A single red maple leaf on a plain white background.",
});

console.log(data);
```

```bash cURL
curl https://api.comfy.org/v2/models/bfl/flux-2-max \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"prompt\": \"A single red maple leaf on a plain white background.\"}"
```
</CodeGroup>
  </Tab>
  <Tab title="대기열에 넣고 나중에 수집">
동일한 본문을 `POST https://api.comfy.org/v2/models/bfl/flux-2-max/requests`로 보냅니다. Router는 실행이 접수되는 즉시 `201`과 `request_id`로 응답하며, 결과는 준비가 되면 이 프로세스든 다른 프로세스든 수집할 수 있습니다. 상태, 취소, 수집은 [큐 전송](/ko/development/comfy-router/queue)에서 자세히 다룹니다.

<CodeGroup>
```python Python
import asyncio
from comfy_sdk import AsyncComfy

# 환경에서 COMFY_API_KEY를 읽습니다.
# 각 submit() 호출은 자체 Idempotency-Key를 발급하고 자동 재시도에 재사용합니다.
async def main():
    async with AsyncComfy() as client:
        handle = await client.models.submit(
            "bfl/flux-2-max",
            {
                "prompt": "A single red maple leaf on a plain white background.",
            },
        )
        print("request_id:", handle.request_id)  # 모델 ID와 함께라면 다른 프로세스에 필요한 전부입니다

        # 서버가 지정한 Retry-After를 기다리며 요청이 완료될 때까지 폴링합니다.
        async for update in handle.iter_events():
            print(update.status, update.queue_position)

        # 공급자의 자체 페이로드이며, models.run()이 반환하는 것과 동일한 값입니다.
        # 실패하거나 취소된 요청은 여기에서 타입이 지정된 Router 오류를 발생시킵니다.
        result = await handle.get()

    print(result)

asyncio.run(main())
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// 환경에서 COMFY_API_KEY를 읽습니다.
// 각 submit() 호출은 자체 Idempotency-Key를 발급하고 자동 재시도에 재사용합니다.
const handle = await comfy.models.submit("bfl/flux-2-max", {
  prompt: "A single red maple leaf on a plain white background.",
});
console.log("requestId:", handle.requestId); // 모델 ID와 함께라면 다른 프로세스에 필요한 전부입니다

// 서버가 지정한 Retry-After를 기다리며 요청이 완료될 때까지 폴링합니다.
for await (const update of handle.events()) {
  console.log(update.status, update.queuePosition);
}

// models.run()이 반환하는 것과 동일한 결과입니다. 실패하거나 취소된 요청은 여기에서 거부됩니다.
const result = await handle.get();

console.log(result.data);
```

```bash cURL
# 1. 제출. Router는 request_id, status_url, response_url 및 cancel_url과 함께 201을 응답합니다.
curl https://api.comfy.org/v2/models/bfl/flux-2-max/requests \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"prompt\": \"A single red maple leaf on a plain white background.\"}"

# 2. 각 응답이 지정한 Retry-After 초를 기다리며 상태가 COMPLETED가 될 때까지 폴링합니다.
REQUEST_ID="<request_id from the 201 body>"
curl -i https://api.comfy.org/v2/models/bfl/flux-2-max/requests/$REQUEST_ID/status \
  -H "X-API-Key: $COMFY_API_KEY"

# 3. 수집. 모델의 네이티브 출력과 함께 200, 아직 실행 중이면 상태 본문과 함께 202를 반환합니다.
curl https://api.comfy.org/v2/models/bfl/flux-2-max/requests/$REQUEST_ID \
  -H "X-API-Key: $COMFY_API_KEY"
```
</CodeGroup>
  </Tab>
</Tabs>

## 스키마

### 입력

<ParamField body="height" type="integer" default="1024">
  이미지의 높이.

  범위: `256` ~ `2048`
</ParamField>

<ParamField body="input_image" type="string">
  이미지 기반 이미지 생성을 위한 Base64 인코딩 이미지.
</ParamField>

<ParamField body="input_image_2" type="string">
  이미지 기반 이미지 생성을 위한 Base64 인코딩 이미지.
</ParamField>

<ParamField body="input_image_3" type="string">
  이미지 기반 이미지 생성을 위한 Base64 인코딩 이미지.
</ParamField>

<ParamField body="input_image_4" type="string">
  이미지 기반 이미지 생성을 위한 Base64 인코딩 이미지.
</ParamField>

<ParamField body="input_image_5" type="string">
  이미지 기반 이미지 생성을 위한 Base64 인코딩 이미지.
</ParamField>

<ParamField body="input_image_6" type="string">
  이미지 기반 이미지 생성을 위한 Base64 인코딩 이미지.
</ParamField>

<ParamField body="input_image_7" type="string">
  이미지 기반 이미지 생성을 위한 Base64 인코딩 이미지.
</ParamField>

<ParamField body="input_image_8" type="string">
  이미지 기반 이미지 생성을 위한 Base64 인코딩 이미지.
</ParamField>

<ParamField body="input_image_9" type="string">
  이미지 기반 이미지 생성을 위한 Base64 인코딩 이미지.
</ParamField>

<ParamField body="output_format" type="string" default="&quot;jpeg&quot;">
  생성된 이미지의 출력 형식.

  가능한 값: `jpeg`, `png`
</ParamField>

<ParamField body="prompt" type="string" required>
  생성할 이미지에 대한 텍스트 설명.
</ParamField>

<ParamField body="prompt_upsampling" type="boolean" default="true">
  생성을 위해 프롬프트를 자동으로 수정합니다.
</ParamField>

<ParamField body="safety_tolerance" type="integer" default="2">
  검열 허용치 수준 (Flux 2 Max 전용).

  범위: `0` ~ `5`
</ParamField>

<ParamField body="seed" type="integer">
  재현성을 위한 시드.
</ParamField>

<ParamField body="width" type="integer" default="1024">
  이미지의 너비.

  범위: `256` ~ `2048`
</ParamField>

Router가 `GET /v2/models/bfl/flux-2-max/openapi.json`에서 제공하는 스키마에서 생성되었으며, 이는 요청이 공급자에게 도달하기 전에 호출을 검증하는 기준이 되는 것과 동일한 문서입니다.

### 출력

<ResponseField name="cost" type="number">
  공급자가 보고한 크레딧 단위 비용으로, 작업이 Ready 상태가 되면 채워집니다.

  형식: `float`
</ResponseField>

<ResponseField name="id" type="string" required>
  BFL 작업 식별자.
</ResponseField>

<ResponseField name="progress" type="number">
  BFL이 보고하는 선택적 생성 진행률.

  범위: `0` ~ `1`

  형식: `float`
</ResponseField>

<ResponseField name="result" type="object" required>
  완료된 생성 결과입니다. 여기서는 null이 될 수 없습니다. 이 컴포넌트의 `required` 항목은 `200` 응답이 결과를 포함한다는 약속이며, `result`가 null을 허용하면 이는 키 존재 여부 확인으로 전락하게 됩니다.
</ResponseField>

<ResponseField name="result.cost" type="number">
  공급자가 보고한 생성 비용입니다. 이는 BFL의 수치이며 Comfy 청구 금액이 아닙니다.

  형식: `double`
</ResponseField>

<ResponseField name="result.duration" type="number">
  공급자가 보고한 생성 소요 시간(초)입니다.

  형식: `double`
</ResponseField>

<ResponseField name="result.end_time" type="number">
  공급자가 보고한 생성 완료 시간으로, Unix 에포크 이후 경과한 초 단위입니다. `start_time`과 같은 이유로 `double`입니다.

  형식: `double`
</ResponseField>

<ResponseField name="result.prompt" type="string">
  프롬프트 업샘플링을 거친 후 실제로 생성에 사용된 프롬프트입니다.
</ResponseField>

<ResponseField name="result.sample" type="string (uri)">
  생성된 에셋에 대한 서명된 URL입니다. Router는 에셋을 Comfy 스토리지에 다시 호스팅하고 이 필드를 재작성하므로, 일반적으로 최대 24시간 동안 유효한 Comfy 호스팅 URL입니다. 발급 시 24시간으로 서명되고 23시간짜리 메모에서 재생되므로, 나중에 폴링하면 남은 시간이 1시간도 되지 않는 URL을 받을 수 있습니다. 재호스팅이 수행되지 못한 리프는 대신 BFL 자체의 수명이 짧은 전송 URL을 유지합니다(비디오는 대략 2시간, 이미지는 대략 10분). 어느 쪽이든 링크는 만료되므로 URL을 저장하지 말고 에셋을 다운로드하세요.

  형식: `uri`
</ResponseField>

<ResponseField name="result.seed" type="integer">
  제공되었든 공급자가 선택했든, 생성에 사용된 시드입니다. BFL이 2^31을 초과하는 시드(예: 2784347701)를 반환하기 때문에 `int64`로 선언되었습니다. 형식이 지정되지 않은 `integer`는 많은 SDK 생성기에서 32비트 필드로 생성됩니다.

  형식: `int64`
</ResponseField>

<ResponseField name="result.start_time" type="number">
  공급자가 보고한 생성 시작 시간으로, Unix 에포크 이후 경과한 초 단위입니다. `float`이 아닌 `double`입니다. 현재 epoch 값 근처에서 float32의 간격은 약 128초이므로, 생성 전체의 시간 범위가 단일 디코딩 값으로 뭉개집니다.

  형식: `double`
</ResponseField>

<ResponseField name="status" type="string" required>
  작업 상태: Pending, Reasoning, Generating, Ready, Request Moderated, Content Moderated, Error 또는 Task not found.
</ResponseField>

## 예시

### 입력

```json
{
  "prompt": "A single red maple leaf on a plain white background."
}
```

### 출력

```json
{
  "cost": null,
  "id": "b2e0c1a4-0f2f-4a55-9f2e-2f9a1c0d4e77",
  "progress": null,
  "result": {
    "cost": null,
    "duration": 3.4,
    "end_time": 1767225603.4,
    "prompt": "A watercolor painting of a lighthouse at dawn, soft light on the water",
    "sample": "https://example.invalid/bfl/flux-pro-1.1/sample.png",
    "seed": 2784347701,
    "start_time": 1767225600
  },
  "status": "Ready"
}
```

<RouterCodeFooter />
