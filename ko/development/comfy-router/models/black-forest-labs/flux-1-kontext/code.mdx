---
title: "Comfy Router로 FLUX.1 Kontext 사용하기"
description: "Comfy Router를 통해 HTTP로 FLUX.1 Kontext Pro와 Kontext Max를 호출하기 위한 Python, TypeScript, cURL 스니펫과 요청 필드 및 결과 형태"
sidebarTitle: "FLUX.1 Kontext"
translationSourceHash: 6016f7f5
translationFrom: development/comfy-router/models/black-forest-labs/flux-1-kontext/code.mdx
---

{/* GENERATED FILE. Edit code.yaml in this directory and run `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/ko/comfy-router/model-code-footer.mdx";

FLUX.1 Kontext API 레퍼런스입니다. FLUX.1 Kontext는 Black Forest Labs의 명령 기반 이미지 편집 모델입니다. 이미지와 텍스트 명령을 보내면 나머지 장면은 그대로 유지한 채 편집된 이미지를 돌려받습니다.

## 빠른 시작

[Comfy 워크스페이스](https://platform.comfy.org/profile/api-keys)에서 키를 생성하고 `COMFY_API_KEY`로 내보내세요. Python 및 TypeScript 스니펫은 Comfy SDK(`pip install comfy-sdk`, `npm install @comfyorg/sdk`)를 사용하며, cURL 스니펫은 동일한 호출을 원시 HTTP로 수행한 것입니다.

호출할 모델을 선택하세요. 아래의 모든 내용은 스니펫부터 스키마, 예제까지 선택에 따라 달라집니다.

<Tabs>
  <Tab title="Kontext Pro">
**Model ID:** `bfl/flux-kontext-pro`

**Endpoint:** `POST https://api.comfy.org/v2/models/bfl/flux-kontext-pro`

<CodeGroup>
```python Python
import base64

from comfy_sdk import Comfy

with open("input.jpg", "rb") as f:
    input_image = base64.b64encode(f.read()).decode()

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "bfl/flux-kontext-pro",
        {
            "prompt": "replace the background with a sunlit beach, keep the subject unchanged",
            "input_image": input_image,
            "aspect_ratio": "1:1",
        },
    )

print("image:", result["result"]["sample"])
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";
import { readFile } from "node:fs/promises";

const inputImage = (await readFile("input.jpg")).toString("base64");

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
type Result = { result: { sample: string } };
const { data } = await comfy.models.run<Result>("bfl/flux-kontext-pro", {
  prompt: "replace the background with a sunlit beach, keep the subject unchanged",
  input_image: inputImage,
  aspect_ratio: "1:1",
});

console.log("image:", data.result.sample);
```

```bash cURL
INPUT_IMAGE=$(base64 < input.jpg | tr -d '\n')

curl https://api.comfy.org/v2/models/bfl/flux-kontext-pro \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"prompt\": \"replace the background with a sunlit beach, keep the subject unchanged\", \"input_image\": \"$INPUT_IMAGE\", \"aspect_ratio\": \"1:1\"}"
```
</CodeGroup>

<h2>Schema</h2>

<h3>입력</h3>

<ParamField body="aspect_ratio" type="string">
  21:9에서 9:21 사이의 출력 비율(예: 16:9)입니다. 입력 이미지가 주어지면 해당 이미지의 비율을 기본값으로 사용하고, 그렇지 않으면 1:1입니다.
</ParamField>

<ParamField body="input_image" type="string">
  편집할 이미지로, base64로 인코딩된 이미지 또는 http(s) URL입니다. 선택 사항이며, 없으면 모델은 프롬프트만으로 생성합니다.
</ParamField>

<ParamField body="input_image_2" type="string">
  추가 참조 이미지로, base64로 인코딩되었거나 http(s) URL입니다(실험적 다중 참조).
</ParamField>

<ParamField body="input_image_3" type="string">
  추가 참조 이미지로, base64로 인코딩되었거나 http(s) URL입니다(실험적 다중 참조).
</ParamField>

<ParamField body="input_image_4" type="string">
  추가 참조 이미지로, base64로 인코딩되었거나 http(s) URL입니다(실험적 다중 참조).
</ParamField>

<ParamField body="output_format" type="string" default="&quot;png&quot;">
  출력 이미지 형식입니다.

  가능한 값: `jpeg`, `png`, `webp`
</ParamField>

<ParamField body="prompt" type="string" required>
  input_image에 적용할 편집을 설명하는 텍스트 프롬프트이며, input_image가 없을 때는 생성할 이미지를 설명합니다.
</ParamField>

<ParamField body="prompt_upsampling" type="boolean" default="false">
  프롬프트를 업샘플링할지 여부입니다. 활성화하면 더 창의적인 생성을 위해 프롬프트가 자동으로 수정됩니다.
</ParamField>

<ParamField body="safety_tolerance" type="integer" default="2">
  입력 및 출력 검열에 대한 허용치 수준으로, 0(가장 엄격)에서 6(가장 느슨) 사이입니다.

  범위: `0`부터 `6`까지
</ParamField>

<ParamField body="seed" type="integer">
  재현성을 위한 선택적 seed입니다. 생략하면 임의의 seed가 사용됩니다.
</ParamField>

<ParamField body="webhook_secret" type="string">
  웹훅 서명 검증을 위한 선택적 시크릿입니다.
</ParamField>

<ParamField body="webhook_url" type="string (uri)">
  웹훅 알림을 받을 URL입니다.

  형식: `uri`
</ParamField>

Router가 `GET /v2/models/bfl/flux-kontext-pro/openapi.json`에서 제공하는 스키마로부터 생성되었으며, 이는 요청이 공급자에게 도달하기 전에 호출을 검증하는 데 사용하는 것과 동일한 문서입니다.

<h3>출력</h3>

<ResponseField name="cost" type="number">
  공급자가 보고한 크레딧 단위 비용으로, 작업이 Ready가 되면 채워집니다.

  형식: `float`
</ResponseField>

<ResponseField name="id" type="string" required>
  BFL 작업 식별자입니다.
</ResponseField>

<ResponseField name="progress" type="number">
  BFL이 보고하는 선택적 생성 진행률입니다.

  범위: `0`부터 `1`까지

  형식: `float`
</ResponseField>

<ResponseField name="result" type="object" required>
  완료된 생성 결과입니다. 여기서는 null을 허용하지 않습니다. 이 컴포넌트의 `required` 항목은 `200` 응답이 결과를 포함한다는 약속이며, `result`가 null을 허용하면 이는 키 존재 여부 확인으로 전락합니다.
</ResponseField>

<ResponseField name="result.cost" type="number">
  공급자가 보고한 생성 비용입니다. 이는 BFL의 수치이며 Comfy가 청구하는 금액이 아닙니다.

  형식: `double`
</ResponseField>

<ResponseField name="result.duration" type="number">
  공급자가 보고한 생성 소요 시간(초)입니다.

  형식: `double`
</ResponseField>

<ResponseField name="result.end_time" type="number">
  공급자가 보고한 생성 완료 시각으로, Unix 에포크 이후 초 단위입니다. `start_time`과 같은 이유로 `double`입니다.

  형식: `double`
</ResponseField>

<ResponseField name="result.prompt" type="string">
  프롬프트 업샘플링 이후 실제로 생성에 사용된 프롬프트입니다.
</ResponseField>

<ResponseField name="result.sample" type="string (uri)">
  생성된 에셋의 서명된 URL입니다. Router는 에셋을 Comfy 스토리지에 다시 호스팅하고 이 필드를 재작성하므로, 일반적으로 최대 24시간 동안 유효한 Comfy 호스팅 URL입니다. 발급 시 24시간으로 서명되고 23시간짜리 메모에서 재생되므로, 나중에 폴링하면 남은 시간이 1시간도 되지 않는 URL을 받을 수 있습니다. 재호스팅을 수행할 수 없었던 리프는 대신 BFL 자체의 수명이 짧은 전달 URL을 유지합니다. 비디오는 대략 2시간, 이미지는 대략 10분입니다. 어느 쪽이든 링크는 만료되므로 URL을 저장하기보다 에셋을 다운로드하세요.

  형식: `uri`
</ResponseField>

<ResponseField name="result.seed" type="integer">
  생성에 사용된 seed로, 제공되었든 공급자가 선택했든 관계없습니다. BFL이 2^31을 초과하는 seed(예: 2784347701)를 반환하기 때문에 `int64`로 선언되었으며, 포맷되지 않은 `integer`는 많은 SDK 생성기에서 32비트 필드로 생성됩니다.

  형식: `int64`
</ResponseField>

<ResponseField name="result.start_time" type="number">
  공급자가 보고한 생성 시작 시각으로, Unix 에포크 이후 초 단위입니다. `float`이 아닌 `double`을 사용합니다. 현재의 에포크 값 근처에서 float32의 간격은 약 128초이므로, 전체 생성 구간이 단일 디코딩 값으로 압축됩니다.

  형식: `double`
</ResponseField>

<ResponseField name="status" type="string" required>
  작업 상태: Pending, Reasoning, Generating, Ready, Request Moderated, Content Moderated, Error 또는 Task not found.
</ResponseField>

<h2>예제</h2>

<h3>입력</h3>

```json
{
  "prompt": "replace the background with a sunlit beach, keep the subject unchanged",
  "input_image": "<base64 of input.jpg>",
  "aspect_ratio": "1:1"
}
```

<h3>출력</h3>

```json
{
  "id": "0a1b2c3d-...",
  "status": "Ready",
  "result": {
    "sample": "https://.../out.png",
    "prompt": "replace the background with a sunlit beach, keep the subject unchanged",
    "seed": 1234567890
  }
}
```

URL은 임시입니다. 이미지를 보관해야 한다면 즉시 다운로드하세요.
  </Tab>
  <Tab title="Kontext Max">
**Model ID:** `bfl/flux-kontext-max`

**Endpoint:** `POST https://api.comfy.org/v2/models/bfl/flux-kontext-max`

<CodeGroup>
```python Python
import base64

from comfy_sdk import Comfy

with open("input.jpg", "rb") as f:
    input_image = base64.b64encode(f.read()).decode()

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "bfl/flux-kontext-max",
        {
            "prompt": "replace the background with a sunlit beach, keep the subject unchanged",
            "input_image": input_image,
            "aspect_ratio": "1:1",
        },
    )

print("image:", result["result"]["sample"])
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";
import { readFile } from "node:fs/promises";

const inputImage = (await readFile("input.jpg")).toString("base64");

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
type Result = { result: { sample: string } };
const { data } = await comfy.models.run<Result>("bfl/flux-kontext-max", {
  prompt: "replace the background with a sunlit beach, keep the subject unchanged",
  input_image: inputImage,
  aspect_ratio: "1:1",
});

console.log("image:", data.result.sample);
```

```bash cURL
INPUT_IMAGE=$(base64 < input.jpg | tr -d '\n')

curl https://api.comfy.org/v2/models/bfl/flux-kontext-max \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"prompt\": \"replace the background with a sunlit beach, keep the subject unchanged\", \"input_image\": \"$INPUT_IMAGE\", \"aspect_ratio\": \"1:1\"}"
```
</CodeGroup>

<h2>Schema</h2>

<h3>입력</h3>

<ParamField body="aspect_ratio" type="string">
  21:9에서 9:21 사이의 출력 비율(예: 16:9)입니다. 입력 이미지가 주어지면 해당 이미지의 비율을 기본값으로 사용하고, 그렇지 않으면 1:1입니다.
</ParamField>

<ParamField body="input_image" type="string">
  편집할 이미지로, base64로 인코딩된 이미지 또는 http(s) URL입니다. 선택 사항이며, 없으면 모델은 프롬프트만으로 생성합니다.
</ParamField>

<ParamField body="input_image_2" type="string">
  추가 참조 이미지로, base64로 인코딩되었거나 http(s) URL입니다(실험적 다중 참조).
</ParamField>

<ParamField body="input_image_3" type="string">
  추가 참조 이미지로, base64로 인코딩되었거나 http(s) URL입니다(실험적 다중 참조).
</ParamField>

<ParamField body="input_image_4" type="string">
  추가 참조 이미지로, base64로 인코딩되었거나 http(s) URL입니다(실험적 다중 참조).
</ParamField>

<ParamField body="output_format" type="string" default="&quot;png&quot;">
  출력 이미지 형식입니다.

  가능한 값: `jpeg`, `png`, `webp`
</ParamField>

<ParamField body="prompt" type="string" required>
  input_image에 적용할 편집을 설명하는 텍스트 프롬프트이며, input_image가 없을 때는 생성할 이미지를 설명합니다.
</ParamField>

<ParamField body="prompt_upsampling" type="boolean" default="false">
  프롬프트를 업샘플링할지 여부입니다. 활성화하면 더 창의적인 생성을 위해 프롬프트가 자동으로 수정됩니다.
</ParamField>

<ParamField body="safety_tolerance" type="integer" default="2">
  입력 및 출력 검열에 대한 허용치 수준으로, 0(가장 엄격)에서 6(가장 느슨) 사이입니다.

  범위: `0`부터 `6`까지
</ParamField>

<ParamField body="seed" type="integer">
  재현성을 위한 선택적 seed입니다. 생략하면 임의의 seed가 사용됩니다.
</ParamField>

<ParamField body="webhook_secret" type="string">
  웹훅 서명 검증을 위한 선택적 시크릿입니다.
</ParamField>

<ParamField body="webhook_url" type="string (uri)">
  웹훅 알림을 받을 URL입니다.

  형식: `uri`
</ParamField>

Router가 `GET /v2/models/bfl/flux-kontext-max/openapi.json`에서 제공하는 스키마로부터 생성되었으며, 이는 요청이 공급자에게 도달하기 전에 호출을 검증하는 데 사용하는 것과 동일한 문서입니다.

<h3>출력</h3>

<ResponseField name="cost" type="number">
  공급자가 보고한 크레딧 단위 비용으로, 작업이 Ready가 되면 채워집니다.

  형식: `float`
</ResponseField>

<ResponseField name="id" type="string" required>
  BFL 작업 식별자입니다.
</ResponseField>

<ResponseField name="progress" type="number">
  BFL이 보고하는 선택적 생성 진행률입니다.

  범위: `0`부터 `1`까지

  형식: `float`
</ResponseField>

<ResponseField name="result" type="object" required>
  완료된 생성 결과입니다. 여기서는 null을 허용하지 않습니다. 이 컴포넌트의 `required` 항목은 `200` 응답이 결과를 포함한다는 약속이며, `result`가 null을 허용하면 이는 키 존재 여부 확인으로 전락합니다.
</ResponseField>

<ResponseField name="result.cost" type="number">
  공급자가 보고한 생성 비용입니다. 이는 BFL의 수치이며 Comfy가 청구하는 금액이 아닙니다.

  형식: `double`
</ResponseField>

<ResponseField name="result.duration" type="number">
  공급자가 보고한 생성 소요 시간(초)입니다.

  형식: `double`
</ResponseField>

<ResponseField name="result.end_time" type="number">
  공급자가 보고한 생성 완료 시각으로, Unix 에포크 이후 초 단위입니다. `start_time`과 같은 이유로 `double`입니다.

  형식: `double`
</ResponseField>

<ResponseField name="result.prompt" type="string">
  프롬프트 업샘플링 이후 실제로 생성에 사용된 프롬프트입니다.
</ResponseField>

<ResponseField name="result.sample" type="string (uri)">
  생성된 에셋의 서명된 URL입니다. Router는 에셋을 Comfy 스토리지에 다시 호스팅하고 이 필드를 재작성하므로, 일반적으로 최대 24시간 동안 유효한 Comfy 호스팅 URL입니다. 발급 시 24시간으로 서명되고 23시간짜리 메모에서 재생되므로, 나중에 폴링하면 남은 시간이 1시간도 되지 않는 URL을 받을 수 있습니다. 재호스팅을 수행할 수 없었던 리프는 대신 BFL 자체의 수명이 짧은 전달 URL을 유지합니다. 비디오는 대략 2시간, 이미지는 대략 10분입니다. 어느 쪽이든 링크는 만료되므로 URL을 저장하기보다 에셋을 다운로드하세요.

  형식: `uri`
</ResponseField>

<ResponseField name="result.seed" type="integer">
  생성에 사용된 seed로, 제공되었든 공급자가 선택했든 관계없습니다. BFL이 2^31을 초과하는 seed(예: 2784347701)를 반환하기 때문에 `int64`로 선언되었으며, 포맷되지 않은 `integer`는 많은 SDK 생성기에서 32비트 필드로 생성됩니다.

  형식: `int64`
</ResponseField>

<ResponseField name="result.start_time" type="number">
  공급자가 보고한 생성 시작 시각으로, Unix 에포크 이후 초 단위입니다. `float`이 아닌 `double`을 사용합니다. 현재의 에포크 값 근처에서 float32의 간격은 약 128초이므로, 전체 생성 구간이 단일 디코딩 값으로 압축됩니다.

  형식: `double`
</ResponseField>

<ResponseField name="status" type="string" required>
  작업 상태: Pending, Reasoning, Generating, Ready, Request Moderated, Content Moderated, Error 또는 Task not found.
</ResponseField>

<h2>예제</h2>

<h3>입력</h3>

```json
{
  "prompt": "replace the background with a sunlit beach, keep the subject unchanged",
  "input_image": "<base64 of input.jpg>",
  "aspect_ratio": "1:1"
}
```

<h3>출력</h3>

```json
{
  "id": "0a1b2c3d-...",
  "status": "Ready",
  "result": {
    "sample": "https://.../out.png",
    "prompt": "replace the background with a sunlit beach, keep the subject unchanged",
    "seed": 1234567890
  }
}
```

URL은 임시입니다. 이미지를 보관해야 한다면 즉시 다운로드하세요.
  </Tab>
</Tabs>

<RouterCodeFooter />