---
title: "Comfy Router로 Ultimate Image Upscaler 사용하기"
description: "Comfy Router를 통해 wavespeed/ultimate-image-upscaler를 호출합니다: endpoint, 요청 형태, 그리고 Router가 반환하는 응답입니다."
sidebarTitle: "Ultimate Image Upscaler"
translationSourceHash: 51cc32d4
translationFrom: development/comfy-router/models/wavespeed/ultimate-image-upscaler/code.mdx
translationBlockHashes:
  "_intro": 45710ad8
  "Quick start": 0df9b0ec
  "Schema": 68a45d0b
  "Examples": f2dbccf3
---
{/* GENERATED FILE. Generated from router-schemas/wavespeed/ultimate-image-upscaler.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/ko/comfy-router/model-code-footer.mdx";

`wavespeed/ultimate-image-upscaler`의 API 레퍼런스이며, Comfy Router가 WaveSpeed를 통해 제공합니다.

## 빠른 시작

[Comfy 워크스페이스](https://platform.comfy.org/profile/api-keys)에서 키를 생성하고 `COMFY_API_KEY`로 내보내세요. Python 및 TypeScript 스니펫은 Comfy SDK(`pip install comfy-sdk`, `npm install @comfyorg/sdk`)를 사용하며, cURL 스니펫은 동일한 호출을 원시 HTTP로 수행합니다.

**모델 ID:** `wavespeed/ultimate-image-upscaler`

**엔드포인트:** `POST https://api.comfy.org/v2/models/wavespeed/ultimate-image-upscaler`

<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "wavespeed/ultimate-image-upscaler",
        {
            "image": "https://images.pexels.com/photos/346529/pexels-photo-346529.jpeg",
            "target_resolution": "4k",
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const { data } = await comfy.models.run("wavespeed/ultimate-image-upscaler", {
  image: "https://images.pexels.com/photos/346529/pexels-photo-346529.jpeg",
  target_resolution: "4k",
});

console.log(data);
```

```bash cURL
curl https://api.comfy.org/v2/models/wavespeed/ultimate-image-upscaler \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"image\": \"https://images.pexels.com/photos/346529/pexels-photo-346529.jpeg\", \"target_resolution\": \"4k\"}"
```
</CodeGroup>

## 스키마

### 입력

<ParamField body="enable_base64_output" type="boolean" default="false">
  활성화하면 출력이 URL 대신 BASE64 문자열로 인코딩됩니다.
</ParamField>

<ParamField body="image" type="string" required>
  업스케일할 이미지의 URL입니다.
</ParamField>

<ParamField body="output_format" type="string" default="&quot;jpeg&quot;">
  출력 이미지의 형식입니다.

  가능한 값: `jpeg`, `png`, `webp`
</ParamField>

<ParamField body="target_resolution" type="string" default="&quot;4k&quot;">
  출력 이미지의 목표 해상도입니다.

  가능한 값: `2k`, `4k`, `8k`
</ParamField>

`GET /v2/models/wavespeed/ultimate-image-upscaler/openapi.json`에서 Router가 제공하는 스키마에서 생성되었으며, 요청이 공급자에게 도달하기 이전에 호출을 검증하는 데 사용하는 것과 동일한 문서입니다.

### 출력

Router는 이 모델에 대한 출력 스키마를 게시하지 않습니다.

## 예시

### 입력

```json
{
  "image": "https://images.pexels.com/photos/346529/pexels-photo-346529.jpeg",
  "target_resolution": "4k"
}
```


<RouterCodeFooter />
