---
title: "Comfy Router로 LTX 2.5 Pro 사용하기"
description: "Comfy Router를 통해 ltx/ltx-2-5-pro를 호출하세요: endpoint, 요청 형태, 그리고 Router가 반환하는 응답입니다."
sidebarTitle: "LTX 2.5 Pro"
translationSourceHash: 29caa5e3
translationFrom: development/comfy-router/models/ltx/ltx-2-5-pro/code.mdx
---

{/* GENERATED FILE. Generated from router-schemas/ltx/ltx-2-5-pro.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/ko/comfy-router/model-code-footer.mdx";

LTX가 Comfy Router를 통해 제공하는 `ltx/ltx-2-5-pro`의 API 레퍼런스입니다.

## 요청 설정

[Comfy 워크스페이스](https://platform.comfy.org/profile/api-keys)에서 키를 생성하고 `COMFY_API_KEY`로 내보내세요. Python의 경우 `pip install comfy-sdk`를 실행하세요. TypeScript의 경우 `npm install @comfyorg/sdk`를 실행하세요. cURL은 raw HTTP를 사용합니다.

**모델 ID:** `ltx/ltx-2-5-pro`

**Endpoint:** `POST https://api.comfy.org/v2/models/ltx/ltx-2-5-pro`

<Note>
Router는 이 모델에 대해 작성된 입력 스키마를 공개하지 않았으므로, 스니펫을 생성할 요청 예시가 없습니다. Router는 본문을 LTX로 그대로 전달합니다: LTX 자체 API 문서를 기반으로 요청을 구성한 뒤 [Router 빠른 시작](/ko/development/comfy-router/quickstart)과 함께 전송하세요.
</Note>

## 스키마

### 입력

<Note>
Router는 아직 이 모델에 대해 작성된 입력 스키마를 공개하지 않았습니다: `GET /v2/models/ltx/ltx-2-5-pro/openapi.json`은 `x-comfy-input-schema-authored: false`가 포함된 개방형 객체를 반환합니다. Router는 본문을 LTX로 그대로 전달하므로, 요청 필드에 대해서는 LTX 자체 API 문서가 기준이며, Router는 모델별 입력 검증을 수행하지 않습니다. 공급자 검증은 여전히 적용됩니다.
</Note>

### 출력

<ResponseField name="completed_at" type="string">
  작업 완료 타임스탬프 (ISO 8601)
</ResponseField>

<ResponseField name="created_at" type="string">
  작업 생성 타임스탬프 (ISO 8601)
</ResponseField>

<ResponseField name="error" type="object">
  상태가 실패일 때 표시됩니다
</ResponseField>

<ResponseField name="error.message" type="string">
   
</ResponseField>

<ResponseField name="error.type" type="string">
   
</ResponseField>

<ResponseField name="id" type="string">
  고유한 작업 식별자
</ResponseField>

<ResponseField name="result" type="object">
  상태가 완료됨일 때 표시됩니다; 출력 URL은 완료 후 24시간 뒤에 만료됩니다
</ResponseField>

<ResponseField name="result.video_url" type="string">
  생성된 비디오의 URL
</ResponseField>

<ResponseField name="status" type="string">
  작업 상태 (대기 중, processing, 완료됨, 실패)
</ResponseField>

## 예시

### 출력

```json
{
  "completed_at": "2026-01-01T00:02:10Z",
  "created_at": "2026-01-01T00:00:00Z",
  "id": "3f7a1b28-5c0d-4e91-8a6f-1b2c3d4e5f60",
  "result": {
    "video_url": "https://example.invalid/ltx/generated.mp4"
  },
  "status": "completed"
}
```


<RouterCodeFooter />