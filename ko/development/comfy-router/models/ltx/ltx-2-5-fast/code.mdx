---
title: "Comfy Router로 LTX 2.5 Fast 사용하기"
description: "Comfy Router를 통해 ltx/ltx-2-5-fast를 호출합니다: 엔드포인트, 요청 형태, Router가 반환하는 응답입니다."
sidebarTitle: "LTX 2.5 Fast"
translationSourceHash: 1861291c
translationFrom: development/comfy-router/models/ltx/ltx-2-5-fast/code.mdx
---

{/* GENERATED FILE. Generated from router-schemas/ltx/ltx-2-5-fast.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/ko/comfy-router/model-code-footer.mdx";

Comfy Router가 LTX에서 제공하는 `ltx/ltx-2-5-fast`에 대한 API 레퍼런스입니다.

## 요청 설정

[Comfy 워크스페이스](https://platform.comfy.org/profile/api-keys)에서 키를 생성하고 `COMFY_API_KEY`로 내보내세요. Python의 경우 `pip install comfy-sdk`를 실행합니다. TypeScript의 경우 `npm install @comfyorg/sdk`를 실행합니다. cURL은 원시 HTTP를 사용합니다.

**모델 ID:** `ltx/ltx-2-5-fast`

**엔드포인트:** `POST https://api.comfy.org/v2/models/ltx/ltx-2-5-fast`

<Note>
Router는 이 모델에 대해 작성된 입력 스키마를 게시하지 않았으므로, 스니펫을 생성할 요청 예시가 없습니다. Router는 본문을 변경 없이 LTX로 전달합니다. LTX 자체 API 문서를 바탕으로 본문을 구성한 다음, [Router 퀵스타트](/ko/development/comfy-router/quickstart)를 참고하여 전송하세요.
</Note>

## 스키마

### 입력

<Note>
Router는 아직 이 모델에 대해 작성된 입력 스키마를 게시하지 않았습니다. `GET /v2/models/ltx/ltx-2-5-fast/openapi.json`은 `x-comfy-input-schema-authored: false`인 개방형 객체를 반환합니다. Router는 본문을 변경 없이 LTX로 전달하므로, 요청 필드에 대해서는 LTX 자체 API 문서가 기준이며, Router는 모델별 입력 검증을 수행하지 않습니다. 공급자 검증은 여전히 적용됩니다.
</Note>

### 출력

<ResponseField name="completed_at" type="string">
  작업 완료 타임스탬프(ISO 8601)
</ResponseField>

<ResponseField name="created_at" type="string">
  작업 생성 타임스탬프(ISO 8601)
</ResponseField>

<ResponseField name="error" type="object">
  status가 실패일 때 존재합니다
</ResponseField>

<ResponseField name="error.message" type="string">
   
</ResponseField>

<ResponseField name="error.type" type="string">
   
</ResponseField>

<ResponseField name="id" type="string">
  고유한 작업 식별자
</ResponseField>

<ResponseField name="result" type="object">
  status가 완료됨일 때 존재합니다. 출력 URL은 완료 후 24시간 뒤에 만료됩니다
</ResponseField>

<ResponseField name="result.video_url" type="string">
  생성된 비디오의 URL
</ResponseField>

<ResponseField name="status" type="string">
  작업 상태(대기 중, 처리 중, 완료됨, 실패)
</ResponseField>

## 예시

### 출력

```json
{
  "completed_at": "2026-01-01T00:02:10Z",
  "created_at": "2026-01-01T00:00:00Z",
  "id": "3f7a1b28-5c0d-4e91-8a6f-1b2c3d4e5f60",
  "result": {
    "video_url": "https://example.invalid/ltx/generated.mp4"
  },
  "status": "completed"
}
```


<RouterCodeFooter />