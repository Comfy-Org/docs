---
title: "Comfy Router로 Grok Imagine Video 1.5 사용하기"
description: "Comfy Router를 통해 xai/grok-imagine-video-1.5를 호출합니다: 엔드포인트, 요청 형태, 그리고 Router가 반환하는 응답입니다."
sidebarTitle: "Grok Imagine Video 1.5"
translationSourceHash: 08800464
translationFrom: development/comfy-router/models/xai/grok-imagine-video-1-5/code.mdx
translationBlockHashes:
  "_intro": 9cd89e4e
  "Quick start": 7b4ef7d7
  "Schema": 7567f6f2
  "Examples": 53c80746
---
{/* GENERATED FILE. Generated from router-schemas/xai/grok-imagine-video-1.5.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/ko/comfy-router/model-code-footer.mdx";

xAI가 Comfy Router를 통해 제공하는 `xai/grok-imagine-video-1.5`의 API 레퍼런스입니다.

## 빠른 시작

[Comfy 워크스페이스](https://platform.comfy.org/profile/api-keys?onboarding=router)에서 키를 생성하고 `COMFY_API_KEY`로 내보내세요. Python, TypeScript, Swift 스니펫은 Comfy SDK를 사용하며(`pip install comfy-sdk`, `npm install @comfyorg/sdk`, 그리고 [`ComfySwiftSDK`](https://github.com/Comfy-Org/comfy-swift-sdk) Swift 패키지), cURL 스니펫은 동일한 호출을 원시 HTTP로 전송합니다.

**모델 ID:** `xai/grok-imagine-video-1.5`

**엔드포인트:** `POST https://api.comfy.org/v2/models/xai/grok-imagine-video-1.5`

<Tabs>
  <Tab title="Wait for the result">
<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "xai/grok-imagine-video-1.5",
        {
            "duration": 4,
            "prompt": "a single red maple leaf falling onto still water, slow motion",
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const { data } = await comfy.models.run("xai/grok-imagine-video-1.5", {
  duration: 4,
  prompt: "a single red maple leaf falling onto still water, slow motion",
});

console.log(data);
```

```swift Swift
import Foundation
import ComfySwiftSDK

// Reads COMFY_API_KEY from the environment.
// The SDK mints an idempotency key per call and reuses it for automatic retries.
let client = ComfyCloudClient(apiKey: ProcessInfo.processInfo.environment["COMFY_API_KEY"]!)
let result = try await client.models.run(
    "xai/grok-imagine-video-1.5",
    input: [
        "duration": 4,
        "prompt": "a single red maple leaf falling onto still water, slow motion",
    ]
)

print(result.output)
```

```bash cURL
curl https://api.comfy.org/v2/models/xai/grok-imagine-video-1.5 \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"duration\": 4, \"prompt\": \"a single red maple leaf falling onto still water, slow motion\"}"
```
</CodeGroup>
  </Tab>
  <Tab title="Queue and collect later">
동일한 본문을 `POST https://api.comfy.org/v2/models/xai/grok-imagine-video-1.5/requests` 로 보냅니다. Router는 실행이 접수되는 즉시 `201` 과 `request_id` 를 응답하며, 결과는 준비가 되는 대로 이 프로세스나 다른 프로세스에서 수집할 수 있습니다. 상태, 취소, 수집 방법은 [큐 전송](/ko/development/comfy-router/queue) 를 참고하세요.

<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# Each submit() call mints its own Idempotency-Key and reuses it for automatic retries.
with Comfy() as client:
    handle = client.models.submit(
        "xai/grok-imagine-video-1.5",
        {
            "duration": 4,
            "prompt": "a single red maple leaf falling onto still water, slow motion",
        },
    )
    print("request_id:", handle.request_id)  # with the model ID, all another process needs

    # Poll until the request completes, waiting the Retry-After the server names.
    for update in handle.iter_events():
        print(update.status, update.queue_position)

    # The provider's own payload, the same value models.run() returns.
    # A request that failed or was cancelled raises the typed Router error here.
    result = handle.get()

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// Each submit() call mints its own Idempotency-Key and reuses it for automatic retries.
const handle = await comfy.models.submit("xai/grok-imagine-video-1.5", {
  duration: 4,
  prompt: "a single red maple leaf falling onto still water, slow motion",
});
console.log("requestId:", handle.requestId); // with the model ID, all another process needs

// Poll until the request completes, waiting the Retry-After the server names.
for await (const update of handle.events()) {
  console.log(update.status, update.queuePosition);
}

// The same result models.run() returns. A request that failed or was cancelled rejects here.
const result = await handle.get();

console.log(result.data);
```

```swift Swift
import Foundation
import ComfySwiftSDK

// Reads COMFY_API_KEY from the environment.
// Each submit() call mints its own Idempotency-Key and reuses it for automatic retries.
let client = ComfyCloudClient(apiKey: ProcessInfo.processInfo.environment["COMFY_API_KEY"]!)
let handle = try await client.models.submit(
    "xai/grok-imagine-video-1.5",
    input: [
        "duration": 4,
        "prompt": "a single red maple leaf falling onto still water, slow motion",
    ]
)
print("requestId:", handle.requestId)  // with the model ID, all another process needs

// Poll until the request completes, waiting the Retry-After the server names.
for try await update in handle.events() {
    print(update.state.rawValue, update.queuePosition.map(String.init) ?? "unknown")
}

// The provider's own payload, the same value models.run() returns.
// A request that failed or was cancelled throws the typed Router error here.
let result = try await handle.result()

print(result.output)
```

```bash cURL
# 1. Submit. Router answers 201 with request_id, status_url, response_url and cancel_url.
curl https://api.comfy.org/v2/models/xai/grok-imagine-video-1.5/requests \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"duration\": 4, \"prompt\": \"a single red maple leaf falling onto still water, slow motion\"}"

# 2. Poll until status is COMPLETED, waiting the Retry-After seconds each response names.
REQUEST_ID="<request_id from the 201 body>"
curl -i https://api.comfy.org/v2/models/xai/grok-imagine-video-1.5/requests/$REQUEST_ID/status \
  -H "X-API-Key: $COMFY_API_KEY"

# 3. Collect. 200 with the model's native output, 202 with the status body while it is still running.
curl https://api.comfy.org/v2/models/xai/grok-imagine-video-1.5/requests/$REQUEST_ID \
  -H "X-API-Key: $COMFY_API_KEY"
```
</CodeGroup>
  </Tab>
</Tabs>


## 스키마

### 입력

<ParamField body="aspect_ratio" type="string" default="&quot;16:9&quot;">
  생성된 비디오의 화면 비율

  가능한 값: `1:1`, `16:9`, `9:16`, `4:3`, `3:4`, `3:2`, `2:3`
</ParamField>

<ParamField body="duration" type="integer" default="8">
  비디오 재생 시간(초). 범위 [1, 15]. 기본값 8.

  범위: `1` ~ `15`
</ParamField>

<ParamField body="image" type="object">
  xAI 엔드포인트용 입력 이미지 객체
</ParamField>

<ParamField body="image.type" type="string">
  이미지 입력의 유형

  가능한 값: `image_url`
</ParamField>

<ParamField body="image.url" type="string" required>
  입력 이미지의 URL(공개 URL 또는 base64로 인코딩된 data URI)
</ParamField>

<ParamField body="model" type="string">
  사용할 모델. 지원: grok-imagine-video(기본값), grok-imagine-video-1.5-preview, grok-imagine-video-1.5. 지원 중단된 grok-imagine-video-beta id는 grok-imagine-video의 별칭입니다.
</ParamField>

<ParamField body="output" type="object">
  생성된 비디오의 선택적 출력 대상
</ParamField>

<ParamField body="prompt" type="string" required>
  비디오 생성을 위한 프롬프트. 최대 4,096자.
</ParamField>

<ParamField body="reference_images" type="object[]">
  비디오 생성을 안내할 하나 이상의 레퍼런스 이미지(레퍼런스 기반 비디오 생성 모드). image와 함께 사용할 수 없습니다. Router는 application/json만 전달하며 이 객체와 XAIImageObject 모두 file_id를 허용하지 않으므로, 이 경로에서는 파일 기반 이미지 입력이 지원되지 않습니다.
</ParamField>

<ParamField body="reference_images[].url" type="string" required>
  레퍼런스 이미지의 URL. HTTPS URL(공개) 또는 base64로 인코딩된 data URL(예: data:image/jpeg;base64,...)을 지원합니다.
</ParamField>

<ParamField body="resolution" type="string">
  출력 비디오의 해상도
</ParamField>

<ParamField body="size" type="string">
  출력 비디오의 크기
</ParamField>

<ParamField body="user" type="string">
  최종 사용자를 나타내는 고유 식별자
</ParamField>

Router가 `GET /v2/models/xai/grok-imagine-video-1.5/openapi.json`에서 제공하는 스키마에서 생성되었으며, 이는 요청이 공급자에 도달하기 이전에 Router가 호출을 검증하는 바로 그 문서입니다.

### 출력

<ResponseField name="block_reason" type="string">
  입력 검열로 요청이 차단된 경우 차단 사유를 포함합니다
</ResponseField>

<ResponseField name="model" type="string">
  비디오 생성에 사용된 모델
</ResponseField>

<ResponseField name="status" type="string">
  지연된 요청의 상태: "pending" 또는 "done"

  가능한 값: `pending`, `done`
</ResponseField>

<ResponseField name="usage" type="object">
  비디오 생성 요청의 사용량 정보
</ResponseField>

<ResponseField name="usage.cost_in_usd_ticks" type="integer">
  이 요청의 비용을 USD tick 단위로 나타낸 값. 1 미국 센트는 100,000,000 tick이고, 1 미국 달러는 10,000,000,000 tick입니다.
</ResponseField>

<ResponseField name="video" type="object">
  xAI가 생성한 비디오
</ResponseField>

<ResponseField name="video.duration" type="integer">
  생성된 비디오의 재생 시간(초)
</ResponseField>

<ResponseField name="video.respect_moderation" type="boolean">
  모델이 생성한 비디오가 검열 규칙을 준수하는지 여부
</ResponseField>

<ResponseField name="video.url" type="string">
  생성된 비디오의 다운로드 URL. Router는 비디오를 Comfy 스토리지에 다시 호스팅하고 이 필드를 재작성하므로, 일반적으로 최대 24시간 동안 유효한 Comfy 서명 URL입니다. 발급 시 24시간으로 서명되고 23시간 메모에서 재생되므로, 나중에 폴링하면 남은 유효 기간이 1시간도 되지 않는 URL을 받을 수 있습니다. 재호스팅을 수행할 수 없었던 경우에는 이 필드가 xAI 자체의 단기 URL을 유지합니다. NULLABLE: `url`이 비어 있는 성공은 완료된 생성이 아닙니다. 어느 경우든 링크는 만료되므로, URL을 저장하지 말고 비디오를 다운로드하세요.
</ResponseField>

## 예시

### 입력

```json
{
  "duration": 4,
  "prompt": "a single red maple leaf falling onto still water, slow motion"
}
```

### 출력

```json
{
  "model": "grok-imagine-video-1.5",
  "status": "done",
  "usage": {
    "cost_in_usd_ticks": 3500000000
  },
  "video": {
    "duration": 4,
    "respect_moderation": true,
    "url": "https://example.invalid/xai/grok-imagine-video/generated.mp4"
  }
}
```


<RouterCodeFooter />
