---
title: "Comfy Router로 Grok Imagine Video 사용하기"
description: "Comfy Router를 통해 xai/grok-imagine-video 호출하기: 엔드포인트, 요청 형태, Router가 반환하는 응답."
sidebarTitle: "Grok Imagine Video"
translationSourceHash: 81ba818e
translationFrom: development/comfy-router/models/xai/grok-imagine-video/code.mdx
translationBlockHashes:
  "_intro": aaeed1e5
  "Quick start": 170d6503
  "Schema": 8371176e
  "Examples": 1085f4b8
---
{/* GENERATED FILE. Generated from router-schemas/xai/grok-imagine-video.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/ko/comfy-router/model-code-footer.mdx";

xAI의 `xai/grok-imagine-video`에 대한 API 레퍼런스로, Comfy Router를 통해 제공됩니다.

## 빠른 시작

[Comfy 워크스페이스](https://platform.comfy.org/profile/api-keys)에서 키를 생성하고 `COMFY_API_KEY`로 내보내세요. Python 및 TypeScript 스니펫은 Comfy SDK(`pip install comfy-sdk`, `npm install @comfyorg/sdk`)를 사용하며, cURL 스니펫은 동일한 호출을 원시 HTTP로 수행합니다.

**모델 ID:** `xai/grok-imagine-video`

**엔드포인트:** `POST https://api.comfy.org/v2/models/xai/grok-imagine-video`

<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "xai/grok-imagine-video",
        {
            "duration": 4,
            "prompt": "a single red maple leaf falling onto still water, slow motion",
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const { data } = await comfy.models.run("xai/grok-imagine-video", {
  duration: 4,
  prompt: "a single red maple leaf falling onto still water, slow motion",
});

console.log(data);
```

```bash cURL
curl https://api.comfy.org/v2/models/xai/grok-imagine-video \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"duration\": 4, \"prompt\": \"a single red maple leaf falling onto still water, slow motion\"}"
```
</CodeGroup>

## 스키마

### 입력

<ParamField body="aspect_ratio" type="string" default="&quot;16:9&quot;">
  생성된 비디오의 화면 비율

  가능한 값: `1:1`, `16:9`, `9:16`, `4:3`, `3:4`, `3:2`, `2:3`
</ParamField>

<ParamField body="duration" type="integer" default="8">
  비디오 재생 시간(초). 범위 [1, 15]. 기본값 8.

  범위: `1` ~ `15`
</ParamField>

<ParamField body="image" type="object">
  xAI 엔드포인트용 입력 이미지 객체
</ParamField>

<ParamField body="image.type" type="string">
  이미지 입력의 유형

  가능한 값: `image_url`
</ParamField>

<ParamField body="image.url" type="string" required>
  입력 이미지의 URL (공개 URL 또는 base64로 인코딩된 데이터 URI)
</ParamField>

<ParamField body="model" type="string">
  사용할 모델. 지원: grok-imagine-video(기본값), grok-imagine-video-1.5-preview, grok-imagine-video-1.5. 지원 중단된 grok-imagine-video-beta id는 grok-imagine-video의 별칭입니다.
</ParamField>

<ParamField body="output" type="object">
  생성된 비디오의 선택적 출력 대상
</ParamField>

<ParamField body="prompt" type="string" required>
  비디오 생성을 위한 프롬프트. 최대 4,096자.
</ParamField>

<ParamField body="reference_images" type="object[]">
  비디오 생성을 유도하기 위한 하나 이상의 레퍼런스 이미지(레퍼런스 기반 비디오 생성 모드). image와 함께 사용할 수 없습니다. Router는 application/json만 전달하며 이 객체와 XAIImageObject 모두 file_id를 허용하지 않으므로, 이 경로에서는 파일 기반 이미지 입력이 지원되지 않습니다.
</ParamField>

<ParamField body="reference_images[].url" type="string" required>
  레퍼런스 이미지의 URL. HTTPS URL(공개) 또는 base64로 인코딩된 데이터 URL(예: data:image/jpeg;base64,...)을 지원합니다.
</ParamField>

<ParamField body="resolution" type="string">
  출력 비디오의 해상도
</ParamField>

<ParamField body="size" type="string">
  출력 비디오의 크기
</ParamField>

<ParamField body="user" type="string">
  최종 사용자를 나타내는 고유 식별자
</ParamField>

Router가 `GET /v2/models/xai/grok-imagine-video/openapi.json`에서 제공하는 스키마에서 생성되었으며, 이 문서는 요청이 공급자에게 도달하기 이전에 호출을 검증하는 데 사용하는 것과 동일한 문서입니다.

### 출력

<ResponseField name="block_reason" type="string">
  입력 검열로 인해 요청이 차단된 경우 차단 사유를 포함합니다
</ResponseField>

<ResponseField name="model" type="string">
  비디오를 생성하는 데 사용된 모델
</ResponseField>

<ResponseField name="status" type="string">
  지연된 요청의 상태: "pending" 또는 "done"

  가능한 값: `pending`, `done`
</ResponseField>

<ResponseField name="usage" type="object">
  비디오 생성 요청에 대한 사용량 정보
</ResponseField>

<ResponseField name="usage.cost_in_usd_ticks" type="integer">
  USD 틱으로 표시된 이 요청의 비용입니다. 1 USD 센트는 100,000,000틱이므로, 1 미국 달러는 10,000,000,000틱입니다.
</ResponseField>

<ResponseField name="video" type="object">
  xAI에서 생성된 비디오
</ResponseField>

<ResponseField name="video.duration" type="integer">
  생성된 비디오의 재생 시간(초)
</ResponseField>

<ResponseField name="video.respect_moderation" type="boolean">
  모델이 생성한 비디오가 검열 규칙을 준수하는지 여부
</ResponseField>

<ResponseField name="video.url" type="string">
  생성된 비디오의 다운로드 URL. Router는 비디오를 Comfy 스토리지에 다시 호스팅하고 이 필드를 재작성하므로, 일반적으로 최대 24시간 동안 유효한 Comfy 서명 URL입니다. 발급 시점에 24시간으로 서명되며 23시간짜리 메모에서 재생되므로, 나중에 폴링하면 남은 유효 시간이 1시간도 되지 않은 URL이 반환될 수 있습니다. 다시 호스팅할 수 없는 경우에는 이 필드가 xAI 자체의 수명이 짧은 URL을 그대로 유지합니다. NULLABLE: `url`이 비어 있는 성공은 완료된 생성이 아닙니다. 어느 경우든 링크는 만료되므로 URL을 저장하기보다 비디오를 다운로드하세요.
</ResponseField>

## 예시

### 입력

```json
{
  "duration": 4,
  "prompt": "a single red maple leaf falling onto still water, slow motion"
}
```

### 출력

```json
{
  "model": "grok-imagine-video",
  "status": "done",
  "usage": {
    "cost_in_usd_ticks": 3500000000
  },
  "video": {
    "duration": 4,
    "respect_moderation": true,
    "url": "https://example.invalid/xai/grok-imagine-video/generated.mp4"
  }
}
```


<RouterCodeFooter />
