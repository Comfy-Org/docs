---
title: "Comfy Router로 Grok Imagine Image 사용하기"
description: "Comfy Router를 통해 xai/grok-imagine-image를 호출합니다: 엔드포인트, 요청 형태, 그리고 Router가 반환하는 응답을 설명합니다."
sidebarTitle: "Grok Imagine Image"
translationSourceHash: e195d193
translationFrom: development/comfy-router/models/xai/grok-imagine-image/code.mdx
translationBlockHashes:
  "_intro": f267d74f
  "Quick start": a5495331
  "Schema": bb2b9710
  "Examples": 3222291b
---


{/* GENERATED FILE. Generated from router-schemas/xai/grok-imagine-image.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/ko/comfy-router/model-code-footer.mdx";

Comfy Router가 xAI로부터 제공하는 `xai/grok-imagine-image`의 API 레퍼런스입니다.

## 빠른 시작

[Comfy 워크스페이스](https://platform.comfy.org/profile/api-keys?onboarding=router)에서 키를 생성하고 `COMFY_API_KEY`로 내보내세요. Python과 TypeScript 스니펫은 Comfy SDK를 사용하며(`pip install comfy-sdk`와 `npm install @comfyorg/sdk`), cURL 스니펫은 동일한 호출을 raw HTTP로 수행합니다.

**모델 ID:** `xai/grok-imagine-image`

**엔드포인트:** `POST https://api.comfy.org/v2/models/xai/grok-imagine-image`

<Tabs defaultTabIndex={1}>
  <Tab title="결과 기다리기">
<CodeGroup>
```python Python
from comfy_sdk import Comfy

# 환경 변수에서 COMFY_API_KEY를 읽습니다.
# SDK는 자동으로 멱등성 키를 생성하고 자동 재시도에 재사용합니다.
with Comfy() as client:
    result = client.models.run(
        "xai/grok-imagine-image",
        {
            "n": 1,
            "prompt": "A single red maple leaf on a plain white background.",
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// 환경 변수에서 COMFY_API_KEY를 읽습니다.
// SDK는 자동으로 멱등성 키를 생성하고 자동 재시도에 재사용합니다.
const { data } = await comfy.models.run("xai/grok-imagine-image", {
  n: 1,
  prompt: "A single red maple leaf on a plain white background.",
});

console.log(data);
```

```bash cURL
curl https://api.comfy.org/v2/models/xai/grok-imagine-image \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"n\": 1, \"prompt\": \"A single red maple leaf on a plain white background.\"}"
```
</CodeGroup>
  </Tab>
  <Tab title="대기열에 넣고 나중에 수집">
동일한 본문을 `POST https://api.comfy.org/v2/models/xai/grok-imagine-image/requests`로 전송합니다. Router는 실행이 수락되는 즉시 `request_id`와 함께 `201`로 응답하며, 결과는 준비되면 이 프로세스 또는 다른 프로세스에서 수집됩니다. [대기 중 전달](/ko/development/comfy-router/queue)에서 상태, 취소, 수집 방법을 다룹니다.

<CodeGroup>
```python Python
import asyncio
from comfy_sdk import AsyncComfy

# 환경 변수에서 COMFY_API_KEY를 읽습니다.
# 각 submit() 호출은 자체 Idempotency-Key를 발급하고 자동 재시도에 재사용합니다.
async def main():
    async with AsyncComfy() as client:
        handle = await client.models.submit(
            "xai/grok-imagine-image",
            {
                "n": 1,
                "prompt": "A single red maple leaf on a plain white background.",
            },
        )
        print("request_id:", handle.request_id)  # 모델 ID와 함께라면 다른 프로세스에 필요한 전부입니다

        # 서버가 지정한 Retry-After만큼 기다리며 요청이 완료될 때까지 폴링합니다.
        async for update in handle.iter_events():
            print(update.status, update.queue_position)

        # 공급자의 자체 페이로드로, models.run()이 반환하는 것과 같은 값입니다.
        # 실패했거나 취소된 요청은 여기에서 타입이 지정된 Router 오류를 발생시킵니다.
        result = await handle.get()

    print(result)

asyncio.run(main())
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// 환경 변수에서 COMFY_API_KEY를 읽습니다.
// 각 submit() 호출은 자체 Idempotency-Key를 발급하고 자동 재시도에 재사용합니다.
const handle = await comfy.models.submit("xai/grok-imagine-image", {
  n: 1,
  prompt: "A single red maple leaf on a plain white background.",
});
console.log("requestId:", handle.requestId); // 모델 ID와 함께라면 다른 프로세스에 필요한 전부입니다

// 서버가 지정한 Retry-After만큼 기다리며 요청이 완료될 때까지 폴링합니다.
for await (const update of handle.events()) {
  console.log(update.status, update.queuePosition);
}

// models.run()이 반환하는 것과 같은 결과입니다. 실패했거나 취소된 요청은 여기에서 거부됩니다.
const result = await handle.get();

console.log(result.data);
```

```bash cURL
# 1. 제출합니다. Router는 request_id, status_url, response_url, cancel_url과 함께 201로 응답합니다.
curl https://api.comfy.org/v2/models/xai/grok-imagine-image/requests \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"n\": 1, \"prompt\": \"A single red maple leaf on a plain white background.\"}"

# 2. 각 응답이 지정한 Retry-After 초만큼 기다리며 상태가 COMPLETED가 될 때까지 폴링합니다.
REQUEST_ID="<request_id from the 201 body>"
curl -i https://api.comfy.org/v2/models/xai/grok-imagine-image/requests/$REQUEST_ID/status \
  -H "X-API-Key: $COMFY_API_KEY"

# 3. 수집합니다. 모델의 네이티브 출력과 함께 200, 아직 실행 중이면 상태 본문과 함께 202.
curl https://api.comfy.org/v2/models/xai/grok-imagine-image/requests/$REQUEST_ID \
  -H "X-API-Key: $COMFY_API_KEY"
```
</CodeGroup>
  </Tab>
</Tabs>

## 스키마

### 입력

<ParamField body="aspect_ratio" type="string" default="&quot;auto&quot;">
  생성된 이미지의 비율입니다. 프롬프트에 가장 적합한 비율을 자동으로 선택하려면 기본값인 auto를 사용합니다.

  가능한 값: `1:1`, `3:4`, `4:3`, `9:16`, `16:9`, `2:3`, `3:2`, `9:19.5`, `19.5:9`, `9:20`, `20:9`, `1:2`, `2:1`, `auto`
</ParamField>

<ParamField body="model" type="string" default="&quot;grok-imagine-image&quot;">
  사용할 모델입니다. 지원: grok-imagine-image (기본값), grok-imagine-image-pro, grok-imagine-image-quality, grok-imagine-image-2.0. 지원 중단된 -beta id는 해당 GA 모델의 별칭으로 연결됩니다.
</ParamField>

<ParamField body="n" type="integer" default="1">
  생성할 이미지 수

  범위: `1` ~ `10`
</ParamField>

<ParamField body="prompt" type="string" required>
  이미지 생성을 위한 프롬프트
</ParamField>

<ParamField body="quality" type="string">
  출력 이미지의 품질입니다. grok-imagine-image-2.0의 경우 가격 등급을 선택합니다(low/medium, 기본값은 medium). 다른 모델은 현재 이 값을 무시합니다.

  가능한 값: `low`, `medium`, `high`
</ParamField>

<ParamField body="resolution" type="string" default="&quot;1k&quot;">
  생성된 이미지의 해상도입니다. 기본값은 1k입니다.

  가능한 값: `1k`, `2k`
</ParamField>

<ParamField body="response_format" type="string" default="&quot;url&quot;">
  이미지를 반환할 응답 형식입니다. url 또는 b64_json일 수 있습니다. Comfy는 아웃바운드 요청에서 이 값을 `url`로 강제 변환합니다. Comfy Router 디스패치(`POST /v2/models/xai/{model}`)와 이 `/proxy/` 경로 모두에서 마찬가지인데, 어느 쪽이든 이미지 결과가 다시 호스팅된 URL로 제공되기 때문입니다. 이 필드는 거부되지 않고 허용된 뒤 무시됩니다. 보내든 생략하든 응답은 동일합니다.

  가능한 값: `url`, `b64_json`
</ParamField>

<ParamField body="size" type="string">
  이미지의 크기 (지원되지 않음)
</ParamField>

<ParamField body="style" type="string">
  이미지의 스타일 (지원되지 않음)
</ParamField>

<ParamField body="user" type="string">
  최종 사용자를 나타내는 고유 식별자로, xAI가 악용을 모니터링하고 탐지하는 데 도움이 됩니다
</ParamField>

Router가 `GET /v2/models/xai/grok-imagine-image/openapi.json`에서 제공하는 스키마를 기반으로 생성되었으며, 이는 요청이 공급자에게 도달하기 전에 호출을 검증하는 데 사용하는 것과 동일한 문서입니다.

### 출력

<ResponseField name="block_reason" type="string">
  입력 검열로 인해 요청이 차단된 경우 차단 사유가 포함됩니다
</ResponseField>

<ResponseField name="data" type="object[]">
  생성된 이미지 객체의 목록
</ResponseField>

<ResponseField name="data[].b64_json" type="string">
  jpeg 인코딩으로 생성된 이미지의 base64 인코딩 문자열 표현입니다(response_format이 b64_json인 경우)
</ResponseField>

<ResponseField name="data[].mime_type" type="string">
  생성된 이미지의 MIME 타입입니다(예: image/png, image/jpeg, image/webp).
</ResponseField>

<ResponseField name="data[].url" type="string">
  생성된 이미지의 url입니다(response_format이 url인 경우)
</ResponseField>

<ResponseField name="usage" type="object">
  이미지 생성 요청에 대한 사용 정보
</ResponseField>

<ResponseField name="usage.cost_in_usd_ticks" type="integer">
  이 요청의 정확한 비용을 USD ticks 단위로 나타냅니다(10,000,000,000 ticks = 1 USD)
</ResponseField>

## 예시

### 입력

```json
{
  "n": 1,
  "prompt": "A single red maple leaf on a plain white background."
}
```

### 출력

```json
{
  "data": [
    {
      "mime_type": "image/jpeg",
      "url": "https://example.invalid/xai/grok-imagine-image/generated.jpg"
    },
    {
      "mime_type": "image/jpeg",
      "url": "https://example.invalid/xai/grok-imagine-image/generated-2.jpg"
    }
  ],
  "usage": {
    "cost_in_usd_ticks": 200000000
  }
}
```

<RouterCodeFooter />
