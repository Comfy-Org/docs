---
title: "Comfy Router로 Veo 2.0 Generate 001 사용하기"
description: "Comfy Router를 통해 veo/veo-2.0-generate-001을 호출합니다. 엔드포인트, 요청 형태, Router가 반환하는 응답을 설명합니다."
sidebarTitle: "Veo 2.0 Generate 001"
translationSourceHash: 7d6a780f
translationFrom: development/comfy-router/models/veo/veo-2-0-generate-001/code.mdx
translationBlockHashes:
  "_intro": 0db7d712
  "Quick start": f29101c0
  "Schema": fb4a9a14
  "Examples": cfcc0f09
---
{/* GENERATED FILE. Generated from router-schemas/veo/veo-2.0-generate-001.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/ko/comfy-router/model-code-footer.mdx";

Comfy Router가 Veo에서 제공하는 `veo/veo-2.0-generate-001`의 API 레퍼런스입니다.

## 빠른 시작

[Comfy 워크스페이스](https://platform.comfy.org/profile/api-keys)에서 키를 생성하고 `COMFY_API_KEY`로 내보내세요. Python 및 TypeScript 스니펫은 Comfy SDK(`pip install comfy-sdk`, `npm install @comfyorg/sdk`)를 사용하며, cURL 스니펫은 동일한 호출을 raw HTTP로 수행합니다.

**모델 ID:** `veo/veo-2.0-generate-001`

**엔드포인트:** `POST https://api.comfy.org/v2/models/veo/veo-2.0-generate-001`

<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "veo/veo-2.0-generate-001",
        {
            "instances": [
                {
                    "prompt": "a single red maple leaf falling onto still water, slow motion",
                },
            ],
            "parameters": {
                "durationSeconds": 6,
                "sampleCount": 1,
            },
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const { data } = await comfy.models.run("veo/veo-2.0-generate-001", {
  instances: [
    {
      prompt: "a single red maple leaf falling onto still water, slow motion",
    },
  ],
  parameters: {
    durationSeconds: 6,
    sampleCount: 1,
  },
});

console.log(data);
```

```bash cURL
curl https://api.comfy.org/v2/models/veo/veo-2.0-generate-001 \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"instances\": [{\"prompt\":\"a single red maple leaf falling onto still water, slow motion\"}], \"parameters\": {\"durationSeconds\":6,\"sampleCount\":1}}"
```
</CodeGroup>

## 스키마

### 입력

<ParamField body="instances" type="object[]">
   
</ParamField>

<ParamField body="instances[].image" type="object">
  비디오 생성을 안내하는 선택적 이미지
</ParamField>

<ParamField body="instances[].image.bytesBase64Encoded" type="string (byte)">
  형식: `byte`
</ParamField>

<ParamField body="instances[].image.gcsUri" type="string">
   
</ParamField>

<ParamField body="instances[].image.mimeType" type="string">
   
</ParamField>

<ParamField body="instances[].prompt" type="string" required>
  비디오에 대한 텍스트 설명
</ParamField>

<ParamField body="parameters" type="object">
   
</ParamField>

<ParamField body="parameters.aspectRatio" type="string">
   
</ParamField>

<ParamField body="parameters.durationSeconds" type="integer">
   
</ParamField>

<ParamField body="parameters.enhancePrompt" type="boolean">
   
</ParamField>

<ParamField body="parameters.negativePrompt" type="string">
   
</ParamField>

<ParamField body="parameters.personGeneration" type="string">
  생성된 비디오에 등장하는 사람을 제어합니다. `dont_allow`, `allow_adult`, `allowAll`은 Vertex AI 자체 표기이며, VeoGenVidRequest가 동일한 필드에 대해 게시하는 값입니다. `ALLOW`와 `BLOCK`은 이 컴포넌트가 Router에서 작성되기 이전에 이를 기반으로 생성된 클라이언트와의 호환성을 위해 유지됩니다. Veo2GenVidRequest 상단의 참고 사항을 확인하세요.

  가능한 값: `ALLOW`, `BLOCK`, `dont_allow`, `allow_adult`, `allowAll`
</ParamField>

<ParamField body="parameters.sampleCount" type="integer">
   
</ParamField>

<ParamField body="parameters.seed" type="integer">
  형식: `uint32`
</ParamField>

<ParamField body="parameters.storageUri" type="string">
  비디오를 업로드할 선택적 Cloud Storage URI
</ParamField>

Router가 `GET /v2/models/veo/veo-2.0-generate-001/openapi.json`에서 제공하는 스키마를 기반으로 생성되었으며, 이는 요청이 공급자에 도달하기 전에 호출을 검증하는 데 사용하는 동일한 문서입니다.

### 출력

<ResponseField name="done" type="boolean">
  작업이 완료되었는지 여부
</ResponseField>

<ResponseField name="error" type="object">
  작업이 실패한 경우 표시되는 오류 세부 정보
</ResponseField>

<ResponseField name="error.code" type="integer">
  gRPC 오류 코드
</ResponseField>

<ResponseField name="error.message" type="string">
  오류 메시지
</ResponseField>

<ResponseField name="name" type="string">
  작업 리소스 이름
</ResponseField>

<ResponseField name="response" type="object">
  done이 참일 때 표시되는 예측 응답
</ResponseField>

<ResponseField name="response.@type" type="string">
   
</ResponseField>

<ResponseField name="response.raiMediaFilteredCount" type="integer">
  책임 있는 AI 정책에 의해 필터링된 비디오 수
</ResponseField>

<ResponseField name="response.raiMediaFilteredReasons" type="string[]">
  책임 있는 AI 정책에 의해 비디오가 필터링된 이유
</ResponseField>

<ResponseField name="response.videos" type="object[]">
   
</ResponseField>

<ResponseField name="response.videos[].bytesBase64Encoded" type="string">
  Base64로 인코딩된 비디오 콘텐츠
</ResponseField>

<ResponseField name="response.videos[].gcsUri" type="string">
  생성된 비디오의 Cloud Storage URI
</ResponseField>

<ResponseField name="response.videos[].mimeType" type="string">
  비디오 MIME 유형(video/mp4)
</ResponseField>

## 예제

### 입력

```json
{
  "instances": [
    {
      "prompt": "a single red maple leaf falling onto still water, slow motion"
    }
  ],
  "parameters": {
    "durationSeconds": 6,
    "sampleCount": 1
  }
}
```

### 출력

```json
{
  "done": true,
  "name": "projects/example-project/locations/us-central1/publishers/google/models/veo-3.1-fast-generate-001/operations/1a2b3c4d",
  "response": {
    "@type": "type.googleapis.com/cloud.ai.large_models.vision.GenerateVideoResponse",
    "raiMediaFilteredCount": 0,
    "videos": [
      {
        "gcsUri": "https://storage.googleapis.com/EXAMPLE_BUCKET/veo/USER_ID/REQUEST_ID/sample_0.mp4",
        "mimeType": "video/mp4"
      }
    ]
  }
}
```

<RouterCodeFooter />
