---
title: "Comfy Router에서 GPT Image 2 사용하기"
description: "Comfy Router를 통해 openai/gpt-image-2를 호출합니다: 엔드포인트, 요청 형태, 그리고 Router가 반환하는 응답을 설명합니다."
sidebarTitle: "GPT Image 2"
translationSourceHash: 3f7d4810
translationFrom: development/comfy-router/models/openai/gpt-image-2/code.mdx
translationBlockHashes:
  "_intro": 846b7fa3
  "Quick start": 28ebfa70
  "Schema": ca8ae8e5
  "Examples": 7b3879cc
---
{/* GENERATED FILE. Generated from router-schemas/openai/gpt-image-2.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/ko/comfy-router/model-code-footer.mdx";

Comfy Router가 OpenAI로부터 제공하는 `openai/gpt-image-2`의 API Reference입니다.

## Quick start {#quick-start}

[Comfy 워크스페이스](https://platform.comfy.org/profile/api-keys)에서 키를 생성하고 `COMFY_API_KEY`로 내보내세요. Python 및 TypeScript 스니펫은 Comfy SDK(`pip install comfy-sdk`, `npm install @comfyorg/sdk`)를 사용하며, cURL 스니펫은 동일한 호출을 원시 HTTP로 수행합니다.

**모델 ID:** `openai/gpt-image-2`

**엔드포인트:** `POST https://api.comfy.org/v2/models/openai/gpt-image-2`

<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "openai/gpt-image-2",
        {
            "n": 1,
            "prompt": "a red circle",
            "quality": "low",
            "size": "1024x1024",
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const { data } = await comfy.models.run("openai/gpt-image-2", {
  n: 1,
  prompt: "a red circle",
  quality: "low",
  size: "1024x1024",
});

console.log(data);
```

```bash cURL
curl https://api.comfy.org/v2/models/openai/gpt-image-2 \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"n\": 1, \"prompt\": \"a red circle\", \"quality\": \"low\", \"size\": \"1024x1024\"}"
```
</CodeGroup>

## 스키마

### 입력

<ParamField body="background" type="string">
  배경 투명도

  가능한 값: `transparent`, `opaque`
</ParamField>

<ParamField body="model" type="string">
  이미지 생성에 사용할 모델 (예: gpt-image-1, gpt-image-1.5, gpt-image-2, gpt-image-2.5-flare, gpt-image-2.5-sunburst)
</ParamField>

<ParamField body="moderation" type="string">
  콘텐츠 검열 설정

  가능한 값: `low`, `auto`
</ParamField>

<ParamField body="n" type="integer">
  생성할 이미지 수 (1-10).
</ParamField>

<ParamField body="output_compression" type="integer">
  JPEG 또는 WebP의 압축 수준 (0-100)
</ParamField>

<ParamField body="output_format" type="string">
  출력 이미지의 형식

  가능한 값: `png`, `webp`, `jpeg`
</ParamField>

<ParamField body="prompt" type="string" required>
  원하는 이미지에 대한 텍스트 설명
</ParamField>

<ParamField body="quality" type="string">
  생성된 이미지의 품질

  가능한 값: `low`, `medium`, `high`, `standard`, `hd`
</ParamField>

<ParamField body="response_format" type="string">
  이미지 데이터의 응답 형식

  가능한 값: `url`, `b64_json`
</ParamField>

<ParamField body="size" type="string">
  이미지 크기 (예: 1024x1024, 1536x1024, auto)
</ParamField>

<ParamField body="style" type="string">
  이미지의 스타일. 이 작업이 허용하는 gpt-image 모델에서는 사용되지 않습니다. dall-e-3 전용 매개변수였으며, OpenAI가 2026-05-12에 해당 id들을 종료하면서 함께 폐기되었습니다.

  가능한 값: `vivid`, `natural`
</ParamField>

<ParamField body="user" type="string">
  최종 사용자 모니터링을 위한 고유 식별자
</ParamField>

Router가 `GET /v2/models/openai/gpt-image-2/openapi.json`에서 제공하는 스키마에서 생성되었으며, 이는 요청이 공급자에 도달하기 전에 호출을 검증하는 데 사용하는 것과 동일한 문서입니다.

### 출력

<ResponseField name="data" type="object[]">
   
</ResponseField>

<ResponseField name="data[].b64_json" type="string">
  Base64로 인코딩된 이미지 데이터
</ResponseField>

<ResponseField name="data[].revised_prompt" type="string">
  수정된 프롬프트
</ResponseField>

<ResponseField name="data[].url" type="string">
  이미지의 URL
</ResponseField>

<ResponseField name="usage" type="object">
   
</ResponseField>

<ResponseField name="usage.input_tokens" type="integer">
   
</ResponseField>

<ResponseField name="usage.input_tokens_details" type="object">
   
</ResponseField>

<ResponseField name="usage.input_tokens_details.image_tokens" type="integer">
   
</ResponseField>

<ResponseField name="usage.input_tokens_details.text_tokens" type="integer">
   
</ResponseField>

<ResponseField name="usage.output_tokens" type="integer">
   
</ResponseField>

<ResponseField name="usage.output_tokens_details" type="object">
   
</ResponseField>

<ResponseField name="usage.output_tokens_details.image_tokens" type="integer">
   
</ResponseField>

<ResponseField name="usage.output_tokens_details.text_tokens" type="integer">
   
</ResponseField>

<ResponseField name="usage.total_tokens" type="integer">
   
</ResponseField>

<ResponseField name="background" type="string">
  생성된 이미지의 배경이 불투명인지 투명인지 여부. fal 제공 분기에서만 채워지며, 여기서는 `opaque`를 보고합니다.
</ResponseField>

<ResponseField name="created" type="integer">
  생성이 완료된 시각의 Unix 타임스탬프(초). 현재의 epoch 값은 2^31에 충분히 가까워서, 서식을 지정하지 않은 `integer`는 많은 SDK 생성기에서 32비트 필드를 만들어내므로 `int64`로 선언되었습니다.

  형식: `int64`
</ResponseField>

<ResponseField name="output_format" type="string">
  `data[].b64_json`에 담긴 바이트의 인코딩 (예: `png`). fal 제공 분기에서 채워지며, OpenAI 제공 분기에서는 존재하지 않습니다. 이 경우 호출자가 요청한 `output_format`이 기준이 됩니다.
</ResponseField>

<ResponseField name="quality" type="string">
  생성이 실제로 실행된 품질 등급. fal 제공 분기에서 이를 확인할 수 있을 때 채워지고, 그렇지 않으면 존재하지 않습니다.
</ResponseField>

<ResponseField name="size" type="string">
  생성이 실제로 실행된 픽셀 크기로, `<width>x<height>` 형식입니다. fal 제공 분기에서 이를 확인할 수 있을 때 채워지고, 그렇지 않으면 존재하지 않습니다.
</ResponseField>

## 예제

### 입력

```json
{
  "n": 1,
  "prompt": "a red circle",
  "quality": "low",
  "size": "1024x1024"
}
```

### 출력

```json
{
  "created": 1767225600,
  "data": [
    {
      "b64_json": "PGJhc2U2ND4="
    }
  ],
  "usage": {
    "input_tokens": 12,
    "output_tokens": 1056,
    "total_tokens": 1068
  }
}
```


<RouterCodeFooter />
