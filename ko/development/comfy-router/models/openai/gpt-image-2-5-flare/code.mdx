---
title: "Comfy Router에서 GPT Image 2.5 Flare 사용하기"
description: "Comfy Router를 통해 openai/gpt-image-2.5-flare를 호출합니다. 엔드포인트, 요청 형태, 그리고 Router가 반환하는 응답을 설명합니다."
sidebarTitle: "GPT Image 2.5 Flare"
translationSourceHash: a3bd711d
translationFrom: development/comfy-router/models/openai/gpt-image-2-5-flare/code.mdx
translationBlockHashes:
  "_intro": c2dfdd54
  "Quick start": 36a7ad58
  "Schema": eb1113b3
  "Examples": 7b3879cc
---
{/* 생성된 파일. `pnpm code-pages:gen`이 router-schemas/openai/gpt-image-2.5-flare.json에서 생성했습니다. */}

import RouterCodeFooter from "/snippets/ko/comfy-router/model-code-footer.mdx";

`openai/gpt-image-2.5-flare`에 대한 API 레퍼런스이며, OpenAI가 Comfy Router를 통해 제공합니다.

## 빠른 시작

[Comfy 워크스페이스](https://platform.comfy.org/profile/api-keys)에서 키를 생성하고 `COMFY_API_KEY`로 내보내세요. Python 및 TypeScript 스니펫은 Comfy SDK(`pip install comfy-sdk`, `npm install @comfyorg/sdk`)를 사용하며, cURL 스니펫은 동일한 호출을 raw HTTP로 수행한 것입니다.

**Model ID:** `openai/gpt-image-2.5-flare`

**Endpoint:** `POST https://api.comfy.org/v2/models/openai/gpt-image-2.5-flare`

<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "openai/gpt-image-2.5-flare",
        {
            "n": 1,
            "prompt": "a red circle",
            "quality": "low",
            "size": "1024x1024",
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const { data } = await comfy.models.run("openai/gpt-image-2.5-flare", {
  n: 1,
  prompt: "a red circle",
  quality: "low",
  size: "1024x1024",
});

console.log(data);
```

```bash cURL
curl https://api.comfy.org/v2/models/openai/gpt-image-2.5-flare \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"n\": 1, \"prompt\": \"a red circle\", \"quality\": \"low\", \"size\": \"1024x1024\"}"
```
</CodeGroup>

## 스키마

### 입력

<ParamField body="background" type="string">
  배경 투명도

  가능한 값: `transparent`, `opaque`
</ParamField>

<ParamField body="model" type="string">
  이미지 생성에 사용할 모델 (예: gpt-image-1, gpt-image-1.5, gpt-image-2, gpt-image-2.5-flare, gpt-image-2.5-sunburst)
</ParamField>

<ParamField body="moderation" type="string">
  콘텐츠 검열 설정

  가능한 값: `low`, `auto`
</ParamField>

<ParamField body="n" type="integer">
  생성할 이미지 수 (1-10).
</ParamField>

<ParamField body="output_compression" type="integer">
  JPEG 또는 WebP의 압축 수준 (0-100)
</ParamField>

<ParamField body="output_format" type="string">
  출력 이미지의 형식

  가능한 값: `png`, `webp`, `jpeg`
</ParamField>

<ParamField body="prompt" type="string" required>
  원하는 이미지에 대한 텍스트 설명
</ParamField>

<ParamField body="quality" type="string">
  생성된 이미지의 품질

  가능한 값: `low`, `medium`, `high`, `standard`, `hd`
</ParamField>

<ParamField body="response_format" type="string">
  이미지 데이터의 응답 형식

  가능한 값: `url`, `b64_json`
</ParamField>

<ParamField body="size" type="string">
  이미지 크기 (예: 1024x1024, 1536x1024, auto)
</ParamField>

<ParamField body="style" type="string">
  이미지의 스타일. 이 작업이 허용하는 gpt-image 모델에서는 사용되지 않습니다. dall-e-3 전용 파라미터였으며, OpenAI가 2026-05-12에 해당 id들을 종료하면서 폐기되었습니다.

  가능한 값: `vivid`, `natural`
</ParamField>

<ParamField body="user" type="string">
  최종 사용자 모니터링을 위한 고유 식별자
</ParamField>

Router가 `GET /v2/models/openai/gpt-image-2.5-flare/openapi.json`에서 제공하는 스키마에서 생성되었으며, 이는 요청이 공급자에 도달하기 전에 호출을 검증할 때 사용하는 것과 동일한 문서입니다.

### 출력

<ResponseField name="data" type="object[]">
   
</ResponseField>

<ResponseField name="data[].b64_json" type="string">
  Base64로 인코딩된 이미지 데이터
</ResponseField>

<ResponseField name="data[].revised_prompt" type="string">
  수정된 프롬프트
</ResponseField>

<ResponseField name="data[].url" type="string">
  이미지의 URL
</ResponseField>

<ResponseField name="usage" type="object">
   
</ResponseField>

<ResponseField name="usage.input_tokens" type="integer">
   
</ResponseField>

<ResponseField name="usage.input_tokens_details" type="object">
   
</ResponseField>

<ResponseField name="usage.input_tokens_details.image_tokens" type="integer">
   
</ResponseField>

<ResponseField name="usage.input_tokens_details.text_tokens" type="integer">
   
</ResponseField>

<ResponseField name="usage.output_tokens" type="integer">
   
</ResponseField>

<ResponseField name="usage.output_tokens_details" type="object">
   
</ResponseField>

<ResponseField name="usage.output_tokens_details.image_tokens" type="integer">
   
</ResponseField>

<ResponseField name="usage.output_tokens_details.text_tokens" type="integer">
   
</ResponseField>

<ResponseField name="usage.total_tokens" type="integer">
   
</ResponseField>

<ResponseField name="background" type="string">
  생성된 이미지의 배경이 불투명인지 투명인지 여부. fal이 제공하는 분기에서만 채워지며, 해당 분기는 `opaque`를 보고합니다.
</ResponseField>

<ResponseField name="created" type="integer">
  생성이 완료된 시점의 Unix 타임스탬프(초). 현재의 epoch 값이 2^31에 충분히 가까워 서식을 지정하지 않은 `integer`가 많은 SDK 생성기에서 32비트 필드를 만들기 때문에 `int64`로 선언되었습니다.

  형식: `int64`
</ResponseField>

<ResponseField name="output_format" type="string">
  `data[].b64_json`에 있는 바이트의 인코딩 (예: `png`). fal이 제공하는 분기에서 채워지며, 호출자가 요청한 `output_format`이 우선하는 OpenAI 제공 분기에서는 존재하지 않습니다.
</ResponseField>

<ResponseField name="quality" type="string">
  생성이 실제로 실행된 품질 등급. fal이 제공하는 분기에서 이를 확인할 수 있을 때 채워지며, 그렇지 않으면 존재하지 않습니다.
</ResponseField>

<ResponseField name="size" type="string">
  생성이 실제로 실행된 픽셀 크기로, `<width>x<height>` 형식입니다. fal이 제공하는 분기에서 이를 확인할 수 있을 때 채워지며, 그렇지 않으면 존재하지 않습니다.
</ResponseField>

## 예제

### 입력

```json
{
  "n": 1,
  "prompt": "a red circle",
  "quality": "low",
  "size": "1024x1024"
}
```

### 출력

```json
{
  "created": 1767225600,
  "data": [
    {
      "b64_json": "PGJhc2U2ND4="
    }
  ],
  "usage": {
    "input_tokens": 12,
    "output_tokens": 1056,
    "total_tokens": 1068
  }
}
```


<RouterCodeFooter />
