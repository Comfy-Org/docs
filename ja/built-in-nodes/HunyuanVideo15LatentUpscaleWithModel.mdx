---
title: "HunyuanVideo15LatentUpscaleWithModel - ComfyUI Built-in Node Documentation"
description: "Hunyuan Video 15 Latent Upscale With Model ノードは、潜在画像表現の解像度を高めます。"
sidebarTitle: "HunyuanVideo15LatentUpscaleWithModel"
icon: "circle"
mode: wide
---

Hunyuan Video 15 Latent Upscale With Model ノードは、潜在画像表現の解像度を高めます。まず、選択した補間方法を使用して潜在サンプルを指定サイズにアップスケールし、その後、専用の Hunyuan Video 1.5 アップスケールモデルを使用してアップスケール結果を精緻化し、品質を向上させます。

## 入力

| パラメータ | 説明 | データ型 | 必須 | 範囲 |
| --- | --- | --- | --- | --- |
| `モデル` | アップスケールされたサンプルを精緻化するために使用される Hunyuan Video 1.5 潜在アップスケールモデルです。 | LATENT_UPSCALE_MODEL | はい | N/A |
| `サンプル` | アップスケールされる潜在画像表現です。 | LATENT | はい | N/A |
| `アップスケール方法` | 初期アップスケールステップで使用される補間アルゴリズムです（デフォルト: `"bilinear"`）。 | COMBO | いいえ | `"nearest-exact"`<br />`"bilinear"`<br />`"area"`<br />`"bicubic"`<br />`"bislerp"` |
| `幅` | アップスケールされた潜在表現のターゲット幅（ピクセル単位）です。0 を指定すると、ターゲットの高さと元のアスペクト比に基づいて幅が自動的に計算されます。最終的な出力幅は 16 の倍数になります（デフォルト: 1280）。 | INT | いいえ | 0〜16384 (step: 8) |
| `高さ` | アップスケールされた潜在表現のターゲット高さ（ピクセル単位）です。0 を指定すると、ターゲットの幅と元のアスペクト比に基づいて高さが自動的に計算されます。最終的な出力高さは 16 の倍数になります（デフォルト: 720）。 | INT | いいえ | 0〜16384 (step: 8) |
| `切り抜き` | アップスケールされた潜在表現をターゲット寸法に合わせるためのクロップ方法を決定します。 | COMBO | いいえ | `"disabled"`<br />`"center"` |

**寸法に関する注意：** `width` と `height` の両方が 0 に設定されている場合、ノードは入力 `samples` を変更せずに返します。いずれか一方の寸法だけが 0 の場合、もう一方の寸法は元のアスペクト比を維持するように計算されます。最終的な寸法は常に少なくとも 64 ピクセルになり、16 で割り切れるように調整されます。

## 出力

| 出力名 | 説明 | データ型 |
| --- | --- | --- |
| `LATENT` | アップスケールされ、モデルによって精緻化された潜在画像表現です。 | LATENT |

> このドキュメントは AI によって生成されました。エラーを見つけた場合や改善のご提案がある場合は、ぜひ貢献してください！ [GitHub で編集](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/HunyuanVideo15LatentUpscaleWithModel/ja.md)

---
**Source fingerprint (SHA-256):** `71af49eefb010aadd30f8699b751ad901b1ee385d6cbeedd3a83995a1a623516`
