---
title: "MeshyTextureNode - ComfyUI Built-in Node Documentation"
description: "The Meshy: Texture ノードは、AI が生成したテクスチャを 3D モデルに適用します。"
sidebarTitle: "MeshyTextureNode"
icon: "circle"
mode: wide
---

The Meshy: Texture ノードは、AI が生成したテクスチャを 3D モデルに適用します。以前の Meshy 3D 生成または変換ノードからタスク ID を受け取り、テキスト説明または参照画像のいずれかを使用して、モデル用の新しいテクスチャを作成します。このノードは、テクスチャ付きモデルを GLB および FBX ファイル形式で出力します。

## 入力

| パラメータ | 説明 | データ型 | 必須 | 範囲 |
| --- | --- | --- | --- | --- |
| `モデル` | テクスチャリングに使用する AI モデルのバージョン。 | COMBO | 必須 | `"meshy-7"`<br />`"meshy-6"`<br />`"latest"` |
| `meshy_task_id` | 以前の Meshy 3D 生成または変換タスクからの一意の識別子（タスク ID）。テクスチャを適用するベースとなる 3D モデルを提供します。 | MESHY_TASK_ID | 必須 | - |
| `元のUVを使用` | 新しい UV を生成せずに、モデルの元の UV を使用します。有効にすると（デフォルト: `True`）、Meshy はアップロードされたモデルの既存テクスチャを保持します。モデルに元の UV がない場合、出力の品質が低下する可能性があります。これは上級者向けのオプションです。 | BOOLEAN | 任意 | true / false |
| `PBR` | テクスチャリングされたモデルに物理ベースレンダリング（PBR）マテリアル出力を有効にします（デフォルト: `False`）。これは上級者向けのオプションです。 | BOOLEAN | 任意 | true / false |
| `テキストスタイルプロンプト` | テキストでオブジェクトの希望するテクスチャスタイルを説明します。最大 600 文字です。`image_style` と同時に使用することはできません。 | STRING | 任意 | - |
| `イメージスタイル` | テクスチャリング処理をガイドする 2D 画像。`text_style_prompt` と同時に使用することはできません。 | IMAGE | 任意 | - |
| `texture_resolution` | ベースカラーテクスチャの解像度。解像度が高いほど、より詳細な表面をキャプチャできます。 | COMBO | 必須 | `"2k"`<br />`"4k"`<br />`"8k"` |

**パラメータ制約:**

* `text_style_prompt` または `image_style` のいずれかを指定する必要がありますが、両方を同時に指定することはできません。
* `text_style_prompt` は最大 600 文字に制限されています。

## 出力

| 出力名 | 説明 | データ型 |
| --- | --- | --- |
| `モデルファイル` | 生成された GLB モデルのファイル名。この出力は後方互換性のためだけに提供されています。 | STRING |
| `meshy_task_id` | このテクスチャリングジョブの一意のタスク識別子。結果を参照するために使用できます。 | MESHY_TASK_ID |
| `GLB` | GLB ファイル形式で保存されたテクスチャ付き 3D モデル。 | FILE3DGLB |
| `FBX` | FBX ファイル形式で保存されたテクスチャ付き 3D モデル。 | FILE3DFBX |

> このドキュメントは AI によって生成されました。エラーを見つけた場合や改善のご提案がある場合は、ぜひ貢献してください！ [GitHub で編集](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/MeshyTextureNode/ja.md)

---
**Source fingerprint (SHA-256):** `30d51f6efe3602f27d99706840c974baf2c4397a4f9a191f3478e7eff372e319`
