---
title: "SeedVR2TemporalChunk - ComfyUI Built-in Node Documentation"
description: "このノードは、SeedVR2ビデオ潜在表現を、利用可能なVRAM内で一度に処理できる小さな時間的チャンクに分割します。"
sidebarTitle: "SeedVR2TemporalChunk"
icon: "circle"
mode: wide
---

このノードは、SeedVR2ビデオ潜在表現を、利用可能なVRAM内で一度に処理できる小さな時間的チャンクに分割します。GPUメモリに基づいて最適なチャンクサイズを自動計算するか、手動でチャンクサイズを指定でき、処理のためにチャンクを順番に出力します。

## 入力

| パラメータ | 説明 | データ型 | 必須 | 範囲 |
|-----------|-------------|-----------|----------|-------|
| `latent` | 分割するVAEエンコード済みSeedVR2潜在表現。SeedVR2の期待される潜在チャンネル数を持つ5次元テンソル（B, C, T, H, W）である必要があります。 | LATENT | はい | - |
| `temporal_overlap` | 隣接するチャンク間で共有され、マージ時にクロスフェードされる潜在フレーム数。0はオーバーラップなし（デフォルト：0）。有効なオーバーラップは、チャンクの潜在フレーム数より1少ない値に制限されます。 | INT | いいえ | 0～16384 |
| `chunking_mode` | Manualはframes_per_chunkをそのまま使用。autoは空きVRAMに収まる最大のチャンクを自動予測。 | COMBO | はい | "auto"<br />"manual" |

`chunking_mode`が"manual"に設定されている場合、追加のパラメータが利用可能になります：

| パラメータ | 説明 | データ型 | 必須 | 範囲 |
|-----------|-------------|-----------|----------|-------|
| `frames_per_chunk` | 時間的チャンクあたりのピクセルフレーム数（4n+1：1、5、9、13、17、21...）。UIは4ずつステップします（デフォルト：21）。 | INT | はい | 1～16384 |

注：`frames_per_chunk`パラメータは、`chunking_mode`が"manual"に設定されている場合にのみ表示されます。値は`(frames_per_chunk - 1) % 4 == 0`の式を満たす必要があり、つまり1、5、9、13、17、21などのいずれかでなければなりません。入力潜在表現は5次元で、SeedVR2の期待される潜在チャンネル数を含む必要があります。そうでない場合、ノードはエラーを発生させます。潜在表現内の総ピクセルフレーム数が選択したチャンクサイズ（または自動計算されたサイズ）以下である場合、ノードは元の潜在表現をオーバーラップなしの単一チャンクとして返します。

## 出力

| 出力名 | 説明 | データ型 |
|-----------|-------------|-----------|
| `latents` | 順番に並べられた時間的チャンク。 | LATENT |
| `temporal_overlap` | 隣接チャンク間の実効的な潜在フレームオーバーラップ。Merge SeedVR2 Latents用。 | INT |

> このドキュメントは AI によって生成されました。エラーを見つけた場合や改善のご提案がある場合は、ぜひ貢献してください！ [GitHub で編集](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/SeedVR2TemporalChunk/ja.md)

---
**Source fingerprint (SHA-256):** `40af2b690f74555efbe38f4cf76825417f52735ce90296a9ad662f78cfe4c6bf`
