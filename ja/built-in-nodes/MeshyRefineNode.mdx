---
title: "MeshyRefineNode - ComfyUI Built-in Node Documentation"
description: "「Meshy: Refine Draft Model」ノードは、以前の Meshy タスクで生成された 3D ドラフトモデルを受け取り、それを改善し、オプションでテキストプロンプトまたは参照画像を使用してテクスチャを追加します。"
sidebarTitle: "MeshyRefineNode"
icon: "circle"
mode: wide
---

「Meshy: Refine Draft Model」ノードは、以前の Meshy タスクで生成された 3D ドラフトモデルを受け取り、それを改善し、オプションでテキストプロンプトまたは参照画像を使用してテクスチャを追加します。このノードは、リファインメントジョブを Meshy API に送信し、タスクが完了すると完成したモデルを GLB および FBX ファイルとして返します。

## 入力

| パラメータ | 説明 | データ型 | 必須 | 範囲 |
|-----------|-------------|-----------|----------|-------|
| `model` | ドラフトモデルのリファインに使用する AI モデル。 | COMBO | はい | `"meshy-7"`<br />`"meshy-6"`<br />`"latest"` |
| `meshy_task_id` | リファインしたいドラフトモデルの一意のタスク ID。 | MESHY_TASK_ID | はい | - |
| `enable_pbr` | ベースカラーに加えて PBR マップ（メタリック、ラフネス、ノーマル）を生成します。注意: Sculpture スタイルを使用する場合は false に設定してください。Sculpture スタイルは独自の PBR マップを生成するためです。（デフォルト: False） | BOOLEAN | はい | - |
| `texture_prompt` | テクスチャ処理をガイドするテキストプロンプトを指定します。最大 600 文字です。`texture_image` とは同時に使用できません。（デフォルト: 空文字列） | STRING | はい | - |
| `texture_image` | `texture_image` と `texture_prompt` は、同時に使用できるのはいずれか一方のみです。 | IMAGE | いいえ | - |
| `texture_resolution` | ベースカラーテクスチャの解像度。解像度が高いほど、より詳細なサーフェスのディテールを捉えます。 | COMBO | はい | `"2k"`<br />`"4k"`<br />`"8k"` |

**注意:** `texture_prompt` 入力と `texture_image` 入力は相互に排他的です。同じ操作でテクスチャ用のテキストプロンプトと画像の両方を提供することはできません。

## 出力

| 出力名 | 説明 | データ型 |
|-------------|-------------|-----------|
| `model_file` | 生成された GLB モデルのファイル名。（後方互換性のためだけに存在します） | STRING |
| `meshy_task_id` | 送信されたリファインメントジョブの一意のタスク ID。 | MESHY_TASK_ID |
| `GLB` | GLB 形式の最終リファイン済み 3D モデル。 | FILE3DGLB |
| `FBX` | FBX 形式の最終リファイン済み 3D モデル。 | FILE3DFBX |

> このドキュメントは AI によって生成されました。エラーを見つけた場合や改善のご提案がある場合は、ぜひ貢献してください！ [GitHub で編集](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/MeshyRefineNode/ja.md)

---
**Source fingerprint (SHA-256):** `73c9d712c4fd9fdd2792600ce874916ce9447d386407353c886f624641fa0e0f`
