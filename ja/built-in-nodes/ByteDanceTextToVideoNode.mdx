---
title: "ByteDanceTextToVideoNode - ComfyUI Built-in Node Documentation"
description: "The ByteDance Text to Video ノードは、テキストプロンプトに基づき、ByteDance モデルを API 経由で使用して動画を生成します。"
sidebarTitle: "ByteDanceTextToVideoNode"
icon: "circle"
mode: wide
---

The ByteDance Text to Video ノードは、テキストプロンプトに基づき、ByteDance モデルを API 経由で使用して動画を生成します。プロンプトを入力し、モデル、解像度、アスペクト比、長さなどの設定を選択すると、ノードが生成リクエストを送信し、生成された動画を返します。

## 入力

| パラメータ | 説明 | データ型 | 必須 | 範囲 |
|-----------|-------------|-----------|----------|-------|
| `モデル` | 動画の生成に使用する ByteDance モデル（デフォルト: `"seedance-1-0-pro-fast-251015"`）。 | COMBO | はい | `"seedance-1-5-pro-251215"`<br />`"seedance-1-0-pro-250528"`<br />`"seedance-1-0-pro-fast-251015"` |
| `プロンプト` | 動画の生成に使用するテキストプロンプト。 | STRING | はい | - |
| `解像度` | 出力動画の解像度。 | COMBO | はい | `"480p"`<br />`"720p"`<br />`"1080p"` |
| `アスペクト比` | 出力動画のアスペクト比。 | COMBO | はい | `"16:9"`<br />`"4:3"`<br />`"1:1"`<br />`"3:4"`<br />`"9:16"`<br />`"21:9"` |
| `長さ` | 出力動画の長さ（秒）（デフォルト: 5）。 | INT | はい | 3〜12 |
| `シード` | 生成に使用するシード（デフォルト: 0）。 | INT | いいえ | 0〜2147483647 |
| `カメラ固定` | カメラを固定するかどうかを指定します。プラットフォームはカメラを固定する指示をプロンプトに追加しますが、実際の効果は保証されません（デフォルト: False）。 | BOOLEAN | いいえ | - |
| `透かし` | 動画に「AI 生成」の透かしを追加するかどうか（デフォルト: False）。 | BOOLEAN | いいえ | - |
| `generate_audio` | このパラメータは、`seedance-1-5-pro-251215` 以外のモデルでは無視されます（デフォルト: False）。 | BOOLEAN | いいえ | - |

**パラメータの制約事項:**

- `prompt` パラメータには、空白を除去した後に少なくとも1文字が含まれている必要があります。
- `prompt` パラメータには、次のテキストパラメータを含めることはできません: "resolution", "ratio", "duration", "seed", "camerafixed", "watermark"。
- ノードは、選択した `resolution`、`aspect_ratio`、`duration`、`seed`、`camera_fixed`、`watermark` の各設定をプロンプトテキストに追加して、最終的なプロンプトを構築します。
- `duration` パラメータは 3 秒から 12 秒の範囲に制限されます。`seedance-1-5-pro-251215` モデルの場合、サポートされる最小の長さは 4 秒です。
- `seed` パラメータは 0 から 2,147,483,647 の値を受け入れます。
- `generate_audio` パラメータは、`model` が `seedance-1-5-pro-251215` に設定されている場合にのみ効果があり、他のすべてのモデルでは無視されます。

## 出力

| 出力名 | 説明 | データ型 |
|-------------|-------------|-----------|
| `output` | 生成された動画ファイル | VIDEO |

> このドキュメントは AI によって生成されました。エラーを見つけた場合や改善のご提案がある場合は、ぜひ貢献してください！ [GitHub で編集](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/ByteDanceTextToVideoNode/ja.md)

---
**Source fingerprint (SHA-256):** `83b805b63e59a76cae378b0407b409e1bfe900677ef1e01fc836fede47283eee`
