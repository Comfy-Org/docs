---
title: "Comfy Router で LTX 2.5 Pro を使用する"
description: "Comfy Router 経由で ltx/ltx-2-5-pro を呼び出します: エンドポイント、リクエストの形状、Router が返すレスポンスについて。"
sidebarTitle: "LTX 2.5 Pro"
translationSourceHash: 29caa5e3
translationFrom: development/comfy-router/models/ltx/ltx-2-5-pro/code.mdx
---

{/* GENERATED FILE. Generated from router-schemas/ltx/ltx-2-5-pro.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/ja/comfy-router/model-code-footer.mdx";

`ltx/ltx-2-5-pro` の API リファレンスです。Comfy Router が LTX から提供します。

## リクエストのセットアップ

[Comfy ワークスペース](https://platform.comfy.org/profile/api-keys)でキーを作成し、`COMFY_API_KEY` としてエクスポートします。Python では `pip install comfy-sdk` を実行します。TypeScript では `npm install @comfyorg/sdk` を実行します。cURL は生の HTTP を使用します。

**モデル ID:** `ltx/ltx-2-5-pro`

**エンドポイント:** `POST https://api.comfy.org/v2/models/ltx/ltx-2-5-pro`

<Note>
Router はこのモデル用の作成済み入力スキーマを公開していないため、スニペットを生成する元となるリクエスト例がありません。Router はボディをそのまま LTX に転送します。LTX 自身の API ドキュメントに従ってボディを構築し、[Router クイックスタート](/ja/development/comfy-router/quickstart)を使って送信してください。
</Note>

## スキーマ

### 入力

<Note>
Router はこのモデル用の作成済み入力スキーマをまだ公開していません。`GET /v2/models/ltx/ltx-2-5-pro/openapi.json` は `x-comfy-input-schema-authored: false` を持つオープンなオブジェクトを返します。Router はボディをそのまま LTX に転送するため、リクエストのフィールドについては LTX 自身の API ドキュメントが正式な情報源となり、Router はモデル固有の入力検証を行いません。プロバイダー側の検証は引き続き適用されます。
</Note>

### 出力

<ResponseField name="completed_at" type="string">
  ジョブの完了タイムスタンプ（ISO 8601）
</ResponseField>

<ResponseField name="created_at" type="string">
  ジョブの作成タイムスタンプ（ISO 8601）
</ResponseField>

<ResponseField name="error" type="object">
  status が failed の場合に存在します
</ResponseField>

<ResponseField name="error.message" type="string">
   
</ResponseField>

<ResponseField name="error.type" type="string">
   
</ResponseField>

<ResponseField name="id" type="string">
  一意のジョブ識別子
</ResponseField>

<ResponseField name="result" type="object">
  status が completed の場合に存在します。出力 URL は完了から 24 時間後に失効します
</ResponseField>

<ResponseField name="result.video_url" type="string">
  生成済みビデオの URL
</ResponseField>

<ResponseField name="status" type="string">
  ジョブのステータス（pending、processing、completed、failed）
</ResponseField>

## 例

### 出力

```json
{
  "completed_at": "2026-01-01T00:02:10Z",
  "created_at": "2026-01-01T00:00:00Z",
  "id": "3f7a1b28-5c0d-4e91-8a6f-1b2c3d4e5f60",
  "result": {
    "video_url": "https://example.invalid/ltx/generated.mp4"
  },
  "status": "completed"
}
```


<RouterCodeFooter />