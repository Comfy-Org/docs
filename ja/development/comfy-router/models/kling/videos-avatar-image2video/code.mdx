---
title: "Comfy Router で Videos Avatar Image 2video を使用する"
description: "Comfy Router 経由で kling/videos-avatar-image2video を呼び出します: エンドポイント、リクエスト形状、Router が返すレスポンスについて説明します。"
sidebarTitle: "Videos Avatar Image 2video"
translationSourceHash: 2d32d740
translationFrom: development/comfy-router/models/kling/videos-avatar-image2video/code.mdx
translationBlockHashes:
  "_intro": 6e4b5479
  "Quick start": f4efb7f6
  "Schema": eeb06fe1
  "Examples": ce6803dd
---
{/* GENERATED FILE. Generated from router-schemas/kling/videos-avatar-image2video.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/ja/comfy-router/model-code-footer.mdx";

`kling/videos-avatar-image2video` の API リファレンス。Kling から Comfy Router によって提供されます。

## クイックスタート

[Comfy ワークスペース](https://platform.comfy.org/profile/api-keys)でキーを作成し、`COMFY_API_KEY` としてエクスポートします。Python と TypeScript のスニペットは Comfy SDK（`pip install comfy-sdk`、`npm install @comfyorg/sdk`）を使用しており、cURL のスニペットは同じ呼び出しを生の HTTP で実行するものです。

**モデル ID:** `kling/videos-avatar-image2video`

**エンドポイント:** `POST https://api.comfy.org/v2/models/kling/videos-avatar-image2video`

<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "kling/videos-avatar-image2video",
        {
            "image": "https://example.invalid/kling/avatar.png",
            "mode": "std",
            "prompt": "The presenter smiles and gestures towards the camera.",
            "sound_file": "https://example.invalid/kling/voice.mp3",
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const { data } = await comfy.models.run("kling/videos-avatar-image2video", {
  image: "https://example.invalid/kling/avatar.png",
  mode: "std",
  prompt: "The presenter smiles and gestures towards the camera.",
  sound_file: "https://example.invalid/kling/voice.mp3",
});

console.log(data);
```

```bash cURL
curl https://api.comfy.org/v2/models/kling/videos-avatar-image2video \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"image\": \"https://example.invalid/kling/avatar.png\", \"mode\": \"std\", \"prompt\": \"The presenter smiles and gestures towards the camera.\", \"sound_file\": \"https://example.invalid/kling/voice.mp3\"}"
```
</CodeGroup>

## Schema

### 入力

<ParamField body="audio_id" type="string">
  TTS API で生成されたオーディオ ID。過去 30 日以内に生成された 2～300 秒のオーディオのみをサポートします。audio_id または sound_file のいずれかを指定する必要があります (相互排他)。
</ParamField>

<ParamField body="callback_url" type="string (uri)">
  このタスクの結果を受け取るコールバック通知アドレス。

  形式: `uri`
</ParamField>

<ParamField body="external_task_id" type="string">
  カスタムタスク ID。単一のユーザーアカウント内で一意である必要があります。
</ParamField>

<ParamField body="image" type="string" required>
  アバター参照画像。Base64 エンコードまたは画像 URL をサポートします。対応形式: .jpg/.jpeg/.png。最大 10MB、幅/高さの最小 300px、アスペクト比は 1:2.5 から 2.5:1 の間。
</ParamField>

<ParamField body="mode" type="string" default="&quot;std&quot;">
  ビデオ生成モード。std: スタンダードモード (コスト効率重視)、pro: プロフェッショナルモード (より長い再生時間、高品質)。

  指定可能な値: `std`, `pro`
</ParamField>

<ParamField body="prompt" type="string">
  ポジティブテキストプロンプト。アバターのアクション、感情、カメラの動きを定義できます。
</ParamField>

<ParamField body="sound_file" type="string">
  サウンドファイル。Base64 エンコードされたオーディオまたはアクセス可能なオーディオ URL をサポートします。対応形式: .mp3/.wav/.m4a/.aac (最大 5MB)、2～300 秒。audio_id または sound_file のいずれかを指定する必要があります (相互排他)。
</ParamField>

<ParamField body="watermark_info" type="object">
   
</ParamField>

<ParamField body="watermark_info.enabled" type="boolean">
  ウォーターマーク付きの結果を同時に生成するかどうか。
</ParamField>

Router が `GET /v2/models/kling/videos-avatar-image2video/openapi.json` で提供するスキーマから生成されており、リクエストがプロバイダーに到達する前に呼び出しを検証する際に使用されるドキュメントと同じものです。

### 出力

<ResponseField name="code" type="integer">
  エラーコード
</ResponseField>

<ResponseField name="data" type="object">
   
</ResponseField>

<ResponseField name="data.created_at" type="integer">
  タスク作成時間、Unix タイムスタンプ (ミリ秒)
</ResponseField>

<ResponseField name="data.final_unit_deduction" type="string">
  タスクの差し引き単位
</ResponseField>

<ResponseField name="data.task_id" type="string">
  タスク ID
</ResponseField>

<ResponseField name="data.task_info" type="object">
   
</ResponseField>

<ResponseField name="data.task_info.external_task_id" type="string">
   
</ResponseField>

<ResponseField name="data.task_result" type="object">
   
</ResponseField>

<ResponseField name="data.task_result.videos" type="object[]">
   
</ResponseField>

<ResponseField name="data.task_result.videos[].duration" type="string">
  ビデオの合計再生時間 (秒)
</ResponseField>

<ResponseField name="data.task_result.videos[].id" type="string">
  生成されたビデオ ID
</ResponseField>

<ResponseField name="data.task_result.videos[].url" type="string (uri)">
  生成されたビデオの URL

  形式: `uri`
</ResponseField>

<ResponseField name="data.task_result.videos[].watermark_url" type="string (uri)">
  ウォーターマーク付きで生成されたビデオの URL、ホットリンク保護形式

  形式: `uri`
</ResponseField>

<ResponseField name="data.task_status" type="string">
  タスクステータス

  指定可能な値: `submitted`, `processing`, `succeed`, `failed`
</ResponseField>

<ResponseField name="data.task_status_msg" type="string">
  タスクステータス情報。タスクが失敗した場合は失敗理由を表示します。
</ResponseField>

<ResponseField name="data.updated_at" type="integer">
  タスク更新時間、Unix タイムスタンプ (ミリ秒)
</ResponseField>

<ResponseField name="data.watermark_info" type="object">
   
</ResponseField>

<ResponseField name="data.watermark_info.enabled" type="boolean">
   
</ResponseField>

<ResponseField name="message" type="string">
  エラーメッセージ
</ResponseField>

<ResponseField name="request_id" type="string">
  リクエスト ID
</ResponseField>

## 例

### 入力

```json
{
  "image": "https://example.invalid/kling/avatar.png",
  "mode": "std",
  "prompt": "The presenter smiles and gestures towards the camera.",
  "sound_file": "https://example.invalid/kling/voice.mp3"
}
```

### 出力

```json
{
  "code": 0,
  "data": {
    "created_at": 1798761600000,
    "task_id": "kling-avatar-task-8b7a6c5d4e3f",
    "task_result": {
      "videos": [
        {
          "duration": "10",
          "id": "kling-video-1c2d3e4f5a6b",
          "url": "https://example.invalid/kling/videos-avatar-image2video/generated.mp4"
        }
      ]
    },
    "task_status": "succeed",
    "task_status_msg": "",
    "updated_at": 1798761900000
  },
  "message": "SUCCEED",
  "request_id": "b41d7c58-9e26-4f03-a7d1-5c8e0b3f2a69"
}
```


<RouterCodeFooter />
