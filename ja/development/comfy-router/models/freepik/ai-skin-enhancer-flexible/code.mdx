---
title: "Comfy Router で AI Skin Enhancer Flexible を使う"
description: "Comfy Router 経由で freepik/ai-skin-enhancer-flexible を呼び出します: エンドポイント、リクエストの形状、Router が返すレスポンスについて説明します。"
sidebarTitle: "AI Skin Enhancer Flexible"
translationSourceHash: 5a853777
translationFrom: development/comfy-router/models/freepik/ai-skin-enhancer-flexible/code.mdx
translationBlockHashes:
  "_intro": d6c2cb7e
  "Quick start": 07a42926
  "Schema": 70d4c10f
  "Examples": 4255df63
---
{/* 生成済みファイル。router-schemas/freepik/ai-skin-enhancer-flexible.json から `pnpm code-pages:gen` によって生成されました。 */}

import RouterCodeFooter from "/snippets/ja/comfy-router/model-code-footer.mdx";

`freepik/ai-skin-enhancer-flexible` のAPIリファレンスです。Comfy Router が Freepik から提供しています。

## クイックスタート

[Comfy ワークスペース](https://platform.comfy.org/profile/api-keys?onboarding=router)でキーを作成し、`COMFY_API_KEY` としてエクスポートします。Python、TypeScript、Swift のスニペットは Comfy SDK（`pip install comfy-sdk`、`npm install @comfyorg/sdk`、および [`ComfySwiftSDK`](https://github.com/Comfy-Org/comfy-swift-sdk) Swift パッケージ）を使用しており、cURL のスニペットは生の HTTP で同じ呼び出しを行います。

**モデル ID:** `freepik/ai-skin-enhancer-flexible`

**エンドポイント:** `POST https://api.comfy.org/v2/models/freepik/ai-skin-enhancer-flexible`

<Tabs>
  <Tab title="Wait for the result">
<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "freepik/ai-skin-enhancer-flexible",
        {
            "image": "https://img.magnific.com/free-photo/portrait-woman_395237-33.jpg?w=740&q=80",
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const { data } = await comfy.models.run("freepik/ai-skin-enhancer-flexible", {
  image: "https://img.magnific.com/free-photo/portrait-woman_395237-33.jpg?w=740&q=80",
});

console.log(data);
```

```swift Swift
import Foundation
import ComfySwiftSDK

// Reads COMFY_API_KEY from the environment.
// The SDK mints an idempotency key per call and reuses it for automatic retries.
let client = ComfyCloudClient(apiKey: ProcessInfo.processInfo.environment["COMFY_API_KEY"]!)
let result = try await client.models.run(
    "freepik/ai-skin-enhancer-flexible",
    input: [
        "image": "https://img.magnific.com/free-photo/portrait-woman_395237-33.jpg?w=740&q=80",
    ]
)

print(result.output)
```

```bash cURL
curl https://api.comfy.org/v2/models/freepik/ai-skin-enhancer-flexible \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"image\": \"https://img.magnific.com/free-photo/portrait-woman_395237-33.jpg?w=740&q=80\"}"
```
</CodeGroup>
  </Tab>
  <Tab title="Queue and collect later">
同じボディを `POST https://api.comfy.org/v2/models/freepik/ai-skin-enhancer-flexible/requests` に送信します。Router は実行が受け付けられ次第 `201` と `request_id` を返し、結果は準備が整った時点で、このプロセスからでも別のプロセスからでも取得できます。ステータス、キャンセル、結果の取得の詳細は [キュー配信](/ja/development/comfy-router/queue) を参照してください。

<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# Each submit() call mints its own Idempotency-Key and reuses it for automatic retries.
with Comfy() as client:
    handle = client.models.submit(
        "freepik/ai-skin-enhancer-flexible",
        {
            "image": "https://img.magnific.com/free-photo/portrait-woman_395237-33.jpg?w=740&q=80",
        },
    )
    print("request_id:", handle.request_id)  # with the model ID, all another process needs

    # Poll until the request completes, waiting the Retry-After the server names.
    for update in handle.iter_events():
        print(update.status, update.queue_position)

    # The provider's own payload, the same value models.run() returns.
    # A request that failed or was cancelled raises the typed Router error here.
    result = handle.get()

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// Each submit() call mints its own Idempotency-Key and reuses it for automatic retries.
const handle = await comfy.models.submit("freepik/ai-skin-enhancer-flexible", {
  image: "https://img.magnific.com/free-photo/portrait-woman_395237-33.jpg?w=740&q=80",
});
console.log("requestId:", handle.requestId); // with the model ID, all another process needs

// Poll until the request completes, waiting the Retry-After the server names.
for await (const update of handle.events()) {
  console.log(update.status, update.queuePosition);
}

// The same result models.run() returns. A request that failed or was cancelled rejects here.
const result = await handle.get();

console.log(result.data);
```

```swift Swift
import Foundation
import ComfySwiftSDK

// Reads COMFY_API_KEY from the environment.
// Each submit() call mints its own Idempotency-Key and reuses it for automatic retries.
let client = ComfyCloudClient(apiKey: ProcessInfo.processInfo.environment["COMFY_API_KEY"]!)
let handle = try await client.models.submit(
    "freepik/ai-skin-enhancer-flexible",
    input: [
        "image": "https://img.magnific.com/free-photo/portrait-woman_395237-33.jpg?w=740&q=80",
    ]
)
print("requestId:", handle.requestId)  // with the model ID, all another process needs

// Poll until the request completes, waiting the Retry-After the server names.
for try await update in handle.events() {
    print(update.state.rawValue, update.queuePosition.map(String.init) ?? "unknown")
}

// The provider's own payload, the same value models.run() returns.
// A request that failed or was cancelled throws the typed Router error here.
let result = try await handle.result()

print(result.output)
```

```bash cURL
# 1. Submit. Router answers 201 with request_id, status_url, response_url and cancel_url.
curl https://api.comfy.org/v2/models/freepik/ai-skin-enhancer-flexible/requests \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"image\": \"https://img.magnific.com/free-photo/portrait-woman_395237-33.jpg?w=740&q=80\"}"

# 2. Poll until status is COMPLETED, waiting the Retry-After seconds each response names.
REQUEST_ID="<request_id from the 201 body>"
curl -i https://api.comfy.org/v2/models/freepik/ai-skin-enhancer-flexible/requests/$REQUEST_ID/status \
  -H "X-API-Key: $COMFY_API_KEY"

# 3. Collect. 200 with the model's native output, 202 with the status body while it is still running.
curl https://api.comfy.org/v2/models/freepik/ai-skin-enhancer-flexible/requests/$REQUEST_ID \
  -H "X-API-Key: $COMFY_API_KEY"
```
</CodeGroup>
  </Tab>
</Tabs>


## スキーマ

### 入力

<ParamField body="image" type="string" required>
  入力画像。Base64 エンコーディングまたは HTTPS URL（公開アクセス可能である必要があります）に対応しています。
</ParamField>

<ParamField body="optimized_for" type="string" default="&quot;enhance_skin&quot;">
  flexible skin enhancer の最適化ターゲット

  指定可能な値: `enhance_skin`、`improve_lighting`、`enhance_everything`、`transform_to_real`、`no_make_up`
</ParamField>

<ParamField body="sharpen" type="integer" default="0">
  シャープ化の強度

  範囲: `0` から `100`
</ParamField>

<ParamField body="smart_grain" type="integer" default="2">
  スマートグレインの強度

  範囲: `0` から `100`
</ParamField>

<ParamField body="webhook_url" type="string (uri)">
  非同期通知用のオプションのコールバック URL。

  フォーマット: `uri`
</ParamField>

Router が `GET /v2/models/freepik/ai-skin-enhancer-flexible/openapi.json` で提供するスキーマから生成されています。これは、リクエストがプロバイダーに届く前に Router が呼び出しを検証する際に使用するのと同じドキュメントです。

### 出力

<ResponseField name="data" type="object" required>
   
</ResponseField>

<ResponseField name="data.generated" type="string (uri)[]" required>
  完了した生成の画像 URL。Router が返すドキュメントでは必ず存在し、空にはなりません。このリストが結果そのものです。リンクは Freepik 自身のものであり、有効期限があります。
</ResponseField>

<ResponseField name="data.status" type="string" required>
  指定可能な値: `COMPLETED`
</ResponseField>

<ResponseField name="data.task_id" type="string (uuid)">
  フォーマット: `uuid`
</ResponseField>

## 例

### 入力

```json
{
  "image": "https://img.magnific.com/free-photo/portrait-woman_395237-33.jpg?w=740&q=80"
}
```

### 出力

```json
{
  "data": {
    "generated": [
      "https://example.invalid/freepik/ai-skin-enhancer-flexible/enhanced.png"
    ],
    "status": "COMPLETED",
    "task_id": "046b6c7f-0b8a-43b9-b35d-6489e6daee91"
  }
}
```


<RouterCodeFooter />
