---
title: "Comfy Router で Fal GPT Image 2 を使用する"
description: "Comfy Router 経由で fal/fal-gpt-image-2 を呼び出します: エンドポイント、リクエストの形状、および Router が返すレスポンス。"
sidebarTitle: "Fal GPT Image 2"
translationSourceHash: b8438202
translationFrom: development/comfy-router/models/fal/fal-gpt-image-2/code.mdx
---

{/* 生成済みファイル。router-schemas/fal/fal-gpt-image-2.json から `pnpm code-pages:gen` によって生成されました。 */}

import RouterCodeFooter from "/snippets/ja/comfy-router/model-code-footer.mdx";

Comfy Router が fal から提供する `fal/fal-gpt-image-2` の API リファレンスです。

## リクエストのセットアップ

[Comfy ワークスペース](https://platform.comfy.org/profile/api-keys)でキーを作成し、`COMFY_API_KEY` としてエクスポートします。Python の場合は `pip install comfy-sdk` を実行します。TypeScript の場合は `npm install @comfyorg/sdk` を実行します。cURL は生の HTTP を使用します。

**モデル ID:** `fal/fal-gpt-image-2`

**エンドポイント:** `POST https://api.comfy.org/v2/models/fal/fal-gpt-image-2`

<Note>
Router はこのモデル向けの作成済み入力スキーマを公開していないため、スニペットを生成する元となるリクエスト例がありません。Router はボディを fal にそのまま転送します。fal 自身の API ドキュメントを基に構築し、[Router クイックスタート](/ja/development/comfy-router/quickstart)を使用して送信してください。
</Note>

## スキーマ

### 入力

<Note>
Router はまだこのモデル向けの作成済み入力スキーマを公開していません。`GET /v2/models/fal/fal-gpt-image-2/openapi.json` は `x-comfy-input-schema-authored: false` を持つオープンなオブジェクトを返します。Router はボディを fal にそのまま転送するため、リクエストのフィールドについては fal 自身の API ドキュメントが信頼できる情報源となり、Router はモデル固有の入力検証を行いません。プロバイダー側の検証は引き続き適用されます。
</Note>

### 出力

_このスキーマは名前付きプロパティを宣言していません。_

<RouterCodeFooter />