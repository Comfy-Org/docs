---
title: "Comfy Router で Starfish を使う"
description: "Comfy Router 経由で heygen/starfish を呼び出します: エンドポイント、リクエスト形状、および Router が返すレスポンスについて説明します。"
sidebarTitle: "Starfish"
translationSourceHash: 0c2999fa
translationFrom: development/comfy-router/models/heygen/starfish/code.mdx
translationBlockHashes:
  "_intro": 2fa9af39
  "Quick start": f2c66126
  "Schema": e2e4a0e8
  "Examples": 33e81ae0
---


{/* 生成済みファイル。`pnpm code-pages:gen` により router-schemas/heygen/starfish.json から生成されました。 */}

import RouterCodeFooter from "/snippets/ja/comfy-router/model-code-footer.mdx";

`heygen/starfish` の API リファレンス。HeyGen から Comfy Router が提供します。

## クイックスタート

[Comfy ワークスペース](https://platform.comfy.org/profile/api-keys?onboarding=router)でキーを作成し、`COMFY_API_KEY` としてエクスポートします。Python と TypeScript のスニペットは Comfy SDK（`pip install comfy-sdk`、`npm install @comfyorg/sdk`）を使用しています。cURL のスニペットは、生の HTTP で同じ呼び出しを行います。

**モデル ID:** `heygen/starfish`

**エンドポイント:** `POST https://api.comfy.org/v2/models/heygen/starfish`

<Tabs defaultTabIndex={1}>
  <Tab title="結果を待つ">
<CodeGroup>
```python Python
from comfy_sdk import Comfy

# 環境変数から COMFY_API_KEY を読み取ります。
# SDK は冪等キーを自動的に作成し、自動リトライのために再利用します。
with Comfy() as client:
    result = client.models.run(
        "heygen/starfish",
        {
            "text": "Hello from Comfy. This voice is generated with the HeyGen Starfish engine.",
            "voice_id": "d2f4f24783d04e22ab49ee8fdc3715e0",
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// 環境変数から COMFY_API_KEY を読み取ります。
// SDK は冪等キーを自動的に作成し、自動リトライのために再利用します。
const { data } = await comfy.models.run("heygen/starfish", {
  text: "Hello from Comfy. This voice is generated with the HeyGen Starfish engine.",
  voice_id: "d2f4f24783d04e22ab49ee8fdc3715e0",
});

console.log(data);
```

```bash cURL
curl https://api.comfy.org/v2/models/heygen/starfish \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"text\": \"Hello from Comfy. This voice is generated with the HeyGen Starfish engine.\", \"voice_id\": \"d2f4f24783d04e22ab49ee8fdc3715e0\"}"
```
</CodeGroup>
  </Tab>
  <Tab title="キューに送信して後で収集する">
同じボディを `POST https://api.comfy.org/v2/models/heygen/starfish/requests` に送信します。Router は実行が受け付けられるとすぐに `201` と `request_id` を返し、結果は準備ができた時点で、このプロセスからでも別のプロセスからでも収集できます。[キュー配信](/ja/development/comfy-router/queue)では、ステータス、キャンセル、収集について説明します。

<CodeGroup>
```python Python
import asyncio
from comfy_sdk import AsyncComfy

# 環境変数から COMFY_API_KEY を読み取ります。
# 各 submit() 呼び出しは独自の Idempotency-Key を発行し、自動リトライのために再利用します。
async def main():
    async with AsyncComfy() as client:
        handle = await client.models.submit(
            "heygen/starfish",
            {
                "text": "Hello from Comfy. This voice is generated with the HeyGen Starfish engine.",
                "voice_id": "d2f4f24783d04e22ab49ee8fdc3715e0",
            },
        )
        print("request_id:", handle.request_id)  # モデル ID と合わせれば、別のプロセスに必要なものはこれだけです

        # リクエストが完了するまでポーリングし、サーバーが指定する Retry-After だけ待機します。
        async for update in handle.iter_events():
            print(update.status, update.queue_position)

        # プロバイダー自身のペイロードで、models.run() が返す値と同じです。
        # 失敗またはキャンセル済みのリクエストは、ここで型付きの Router エラーを送出します。
        result = await handle.get()

    print(result)

asyncio.run(main())
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// 環境変数から COMFY_API_KEY を読み取ります。
// 各 submit() 呼び出しは独自の Idempotency-Key を発行し、自動リトライのために再利用します。
const handle = await comfy.models.submit("heygen/starfish", {
  text: "Hello from Comfy. This voice is generated with the HeyGen Starfish engine.",
  voice_id: "d2f4f24783d04e22ab49ee8fdc3715e0",
});
console.log("requestId:", handle.requestId); // モデル ID と合わせれば、別のプロセスに必要なものはこれだけです

// リクエストが完了するまでポーリングし、サーバーが指定する Retry-After だけ待機します。
for await (const update of handle.events()) {
  console.log(update.status, update.queuePosition);
}

// models.run() が返すのと同じ結果です。失敗またはキャンセル済みのリクエストは、ここで reject されます。
const result = await handle.get();

console.log(result.data);
```

```bash cURL
# 1. 送信。Router は request_id、status_url、response_url、cancel_url とともに 201 を返します。
curl https://api.comfy.org/v2/models/heygen/starfish/requests \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"text\": \"Hello from Comfy. This voice is generated with the HeyGen Starfish engine.\", \"voice_id\": \"d2f4f24783d04e22ab49ee8fdc3715e0\"}"

# 2. ステータスが COMPLETED になるまでポーリングし、各レスポンスが指定する Retry-After 秒だけ待機します。
REQUEST_ID="<request_id from the 201 body>"
curl -i https://api.comfy.org/v2/models/heygen/starfish/requests/$REQUEST_ID/status \
  -H "X-API-Key: $COMFY_API_KEY"

# 3. 収集。モデルのネイティブ出力とともに 200、まだ実行中はステータスボディとともに 202 を返します。
curl https://api.comfy.org/v2/models/heygen/starfish/requests/$REQUEST_ID \
  -H "X-API-Key: $COMFY_API_KEY"
```
</CodeGroup>
  </Tab>
</Tabs>

## スキーマ

### 入力

<ParamField body="input_type" type="string">
  入力の種類: プレーンテキストの場合は 'text'、SSML マークアップの場合は 'ssml'。デフォルトは 'text'
</ParamField>

<ParamField body="language" type="string">
  ベース言語コード (例: 'en')。省略時はテキストから自動検出されます
</ParamField>

<ParamField body="locale" type="string">
  BCP-47 ロケールタグ (例: 'en-US')。設定されている場合、言語はロケールから推定されます
</ParamField>

<ParamField body="speed" type="number">
  速度の倍率 (0.5～2.0)

  範囲: `0.5` ～ `2`

  形式: `double`
</ParamField>

<ParamField body="text" type="string" required>
  合成するテキスト (1～5000 文字)
</ParamField>

<ParamField body="voice_id" type="string" required>
  使用するボイス ID。ボイスは starfish エンジンをサポートしている必要があります
</ParamField>

Router が `GET /v2/models/heygen/starfish/openapi.json` で提供するスキーマから生成されます。これは、リクエストがプロバイダーに到達する前に Router が呼び出しを検証する対象となるドキュメントと同じものです。

### 出力

<ResponseField name="data" type="object" required>
  成功した HeyGen テキスト読み上げレスポンスのペイロード
</ResponseField>

<ResponseField name="data.audio_url" type="string" required>
  生成されたオーディオファイルの URL
</ResponseField>

<ResponseField name="data.duration" type="number">
  オーディオの再生時間 (秒)

  形式: `double`
</ResponseField>

<ResponseField name="data.request_id" type="string">
  この生成リクエストの一意の識別子
</ResponseField>

<ResponseField name="data.word_timestamps" type="object[]">
  単語単位のタイミングデータ
</ResponseField>

<ResponseField name="data.word_timestamps[].end" type="number">
  終了時間 (秒)

  形式: `double`
</ResponseField>

<ResponseField name="data.word_timestamps[].start" type="number">
  開始時間 (秒)

  形式: `double`
</ResponseField>

<ResponseField name="data.word_timestamps[].word" type="string">
  単語
</ResponseField>

## 例

### 入力

```json
{
  "text": "Hello from Comfy. This voice is generated with the HeyGen Starfish engine.",
  "voice_id": "d2f4f24783d04e22ab49ee8fdc3715e0"
}
```

### 出力

```json
{
  "data": {
    "audio_url": "https://example.invalid/heygen/starfish/speech.mp3",
    "duration": 12.5,
    "request_id": "018f2c7a-4b1e-7c3d-9a05-6e2f8b41d0c9"
  }
}
```

<RouterCodeFooter />
