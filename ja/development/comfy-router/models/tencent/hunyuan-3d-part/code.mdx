---
title: "Comfy Router で Hunyuan 3D Part を使用する"
description: "Comfy Router 経由で tencent/hunyuan-3d-part を呼び出します: エンドポイント、リクエスト形状、Router が返すレスポンスについて説明します。"
sidebarTitle: "Hunyuan 3D Part"
translationSourceHash: 48dc2ff1
translationFrom: development/comfy-router/models/tencent/hunyuan-3d-part/code.mdx
translationBlockHashes:
  "_intro": 55a0b224
  "Quick start": 2774fcbb
  "Schema": 24e6a64b
  "Examples": ee4faeb5
---
{/* 生成済みファイル。`pnpm code-pages:gen` により router-schemas/tencent/hunyuan-3d-part.json から生成されました。 */}

import RouterCodeFooter from "/snippets/ja/comfy-router/model-code-footer.mdx";

`tencent/hunyuan-3d-part` の API リファレンス。Tencent のモデルを Comfy Router が提供します。

## クイックスタート

[Comfy ワークスペース](https://platform.comfy.org/profile/api-keys)でキーを作成し、`COMFY_API_KEY` としてエクスポートします。Python と TypeScript のスニペットは Comfy SDK(`pip install comfy-sdk`、`npm install @comfyorg/sdk`)を使用しています。cURL のスニペットは、同じ呼び出しを生の HTTP で実行するものです。

**モデル ID:** `tencent/hunyuan-3d-part`

**エンドポイント:** `POST https://api.comfy.org/v2/models/tencent/hunyuan-3d-part`

<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "tencent/hunyuan-3d-part",
        {
            "File": {
                "Type": "FBX",
                "Url": "https://vcg-test-1258344699.cos.ap-guangzhou.myqcloud.com/test/3d/test.fbx",
            },
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const { data } = await comfy.models.run("tencent/hunyuan-3d-part", {
  File: {
    Type: "FBX",
    Url: "https://vcg-test-1258344699.cos.ap-guangzhou.myqcloud.com/test/3d/test.fbx",
  },
});

console.log(data);
```

```bash cURL
curl https://api.comfy.org/v2/models/tencent/hunyuan-3d-part \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"File\": {\"Type\":\"FBX\",\"Url\":\"https://vcg-test-1258344699.cos.ap-guangzhou.myqcloud.com/test/3d/test.fbx\"}}"
```
</CodeGroup>

## スキーマ

### 入力

<ParamField body="File" type="object">
  コンポーネント生成用の3Dファイル入力
</ParamField>

<ParamField body="File.Type" type="string" required>
  3Dファイル形式のタイプ

  指定可能な値: `FBX`
</ParamField>

<ParamField body="File.Url" type="string (uri)" required>
  コンポーネントに分割する3DファイルのURL

  形式: `uri`
</ParamField>

`GET /v2/models/tencent/hunyuan-3d-part/openapi.json` で Router が提供するスキーマから生成されています。これは、リクエストがプロバイダーに到達する前に Router が呼び出しを検証する際に使用するドキュメントと同じものです。

### 出力

<ResponseField name="Response" type="object" required>
   
</ResponseField>

<ResponseField name="Response.ErrorCode" type="string">
  Tencent のエラーコード。エラーがない場合は空文字列と記載されています。`Status` が存在する場合、Router はこれを読み取りません。
</ResponseField>

<ResponseField name="Response.ErrorMessage" type="string">
  Tencent のエラーメッセージ。エラーがない場合は空文字列と記載されています。`Status` が存在する場合、Router はこれを読み取りません。
</ResponseField>

<ResponseField name="Response.RequestId" type="string">
  Tencent 自身のリクエスト識別子で、Tencent でのトラブルシューティングに使用します。ジョブIDでもアセットでもありません。
</ResponseField>

<ResponseField name="Response.ResultFile3Ds" type="object[]" required>
  完了したジョブの生成済み3Dファイル。Router が返すドキュメントでは存在し、空ではありません。少なくとも1つのエントリが取得可能な `Url` を持ちます。リンクは Tencent 自身のもので、24時間有効と記載されています。
</ResponseField>

<ResponseField name="Response.ResultFile3Ds[].PreviewImageUrl" type="string (uri)">
  プレビュー画像のURL

  形式: `uri`
</ResponseField>

<ResponseField name="Response.ResultFile3Ds[].Type" type="string">
  3Dファイル形式

  指定可能な値: `GLB`, `OBJ`
</ResponseField>

<ResponseField name="Response.ResultFile3Ds[].Url" type="string (uri)">
  ファイルURL（24時間有効）

  形式: `uri`
</ResponseField>

<ResponseField name="Response.Status" type="string" required>
  指定可能な値: `DONE`
</ResponseField>

## 例

### 入力

```json
{
  "File": {
    "Type": "FBX",
    "Url": "https://vcg-test-1258344699.cos.ap-guangzhou.myqcloud.com/test/3d/test.fbx"
  }
}
```

### 出力

```json
{
  "Response": {
    "ErrorCode": "",
    "ErrorMessage": "",
    "RequestId": "9a1c0d4e-77b2-4e0c-8f2e-2f9a1c0d4e77",
    "ResultFile3Ds": [
      {
        "PreviewImageUrl": "https://example.invalid/tencent/hunyuan-3d-part/preview.png",
        "Type": "GLB",
        "Url": "https://example.invalid/tencent/hunyuan-3d-part/parts.glb"
      }
    ],
    "Status": "DONE"
  }
}
```

<RouterCodeFooter />
