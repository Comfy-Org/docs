---
title: "Comfy Router で Hunyuan 3D Uv を使用する"
description: "Comfy Router 経由で tencent/hunyuan-3d-uv を呼び出します: エンドポイント、リクエストの形状、Router が返すレスポンスについて説明します。"
sidebarTitle: "Hunyuan 3D Uv"
translationSourceHash: 41573987
translationFrom: development/comfy-router/models/tencent/hunyuan-3d-uv/code.mdx
translationBlockHashes:
  "_intro": cc16c86e
  "Quick start": d368c7e6
  "Schema": 925d33c0
  "Examples": dcaa8745
---
{/* GENERATED FILE. Generated from router-schemas/tencent/hunyuan-3d-uv.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/ja/comfy-router/model-code-footer.mdx";

`tencent/hunyuan-3d-uv` の API リファレンスです。Tencent のモデルを Comfy Router が提供します。

## クイックスタート

[Comfy ワークスペース](https://platform.comfy.org/profile/api-keys)でキーを作成し、`COMFY_API_KEY` としてエクスポートします。Python と TypeScript のスニペットは Comfy SDK（`pip install comfy-sdk`、`npm install @comfyorg/sdk`）を使用しています。cURL のスニペットは、生の HTTP で同じ呼び出しを行うものです。

**モデル ID:** `tencent/hunyuan-3d-uv`

**エンドポイント:** `POST https://api.comfy.org/v2/models/tencent/hunyuan-3d-uv`

<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "tencent/hunyuan-3d-uv",
        {
            "File": {
                "Type": "GLB",
                "Url": "https://vcg-test-1258344699.cos.ap-guangzhou.myqcloud.com/test/3d/test.glb",
            },
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const { data } = await comfy.models.run("tencent/hunyuan-3d-uv", {
  File: {
    Type: "GLB",
    Url: "https://vcg-test-1258344699.cos.ap-guangzhou.myqcloud.com/test/3d/test.glb",
  },
});

console.log(data);
```

```bash cURL
curl https://api.comfy.org/v2/models/tencent/hunyuan-3d-uv \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"File\": {\"Type\":\"GLB\",\"Url\":\"https://vcg-test-1258344699.cos.ap-guangzhou.myqcloud.com/test/3d/test.glb\"}}"
```
</CodeGroup>

## スキーマ

### 入力

<ParamField body="File" type="object">
  UV展開用の3Dファイル入力
</ParamField>

<ParamField body="File.Type" type="string" required>
  3Dファイル形式のタイプ

  指定可能な値: `FBX`, `OBJ`, `GLB`
</ParamField>

<ParamField body="File.Url" type="string (uri)" required>
  UV展開が必要な3DファイルのURL

  形式: `uri`
</ParamField>

このスキーマは、Router が `GET /v2/models/tencent/hunyuan-3d-uv/openapi.json` で提供するスキーマから生成されたものです。これは、リクエストがプロバイダーに到達する前に Router が呼び出しの検証に使用するドキュメントと同じものです。

### 出力

<ResponseField name="Response" type="object" required>
   
</ResponseField>

<ResponseField name="Response.ErrorCode" type="string">
  Tencent のエラーコード。エラーがない場合は空文字列と記載されています。`Status` が存在する場合、Router はこれを読み取りません。
</ResponseField>

<ResponseField name="Response.ErrorMessage" type="string">
  Tencent のエラーメッセージ。エラーがない場合は空文字列と記載されています。`Status` が存在する場合、Router はこれを読み取りません。
</ResponseField>

<ResponseField name="Response.RequestId" type="string">
  Tencent 自身のリクエスト識別子で、Tencent でのトラブルシューティングに使用します。ジョブIDでもアセットでもありません。
</ResponseField>

<ResponseField name="Response.ResultFile3Ds" type="object[]" required>
  完了したジョブで生成済みの3Dファイル。Router が返すドキュメントに含まれ、空にはなりません。少なくとも1つのエントリに取得可能な `Url` が含まれます。リンクは Tencent 自身のもので、24時間有効と記載されています。
</ResponseField>

<ResponseField name="Response.ResultFile3Ds[].PreviewImageUrl" type="string (uri)">
  プレビュー画像のURL

  形式: `uri`
</ResponseField>

<ResponseField name="Response.ResultFile3Ds[].Type" type="string">
  3Dファイル形式

  指定可能な値: `GLB`, `OBJ`
</ResponseField>

<ResponseField name="Response.ResultFile3Ds[].Url" type="string (uri)">
  ファイルURL（24時間有効）

  形式: `uri`
</ResponseField>

<ResponseField name="Response.Status" type="string" required>
  指定可能な値: `DONE`
</ResponseField>

## 例

### 入力

```json
{
  "File": {
    "Type": "GLB",
    "Url": "https://vcg-test-1258344699.cos.ap-guangzhou.myqcloud.com/test/3d/test.glb"
  }
}
```

### 出力

```json
{
  "Response": {
    "ErrorCode": "",
    "ErrorMessage": "",
    "RequestId": "9a1c0d4e-77b2-4e0c-8f2e-2f9a1c0d4e77",
    "ResultFile3Ds": [
      {
        "PreviewImageUrl": "https://example.invalid/tencent/hunyuan-3d-uv/preview.png",
        "Type": "GLB",
        "Url": "https://example.invalid/tencent/hunyuan-3d-uv/unwrapped.glb"
      }
    ],
    "Status": "DONE"
  }
}
```


<RouterCodeFooter />
