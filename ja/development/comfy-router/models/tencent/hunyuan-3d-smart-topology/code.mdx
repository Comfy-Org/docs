---
title: "Comfy Router で Hunyuan 3D Smart Topology を使用する"
description: "Comfy Router 経由で tencent/hunyuan-3d-smart-topology を呼び出します: エンドポイント、リクエストの形式、Router が返すレスポンスについて説明します。"
sidebarTitle: "Hunyuan 3D Smart Topology"
translationSourceHash: f777e523
translationFrom: development/comfy-router/models/tencent/hunyuan-3d-smart-topology/code.mdx
translationBlockHashes:
  "_intro": d7aa51d0
  "Quick start": 8de9c627
  "Schema": 31015a4b
  "Examples": 6f3af127
---
{/* GENERATED FILE. Generated from router-schemas/tencent/hunyuan-3d-smart-topology.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/ja/comfy-router/model-code-footer.mdx";

`tencent/hunyuan-3d-smart-topology` の API リファレンスです。Tencent のモデルで、Comfy Router によって提供されます。

## クイックスタート

[Comfy ワークスペース](https://platform.comfy.org/profile/api-keys)でキーを作成し、`COMFY_API_KEY` としてエクスポートします。Python と TypeScript のスニペットは Comfy SDK（`pip install comfy-sdk`、`npm install @comfyorg/sdk`）を使用しています。cURL のスニペットは、生の HTTP で同じ呼び出しを行います。

**モデル ID:** `tencent/hunyuan-3d-smart-topology`

**エンドポイント:** `POST https://api.comfy.org/v2/models/tencent/hunyuan-3d-smart-topology`

<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "tencent/hunyuan-3d-smart-topology",
        {
            "File3D": {
                "Type": "GLB",
                "Url": "https://vcg-test-1258344699.cos.ap-guangzhou.myqcloud.com/test/3d/test.glb",
            },
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const { data } = await comfy.models.run("tencent/hunyuan-3d-smart-topology", {
  File3D: {
    Type: "GLB",
    Url: "https://vcg-test-1258344699.cos.ap-guangzhou.myqcloud.com/test/3d/test.glb",
  },
});

console.log(data);
```

```bash cURL
curl https://api.comfy.org/v2/models/tencent/hunyuan-3d-smart-topology \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"File3D\": {\"Type\":\"GLB\",\"Url\":\"https://vcg-test-1258344699.cos.ap-guangzhou.myqcloud.com/test/3d/test.glb\"}}"
```
</CodeGroup>

## Schema

### 入力

<ParamField body="FaceLevel" type="string">
  ポリゴン削減レベル。

  指定可能な値: `high`、`medium`、`low`
</ParamField>

<ParamField body="File3D" type="object" required>
  スマートトポロジー用の3Dファイル入力
</ParamField>

<ParamField body="File3D.Type" type="string" required>
  3Dファイル形式のタイプ

  指定可能な値: `OBJ`、`GLB`
</ParamField>

<ParamField body="File3D.Url" type="string (uri)" required>
  リトポロジーする3DファイルのURL

  形式: `uri`
</ParamField>

<ParamField body="PolygonType" type="string">
  出力メッシュのポリゴンタイプ。デフォルトは triangle。

  指定可能な値: `triangle`、`quadrilateral`
</ParamField>

Router が `GET /v2/models/tencent/hunyuan-3d-smart-topology/openapi.json` で提供するスキーマから生成されています。これは、リクエストがプロバイダーに到達する前に Router が呼び出しを検証する際に使用するドキュメントと同じものです。

### 出力

<ResponseField name="Response" type="object" required>
   
</ResponseField>

<ResponseField name="Response.ErrorCode" type="string">
  Tencent のエラーコード。エラーがない場合は空文字列と記載されています。`Status` が存在する場合、Router はこれを読み取りません。
</ResponseField>

<ResponseField name="Response.ErrorMessage" type="string">
  Tencent のエラーメッセージ。エラーがない場合は空文字列と記載されています。`Status` が存在する場合、Router はこれを読み取りません。
</ResponseField>

<ResponseField name="Response.RequestId" type="string">
  Tencent 自身のリクエスト識別子で、Tencent とのトラブルシューティングに使用します。ジョブID でもアセットでもありません。
</ResponseField>

<ResponseField name="Response.ResultFile3Ds" type="object[]" required>
  完了したジョブで生成された3Dファイル。Router が返すドキュメントでは存在し、空ではありません。少なくとも1つのエントリに取得可能な `Url` が含まれます。リンクは Tencent 自身のもので、24時間有効と記載されています。
</ResponseField>

<ResponseField name="Response.ResultFile3Ds[].PreviewImageUrl" type="string (uri)">
  画像プレビューURL

  形式: `uri`
</ResponseField>

<ResponseField name="Response.ResultFile3Ds[].Type" type="string">
  3Dファイル形式

  指定可能な値: `GLB`、`OBJ`
</ResponseField>

<ResponseField name="Response.ResultFile3Ds[].Url" type="string (uri)">
  ファイルURL（24時間有効）

  形式: `uri`
</ResponseField>

<ResponseField name="Response.Status" type="string" required>
  指定可能な値: `DONE`
</ResponseField>

## 例

### 入力

```json
{
  "File3D": {
    "Type": "GLB",
    "Url": "https://vcg-test-1258344699.cos.ap-guangzhou.myqcloud.com/test/3d/test.glb"
  }
}
```

### 出力

```json
{
  "Response": {
    "ErrorCode": "",
    "ErrorMessage": "",
    "RequestId": "9a1c0d4e-77b2-4e0c-8f2e-2f9a1c0d4e77",
    "ResultFile3Ds": [
      {
        "PreviewImageUrl": "https://example.invalid/tencent/hunyuan-3d-smart-topology/preview.png",
        "Type": "GLB",
        "Url": "https://example.invalid/tencent/hunyuan-3d-smart-topology/retopologised.glb"
      }
    ],
    "Status": "DONE"
  }
}
```

<RouterCodeFooter />
