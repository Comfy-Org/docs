---
title: "Tripo P2: ComfyUIでのクアッドトポロジー3Dモデル生成"
description: "ComfyUIのTripo P2パートナーノードで実運用可能な3Dモデルを生成: ネイティブなクアッドトポロジー、最大50,000フェイスのポリゴン数、2Kから8Kまでのテクスチャレベルに対応。"
sidebarTitle: "Tripo P2"
translationSourceHash: f5ed341e
translationFrom: tutorials/partner-nodes/tripo/tripo-p2.mdx
translationBlockHashes:
  "_intro": 3fd4d2bd
  "What Tripo P2 is good at": 1b7b881b
  "Tripo P2: Text to Model": 169f90a0
  "Tripo P2: Image to Model": 7ac87448
  "Tripo P2: Multiview to Model": c99ac511
  "Node options reference": 4a745c24
---
import ReqHint from "/snippets/ja/tutorials/partner-nodes/req-hint.mdx";
import UpdateReminder from "/snippets/ja/tutorials/update-reminder.mdx";

Tripo P2 は、ネイティブなクアッドトポロジーのメッシュを出力する Tripo の P シリーズ 3D 生成モデルです。従来の text-to-3D や image-to-3D モデルが三角形メッシュを返すのに対し、Tripo P2 は四角形の面からジオメトリを構築し、関節周りにはクリーンなエッジループ、平坦な面には均一な密度を実現します。クアッドはリグ下で予測可能に変形し、きれいに細分割でき、ゲーム、映画、製品ビジュアライゼーションにおけるリトポロジー中心のパイプラインの標準的な入力となります。

Tripo P2 は 3 つの入力モードをサポートしています。テキストからモデル、単一画像からモデル、マルチビューからモデル（正面、左、背面、右のリファレンス）です。メッシュの上限は 50,000 三角形、クアッドモードでは 25,000 クアッドに達します。テクスチャ出力は段階的で、スタンダードは 2K、詳細は 4K、エクストリームは 8K です。ベース色に加えて PBR マップ（メタリック、ラフネス、ノーマル）も利用できます。

ComfyUI では、P2 ノードは他の Tripo パートナーノードと同様に動作します。ノードを追加して入力を指定すると、ワークフローがテクスチャリング、リギング、変換、エクスポートを自動的に処理します。ローカル GPU やモデルのダウンロードは不要です。

<ReqHint/>
<UpdateReminder/>

## Tripo P2 が得意なこと

- **ネイティブなクアッドトポロジー**: クリーンなエッジフローを持つクアッドメッシュで、手動リトポロジーなしでリギングやアニメーションにすぐ使えます
- **大きなメッシュのポリゴン数**: 48～50,000三角形、クアッドモードでは48～25,000クアッド
- **段階的テクスチャリング**: スタンダード（2K）、詳細（4K）、エクストリーム（8K）のベースカラーテクスチャと、オプションのPBRマップ
- **3つの入力モード**: テキストプロンプト、単一の参照画像、または最大4枚のマルチビュー画像（正面、左、背面、右）

料金は選択済みのテクスチャレベルによって異なります。詳細は[料金](/ja/tutorials/partner-nodes/pricing)をご覧ください。

## Tripo P2: テキストからモデル

テキストの説明から3Dモデルを生成します。Tripo P2 はまずプロンプトからコンセプト画像を描画し（`image_seed` で制御）、その画像からジオメトリをモデリングします。

<img src="https://raw.githubusercontent.com/Comfy-Org/workflow_templates/main/templates/api_tripo_p2_text_to_model-1.webp" alt="Tripo P2 Text to Model Workflow" />

<CardGroup cols={2}>
  <Card title="Comfy Cloud で実行" icon="cloud" href="https://cloud.comfy.org/?template=api_tripo_p2_text_to_model&utm_source=docs&utm_medium=referral&utm_campaign=tripo_p2_t2m">
    Comfy Cloud で開く
  </Card>
  <Card title="ワークフローをダウンロード" icon="download" href="https://github.com/Comfy-Org/workflow_templates/blob/main/templates/api_tripo_p2_text_to_model.json">
    JSON をダウンロードするか、テンプレートライブラリで「Tripo P2: Text to Model」を検索してください
  </Card>
</CardGroup>

### 実行手順

1. **プロンプトを入力**: **Tripo P2: Text to Model** ノードに3Dモデルの説明を記入します（最大1024文字）。ネガティブプロンプトは高度なオプションで利用できます
2. **テクスチャレベルを選択**: `standard`（2K）、`detailed`（4K）、`extreme`（8K）、またはテクスチャなしのメッシュにする場合は `none`
3. **quad 出力を切り替え**: `quad` を有効にするとメッシュは FBX として返され、無効にすると GLB として返されます
4. **面の上限を設定**: 目標面数は48～50,000（quad 使用時は48～25,000）の範囲で指定するか、-1 のままにして Tripo に任せます
5. **キューをクリック**: `Ctrl+Enter` を押して生成します

生成後、ノードは選択したテクスチャレベルでリクエストするため、メッシュは生成時にテクスチャリングされ、ワークフローはその結果をそのまま保存します（GLB 出力、`quad`が有効な場合は FBX）。テンプレートには**TripoTextureNodeV2**、**TripoRigNode**、**TripoRetargetNode**、**TripoConversionNode**ノードも含まれていますが、デフォルトではバイパスされ未接続です。これらの追加のポストプロセスが必要な場合は接続して有効化してください（選択して`Ctrl+B`でバイパスを切り替えられます）。

### quad 出力に関する注意

`quad` が有効な場合は、**Save 3D** を FBX 出力スロットに接続します。`quad` が無効な場合は、GLB 出力スロットを使用します。

## Tripo P2: 画像からモデル

1枚の参照画像から3Dモデルを生成します。

<img src="https://raw.githubusercontent.com/Comfy-Org/workflow_templates/main/templates/api_tripo_p2_image_to_model-1.webp" alt="Tripo P2 Image to Model Workflow" />

<CardGroup cols={2}>
  <Card title="Comfy Cloudで実行" icon="cloud" href="https://cloud.comfy.org/?template=api_tripo_p2_image_to_model&utm_source=docs&utm_medium=referral&utm_campaign=tripo_p2_i2m">
    Comfy Cloudで開く
  </Card>
  <Card title="ワークフローをダウンロード" icon="download" href="https://github.com/Comfy-Org/workflow_templates/blob/main/templates/api_tripo_p2_image_to_model.json">
    JSONをダウンロード、またはテンプレートライブラリで「Tripo P2: Image to Model」を検索
  </Card>
</CardGroup>

### 実行手順

1. **参照画像をアップロード**: **LoadImage** ノードで被写体の正面画像を読み込みます
2. **テクスチャレベルを選択**: `standard`、`detailed`、`extreme`、または `none`
3. **クアッド出力を切り替え**: 有効時はFBX、無効時はGLB
4. **テクスチャの位置合わせを設定**: `original_image` は入力画像の色に一致させ、`geometry` は生成された形状にテクスチャを合わせます (高度な機能)
5. **キューをクリック**: `Ctrl+Enter` を押して生成します

### 入力のヒント

- 低解像度または低品質の画像は、`enable_image_autofix` 高度な機能を使ってモデリング前に補正できます
- `align_image` の向きは、入力画像の視点にモデルを回転させます (高度な機能)

## Tripo P2: マルチビューからモデルを生成

参照ビューから3Dモデルを生成します。マルチビューノードは必須の正面ビューに加えて、左・背面・右ビューのうち最大3枚を受け付け、そのうち少なくとも1枚を接続する必要があります。正面ビューだけではエラーになります。他の P2 ノードと同じネイティブなクアッドトポロジーでジオメトリを再構築します。

<img src="https://raw.githubusercontent.com/Comfy-Org/workflow_templates/main/templates/api_tripo_p2_multiview_to_model-1.webp" alt="Tripo P2 Multiview to Model Workflow" />

<CardGroup cols={2}>
  <Card title="Comfy Cloud で実行" icon="cloud" href="https://cloud.comfy.org/?template=api_tripo_p2_multiview_to_model&utm_source=docs&utm_medium=referral&utm_campaign=tripo_p2_mv2m">
    Comfy Cloud で開く
  </Card>
  <Card title="ワークフローをダウンロード" icon="download" href="https://github.com/Comfy-Org/workflow_templates/blob/main/templates/api_tripo_p2_multiview_to_model.json">
    JSON をダウンロードするか、テンプレートライブラリで「Tripo P2: Multiview to Model」を検索してください
  </Card>
</CardGroup>

**入力素材**

対応する`LoadImage`ノードに以下のファイルをアップロードします：

<CardGroup cols={2}>
  <Card title="worker_doll_front.png" icon="image" href="https://raw.githubusercontent.com/Comfy-Org/workflow_templates/main/input/worker_doll_front.png">
    `LoadImage`ノード16 · 正面ビュー
  </Card>
  <Card title="worker_doll_back.png" icon="image" href="https://raw.githubusercontent.com/Comfy-Org/workflow_templates/main/input/worker_doll_back.png">
    `LoadImage`ノード19 · 背面ビュー
  </Card>
</CardGroup>

### 実行手順

1. **参照ビューを読み込む**：テンプレートには正面ビュー（`LoadImage`ノード16）と背面ビュー（`LoadImage`ノード19）が含まれています。このノードは`image_left`と`image_right`入力も備えており、4ビューでの再構築が可能です
2. **テクスチャレベルを選択**：`standard`（2K）、`detailed`（4K）、`extreme`（8K）、または`none`でテクスチャなしのメッシュ
3. **quad 出力を切り替え**：`quad`を有効にすると FBX、無効にすると GLB が返ります
4. **面数上限を設定**：目標面数は48〜50,000（quad では48〜25,000）、または -1 のままにして Tripo に任せます
5. **Queue をクリック**：`Ctrl+Enter`を押して生成します

このワークフローは生成されたメッシュをそのまま保存します。テンプレートに含まれるテクスチャ、リギング、リターゲティング、変換の各ノードは、デフォルトではバイパスされ未接続です。

## ノードオプションリファレンス

3 つの P2 ノードすべてで共通のオプション:

| オプション | 値 | デフォルト | 備考 |
| ------ | ------ | ------- | ----- |
| `quad` | on / off | off | オンのときはクアッドメッシュを FBX で、オフのときはトライアングルメッシュを GLB で出力 |
| `face_limit` | 48 ～ 50,000（quad 使用時は 25,000） | -1 | 目標フェイス数。結果がこの値を超えることもある |
| `texture` | standard / detailed / extreme / none | standard | 2K / 4K / 8K のベースカラー。none はテクスチャなしのメッシュを返す |
| `pbr` | on / off | on | メタリック、ラフネス、ノーマルマップ。texture が none のときは無視される |
| `model_seed` | 0 ～ 2147483647 | 42 | ジオメトリのシード |

高度な機能には `texture_seed`、`auto_size`（メートル単位の実世界スケール）、`export_uv`、`compress_geometry`（Meshopt 圧縮。圧縮されたメッシュは大幅に小さくなりますが、ComfyUI の 3D ビューポートではプレビューできません）があります。
