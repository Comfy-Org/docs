---
title: "Luma Image to Video Partner ノード"
description: "Luma Image to Video Partner ノードのワークフロー：ComfyUI で静止画像を動画に変換します。必要なクレジットとノードパラメータを含みます。"
sidebarTitle: "Luma Image To Video"
translationSourceHash: 4a523367
translationFrom: tutorials/partner-nodes/luma/luma-image-to-video.mdx
translationBlockHashes:
  "_intro": f9369f73
  "What Luma Image to Video is good at": 5dca8ad0
  "Image to Video Workflow with Luma Partner Nodes": 77fb52b5
---
import ReqHint from "/snippets/ja/tutorials/partner-nodes/req-hint.mdx";
import UpdateReminder from "/snippets/ja/tutorials/update-reminder.mdx";


Luma画像から動画へノードを使用すると、Luma AIの高度な機能を活用して、静止画像をスムーズでダイナミックなビデオに変換し、画像に生命と動きをもたらすことができます。

## Luma画像から動画への得意分野

- **画像アニメーション**: 静止画像をスムーズでダイナミックなビデオに変換し、画像に命と動きをもたらします
- **プロンプトによるモーション制御**: テキストプロンプトがビデオのモーション効果を決定します
- **カメラ制御**: Lumaコンセプト機能がカメラの動きを制御し、プロフェッショナルな撮影を実現します
- **柔軟な画像入力**: `first_image`、`last_image`、またはその両方から動作します
- **調整可能な出力**: ビデオの解像度と再生時間は調整可能で、モデルパラメータを介してさまざまなビデオ生成モデルを選択できます

<ReqHint/>
<UpdateReminder/>

## Lumaパートナーノードを使った画像から動画ワークフロー

Luma画像から動画ノードは、ビデオのモーション効果を決定するためのテキストプロンプトに加えて、少なくとも1つの画像入力（`first_image` または `last_image`）が必要です。このガイドでは、Luma AIのビデオ生成機能を紹介するために、`first_image` と `Lumaコンセプト` を使用した例を作成しました。

### 1. ワークフローをダウンロードする

ワークフロー情報は、下のビデオのメタデータに含まれています。ダウンロードしてComfyUIにドラッグすると、ワークフローが読み込まれます。

![Luma画像から動画ワークフロー](https://raw.githubusercontent.com/Comfy-Org/example_workflows/main/api_nodes/luma/i2v/luma_i2v.mp4)

入力として使用する次の画像をダウンロードします：

![入力画像](https://raw.githubusercontent.com/Comfy-Org/example_workflows/main/api_nodes/luma/i2v/input.png)

### 2. ワークフローの手順に従う

![Luma画像から動画ワークフローの手順](/images/tutorial/api_nodes/luma/luma_i2v_step_guide.jpg)

以下の基本的な手順に従って、ワークフローを実行します：

1. `first_image` ノードで入力画像をアップロードします
2. （任意）画像をどのようにアニメーション化するかを説明するプロンプトを、Luma画像から動画ノードに入力します
3. （任意）`Lumaコンセプト` ノードを変更して、プロフェッショナルな撮影のためのカメラの動きを制御します
4. `Run` をクリックするか、`Ctrl(cmd) + Enter` を使用してビデオを生成します
5. APIが結果を返したら、`ビデオを保存` ノードで生成済みのビデオを確認します。ビデオは `ComfyUI/output/` ディレクトリにも保存されます

### 3. 補足事項

- **画像入力の要件**：`first_image` または `last_image` の少なくとも1つが必須です。各入力につき画像は最大1枚です
- **Lumaコンセプト**：プロフェッショナルなビデオエフェクトのためのカメラの動きを制御します
- **シードパラメータ**：ノードを再実行するかどうかのみを決定し、生成結果には影響しません
- **入力ノードの有効化**：パープルの「バイパス」モードのノードを右クリックし、「モード」を「常時」に設定して入力を有効にします
- **モデルの選択**：ビデオ生成モデルにはそれぞれ異なる特性があり、モデルパラメータで調整できます
- **解像度と再生時間**：解像度と再生時間のパラメータを使用して、出力ビデオの解像度と長さを調整します
