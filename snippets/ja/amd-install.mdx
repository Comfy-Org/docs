```
pip3 install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/rocm6.4
```

または、PyTorch の Nightly バージョン（ROCm 7.1）をインストールすることもできます。

<Accordion title="Nightly をインストール">
<Warning>Nightly バージョンのインストール（リスクが高い可能性があります）</Warning>
```
pip3 install --pre torch torchvision torchaudio --index-url https://download.pytorch.org/whl/nightly/rocm7.1
```
</Accordion>
