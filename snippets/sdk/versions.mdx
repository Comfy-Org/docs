{/*
  SINGLE SOURCE OF TRUTH for the published SDK version. Every page that states a
  version imports this snippet instead of writing its own number, so a release
  updates one file. Do not add a version number to an SDK page; add it here.
  Last checked against the registries: 2026-09-19.
*/}

<Note>
  **Current release: 0.4.0.** [`comfy-sdk`](https://pypi.org/project/comfy-sdk/) on PyPI and [`@comfyorg/sdk`](https://www.npmjs.com/package/@comfyorg/sdk) on npm are released together and share a version number. Install the latest (`pip install comfy-sdk`, `npm install @comfyorg/sdk`) or state a floor rather than an exact pin: `comfy-sdk>=0.4`, `@comfyorg/sdk@^0.4`. The registry pages are authoritative if this note is behind.
</Note>
