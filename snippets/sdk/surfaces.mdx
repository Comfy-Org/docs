{/*
  Shared opener naming both SDK surfaces, imported by:
    development/api-development/sdks.mdx   (the Comfy Cloud / Comfy API v2 page)
    development/comfy-router/api.mdx       (the Router page)
  It lives here so neither page can describe the package as if it were the only
  surface. Edit here, not on either page.

  Router's own quickstart.mdx is deliberately NOT in that list: it is sync-owned
  (written upstream and republished here verbatim, see
  .github/scripts/sync-owned), so an import added there would be reverted by the
  next sync. api.mdx is the first editable page in the Router nav.
*/}

**One package, two surfaces.** `comfy-sdk` (PyPI) and `@comfyorg/sdk` (npm) ship two clients that talk to two different services. Method names repeat across them (`run`, `submit`, `events`) and mean different things in each, so check which client a snippet builds before you copy it.

| Surface | What it does | How you reach it | Base URL | Auth |
|---|---|---|---|---|
| **[Comfy Router](/development/comfy-router/quickstart)** | Calls a partner model (Flux, Veo, Gemini, Kling) with that provider's own request body and hands back its native response | Python: `client.models` on a `Comfy()`. TypeScript: `comfy.models` on the module-level `comfy` namespace | `https://api.comfy.org`, route `/v2/models/{provider}/{model}` | `COMFY_API_KEY`, or `comfy.config({ credentials })` |
| **[Comfy Cloud and Comfy API v2](/development/api-development/sdks)** | Runs an API-format ComfyUI workflow end to end: upload assets, submit the graph, follow the job, download outputs | `Comfy(api_key=...)` / `new Comfy({ apiKey })`, then `client.workflows`, `client.assets`, `client.jobs` | `https://cloud.comfy.org` by default, or whatever `COMFY_BASE_URL` names | `api_key` / `apiKey` on the constructor |

Neither surface wraps the other, and one API key created in [your Comfy workspace](https://platform.comfy.org/profile/api-keys) works for both.

<Note>
  The two languages expose the Router surface differently. In Python, `Comfy()` carries both: `client.models.run(...)` for Router and `client.workflows` / `client.assets` / `client.jobs` for Cloud. In TypeScript they are separate exports with deliberately similar names: `comfy` (lowercase, the module-level namespace) holds `comfy.models`, while `Comfy` (the class) is the Cloud client and has no `.models`.
</Note>
