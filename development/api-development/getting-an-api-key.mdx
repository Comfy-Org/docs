---
title: 'Getting an API Key'
description: 'Learn how to create and manage your Comfy Platform API Key for calling Router models, running workflows, and using Partner Nodes.'
icon: 'key'
---

import GetApiKey from '/snippets/get-api-key.mdx'

An API Key is your credential for accessing Comfy Platform services, including:

- [Comfy Router](/development/comfy-router/quickstart): call hosted models directly
- [Workflow API](/development/api-development/quickstart): run ComfyUI workflows programmatically
- [Partner Node API Integration](/development/comfyui-server/api-key-integration): use paid Partner Nodes in your local workflows
- [Agent Tools / MCP](/agent-tools): connect to Comfy through MCP
- [Login to ComfyUI locally](/interface/user#logging-in-with-an-api-key): authenticate with an API Key instead of email

## Creating an API Key

Open [API Keys in the Developer Platform](https://platform.comfy.org/profile/api-keys) and sign in. If sign-in takes you to the platform home, open the API Keys link again.

<GetApiKey />

## Make your first request

Set the key in your terminal. The quickstart SDK examples read `COMFY_API_KEY` from the environment:

```bash
export COMFY_API_KEY="comfyui-..."
```

Keep the key in your local environment or on your server. Do not put it in browser JavaScript or commit it to version control.

<CardGroup cols={2}>
  <Card title="Continue with Comfy Router" icon="rocket" href="/development/comfy-router/quickstart">
    Generate an image with a hosted model. Continue from the request-key step if your API key is already set.
  </Card>
  <Card title="Continue with a workflow" icon="play" href="/development/api-development/quickstart">
    Download the sample workflow, run it, and save its output on Comfy Cloud.
  </Card>
</CardGroup>

Creating a key does not grant access to every service. Check the prerequisites in your chosen quickstart before running it.

## Managing your API Keys

You can view and manage all your API Keys on [platform.comfy.org](https://platform.comfy.org) after logging in.

- **View existing keys**: See the name and creation date of each key
- **Delete a key**: If a key is compromised or no longer needed, delete it immediately
- **Create additional keys**: You can have multiple API Keys for different applications

<Warning>
  API Keys are only shown once upon creation. Save it immediately in a secure location. If you lose a key, delete it and create a new one.
</Warning>

## Using your API Key

Once you have an API Key, you can:

1. **Log in to ComfyUI locally**: go to **Settings → User** and select "Comfy API Key" login
2. **Call Partner Node APIs**: include the key in `extra_data.api_key_comfy_org` when sending workflows
3. **Call Router models or run workflows**: follow the quickstarts above for the appropriate SDK or request headers

For security, never share your API Key or commit it to version control.
