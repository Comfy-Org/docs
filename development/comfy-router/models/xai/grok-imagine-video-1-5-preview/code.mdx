---
title: "Use Grok Imagine Video 1.5 Preview with Comfy Router"
description: "Call xai/grok-imagine-video-1.5-preview through Comfy Router: endpoint, request shape and the response Router returns."
sidebarTitle: "Grok Imagine Video 1.5 Preview"
---

{/* GENERATED FILE. Generated from router-schemas/xai/grok-imagine-video-1.5-preview.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/comfy-router/model-code-footer.mdx";

Use `xai/grok-imagine-video-1.5-preview` with the Comfy Router API. xAI provides the model; Router gives it the shared authentication and request route below.

## Quick start

Create a key at [platform.comfy.org/profile/api-keys](https://platform.comfy.org/profile/api-keys) and export it as `COMFY_API_KEY`. The Python and TypeScript snippets use the Comfy SDKs (`pip install comfy-sdk`, `npm install @comfyorg/sdk`); the cURL snippet is the same call over raw HTTP.

**Model ID:** `xai/grok-imagine-video-1.5-preview`

**Endpoint:** `POST https://api.comfy.org/v2/models/xai/grok-imagine-video-1.5-preview`

<CodeGroup>
```python Python
import uuid
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment. Store this UUID before starting a
# generation. Use the same value if you retry it.
idempotency_key = str(uuid.uuid4())
with Comfy() as client:
    result = client.models.run(
        "xai/grok-imagine-video-1.5-preview",
        {
            "duration": 4,
            "prompt": "a single red maple leaf falling onto still water, slow motion",
        },
        idempotency_key=idempotency_key,
        timeout=660.0,
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment. Store this UUID before starting a
// generation. Use the same value if you retry it.
const idempotencyKey = crypto.randomUUID();
const { data } = await comfy.models.run("xai/grok-imagine-video-1.5-preview", {
  duration: 4,
  prompt: "a single red maple leaf falling onto still water, slow motion",
}, { idempotencyKey, timeoutMs: 660_000 });

console.log(data);
```

```bash cURL
# Run this line once. Reuse ROUTER_REQUEST_KEY if you retry the curl command.
ROUTER_REQUEST_KEY=$(uuidgen)
curl --max-time 660 https://api.comfy.org/v2/models/xai/grok-imagine-video-1.5-preview \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $ROUTER_REQUEST_KEY" \
  -H "Content-Type: application/json" \
  -d "{\"duration\": 4, \"prompt\": \"a single red maple leaf falling onto still water, slow motion\"}"
```
</CodeGroup>

## Schema

### Input

<ParamField body="aspect_ratio" type="string" default="&quot;16:9&quot;">
  Aspect ratio of the generated video

  Possible values: `1:1`, `16:9`, `9:16`, `4:3`, `3:4`, `3:2`, `2:3`
</ParamField>

<ParamField body="duration" type="integer" default="8">
  Video duration in seconds. Range [1, 15]. Default 8.

  Range: `1` to `15`
</ParamField>

<ParamField body="image" type="object">
  Input image object for xAI endpoints
</ParamField>

<ParamField body="image.type" type="string">
  Type of the image input

  Possible values: `image_url`
</ParamField>

<ParamField body="image.url" type="string" required>
  URL of the input image (public URL or base64-encoded data URI)
</ParamField>

<ParamField body="model" type="string">
  Model to be used
</ParamField>

<ParamField body="output" type="object">
  Optional output destination for generated video
</ParamField>

<ParamField body="prompt" type="string" required>
  Prompt for video generation. Maximum 4,096 characters.
</ParamField>

<ParamField body="reference_images" type="object[]">
  One or more reference images to guide the video generation (reference-to-video mode). Mutually exclusive with image. Router forwards application/json only and neither this object nor XAIImageObject accepts a file_id, so file-backed image inputs are not supported on this route.
</ParamField>

<ParamField body="reference_images[].url" type="string" required>
  URL of the reference image. Supports HTTPS URLs (public) or base64-encoded data URLs (e.g., data:image/jpeg;base64,...).
</ParamField>

<ParamField body="resolution" type="string">
  Resolution of the output video
</ParamField>

<ParamField body="size" type="string">
  Size of the output video
</ParamField>

<ParamField body="user" type="string">
  A unique identifier representing your end-user
</ParamField>

Generated from the schema Router serves at `GET /v2/models/xai/grok-imagine-video-1.5-preview/openapi.json`, the same document it validates a call against before the request reaches the provider.

### Output

<ResponseField name="block_reason" type="string">
  If the request was blocked by input moderation, contains the block reason
</ResponseField>

<ResponseField name="model" type="string">
  The model used to generate the video
</ResponseField>

<ResponseField name="status" type="string">
  Status of the deferred request: "pending" or "done"

  Possible values: `pending`, `done`
</ResponseField>

<ResponseField name="usage" type="object">
  Usage information for the video generation request
</ResponseField>

<ResponseField name="usage.cost_in_usd_ticks" type="integer">
  The cost of this request expressed in USD ticks. One USD cent equals 100,000,000 ticks, so one US dollar equals 10,000,000,000 ticks.
</ResponseField>

<ResponseField name="video" type="object">
  A generated video from xAI
</ResponseField>

<ResponseField name="video.duration" type="integer">
  Duration of the generated video in seconds
</ResponseField>

<ResponseField name="video.respect_moderation" type="boolean">
  Whether the video generated by the model respects moderation rules
</ResponseField>

<ResponseField name="video.url" type="string">
  Download URL for the generated video. Router re-hosts the video onto Comfy storage and rewrites this field, so it is normally a Comfy-signed URL valid for up to 24 hours - signed for 24 hours when minted and replayed from a 23-hour memo, so a later poll can hand back one with as little as an hour left. When the re-host could not be performed the field keeps xAI's own short-lived URL instead. NULLABLE: a success whose `url` is empty is not a completed generation. Either way the link expires, so download the video rather than storing the URL.
</ResponseField>

## Examples

### Input

```json
{
  "duration": 4,
  "prompt": "a single red maple leaf falling onto still water, slow motion"
}
```

### Output

This provider example names `grok-imagine-video`, not `xai/grok-imagine-video-1.5-preview`. Use it only for the response shape.

```json
{
  "model": "grok-imagine-video",
  "status": "done",
  "usage": {
    "cost_in_usd_ticks": 3500000000
  },
  "video": {
    "duration": 4,
    "respect_moderation": true,
    "url": "https://example.invalid/xai/grok-imagine-video/generated.mp4"
  }
}
```


<RouterCodeFooter />
