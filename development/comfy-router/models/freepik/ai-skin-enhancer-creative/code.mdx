---
title: "Use AI Skin Enhancer Creative with Comfy Router"
description: "Call freepik/ai-skin-enhancer-creative through Comfy Router: endpoint, request shape and the response Router returns."
sidebarTitle: "AI Skin Enhancer Creative"
---

{/* GENERATED FILE. Generated from router-schemas/freepik/ai-skin-enhancer-creative.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/comfy-router/model-code-footer.mdx";

Use `freepik/ai-skin-enhancer-creative` with the Comfy Router API. Freepik provides the model; Router gives it the shared authentication and request route below.

## Quick start

Create a key at [platform.comfy.org/profile/api-keys](https://platform.comfy.org/profile/api-keys) and export it as `COMFY_API_KEY`. The Python and TypeScript snippets use the Comfy SDKs (`pip install comfy-sdk`, `npm install @comfyorg/sdk`); the cURL snippet is the same call over raw HTTP.

**Model ID:** `freepik/ai-skin-enhancer-creative`

**Endpoint:** `POST https://api.comfy.org/v2/models/freepik/ai-skin-enhancer-creative`

<CodeGroup>
```python Python
import uuid
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment. Store this UUID before starting a
# generation. Use the same value if you retry it.
idempotency_key = str(uuid.uuid4())
with Comfy() as client:
    result = client.models.run(
        "freepik/ai-skin-enhancer-creative",
        {
            "image": "https://img.magnific.com/free-photo/portrait-woman_395237-33.jpg?w=740&q=80",
        },
        idempotency_key=idempotency_key,
        timeout=660.0,
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const { data } = await comfy.models.run("freepik/ai-skin-enhancer-creative", {
  image: "https://img.magnific.com/free-photo/portrait-woman_395237-33.jpg?w=740&q=80",
});

console.log(data);
```

```bash cURL
ROUTER_REQUEST_KEY=$(uuidgen)
curl --max-time 660 https://api.comfy.org/v2/models/freepik/ai-skin-enhancer-creative \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $ROUTER_REQUEST_KEY" \
  -H "Content-Type: application/json" \
  -d "{\"image\": \"https://img.magnific.com/free-photo/portrait-woman_395237-33.jpg?w=740&q=80\"}"
```
</CodeGroup>

## Schema

### Input

<ParamField body="image" type="string" required>
  Input image. Supports Base64 encoding or HTTPS URL (must be publicly accessible).
</ParamField>

<ParamField body="sharpen" type="integer" default="0">
  Sharpening intensity

  Range: `0` to `100`
</ParamField>

<ParamField body="smart_grain" type="integer" default="2">
  Smart grain intensity

  Range: `0` to `100`
</ParamField>

<ParamField body="webhook_url" type="string (uri)">
  Optional callback URL for async notifications.

  Format: `uri`
</ParamField>

Generated from the schema Router serves at `GET /v2/models/freepik/ai-skin-enhancer-creative/openapi.json`, the same document it validates a call against before the request reaches the provider.

### Output

Router does not publish an output schema for this model.

## Examples

### Input

```json
{
  "image": "https://img.magnific.com/free-photo/portrait-woman_395237-33.jpg?w=740&q=80"
}
```


<RouterCodeFooter />
