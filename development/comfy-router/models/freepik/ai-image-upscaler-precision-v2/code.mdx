---
title: "Use AI Image Upscaler Precision V2 with Comfy Router"
description: "Call freepik/ai-image-upscaler-precision-v2 through Comfy Router: endpoint, request shape and the response Router returns."
sidebarTitle: "AI Image Upscaler Precision V2"
---

{/* GENERATED FILE. Generated from router-schemas/freepik/ai-image-upscaler-precision-v2.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/comfy-router/model-code-footer.mdx";

Use `freepik/ai-image-upscaler-precision-v2` with the Comfy Router API. Freepik provides the model; Router gives it the shared authentication and request route below.

## Quick start

Create a key at [platform.comfy.org/profile/api-keys](https://platform.comfy.org/profile/api-keys) and export it as `COMFY_API_KEY`. The Python and TypeScript snippets use the Comfy SDKs (`pip install comfy-sdk`, `npm install @comfyorg/sdk`); the cURL snippet is the same call over raw HTTP.

**Model ID:** `freepik/ai-image-upscaler-precision-v2`

**Endpoint:** `POST https://api.comfy.org/v2/models/freepik/ai-image-upscaler-precision-v2`

<CodeGroup>
```python Python
import uuid
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment. Store this UUID before starting a
# generation. Use the same value if you retry it.
idempotency_key = str(uuid.uuid4())
with Comfy() as client:
    result = client.models.run(
        "freepik/ai-image-upscaler-precision-v2",
        {
            "image": "https://img.magnific.com/free-photo/light-through-mountains_395237-33.jpg?semt=ais_hybrid&w=740&q=80",
            "scale_factor": 8,
        },
        idempotency_key=idempotency_key,
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const { data } = await comfy.models.run("freepik/ai-image-upscaler-precision-v2", {
  image: "https://img.magnific.com/free-photo/light-through-mountains_395237-33.jpg?semt=ais_hybrid&w=740&q=80",
  scale_factor: 8,
});

console.log(data);
```

```bash cURL
ROUTER_REQUEST_KEY=$(uuidgen)
curl --max-time 660 https://api.comfy.org/v2/models/freepik/ai-image-upscaler-precision-v2 \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $ROUTER_REQUEST_KEY" \
  -H "Content-Type: application/json" \
  -d "{\"image\": \"https://img.magnific.com/free-photo/light-through-mountains_395237-33.jpg?semt=ais_hybrid&w=740&q=80\", \"scale_factor\": 8}"
```
</CodeGroup>

## Schema

### Input

<ParamField body="flavor" type="string">
  Image processing flavor:
- sublime: Optimized for artistic and illustrated images
- photo: Optimized for photographic images
- photo_denoiser: Specialized for photos with noise reduction

  Possible values: `sublime`, `photo`, `photo_denoiser`
</ParamField>

<ParamField body="image" type="string" required>
  Source image to upscale. Accepts either:
- A publicly accessible HTTPS URL pointing to the image
- A base64-encoded image string
</ParamField>

<ParamField body="scale_factor" type="integer">
  Image scaling factor. Determines how much larger the output will be compared to input.

  Range: `2` to `16`
</ParamField>

<ParamField body="sharpen" type="integer" default="7">
  Image sharpness intensity control. Higher values increase edge definition and clarity.

  Range: `0` to `100`
</ParamField>

<ParamField body="smart_grain" type="integer" default="7">
  Intelligent grain/texture enhancement. Higher values add more fine-grained texture.

  Range: `0` to `100`
</ParamField>

<ParamField body="ultra_detail" type="integer" default="30">
  Ultra detail enhancement level. Higher values create more intricate details.

  Range: `0` to `100`
</ParamField>

<ParamField body="webhook_url" type="string (uri)">
  Optional callback URL that will receive asynchronous notifications when the upscaling task completes.

  Format: `uri`
</ParamField>

Generated from the schema Router serves at `GET /v2/models/freepik/ai-image-upscaler-precision-v2/openapi.json`, the same document it validates a call against before the request reaches the provider.

### Output

Router does not publish an output schema for this model.

## Examples

### Input

```json
{
  "image": "https://img.magnific.com/free-photo/light-through-mountains_395237-33.jpg?semt=ais_hybrid&w=740&q=80",
  "scale_factor": 8
}
```


<RouterCodeFooter />
