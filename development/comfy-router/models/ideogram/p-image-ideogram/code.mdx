---
title: "Use P Image Ideogram with Comfy Router"
description: "Call ideogram/p-image-ideogram through Comfy Router: endpoint, request shape and the response Router returns."
sidebarTitle: "P Image Ideogram"
---

{/* GENERATED FILE. Generated from router-schemas/ideogram/p-image-ideogram.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/comfy-router/model-code-footer.mdx";

API Reference for `ideogram/p-image-ideogram`, served by Comfy Router from Ideogram.

## Quick start

Create a key in [your Comfy workspace](https://platform.comfy.org/profile/api-keys) and export it as `COMFY_API_KEY`. The Python and TypeScript snippets use the Comfy SDKs (`pip install comfy-sdk`, `npm install @comfyorg/sdk`); the cURL snippet is the same call over raw HTTP.

**Model ID:** `ideogram/p-image-ideogram`

**Endpoint:** `POST https://api.comfy.org/v2/models/ideogram/p-image-ideogram`

<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "ideogram/p-image-ideogram",
        {
            "prompt": "A beautiful mountain landscape",
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const { data } = await comfy.models.run("ideogram/p-image-ideogram", {
  prompt: "A beautiful mountain landscape",
});

console.log(data);
```

```bash cURL
curl https://api.comfy.org/v2/models/ideogram/p-image-ideogram \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"prompt\": \"A beautiful mountain landscape\"}"
```
</CodeGroup>

## Schema

### Input

<ParamField body="aspect_ratio" type="string">
  Aspect ratio in format WxH. Supported values: 1x3, 3x1, 1x2, 2x1, 9x16, 16x9, 10x16, 16x10, 2x3, 3x2, 3x4, 4x3, 4x5, 5x4, 1x1. Defaults to 1x1.
</ParamField>

<ParamField body="enable_copyright_detection" type="boolean">
  Opt into post-generation copyright detection (Hive likeness and logo checks).
</ParamField>

<ParamField body="prompt" type="string" required>
  The prompt for image generation, either natural language or an Ideogram 4.0 JSON structured prompt.
</ParamField>

<ParamField body="prompt_upsampling" type="string">
  Prompt upsampling mode. Supported values are AUTO, ON, and OFF. Defaults to AUTO.
</ParamField>

<ParamField body="quality" type="string">
  Generation quality level. Supported values are VERY_LOW, LOW, MEDIUM, and HIGH. Defaults to MEDIUM.
</ParamField>

<ParamField body="resolution" type="string">
  Output resolution. Supported values are 1K and 2K. Defaults to 1K.
</ParamField>

<ParamField body="seed" type="integer">
  Seed value for reproducible generation
</ParamField>

Generated from the schema Router serves at `GET /v2/models/ideogram/p-image-ideogram/openapi.json`, the same document it validates a call against before the request reaches the provider.

### Output

<ResponseField name="created" type="string (date-time)">
  Timestamp when the generation was created.

  Format: `date-time`
</ResponseField>

<ResponseField name="data" type="object[]">
  Array of generated image information.
</ResponseField>

<ResponseField name="data[].is_image_safe" type="boolean">
  Indicates whether the image is considered safe.
</ResponseField>

<ResponseField name="data[].prompt" type="string">
  The prompt used to generate this image.
</ResponseField>

<ResponseField name="data[].resolution" type="string">
  The resolution of the generated image (e.g., '1024x1024').
</ResponseField>

<ResponseField name="data[].seed" type="integer">
  The seed value used for this generation.
</ResponseField>

<ResponseField name="data[].style_type" type="string">
  The style type used for generation (e.g., 'REALISTIC', 'ANIME').
</ResponseField>

<ResponseField name="data[].url" type="string">
  URL to the generated image.
</ResponseField>

## Examples

### Input

```json
{
  "prompt": "A beautiful mountain landscape"
}
```

### Output

```json
{
  "created": "2026-01-01T00:00:00Z",
  "data": [
    {
      "is_image_safe": true,
      "prompt": "A poster for a jazz festival, bold typography, warm colours",
      "resolution": "1024x1024",
      "seed": 918273645,
      "style_type": "REALISTIC",
      "url": "https://example.invalid/ideogram/p-image-ideogram/generated.png"
    }
  ]
}
```


<RouterCodeFooter />
