---
title: "Use H3 Max with Comfy Router"
description: "Call fal/h3-max through Comfy Router: endpoint, request shape and the response Router returns."
sidebarTitle: "H3 Max"
---

{/* GENERATED FILE. Generated from router-schemas/fal/h3-max.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/comfy-router/model-code-footer.mdx";

Use `fal/h3-max` with the Comfy Router API. fal provides the model; Router gives it the shared authentication and request route below.

## Quick start

Create a key at [platform.comfy.org/profile/api-keys](https://platform.comfy.org/profile/api-keys) and export it as `COMFY_API_KEY`. The Python and TypeScript snippets use the Comfy SDKs (`pip install comfy-sdk`, `npm install @comfyorg/sdk`); the cURL snippet is the same call over raw HTTP.

**Model ID:** `fal/h3-max`

**Endpoint:** `POST https://api.comfy.org/v2/models/fal/h3-max`

<CodeGroup>
```python Python
import uuid
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment. Store this UUID before starting a
# generation. Use the same value if you retry it.
idempotency_key = str(uuid.uuid4())
with Comfy() as client:
    result = client.models.run(
        "fal/h3-max",
        {
            "duration": 5,
            "prompt": "a red fox running through a snowy forest",
            "prompt_expansion_mode": "balanced",
            "resolution": "480P",
        },
        idempotency_key=idempotency_key,
        timeout=660.0,
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const { data } = await comfy.models.run("fal/h3-max", {
  duration: 5,
  prompt: "a red fox running through a snowy forest",
  prompt_expansion_mode: "balanced",
  resolution: "480P",
}, { timeoutMs: 660_000 });

console.log(data);
```

```bash cURL
ROUTER_REQUEST_KEY=$(uuidgen)
curl --max-time 660 https://api.comfy.org/v2/models/fal/h3-max \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $ROUTER_REQUEST_KEY" \
  -H "Content-Type: application/json" \
  -d "{\"duration\": 5, \"prompt\": \"a red fox running through a snowy forest\", \"prompt_expansion_mode\": \"balanced\", \"resolution\": \"480P\"}"
```
</CodeGroup>

## Schema

### Input

<ParamField body="aspect_ratio" type="string" default="&quot;16:9&quot;">
  Aspect ratio: 21:9, 16:9, 4:3, 1:1, 3:4, or 9:16.
</ParamField>

<ParamField body="duration" type="integer" default="5">
  Duration of the generated video in seconds.
</ParamField>

<ParamField body="enable_safety_checker" type="boolean" default="true">
  Enable the safety checker.
</ParamField>

<ParamField body="prompt" type="string" required>
  Text prompt for video generation.
</ParamField>

<ParamField body="prompt_expansion_mode" type="string" required>
  How much effort to spend rewriting the prompt before generation: balanced or quality.
</ParamField>

<ParamField body="resolution" type="string" default="&quot;768P&quot;">
  Resolution of the generated video: 480P or 768P.
</ParamField>

<ParamField body="seed" type="integer">
  Random seed. A random seed is selected when omitted.
</ParamField>

<ParamField body="sync_mode" type="boolean">
  Return the generated video as base64 instead of a CDN URL.
</ParamField>

Generated from the schema Router serves at `GET /v2/models/fal/h3-max/openapi.json`, the same document it validates a call against before the request reaches the provider.

### Output

Router does not publish an output schema for this model.

## Examples

### Input

```json
{
  "duration": 5,
  "prompt": "a red fox running through a snowy forest",
  "prompt_expansion_mode": "balanced",
  "resolution": "480P"
}
```


<RouterCodeFooter />
