---
title: "Use Patina with Comfy Router"
description: "Call fal/patina through Comfy Router: endpoint, request shape and the response Router returns."
sidebarTitle: "Patina"
---

{/* GENERATED FILE. Generated from router-schemas/fal/patina.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/comfy-router/model-code-footer.mdx";

Use `fal/patina` with the Comfy Router API. fal provides the model; Router gives it the shared authentication and request route below.

## Quick start

Create a key at [platform.comfy.org/profile/api-keys](https://platform.comfy.org/profile/api-keys) and export it as `COMFY_API_KEY`. The Python and TypeScript snippets use the Comfy SDKs (`pip install comfy-sdk`, `npm install @comfyorg/sdk`); the cURL snippet is the same call over raw HTTP.

**Model ID:** `fal/patina`

**Endpoint:** `POST https://api.comfy.org/v2/models/fal/patina`

<CodeGroup>
```python Python
import uuid
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment. Store this UUID before starting a
# generation. Use the same value if you retry it.
idempotency_key = str(uuid.uuid4())
with Comfy() as client:
    result = client.models.run(
        "fal/patina",
        {
            "image_url": "https://storage.googleapis.com/falserverless/gallery/patina-blog-hero-render.png",
        },
        idempotency_key=idempotency_key,
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const { data } = await comfy.models.run("fal/patina", {
  image_url: "https://storage.googleapis.com/falserverless/gallery/patina-blog-hero-render.png",
});

console.log(data);
```

```bash cURL
ROUTER_REQUEST_KEY=$(uuidgen)
curl --max-time 660 https://api.comfy.org/v2/models/fal/patina \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $ROUTER_REQUEST_KEY" \
  -H "Content-Type: application/json" \
  -d "{\"image_url\": \"https://storage.googleapis.com/falserverless/gallery/patina-blog-hero-render.png\"}"
```
</CodeGroup>

## Schema

### Input

<ParamField body="enable_safety_checker" type="boolean" default="true">
  Enable the safety checker for images.
</ParamField>

<ParamField body="image_url" type="string" required>
  URL of the input image (photograph or render).
</ParamField>

<ParamField body="maps" type="string[]" default="[&quot;basecolor&quot;,&quot;normal&quot;,&quot;roughness&quot;,&quot;metalness&quot;,&quot;height&quot;]">
  Which PBR maps to predict. Defaults to all five.
</ParamField>

<ParamField body="output_format" type="string" default="&quot;png&quot;">
  Output image format: jpeg, png, or webp.
</ParamField>

<ParamField body="seed" type="integer">
  Random seed for reproducible denoising.
</ParamField>

<ParamField body="sync_mode" type="boolean" default="false">
  If true, return images as data URIs instead of CDN URLs.
</ParamField>

Generated from the schema Router serves at `GET /v2/models/fal/patina/openapi.json`, the same document it validates a call against before the request reaches the provider.

### Output

Router does not publish an output schema for this model.

## Examples

### Input

```json
{
  "image_url": "https://storage.googleapis.com/falserverless/gallery/patina-blog-hero-render.png"
}
```


<RouterCodeFooter />
