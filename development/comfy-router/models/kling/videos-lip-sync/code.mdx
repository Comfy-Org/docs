---
title: "Use Videos Lip Sync with Comfy Router"
description: "Call kling/videos-lip-sync through Comfy Router: endpoint, request shape and the response Router returns."
sidebarTitle: "Videos Lip Sync"
---

{/* GENERATED FILE. Generated from router-schemas/kling/videos-lip-sync.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/comfy-router/model-code-footer.mdx";

Use `kling/videos-lip-sync` with the Comfy Router API. Kling provides the model; Router gives it the shared authentication and request route below.

## Quick start

Create a key at [platform.comfy.org/profile/api-keys](https://platform.comfy.org/profile/api-keys) and export it as `COMFY_API_KEY`. The Python and TypeScript snippets use the Comfy SDKs (`pip install comfy-sdk`, `npm install @comfyorg/sdk`); the cURL snippet is the same call over raw HTTP.

**Model ID:** `kling/videos-lip-sync`

**Endpoint:** `POST https://api.comfy.org/v2/models/kling/videos-lip-sync`

<CodeGroup>
```python Python
import uuid
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment. Store this UUID before starting a
# generation. Use the same value if you retry it.
idempotency_key = str(uuid.uuid4())
with Comfy() as client:
    result = client.models.run(
        "kling/videos-lip-sync",
        {
            "input": {
                "mode": "text2video",
                "text": "Welcome to Comfy Cloud.",
                "video_id": "kling-video-6f5e4d3c2b1a",
                "voice_id": "genshin_vindi2",
                "voice_language": "en",
                "voice_speed": 1,
            },
        },
        idempotency_key=idempotency_key,
        timeout=660.0,
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const { data } = await comfy.models.run("kling/videos-lip-sync", {
  input: {
    mode: "text2video",
    text: "Welcome to Comfy Cloud.",
    video_id: "kling-video-6f5e4d3c2b1a",
    voice_id: "genshin_vindi2",
    voice_language: "en",
    voice_speed: 1,
  },
});

console.log(data);
```

```bash cURL
ROUTER_REQUEST_KEY=$(uuidgen)
curl --max-time 660 https://api.comfy.org/v2/models/kling/videos-lip-sync \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $ROUTER_REQUEST_KEY" \
  -H "Content-Type: application/json" \
  -d "{\"input\": {\"mode\":\"text2video\",\"text\":\"Welcome to Comfy Cloud.\",\"video_id\":\"kling-video-6f5e4d3c2b1a\",\"voice_id\":\"genshin_vindi2\",\"voice_language\":\"en\",\"voice_speed\":1}}"
```
</CodeGroup>

## Schema

### Input

<ParamField body="callback_url" type="string (uri)">
  The callback notification address. Server will notify when the task status changes.

  Format: `uri`
</ParamField>

<ParamField body="input" type="object" required>
   
</ParamField>

<ParamField body="input.audio_file" type="string">
  Local Path of Audio File. Supported formats: .mp3/.wav/.m4a/.aac, maximum file size of 5MB. Base64 code.
</ParamField>

<ParamField body="input.audio_type" type="string">
  Method of Transmitting Audio Files for Lip-Sync. Required when mode is audio2video.

  Possible values: `file`, `url`
</ParamField>

<ParamField body="input.audio_url" type="string">
  Audio File Download URL. Supported formats: .mp3/.wav/.m4a/.aac, maximum file size of 5MB.
</ParamField>

<ParamField body="input.mode" type="string" required>
  Video Generation Mode. text2video: Text-to-video generation mode; audio2video: Audio-to-video generation mode

  Possible values: `text2video`, `audio2video`
</ParamField>

<ParamField body="input.text" type="string">
  Text Content for Lip-Sync Video Generation. Required when mode is text2video. Maximum length is 120 characters.
</ParamField>

<ParamField body="input.video_id" type="string">
  The ID of the video generated by Kling AI. Only supports 5-second and 10-second videos generated within the last 30 days.
</ParamField>

<ParamField body="input.video_url" type="string">
  Get link for uploaded video. Video files support .mp4/.mov, file size does not exceed 100MB, video length between 2-10s.
</ParamField>

<ParamField body="input.voice_id" type="string">
  Voice ID. Required when mode is text2video. The system offers a variety of voice options to choose from.
</ParamField>

<ParamField body="input.voice_language" type="string" default="&quot;en&quot;">
  The voice language corresponds to the Voice ID.

  Possible values: `zh`, `en`
</ParamField>

<ParamField body="input.voice_speed" type="number" default="1">
  Speech Rate. Valid range: 0.8~2.0, accurate to one decimal place.

  Range: `0.8` to `2`
</ParamField>

Generated from the schema Router serves at `GET /v2/models/kling/videos-lip-sync/openapi.json`, the same document it validates a call against before the request reaches the provider.

### Output

Router does not publish an output schema for this model.

## Examples

### Input

```json
{
  "input": {
    "mode": "text2video",
    "text": "Welcome to Comfy Cloud.",
    "video_id": "kling-video-6f5e4d3c2b1a",
    "voice_id": "genshin_vindi2",
    "voice_language": "en",
    "voice_speed": 1
  }
}
```


<RouterCodeFooter />
