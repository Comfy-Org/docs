---
title: "Use Imagen 3.0 Generate 001 with Comfy Router"
description: "Call vertexai/imagen-3.0-generate-001 through Comfy Router: endpoint, request shape and the response Router returns."
sidebarTitle: "Imagen 3.0 Generate 001"
---

{/* GENERATED FILE. Generated from router-schemas/vertexai/imagen-3.0-generate-001.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/comfy-router/model-code-footer.mdx";

API Reference for `vertexai/imagen-3.0-generate-001`, served by Comfy Router from Google.

## Quick start

Create a key at [platform.comfy.org/profile/api-keys](https://platform.comfy.org/profile/api-keys) and export it as `COMFY_API_KEY`. The Python and TypeScript snippets use the Comfy SDKs (`pip install comfy-sdk`, `npm install @comfyorg/sdk`); the cURL snippet is the same call over raw HTTP.

**Model ID:** `vertexai/imagen-3.0-generate-001`

**Endpoint:** `POST https://api.comfy.org/v2/models/vertexai/imagen-3.0-generate-001`

<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment. Each call sends a fresh
# Idempotency-Key and waits up to 10 minutes for the finished result.
with Comfy() as client:
    result = client.models.run(
        "vertexai/imagen-3.0-generate-001",
        {
            "instances": [
                {
                    "prompt": "A single red maple leaf on a plain white background.",
                },
            ],
            "parameters": {
                "sampleCount": 1,
            },
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment. Each call sends a fresh
// Idempotency-Key and waits up to 10 minutes for the finished result.
const { data } = await comfy.models.run("vertexai/imagen-3.0-generate-001", {
  instances: [
    {
      prompt: "A single red maple leaf on a plain white background.",
    },
  ],
  parameters: {
    sampleCount: 1,
  },
});

console.log(data);
```

```bash cURL
curl https://api.comfy.org/v2/models/vertexai/imagen-3.0-generate-001 \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"instances\": [{\"prompt\":\"A single red maple leaf on a plain white background.\"}], \"parameters\": {\"sampleCount\":1}}"
```
</CodeGroup>

## Schema

### Input

<ParamField body="instances" type="object[]" required>
   
</ParamField>

<ParamField body="instances[].prompt" type="string" required>
  Text prompt for image generation
</ParamField>

<ParamField body="parameters" type="object">
   
</ParamField>

<ParamField body="parameters.addWatermark" type="boolean">
   
</ParamField>

<ParamField body="parameters.aspectRatio" type="string">
  Possible values: `1:1`, `9:16`, `16:9`, `3:4`, `4:3`
</ParamField>

<ParamField body="parameters.enhancePrompt" type="boolean">
   
</ParamField>

<ParamField body="parameters.includeRaiReason" type="boolean">
   
</ParamField>

<ParamField body="parameters.includeSafetyAttributes" type="boolean">
   
</ParamField>

<ParamField body="parameters.outputOptions" type="object">
   
</ParamField>

<ParamField body="parameters.outputOptions.compressionQuality" type="integer">
  Range: `0` to `100`
</ParamField>

<ParamField body="parameters.outputOptions.mimeType" type="string">
  Possible values: `image/png`, `image/jpeg`
</ParamField>

<ParamField body="parameters.personGeneration" type="string">
  Possible values: `dont_allow`, `allow_adult`, `allow_all`
</ParamField>

<ParamField body="parameters.safetySetting" type="string">
  Possible values: `block_most`, `block_some`, `block_few`, `block_fewest`
</ParamField>

<ParamField body="parameters.sampleCount" type="integer">
  Range: `1` to `4`
</ParamField>

<ParamField body="parameters.seed" type="integer">
  Format: `uint32`
</ParamField>

<ParamField body="parameters.storageUri" type="string">
   
</ParamField>

Generated from the schema Router serves at `GET /v2/models/vertexai/imagen-3.0-generate-001/openapi.json`, the same document it validates a call against before the request reaches the provider.

### Output

Router does not publish an output schema for this model.

## Examples

### Input

```json
{
  "instances": [
    {
      "prompt": "A single red maple leaf on a plain white background."
    }
  ],
  "parameters": {
    "sampleCount": 1
  }
}
```


<RouterCodeFooter />
