---
title: "Use Starfish with Comfy Router"
description: "Call heygen/starfish through Comfy Router: endpoint, request shape and the response Router returns."
sidebarTitle: "Starfish"
---

{/* GENERATED FILE. Generated from router-schemas/heygen/starfish.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/comfy-router/model-code-footer.mdx";

Use `heygen/starfish` with the Comfy Router API. HeyGen provides the model; Router gives it the shared authentication and request route below.

## Quick start

Create a key at [platform.comfy.org/profile/api-keys](https://platform.comfy.org/profile/api-keys) and export it as `COMFY_API_KEY`. The Python and TypeScript snippets use the Comfy SDKs (`pip install comfy-sdk`, `npm install @comfyorg/sdk`); the cURL snippet is the same call over raw HTTP.

**Model ID:** `heygen/starfish`

**Endpoint:** `POST https://api.comfy.org/v2/models/heygen/starfish`

<CodeGroup>
```python Python
import uuid
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment. Store this UUID before starting a
# generation. Use the same value if you retry it.
idempotency_key = str(uuid.uuid4())
with Comfy() as client:
    result = client.models.run(
        "heygen/starfish",
        {
            "text": "This is a billing verification test for HeyGen speech generation.",
            "voice_id": "d2f4f24783d04e22ab49ee8fdc3715e0",
        },
        idempotency_key=idempotency_key,
        timeout=660.0,
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const { data } = await comfy.models.run("heygen/starfish", {
  text: "This is a billing verification test for HeyGen speech generation.",
  voice_id: "d2f4f24783d04e22ab49ee8fdc3715e0",
}, { timeoutMs: 660_000 });

console.log(data);
```

```bash cURL
ROUTER_REQUEST_KEY=$(uuidgen)
curl --max-time 660 https://api.comfy.org/v2/models/heygen/starfish \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $ROUTER_REQUEST_KEY" \
  -H "Content-Type: application/json" \
  -d "{\"text\": \"This is a billing verification test for HeyGen speech generation.\", \"voice_id\": \"d2f4f24783d04e22ab49ee8fdc3715e0\"}"
```
</CodeGroup>

## Schema

### Input

<ParamField body="input_type" type="string">
  Type of the input: 'text' for plain text, 'ssml' for SSML markup. Defaults to 'text'
</ParamField>

<ParamField body="language" type="string">
  Base language code (e.g. 'en'). Auto-detected from text when omitted
</ParamField>

<ParamField body="locale" type="string">
  BCP-47 locale tag (e.g. 'en-US'). When set, language is inferred from locale
</ParamField>

<ParamField body="speed" type="number">
  Speed multiplier (0.5-2.0)

  Range: `0.5` to `2`

  Format: `double`
</ParamField>

<ParamField body="text" type="string" required>
  Text to synthesize (1-5000 characters)
</ParamField>

<ParamField body="voice_id" type="string" required>
  Voice ID to use. The voice must support the starfish engine
</ParamField>

Generated from the schema Router serves at `GET /v2/models/heygen/starfish/openapi.json`, the same document it validates a call against before the request reaches the provider.

### Output

Router does not publish an output schema for this model.

## Examples

### Input

```json
{
  "text": "This is a billing verification test for HeyGen speech generation.",
  "voice_id": "d2f4f24783d04e22ab49ee8fdc3715e0"
}
```


<RouterCodeFooter />
