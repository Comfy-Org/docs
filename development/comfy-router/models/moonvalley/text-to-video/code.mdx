---
title: "Use Text To Video with Comfy Router"
description: "Call moonvalley/text-to-video through Comfy Router: endpoint, request shape and the response Router returns."
sidebarTitle: "Text To Video"
---

{/* GENERATED FILE. Generated from router-schemas/moonvalley/text-to-video.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/comfy-router/model-code-footer.mdx";

Use `moonvalley/text-to-video` with the Comfy Router API. Moonvalley provides the model; Router gives it the shared authentication and request route below.

## Quick start

Create a key at [platform.comfy.org/profile/api-keys](https://platform.comfy.org/profile/api-keys) and export it as `COMFY_API_KEY`. The Python and TypeScript snippets use the Comfy SDKs (`pip install comfy-sdk`, `npm install @comfyorg/sdk`); the cURL snippet is the same call over raw HTTP.

**Model ID:** `moonvalley/text-to-video`

**Endpoint:** `POST https://api.comfy.org/v2/models/moonvalley/text-to-video`

<CodeGroup>
```python Python
import uuid
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment. Store this UUID before starting a
# generation. Use the same value if you retry it.
idempotency_key = str(uuid.uuid4())
with Comfy() as client:
    result = client.models.run(
        "moonvalley/text-to-video",
        {
            "prompt_text": "a single red maple leaf",
        },
        idempotency_key=idempotency_key,
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const { data } = await comfy.models.run("moonvalley/text-to-video", {
  prompt_text: "a single red maple leaf",
});

console.log(data);
```

```bash cURL
ROUTER_REQUEST_KEY=$(uuidgen)
curl --max-time 660 https://api.comfy.org/v2/models/moonvalley/text-to-video \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $ROUTER_REQUEST_KEY" \
  -H "Content-Type: application/json" \
  -d "{\"prompt_text\": \"a single red maple leaf\"}"
```
</CodeGroup>

## Schema

### Input

<ParamField body="image_url" type="string">
   
</ParamField>

<ParamField body="inference_params" type="object">
   
</ParamField>

<ParamField body="inference_params.guidance_scale" type="number" default="10">
  Guidance scale for generation control

  Format: `float`
</ParamField>

<ParamField body="inference_params.height" type="integer" default="1080">
  Height of the generated video in pixels
</ParamField>

<ParamField body="inference_params.negative_prompt" type="string">
  Negative prompt text
</ParamField>

<ParamField body="inference_params.seed" type="integer" default="9">
  Random seed for generation (default: random)
</ParamField>

<ParamField body="inference_params.steps" type="integer" default="80">
  Number of denoising steps
</ParamField>

<ParamField body="inference_params.use_negative_prompts" type="boolean" default="true">
  Whether to use negative prompts
</ParamField>

<ParamField body="inference_params.width" type="integer" default="1920">
  Width of the generated video in pixels
</ParamField>

<ParamField body="prompt_text" type="string">
   
</ParamField>

<ParamField body="webhook_url" type="string">
   
</ParamField>

Generated from the schema Router serves at `GET /v2/models/moonvalley/text-to-video/openapi.json`, the same document it validates a call against before the request reaches the provider.

### Output

Router does not publish an output schema for this model.

## Examples

### Input

```json
{
  "prompt_text": "a single red maple leaf"
}
```


<RouterCodeFooter />
