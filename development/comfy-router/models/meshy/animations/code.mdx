---
title: "Use Animations with Comfy Router"
description: "Call meshy/animations through Comfy Router: endpoint, request shape and the response Router returns."
sidebarTitle: "Animations"
---

{/* GENERATED FILE. Generated from router-schemas/meshy/animations.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/comfy-router/model-code-footer.mdx";

API Reference for `meshy/animations`, served by Comfy Router from Meshy.

## Quick start

Create a key at [platform.comfy.org/profile/api-keys](https://platform.comfy.org/profile/api-keys) and export it as `COMFY_API_KEY`. The Python and TypeScript snippets use the Comfy SDKs (`pip install comfy-sdk`, `npm install @comfyorg/sdk`); the cURL snippet is the same call over raw HTTP.

**Model ID:** `meshy/animations`

**Endpoint:** `POST https://api.comfy.org/v2/models/meshy/animations`

<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "meshy/animations",
        {
            "action_id": 92,
            "post_process": {
                "fps": 60,
                "operation_type": "change_fps",
            },
            "rig_task_id": "0193abcd-0000-0000-0000-000000000000",
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const { data } = await comfy.models.run("meshy/animations", {
  action_id: 92,
  post_process: {
    fps: 60,
    operation_type: "change_fps",
  },
  rig_task_id: "0193abcd-0000-0000-0000-000000000000",
});

console.log(data);
```

```bash cURL
ROUTER_REQUEST_KEY=$(uuidgen)
curl --max-time 660 https://api.comfy.org/v2/models/meshy/animations \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $ROUTER_REQUEST_KEY" \
  -H "Content-Type: application/json" \
  -d "{\"action_id\": 92, \"post_process\": {\"fps\":60,\"operation_type\":\"change_fps\"}, \"rig_task_id\": \"0193abcd-0000-0000-0000-000000000000\"}"
```
</CodeGroup>

## Schema

### Input

<ParamField body="action_id" type="integer" required>
  The identifier of the animation action to apply.
</ParamField>

<ParamField body="post_process" type="object">
  Parameters for post-processing animation files.
</ParamField>

<ParamField body="post_process.fps" type="integer" default="30">
  The target frame rate. Default is 30. Applicable only when operation_type is change_fps.

  Possible values: `24`, `25`, `30`, `60`
</ParamField>

<ParamField body="post_process.operation_type" type="string" required>
  The type of operation to perform.

  Possible values: `change_fps`, `fbx2usdz`, `extract_armature`
</ParamField>

<ParamField body="rig_task_id" type="string" required>
  The id of a successfully completed rigging task (from POST /openapi/v1/rigging). The character from this task will be animated.
</ParamField>

Generated from the schema Router serves at `GET /v2/models/meshy/animations/openapi.json`, the same document it validates a call against before the request reaches the provider.

### Output

Router does not publish an output schema for this model.

## Examples

### Input

```json
{
  "action_id": 92,
  "post_process": {
    "fps": 60,
    "operation_type": "change_fps"
  },
  "rig_task_id": "0193abcd-0000-0000-0000-000000000000"
}
```


<RouterCodeFooter />
