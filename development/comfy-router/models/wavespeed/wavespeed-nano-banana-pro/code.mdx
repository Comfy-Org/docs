---
title: "Use Wavespeed Nano Banana Pro with Comfy Router"
description: "Call wavespeed/wavespeed-nano-banana-pro through Comfy Router: endpoint, request shape and the response Router returns."
sidebarTitle: "Wavespeed Nano Banana Pro"
---

{/* GENERATED FILE. Generated from router-schemas/wavespeed/wavespeed-nano-banana-pro.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/comfy-router/model-code-footer.mdx";

API Reference for `wavespeed/wavespeed-nano-banana-pro`, served by Comfy Router from WaveSpeed.

## Request setup

Create a key in [your Comfy workspace](https://platform.comfy.org/profile/api-keys) and export it as `COMFY_API_KEY`. For Python, run `pip install comfy-sdk`. For TypeScript, run `npm install @comfyorg/sdk`. For Swift, add the [`ComfySwiftSDK`](https://github.com/Comfy-Org/comfy-swift-sdk) package. cURL uses raw HTTP.

**Model ID:** `wavespeed/wavespeed-nano-banana-pro`

**Endpoint:** `POST https://api.comfy.org/v2/models/wavespeed/wavespeed-nano-banana-pro`

<Note>
Router has not published an authored input schema for this model, so there is no request example to generate a snippet from. Router forwards the body to WaveSpeed unchanged: build it from WaveSpeed's own API documentation, then send it with the [Router quickstart](/development/comfy-router/quickstart).
</Note>

## Schema

### Input

<Note>
Router has not published an authored input schema for this model yet: `GET /v2/models/wavespeed/wavespeed-nano-banana-pro/openapi.json` returns an open object with `x-comfy-input-schema-authored: false`. Router forwards the body to WaveSpeed unchanged, so WaveSpeed's own API documentation is authoritative for the request fields, and Router does not perform model-specific input validation. Provider validation still applies.
</Note>

### Output

_This schema does not declare named properties._

<RouterCodeFooter />
