---
title: "Use Hunyuan 3D Part with Comfy Router"
description: "Call tencent/hunyuan-3d-part through Comfy Router: endpoint, request shape and the response Router returns."
sidebarTitle: "Hunyuan 3D Part"
---

{/* GENERATED FILE. Generated from router-schemas/tencent/hunyuan-3d-part.json by `pnpm code-pages:gen`. */}

import RouterCodeFooter from "/snippets/comfy-router/model-code-footer.mdx";

API Reference for `tencent/hunyuan-3d-part`, served by Comfy Router from Tencent.

## Quick start

Create a key in [your Comfy workspace](https://platform.comfy.org/profile/api-keys) and export it as `COMFY_API_KEY`. The Python and TypeScript snippets use the Comfy SDKs (`pip install comfy-sdk`, `npm install @comfyorg/sdk`); the cURL snippet is the same call over raw HTTP.

**Model ID:** `tencent/hunyuan-3d-part`

**Endpoint:** `POST https://api.comfy.org/v2/models/tencent/hunyuan-3d-part`

<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "tencent/hunyuan-3d-part",
        {
            "File": {
                "Type": "FBX",
                "Url": "https://vcg-test-1258344699.cos.ap-guangzhou.myqcloud.com/test/3d/test.fbx",
            },
        },
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const { data } = await comfy.models.run("tencent/hunyuan-3d-part", {
  File: {
    Type: "FBX",
    Url: "https://vcg-test-1258344699.cos.ap-guangzhou.myqcloud.com/test/3d/test.fbx",
  },
});

console.log(data);
```

```bash cURL
curl https://api.comfy.org/v2/models/tencent/hunyuan-3d-part \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"File\": {\"Type\":\"FBX\",\"Url\":\"https://vcg-test-1258344699.cos.ap-guangzhou.myqcloud.com/test/3d/test.fbx\"}}"
```
</CodeGroup>

## Schema

### Input

<ParamField body="File" type="object">
  3D file input for component generation
</ParamField>

<ParamField body="File.Type" type="string" required>
  3D file format type

  Possible values: `FBX`
</ParamField>

<ParamField body="File.Url" type="string (uri)" required>
  URL of the 3D file to split into components

  Format: `uri`
</ParamField>

Generated from the schema Router serves at `GET /v2/models/tencent/hunyuan-3d-part/openapi.json`, the same document it validates a call against before the request reaches the provider.

### Output

<ResponseField name="Response" type="object" required>
   
</ResponseField>

<ResponseField name="Response.ErrorCode" type="string">
  Tencent's error code, documented as an empty string when there is no error. Router does not read it once `Status` is present.
</ResponseField>

<ResponseField name="Response.ErrorMessage" type="string">
  Tencent's error message, documented as an empty string when there is no error. Router does not read it once `Status` is present.
</ResponseField>

<ResponseField name="Response.RequestId" type="string">
  Tencent's own request identifier, for troubleshooting with Tencent. It is neither the job id nor an asset.
</ResponseField>

<ResponseField name="Response.ResultFile3Ds" type="object[]" required>
  The finished job's generated 3D files. Present and non-empty on the document Router returns; at least one entry carries a fetchable `Url`. The links are Tencent's own and are documented as valid for 24 hours.
</ResponseField>

<ResponseField name="Response.ResultFile3Ds[].PreviewImageUrl" type="string (uri)">
  Preview image URL

  Format: `uri`
</ResponseField>

<ResponseField name="Response.ResultFile3Ds[].Type" type="string">
  3D file format

  Possible values: `GLB`, `OBJ`
</ResponseField>

<ResponseField name="Response.ResultFile3Ds[].Url" type="string (uri)">
  File URL (valid for 24 hours)

  Format: `uri`
</ResponseField>

<ResponseField name="Response.Status" type="string" required>
  Possible values: `DONE`
</ResponseField>

## Examples

### Input

```json
{
  "File": {
    "Type": "FBX",
    "Url": "https://vcg-test-1258344699.cos.ap-guangzhou.myqcloud.com/test/3d/test.fbx"
  }
}
```

### Output

```json
{
  "Response": {
    "ErrorCode": "",
    "ErrorMessage": "",
    "RequestId": "9a1c0d4e-77b2-4e0c-8f2e-2f9a1c0d4e77",
    "ResultFile3Ds": [
      {
        "PreviewImageUrl": "https://example.invalid/tencent/hunyuan-3d-part/preview.png",
        "Type": "GLB",
        "Url": "https://example.invalid/tencent/hunyuan-3d-part/parts.glb"
      }
    ],
    "Status": "DONE"
  }
}
```


<RouterCodeFooter />
