---
title: "Comfy Router provider coverage"
sidebarTitle: "Provider coverage"
description: "See which providers can serve each Comfy Router model, how to choose one, and which provider-specific model IDs are available."
---

{/* GENERATED FILE. Generated from the Router catalog by `pnpm code-pages:gen`. */}

<div className="router-provider-coverage-marker" />

Comfy Router uses one endpoint for every model: `POST /v2/models/{provider}/{model}`. Comfy serves each model by default. Alternate providers use the same model ID, request body, and response format. See [`model_provider`, `strict_mode` and `fallback_provider`](/development/comfy-router/reference#post-v2modelsprovidermodel) in the API reference.

<Note>
To use Comfy, omit `model_provider`. To choose an alternate provider, add `?model_provider=<provider>`. The matrix compares every model with alternate-provider coverage.
</Note>

## Provider coverage

| Model / provider | **Comfy (default)** | **fal** | **Higgsfield** | **Runware** | **WaveSpeed** |
| --- | --- | --- | --- | --- | --- |
| [Dreamina Seedance 2.0 260128](/development/comfy-router/models/byteplus/dreamina-seedance-2-0-260128/code) | ✓ | `fal/fal-seedance-2.0` | - | - | - |
| [Dreamina Seedance 2.5 260628](/development/comfy-router/models/byteplus/dreamina-seedance-2-5-260628/code) | ✓ | `fal/fal-seedance-2.5` | - | - | - |
| [GPT Image 2](/development/comfy-router/models/openai/gpt-image-2/code) | ✓ | `fal/fal-gpt-image-2` | - | `runware/runware-gpt-image-2` | - |
| [Kling 3.0 Turbo](/development/comfy-router/models/kling/kling-3-0-turbo/code) | ✓ | - | `higgsfield/higgsfield-kling-3-turbo` | - | - |
| [Kling V3](/development/comfy-router/models/kling/kling-v3/code) | ✓ | - | `higgsfield/higgsfield-kling-3-std` | - | - |
| [Nano Banana 2](/development/comfy-router/models/google/nano-banana-2/code) | ✓ | `fal/fal-nano-banana-2` | - | `runware/runware-nano-banana-2` | - |
| [Nano Banana Pro](/development/comfy-router/models/google/nano-banana-pro/code) | ✓ | `fal/fal-nano-banana-pro` | - | `runware/runware-nano-banana-pro` | `wavespeed/wavespeed-nano-banana-pro` |
| [Wan 3.0 Video](/development/comfy-router/models/wan/wan3-0-video/code) | ✓ | - | `higgsfield/higgsfield-wan-3` | - | - |


## Try an alternate provider

Choose a model and provider to update the example in all four languages. These examples use the native model ID and request body.

<div className="router-provider-playground" data-router-playground="true">
  <span className="router-provider-playground-label">Model</span>
  <div className="router-provider-picker">
    <button type="button" className="router-provider-model-button" data-router-model-button="true" aria-haspopup="listbox" aria-expanded="false"></button>
    <div className="router-provider-model-menu" data-router-model-menu="true" role="listbox" hidden>
<button type="button" role="option" data-router-option="model-0" aria-selected="false">Dreamina Seedance 2.0 260128 via fal</button>
<button type="button" role="option" data-router-option="model-1" aria-selected="false">Dreamina Seedance 2.5 260628 via fal</button>
<button type="button" role="option" data-router-option="model-2" aria-selected="false">GPT Image 2 via fal</button>
<button type="button" role="option" data-router-option="model-3" aria-selected="false">Kling 3.0 Turbo via Higgsfield</button>
<button type="button" role="option" data-router-option="model-4" aria-selected="false">Kling V3 via Higgsfield</button>
<button type="button" role="option" data-router-option="model-5" aria-selected="false">Nano Banana 2 via fal</button>
<button type="button" role="option" data-router-option="model-6" aria-selected="false">Nano Banana Pro via fal</button>
<button type="button" role="option" data-router-option="model-7" aria-selected="false">Wan 3.0 Video via Higgsfield</button>
    </div>
  </div>
  <div className="router-provider-codegroup" data-router-codegroup="true">
    <div className="router-provider-model-example" data-router-model-example="model-0">
<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "byteplus/dreamina-seedance-2-0-260128",
        {
            "content": [
                {
                    "text": "A red fox trotting through a snowy pine forest",
                    "type": "text",
                },
            ],
            "duration": 5,
            "ratio": "16:9",
            "resolution": "720p",
        },
        model_provider="fal",
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const result = await comfy.models.run("byteplus/dreamina-seedance-2-0-260128", {
  content: [
    {
      text: "A red fox trotting through a snowy pine forest",
      type: "text",
    },
  ],
  duration: 5,
  ratio: "16:9",
  resolution: "720p",
}, {
  modelProvider: "fal",
});
console.log(result.data);
```

```swift Swift
import Foundation
import ComfySwiftSDK

// Reads COMFY_API_KEY from the environment.
// The SDK mints an idempotency key per call and reuses it for automatic retries.
let client = ComfyCloudClient(apiKey: ProcessInfo.processInfo.environment["COMFY_API_KEY"]!)
let result = try await client.models.run(
    "byteplus/dreamina-seedance-2-0-260128",
    input: [
        "content": [
            [
                "text": "A red fox trotting through a snowy pine forest",
                "type": "text",
            ],
        ],
        "duration": 5,
        "ratio": "16:9",
        "resolution": "720p",
    ],
    modelProvider: "fal"
)

print(result.output)
```

```bash cURL
curl https://api.comfy.org/v2/models/byteplus/dreamina-seedance-2-0-260128?model_provider=fal \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"content\": [{\"text\":\"A red fox trotting through a snowy pine forest\",\"type\":\"text\"}], \"duration\": 5, \"ratio\": \"16:9\", \"resolution\": \"720p\"}"
```
</CodeGroup>
    </div>
    <div className="router-provider-model-example" data-router-model-example="model-1" hidden>
<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "byteplus/dreamina-seedance-2-5-260628",
        {
            "content": [
                {
                    "text": "A red fox trotting through a snowy pine forest",
                    "type": "text",
                },
            ],
            "duration": 5,
            "ratio": "16:9",
            "resolution": "720p",
        },
        model_provider="fal",
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const result = await comfy.models.run("byteplus/dreamina-seedance-2-5-260628", {
  content: [
    {
      text: "A red fox trotting through a snowy pine forest",
      type: "text",
    },
  ],
  duration: 5,
  ratio: "16:9",
  resolution: "720p",
}, {
  modelProvider: "fal",
});
console.log(result.data);
```

```swift Swift
import Foundation
import ComfySwiftSDK

// Reads COMFY_API_KEY from the environment.
// The SDK mints an idempotency key per call and reuses it for automatic retries.
let client = ComfyCloudClient(apiKey: ProcessInfo.processInfo.environment["COMFY_API_KEY"]!)
let result = try await client.models.run(
    "byteplus/dreamina-seedance-2-5-260628",
    input: [
        "content": [
            [
                "text": "A red fox trotting through a snowy pine forest",
                "type": "text",
            ],
        ],
        "duration": 5,
        "ratio": "16:9",
        "resolution": "720p",
    ],
    modelProvider: "fal"
)

print(result.output)
```

```bash cURL
curl https://api.comfy.org/v2/models/byteplus/dreamina-seedance-2-5-260628?model_provider=fal \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"content\": [{\"text\":\"A red fox trotting through a snowy pine forest\",\"type\":\"text\"}], \"duration\": 5, \"ratio\": \"16:9\", \"resolution\": \"720p\"}"
```
</CodeGroup>
    </div>
    <div className="router-provider-model-example" data-router-model-example="model-2" hidden>
<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "openai/gpt-image-2",
        {
            "prompt": "a rocketship on a launchpad",
            "quality": "low",
            "size": "1024x1024",
        },
        model_provider="fal",
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const result = await comfy.models.run("openai/gpt-image-2", {
  prompt: "a rocketship on a launchpad",
  quality: "low",
  size: "1024x1024",
}, {
  modelProvider: "fal",
});
console.log(result.data);
```

```swift Swift
import Foundation
import ComfySwiftSDK

// Reads COMFY_API_KEY from the environment.
// The SDK mints an idempotency key per call and reuses it for automatic retries.
let client = ComfyCloudClient(apiKey: ProcessInfo.processInfo.environment["COMFY_API_KEY"]!)
let result = try await client.models.run(
    "openai/gpt-image-2",
    input: [
        "prompt": "a rocketship on a launchpad",
        "quality": "low",
        "size": "1024x1024",
    ],
    modelProvider: "fal"
)

print(result.output)
```

```bash cURL
curl https://api.comfy.org/v2/models/openai/gpt-image-2?model_provider=fal \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"prompt\": \"a rocketship on a launchpad\", \"quality\": \"low\", \"size\": \"1024x1024\"}"
```
</CodeGroup>
    </div>
    <div className="router-provider-model-example" data-router-model-example="model-3" hidden>
<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "kling/kling-3.0-turbo",
        {
            "prompt": "A neon-lit alley in the rain, slow dolly forward.",
            "settings": {
                "aspect_ratio": "16:9",
                "duration": 5,
                "resolution": "1080p",
            },
        },
        model_provider="higgsfield",
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const result = await comfy.models.run("kling/kling-3.0-turbo", {
  prompt: "A neon-lit alley in the rain, slow dolly forward.",
  settings: {
    aspect_ratio: "16:9",
    duration: 5,
    resolution: "1080p",
  },
}, {
  modelProvider: "higgsfield",
});
console.log(result.data);
```

```swift Swift
import Foundation
import ComfySwiftSDK

// Reads COMFY_API_KEY from the environment.
// The SDK mints an idempotency key per call and reuses it for automatic retries.
let client = ComfyCloudClient(apiKey: ProcessInfo.processInfo.environment["COMFY_API_KEY"]!)
let result = try await client.models.run(
    "kling/kling-3.0-turbo",
    input: [
        "prompt": "A neon-lit alley in the rain, slow dolly forward.",
        "settings": [
            "aspect_ratio": "16:9",
            "duration": 5,
            "resolution": "1080p",
        ],
    ],
    modelProvider: "higgsfield"
)

print(result.output)
```

```bash cURL
curl https://api.comfy.org/v2/models/kling/kling-3.0-turbo?model_provider=higgsfield \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"prompt\": \"A neon-lit alley in the rain, slow dolly forward.\", \"settings\": {\"aspect_ratio\":\"16:9\",\"duration\":5,\"resolution\":\"1080p\"}}"
```
</CodeGroup>
    </div>
    <div className="router-provider-model-example" data-router-model-example="model-4" hidden>
<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "kling/kling-v3",
        {
            "aspect_ratio": "16:9",
            "duration": "5",
            "mode": "std",
            "prompt": "A red fox trotting through falling snow, cinematic lighting.",
        },
        model_provider="higgsfield",
    )

print(result)
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
const result = await comfy.models.run("kling/kling-v3", {
  aspect_ratio: "16:9",
  duration: "5",
  mode: "std",
  prompt: "A red fox trotting through falling snow, cinematic lighting.",
}, {
  modelProvider: "higgsfield",
});
console.log(result.data);
```

```swift Swift
import Foundation
import ComfySwiftSDK

// Reads COMFY_API_KEY from the environment.
// The SDK mints an idempotency key per call and reuses it for automatic retries.
let client = ComfyCloudClient(apiKey: ProcessInfo.processInfo.environment["COMFY_API_KEY"]!)
let result = try await client.models.run(
    "kling/kling-v3",
    input: [
        "aspect_ratio": "16:9",
        "duration": "5",
        "mode": "std",
        "prompt": "A red fox trotting through falling snow, cinematic lighting.",
    ],
    modelProvider: "higgsfield"
)

print(result.output)
```

```bash cURL
curl https://api.comfy.org/v2/models/kling/kling-v3?model_provider=higgsfield \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"aspect_ratio\": \"16:9\", \"duration\": \"5\", \"mode\": \"std\", \"prompt\": \"A red fox trotting through falling snow, cinematic lighting.\"}"
```
</CodeGroup>
    </div>
    <div className="router-provider-model-example" data-router-model-example="model-5" hidden>
<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "vertexai/gemini-3.1-flash-image",
        {
            "contents": [
                {
                    "role": "user",
                    "parts": [
                        {
                            "text": "a single red maple leaf on a plain white background, studio lighting",
                        },
                    ],
                },
            ],
            "generationConfig": {
                "responseModalities": ["IMAGE"],
                "imageConfig": {
                    "aspectRatio": "1:1",
                },
            },
        },
        model_provider="fal",
    )

print("image (base64):", result["candidates"][0]["content"]["parts"][0]["inlineData"]["data"])
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
type Result = { candidates: { content: { parts: { inlineData: { data: string } }[] } }[] };
const result = await comfy.models.run<Result>("vertexai/gemini-3.1-flash-image", {
  contents: [
    {
      role: "user",
      parts: [
        {
          text: "a single red maple leaf on a plain white background, studio lighting",
        },
      ],
    },
  ],
  generationConfig: {
    responseModalities: ["IMAGE"],
    imageConfig: {
      aspectRatio: "1:1",
    },
  },
}, {
  modelProvider: "fal",
});
if (result.kind !== "json") throw new Error("expected a JSON result");

console.log("image (base64):", result.data.candidates[0].content.parts[0].inlineData.data);
```

```swift Swift
import Foundation
import ComfySwiftSDK

// Reads COMFY_API_KEY from the environment.
// The SDK mints an idempotency key per call and reuses it for automatic retries.
let client = ComfyCloudClient(apiKey: ProcessInfo.processInfo.environment["COMFY_API_KEY"]!)
let result = try await client.models.run(
    "vertexai/gemini-3.1-flash-image",
    input: [
        "contents": [
            [
                "role": "user",
                "parts": [
                    [
                        "text": "a single red maple leaf on a plain white background, studio lighting",
                    ],
                ],
            ],
        ],
        "generationConfig": [
            "responseModalities": ["IMAGE"],
            "imageConfig": [
                "aspectRatio": "1:1",
            ],
        ],
    ],
    modelProvider: "fal"
)

print("image (base64):", result.output["candidates"][0]["content"]["parts"][0]["inlineData"]["data"].stringValue ?? "")
```

```bash cURL
curl https://api.comfy.org/v2/models/vertexai/gemini-3.1-flash-image?model_provider=fal \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"contents\": [{\"role\":\"user\",\"parts\":[{\"text\":\"a single red maple leaf on a plain white background, studio lighting\"}]}], \"generationConfig\": {\"responseModalities\":[\"IMAGE\"],\"imageConfig\":{\"aspectRatio\":\"1:1\"}}}"
```
</CodeGroup>
    </div>
    <div className="router-provider-model-example" data-router-model-example="model-6" hidden>
<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "vertexai/gemini-3-pro-image",
        {
            "contents": [
                {
                    "role": "user",
                    "parts": [
                        {
                            "text": "a single red maple leaf on a plain white background, studio lighting",
                        },
                    ],
                },
            ],
            "generationConfig": {
                "responseModalities": ["IMAGE"],
                "imageConfig": {
                    "aspectRatio": "1:1",
                },
            },
        },
        model_provider="fal",
    )

print("image (base64):", result["candidates"][0]["content"]["parts"][0]["inlineData"]["data"])
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
type Result = { candidates: { content: { parts: { inlineData: { data: string } }[] } }[] };
const result = await comfy.models.run<Result>("vertexai/gemini-3-pro-image", {
  contents: [
    {
      role: "user",
      parts: [
        {
          text: "a single red maple leaf on a plain white background, studio lighting",
        },
      ],
    },
  ],
  generationConfig: {
    responseModalities: ["IMAGE"],
    imageConfig: {
      aspectRatio: "1:1",
    },
  },
}, {
  modelProvider: "fal",
});
if (result.kind !== "json") throw new Error("expected a JSON result");

console.log("image (base64):", result.data.candidates[0].content.parts[0].inlineData.data);
```

```swift Swift
import Foundation
import ComfySwiftSDK

// Reads COMFY_API_KEY from the environment.
// The SDK mints an idempotency key per call and reuses it for automatic retries.
let client = ComfyCloudClient(apiKey: ProcessInfo.processInfo.environment["COMFY_API_KEY"]!)
let result = try await client.models.run(
    "vertexai/gemini-3-pro-image",
    input: [
        "contents": [
            [
                "role": "user",
                "parts": [
                    [
                        "text": "a single red maple leaf on a plain white background, studio lighting",
                    ],
                ],
            ],
        ],
        "generationConfig": [
            "responseModalities": ["IMAGE"],
            "imageConfig": [
                "aspectRatio": "1:1",
            ],
        ],
    ],
    modelProvider: "fal"
)

print("image (base64):", result.output["candidates"][0]["content"]["parts"][0]["inlineData"]["data"].stringValue ?? "")
```

```bash cURL
curl https://api.comfy.org/v2/models/vertexai/gemini-3-pro-image?model_provider=fal \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"contents\": [{\"role\":\"user\",\"parts\":[{\"text\":\"a single red maple leaf on a plain white background, studio lighting\"}]}], \"generationConfig\": {\"responseModalities\":[\"IMAGE\"],\"imageConfig\":{\"aspectRatio\":\"1:1\"}}}"
```
</CodeGroup>
    </div>
    <div className="router-provider-model-example" data-router-model-example="model-7" hidden>
<CodeGroup>
```python Python
from comfy_sdk import Comfy

# Reads COMFY_API_KEY from the environment.
# The SDK automatically creates an idempotency key and reuses it for automatic retries.
with Comfy() as client:
    result = client.models.run(
        "wan/wan3.0-video",
        {
            "input": {
                "prompt": "the robot lowers its raised arm and steps toward the camera, the flat orange backdrop holds steady",
                "media": [
                    {
                        "type": "first_frame",
                        "url": "https://raw.githubusercontent.com/Comfy-Org/workflow_templates/main/input/pink_robot_front.png",
                    },
                ],
            },
            "parameters": {
                "resolution": "720P",
                "duration": 5,
            },
        },
        model_provider="higgsfield",
    )

print("video:", result["output"]["video_url"])
```

```typescript TypeScript
import { comfy } from "@comfyorg/sdk";

// Reads COMFY_API_KEY from the environment.
// The SDK automatically creates an idempotency key and reuses it for automatic retries.
type Result = { output: { video_url: string } };
const result = await comfy.models.run<Result>("wan/wan3.0-video", {
  input: {
    prompt: "the robot lowers its raised arm and steps toward the camera, the flat orange backdrop holds steady",
    media: [
      {
        type: "first_frame",
        url: "https://raw.githubusercontent.com/Comfy-Org/workflow_templates/main/input/pink_robot_front.png",
      },
    ],
  },
  parameters: {
    resolution: "720P",
    duration: 5,
  },
}, {
  modelProvider: "higgsfield",
});
if (result.kind !== "json") throw new Error("expected a JSON result");

console.log("video:", result.data.output.video_url);
```

```swift Swift
import Foundation
import ComfySwiftSDK

// Reads COMFY_API_KEY from the environment.
// The SDK mints an idempotency key per call and reuses it for automatic retries.
let client = ComfyCloudClient(apiKey: ProcessInfo.processInfo.environment["COMFY_API_KEY"]!)
let result = try await client.models.run(
    "wan/wan3.0-video",
    input: [
        "input": [
            "prompt": "the robot lowers its raised arm and steps toward the camera, the flat orange backdrop holds steady",
            "media": [
                [
                    "type": "first_frame",
                    "url": "https://raw.githubusercontent.com/Comfy-Org/workflow_templates/main/input/pink_robot_front.png",
                ],
            ],
        ],
        "parameters": [
            "resolution": "720P",
            "duration": 5,
        ],
    ],
    modelProvider: "higgsfield"
)

print("video:", result.output["output"]["video_url"].stringValue ?? "")
```

```bash cURL
curl https://api.comfy.org/v2/models/wan/wan3.0-video?model_provider=higgsfield \
  -H "X-API-Key: $COMFY_API_KEY" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"input\": {\"prompt\":\"the robot lowers its raised arm and steps toward the camera, the flat orange backdrop holds steady\",\"media\":[{\"type\":\"first_frame\",\"url\":\"https://raw.githubusercontent.com/Comfy-Org/workflow_templates/main/input/pink_robot_front.png\"}]}, \"parameters\": {\"resolution\":\"720P\",\"duration\":5}}"
```
</CodeGroup>
    </div>
  </div>
  <script type="application/json" data-router-provider-data="true"></script>
</div>
Provider selection works on synchronous calls. The queued `/requests` route does not accept `model_provider`; see the [queued delivery guide](/development/comfy-router/queue) for queued requests.

## Request compatibility

When `strict_mode=false` (the default), Router translates the native request for the selected provider and translates the response back. If a native field cannot be represented, Router drops it and names it in the `X-Comfy-Router-Dropped-Params` response header. See [`model_provider`, `strict_mode` and `fallback_provider`](/development/comfy-router/reference#post-v2modelsprovidermodel) for the other routing options.

<CardGroup cols={2}>
  <Card title="Model catalog" icon="list" href="/development/comfy-router/models">
    Browse every model in the Comfy Router catalog.
  </Card>
  <Card title="Router API reference" icon="code" href="/development/comfy-router/api">
    Read the routing, request, response, retry, and billing details.
  </Card>
</CardGroup>
